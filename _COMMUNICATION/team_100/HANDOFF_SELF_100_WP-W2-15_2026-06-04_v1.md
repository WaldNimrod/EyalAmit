# Session Handoff — team_100 · WP-W2-15 content completion (full)

**Depth:** full · **Engine:** Claude Code · **Generated:** 2026-06-04 (AOS_handoff API offline → hand-authored) · **Mission:** implement all content-reconciliation batches **in parallel via sub-agents**, then validate **internal → external**.

## 1. SESSION ACCOMPLISHED (context)
- **WP-W2-14** (mobile chrome/drawer + elevated pages) — A/B/C/D/E + umbrella **DONE/LOD500_LOCKED**, merged to main, archived.
- **Content-integrity discovered:** elevated pages run team_35 **mockup** copy, not Eyal's text. **team_90 full audit = 33.64% overall, 0/17 pages pass** (`_COMMUNICATION/team_90/CONTENT-ACCURACY-REPORT-2026-06-04.md`).
- **WP-W2-15 opened + planned + L-GATE_SPEC PASS_WITH_FINDINGS (team_190)**; findings F01–F06 applied. **15-A blog pagination FIXED** (branch `wp-w2-15-phase1`, deployed, verified).
- **Completion program + CR sequence registered:** `_COMMUNICATION/team_100/WP-W2-15-CONTENT-COMPLETION-PROGRAM-2026-06-04.md`; roadmap CR1–CR5 + CR-FINAL.
- `scripts/qa/content-diff.mjs` (team_90) committed; Eyal's About + non-indexed sources committed.

## 2. MISSION (this handoff)
Execute **CR1 → CR2 → CR3 → CR4** (CR5 blocked on Eyal) to bring every sourced page to **CONTENT-ACCURACY PASS** (section-cov ≥95% · sentence-cov ≥90% · 0 invented), then **CR-FINAL** validation. Run pages **in parallel via worktree sub-agents** (1 page = 1 agent for L/XL). **Validate internal first (adversarial content-diff sub-agent per page), then external (team_50 → team_190 per batch), then a final full audit (team_90 + team_190 + team_50) before any "ready".**

## 3. EXECUTION — per batch
1. **Dispatch page sub-agents in parallel** (Agent tool, `isolation: worktree`), one per page. Each agent:
   - Reads its **source** `.md` (verbatim SSoT) + the page's **team_90 miss-list** JSON (`_COMMUNICATION/team_90/evidence/content-accuracy-2026-06-04/`).
   - Finds the page's **render code** and replaces the body content **verbatim** from source — every SECTION, correct order, H1/H2/subtitle/CTA/links per source. Reuse the **locked** structure/atoms — **no structural rework, no new tokens, ea-tokens.css untouched**.
   - Self-runs `node scripts/qa/content-diff.mjs` (or the page subset) until **PASS**; `php -l` clean; commits surgically in its worktree.
2. **Internal adversarial verify** (separate sub-agent or orchestrator): re-run content-diff + confirm 0 invented blocks, section order, links/CTAs. Page "build-done" only at PASS.
3. **Serialize-integrate** worktrees → **deploy** (FTP) → **team_100 pre-flight**: content-diff (all batch pages) + 0-overflow @360/390/414/768 + axe 0/0 + 200 + single H1.
4. **External:** route Cursor — **team_50** (content-diff verdict) → **team_190** (L-GATE_VALIDATE). Close batch on dual-PASS → merge → lock roadmap (DONE/LOD500_LOCKED).

## 4. PAGE → SOURCE → render map
Source root: `docs/.../from-eyal/תוכן לאתר 25.5.26/`. Renders (discover/confirm):
- `/method`,`/treatment`,`/sound-healing`,`/lessons` → service path `inc/wave2-w2-04*.php` (`ea_wave2_service_route_content`).
- `/` home → `inc/wave2-stage-b.php` blocks + `template-parts/blocks/*`.
- `/faq` → `template-parts/blocks/block-faq-list.php` / faq module.
- `/muzza` + books → `inc/wave2-w2-05*.php` (commerce).
- `/eyal-amit` (About), `/about/press`, memorial → `inc/wave2-w2-07.php` (editorial).
- shop (didgeridoos/bags/stands/floor/repair) → `inc/wave2-w2-05*.php`.
- catalog (galleries/media/mokesh) → `inc/wave2-w2-14e.php`.
Source files: method.md · treatment.md · sound_healing_final.md · lesons.md · homepage1-3 v2.md · FAQ FINAL.md · MUZZA.md · אודות - אייל עמית.md · eyal_tsva_FINAL.md · kushi_full.md · vekatavta.md · buy didgeridoo.md · bags for didg.md · stend for hanging.md · stend for playing.md · build didg.md · (mokesh: ומה היום.docx — background, H1 decision pending).

## 5. CR-FINAL (after CR1–CR4; CR5 when Eyal delivers)
**team_90** full re-audit (target: every sourced page ≥ gate, overall ≈100%) + **team_190** full-site cross-engine L-GATE_VALIDATE + **team_50** full E2E. **HARD RULE: no "ready"/sign-off message to Eyal before this triple-PASS.** "Done" = measured.

## 6. Standing rules
ADR034 named branch per batch (merge on team_00 go); surgical per-file commits (`Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>`); roadmap single-writer = team_100; `validate_aos .` 0 FAIL + `php -l` clean each step; cross-engine validate in Cursor; **content VERBATIM — zero paraphrase/invention**. Branch `wp-w2-15-phase1` carries the 15-A blog fix (merge with the first batch).

## 6. ACTIVATION PROMPT
```
HANDOFF_DEPTH: full
ACTIVATION_SCOPE: team_100

You are team_100 (Chief Architect), Claude Code, on EyalAmit.co.il-2026.
MISSION: complete site content to 100% — WP-W2-15 CR batches, parallel via
worktree sub-agents, with internal-then-external validation.

READ FIRST:
- _COMMUNICATION/team_100/WP-W2-15-CONTENT-COMPLETION-PROGRAM-2026-06-04.md (the plan)
- _COMMUNICATION/team_100/HANDOFF_SELF_100_WP-W2-15_2026-06-04_v1.md (this handoff §3–§5)
- _COMMUNICATION/team_90/CONTENT-ACCURACY-REPORT-2026-06-04.md (baseline 33.6% + per-page miss-lists)
- _aos/roadmap.yaml WP-W2-15-CR* ; source root from-eyal/תוכן לאתר 25.5.26/

DO:
1. CR1 first (worst): dispatch 4 parallel worktree sub-agents — /muzza, /method,
   /eyal-amit (About), / home — each rebuilds its page render VERBATIM from its
   25.5.26 source (.md), self-validates with `node scripts/qa/content-diff.mjs`
   to PASS (section≥95% / sentence≥90% / 0 invented), php -l clean, commits in
   worktree. NO structural rework / no new tokens / ea-tokens.css untouched.
2. Internal adversarial verify per page (separate check) → serialize-merge →
   deploy (scripts/ftp_deploy_site_wp_content.py) → team_100 pre-flight
   (content-diff + 0-overflow @360/390/414/768 + axe 0/0 + 200 + single H1).
3. Route external per batch: team_50 (content-diff) → team_190 (L-GATE_VALIDATE);
   close on dual-PASS → merge → lock roadmap.
4. Repeat CR2 (treatment/sound-healing/lessons/faq +TOC/show-all),
   CR3 (books×3 + muzza-bundle), CR4 (shop×5). CR5 = await Eyal (mokesh/galleries/media).
5. CR-FINAL: team_90 full re-audit (≈100%) + team_190 full-site + team_50 E2E.

HARD RULE: do NOT report "ready" to team_00/Eyal until CR-FINAL triple-PASS.
Content is VERBATIM from source — zero invention.

FIRST ACTION: branch wp-w2-15-cr1 off main; dispatch the 4 CR1 page sub-agents
(worktree-isolated), each with its source path + miss-list + the content-diff
PASS bar; then internal-verify → integrate → deploy → pre-flight → route.
```
