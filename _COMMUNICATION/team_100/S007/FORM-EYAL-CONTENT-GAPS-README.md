# Where Eyal's form lives — 2026-09-20

**Live URL for Eyal** (staging, plain HTTP by design — the staging certificate is invalid on purpose):

    http://eyalamit-co-il-2026.s887.upress.link/ea-eyal-hub/s007-content-gaps.html

Reachable from the client hub's home page as «השלמות תוכן — 20 בספטמבר 2026», the lead card,
plus the top navigation and the secondary link row.

    http://eyalamit-co-il-2026.s887.upress.link/ea-eyal-hub/

## Source of truth (21 בספטמבר 2026)

The form is a **derivative**. Do not hand-edit status in this HTML.

- Work SSOT: [S007-WORK-SSOT.json](file:///Users/nimrod/Documents/AOS_V5/EyalAmit.co.il-2026/_COMMUNICATION/team_100/S007/S007-WORK-SSOT.json)
- Renderer: `python3 scripts/s007_render_work_ssot.py` then copy to `hub/dist/s007-content-gaps.html`
- Single-file FTP: `python3 scripts/s007_render_work_ssot.py --ftp` (no hub rebuild)
- Nimrod board (same renderer): [GALLERY.html](file:///Users/nimrod/Documents/AOS_V5/EyalAmit.co.il-2026/_COMMUNICATION/team_100/S007/content-gaps-2026-09-21/GALLERY.html) — title «לוח עבודה»

Header of the HTML carries `generatedAt` and `ssotSha12`. If they disagree with the JSON file, regenerate.

## How it got there

The tracked source is `_COMMUNICATION/team_100/S007/FORM-EYAL-CONTENT-GAPS-2026-09-20.html`.
It is copied to `hub/dist/s007-content-gaps.html` and published as **that one file**.
Do **not** run `scripts/build_eyal_client_hub.py` for this update — a full rebuild drops the three home-page links to this form.

## Two things that will bite whoever touches this next

**`hub/dist/` is gitignored.** The copy on the server and the copy in `hub/dist/` are build
output. The tracked source is the one under `_COMMUNICATION/`; regenerate from the SSOT, copy, publish.

**The hub index edit is NOT tracked and NOT generated by the builder in a way that knows about
this form.** If `scripts/build_eyal_client_hub.py` regenerates `hub/dist/index.html`, the three
links to this form disappear from the hub home page while the form itself stays reachable at its
URL. After any hub rebuild, re-add them — top nav, lead card, secondary row — or fold the form
into the builder properly.

**Do not publish the hub from a git worktree.** The default prune deletes remote files absent
locally, and the ~1,000 media files under `hub/dist/files/` are gitignored and therefore missing
in a worktree. Publish from the main checkout, or pass `--no-prune`.

## What the form contains

129 answerable items (surface `eyal_form` or `both` on the SSOT). Status chips are copied from
the JSON. Radios/notes are Eyal's C answers. They are not page approvals.
