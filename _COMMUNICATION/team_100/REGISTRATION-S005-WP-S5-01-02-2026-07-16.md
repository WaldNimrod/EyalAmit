---
id: REGISTRATION-S005-WP-S5-01-02-2026-07-16
from_team: team_100
to_team: team_110
cc: team_00
date: 2026-07-16
revision: 2 (supersedes rev-1 of the same file — see §0)
type: registration-confirmation
milestone: S005
wp: [WP-S5-01, WP-S5-02, WP-S5-05, WP-S5-06]
authorized_by: "team_00 (נמרוד) 2026-07-16 — «פרומט לסשן צוות 100 להשלמת הרישום» + «מאשר פוש»"
in_reply_to:
  - _COMMUNICATION/team_110/ROUTING-REQUEST-TEAM100-S005-REGISTRATION-2026-07-16.md
  - _COMMUNICATION/team_110/ADDENDUM-TEAM100-WP-S5-06-FACADE-MANDATORY-2026-07-16.md
branch: chore/s005-registration
validate_aos: 47 PASS / 31 SKIP / 0 FAIL
---

# REGISTRATION CONFIRMED — WP-S5-01 + WP-S5-02 closed · WP-S5-06 opened

## 0. ⚠ Rev-2 — two self-corrections (rev-1 was written against a superseded world)

Rev-1 of this artifact was authored from the **original** routing request, before commit `6f83d59` landed.
Two things in it were **wrong** and are corrected here. Flagged loudly rather than quietly rewritten:

1. **§3.2 facade — rev-1 registered it as "deferred debt."** team_00 **reversed** that ruling the same day
   («ליזי מחוייב»). It is now **WP-S5-06, mandatory, blocking cutover.** The debt record has been removed
   from the roadmap and replaced with a SUPERSEDED marker pointing at WP-S5-06.
2. **ADR034 root cause — rev-1 got it wrong.** team_100 independently "confirmed" the routing request's §4
   diagnosis (*"stale DB snapshot never backfilled"*) and escalated **"backfill the DB roadmap vs record as
   file-SSoT."** That diagnosis was wrong; **the backfill recommendation is withdrawn.** team_120 traced the
   real cause (§4c below). The *observation* (API serves S002-era data) was correct — the *cause* and the
   *remedy* were not. Corrected in-place on the WP rows and in the project notes.

## 1. Registered — gate closures

| WP | Before | After |
|----|--------|-------|
| **WP-S5-01** | `PLANNED` / `L-GATE_SPEC` / `LOD400` | **`COMPLETE` / `L-GATE_VALIDATE` / `LOD500_LOCKED`** |
| **WP-S5-02** | `PLANNED` / `L-GATE_SPEC` / `LOD400` | **`COMPLETE` / `L-GATE_VALIDATE` / `LOD500_LOCKED`** |

Basis — canon `lean-kit/modules/gate-workflow/gates/L-GATE_VALIDATE.md` §*"roadmap.yaml update on
L-GATE_VALIDATE PASS"*: **PASS + 0 blockers → `COMPLETE` + `LOD500_LOCKED`**. Both verdicts carry
`gate: L-GATE_VALIDATE`, **PASS**, **0 findings**, cycle 1, no correction loop. Iron Rule #1 satisfied
(`claude-opus-4-8`/Anthropic ≠ `composer-2.5`/Cursor). `gate_history` appended to both.

**WP-S5-02's lock stands despite the §3.2 reversal** — the facade was explicitly *out of its scope*, and
team_90 validated it as a ratified guardrail. The reversal creates new work (S5-06); it does not reopen S5-02.

## 2. Registered — WP-S5-06 (new)

Per the addendum's "Requested of team_100" items 1–2:

- **`WP-S5-06`** — *"QR embed facade — click-to-load (deliver the lazy-load commitment)"*; `milestone_ref: S005`;
  `blocked_by: []`; `next_wp: WP-S5-05`; `assigned_builder: team_10/110`; `assigned_validator: team_90
  (cross-engine, Iron Rule #1)`; `priority: HIGH`; `lod_status: LOD200` (LOD400 pending — see §5.1).
- **`WP-S5-05.blocked_by`** now includes `WP-S5-06` → **the cutover gate depends on it.**

Carried into the roadmap row so it cannot be lost, from the addendum:

- 🔴 **`critical_constraint`** — the facade **MUST** be a `the_content` render filter; **the reseed route is
  forbidden.** `ea-w2-seo-schema.php` **L266** regexes raw `post_content`, so a seed-change + reseed (the
  obvious route, and exactly how §3.1 was just done) would make `preg_match_all` return 0 and **silently delete
  every VideoObject node from all 42 QR pages** — destroying the WP-S5-02 build that just passed cross-engine.
  No error, no warning. **AC-2 is the regression guard.**
- **`implications_for_lod400`** — all 8 (filter priority ≥20 + QR-only scoping, preserve `youtube-nocookie`,
  thumbnail decision *must be decided* (recommend accept `i.ytimg.com` for v1 ≈ 97% win), a11y 100 / axe 0 must
  not regress, RTL, `prefers-reduced-motion`, reseed drop-in cleanup, keep `lazy` on the swapped-in iframe).
- **`acceptance_criteria`** — AC-1..AC-5, reusing `qr_cwv_probe.mjs` as the harness rather than inventing one.
- **Transcripts EXCLUDED** (team_00) — Eyal content task; bundling would make S5-06 permanently Eyal-blocked
  and thus block cutover.

## 3. Carry-forwards recorded (not closed)

- **QR direct-200 (49 rows)** — on WP-S5-01 as `carried_forward_to_WP-S5-05`, mirrored as a hard
  `inherited_obligations` entry on **WP-S5-05**. The LOD500 lock covers S5-01's mandated scope
  (verification-as-specified) and **does not** close the prod assertion. Staging PASS does not substitute —
  prod re-run via `scripts/final_pre_cutover_check.sh`, no `-L`, assert 200 **and** no `Location` header.
- **§3.2 facade** — no longer debt. Now WP-S5-06 (§2), also mirrored in WP-S5-05's `inherited_obligations`.
  The measurement is retained **as S5-06's evidence baseline**; the *"native lazy-load already meets the CWV
  bar"* justification is recorded as **factually wrong as a mechanism**, and *"revisit post-cutover"* is void.

## 4. Corrections to the request (verified, not accepted as stated)

**(a) Item 5 — Check-32 FAIL: already resolved.** Check 32 read **PASS** and `_aos/roadmap.yaml` was **clean**
at `6c42f20`; the LOD400-authoring drift had already been committed in `e8b02b9`/`6c42f20`. No action taken.

**(b) Item 3 — cited precedent is wrong.** The request asks to close S5-01/02 *"consistent with how S004's WPs
were closed."* **S004's WPs were never closed** — all 8 still read `IN_PROGRESS` / `L-GATE_BUILD` / `LOD400`
despite S004 being declared COMPLETE. Following it literally would have left S5-01/02 open. Used canon + the
last genuinely-closed packages (S002 `WP-W2-0N`, `WP-CANON`) instead: `COMPLETE` / `LOD500_LOCKED`.

**(c) §4 ADR034 — team_120's correction ACCEPTED; team_100's rev-1 escalation WITHDRAWN.** Recorded now:
`/api/l0/<proj>/roadmap` reads `_aos/roadmap.yaml` from a **server-side git checkout** at the registered
`server_path` (`/data/projects/eyalamit`) — **there is no DB roadmap table in this path** (proof: GET
`/api/l0/microgreens/roadmap` → 404 *"Project root not found: neither local_p…"*). That checkout: `8270d98`
(2026-05-29), **450 commits behind `origin/main`**, clean, correct remote, never pulled; `tiktrack-bare-sync.timer`
exists, **no eyalamit sync timer does**; `smallfarmsagents` 0 behind. → **missing sync mechanism, not a DB
backfill, not fleet-wide.** team_120's consequence stands and is recorded in the project notes: **ADR034 /
Iron Rule #7 is a live trap** — health says `db:online` while the API serves an S002 world with no `WP-S5-*`, so
an agent obeying *"API-only when DB online"* would mutate against a 450-commit-stale world.

## 5. ⚠ Open for team_00 — three items team_100 did NOT decide

1. **WP-S5-06's LOD400 is not yet authored** (addendum "Requested of team_100" **item 3**). The roadmap row
   carries the constraint + implications + ACs, but the LOD400 itself — to the S5-01/02/03 bar, buildable by a
   fresh agent with no guesses, then team_90 cross-engine validated — is the **next substantial team_100
   deliverable** and was not attempted in this registration session. **team_110 is blocked on it for S5-06**
   (not for S5-03, which already has its handoff).
2. **S004 status drift.** All 8 S004 WPs sit at `IN_PROGRESS` / `L-GATE_BUILD` / `LOD400` while the milestone is
   recorded COMPLETE with a team_190 constitutional PASS_WITH_FINDINGS + GO at WP-S4-08. Deliberately **not**
   mass-mutated. Ruling needed: close all 8 to `COMPLETE`/`LOD500_LOCKED`, or leave pending a formal S004
   closeout artifact.
3. **Iron Rule #15 archival** is mandated post-LOD500_LOCK (artifacts → `_archive/[WP-ID]/` + manifest; LOD500
   as-built under `_aos/work_packages/[WP-ID]/`). Not done for S5-01/02 — **nor for S004 or WP-CANON**, so a
   standing spoke-wide gap, not an S005 regression. team_191 mandate at team_00's discretion; not blocking S5-03.

## 6. State

- Branch **`chore/s005-registration`**, rebased onto `main` @ `6f83d59` (R8 file-SSoT mutation on a named branch,
  never `main`, per the S002-closeout precedent). Pushed. **Merge to `main` pending team_00 go.**
- `validate_aos.sh` → **47 PASS / 31 SKIP / 0 FAIL**, L-GATE_BUILD EXIT CRITERION SATISFIED.
- **Unblocked for team_110:** **WP-S5-03** (handoff exists) and **WP-S5-06** (registered; LOD400 pending §5.1).
- **WP-S5-05 stays BLOCKED** — `blocked_by: [S5-01, S5-02, S5-03, S5-04, S5-06, M-EYAL-INPUTS]`; go-live still
  requires explicit team_00/Eyal approval (§7 item 15).
- ⚠ **Concurrency note:** this repo's working tree was switched to `main` by another session mid-registration
  (commits `6f83d59`, `8d3449c` landed during it). Two sessions sharing one working tree is a live hazard —
  worth a worktree per session.
