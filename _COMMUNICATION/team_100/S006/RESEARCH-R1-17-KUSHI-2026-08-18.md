RECOMMEND: BUILD · ברור 3 · לא-ברור נימרוד 0 · לא-ברור אייל 5

# RESEARCH — R1-17 `/books/kushi-blantis/` · פאזה א׳ בלבד · 2026-08-18

**צוות:** 100 (S006 page researcher) · **לא בנאי · לא מאמת**
**סקואופ נעול:** `סקואופ אושר: R1-17, R1-18, R1-19, R1-25, R1-28 בלבד` + הקפאת `R1-07, R1-08, R1-09, R1-27`
**עמוד:** R1-17 `/books/kushi-blantis/` בלבד · http://eyalamit-co-il-2026.s887.upress.link/books/kushi-blantis/
**דין:** אמנה §2 «אין סקירה» · §3א נעילת סיווג · §3ג · מנדט `file:///Users/nimrod/Documents/AOS_V5/EyalAmit.co.il-2026/_COMMUNICATION/team_100/S006/MANDATE-R1-17-KUSHI-BUILD-2026-08-18.md`
**פאזה ב׳:** לא. אין `GO BUILD R1-17`. אפס עריכות PHP / xlsx / git / FTP / אחים / `muzza-defaults.php`.

סעיפי טרקר: `file:///Users/nimrod/Documents/AOS_V5/EyalAmit.co.il-2026/_COMMUNICATION/team_100/S006/tracker/r1-17-items.json` · קידומת `KSH-`.

---

## 1. חומר

SSOT (ה-md הוא הרצוי; אין קובץ סקירה → «אין הערה = מאושר» **אינו חל**):

`file:///Users/nimrod/Documents/AOS_V5/EyalAmit.co.il-2026/EyalAmit_Site_GoogleDrive_Sync/content 13.8.26/כושי בלאנטיס/kushi_full.md`

| בדיקה | תוצאה |
|---|---|
| תיקיית `כושי בלאנטיס/` | **כן** — קובץ יחיד `kushi_full.md` (18 391 בתים, 279 שורות, SECTION 01–14) |
| `סקירה כושי` ליד הסנכרון | **אין.** קובץ סקירה יחיד בחבילה: `file:///Users/nimrod/Documents/AOS_V5/EyalAmit.co.il-2026/EyalAmit_Site_GoogleDrive_Sync/סקירה דף הבית.xlsx` |
| מדיה בתיקייה (jpg/png/mp4/שם קובץ) | **אין** |
| DEV NOTES | בתוך אותו md (לא קובץ נפרד) |
| `from-eyal/` | **לא נפתח** (אמנה §2) |
| אב `/books/` (R1-16) | **הוגש** — לא נגענו ב-`muzza-defaults.php` |
| `WP-EI-01` / `WP-EI-02` | ON_HOLD — **נדחים** למחזור הזה (מנדט; תקדים BK-03) |

ערוץ כתיבה (פאזה ב׳ בלבד): `file:///Users/nimrod/Documents/AOS_V5/EyalAmit.co.il-2026/site/wp-content/themes/ea-eyalamit/inc/chapters/defaults/kushi-blantis-defaults.php`

**אחים בגל (לא לגעת):** `tsva-bekahol-defaults.php` · `vekatavta-defaults.php` · `muzza-defaults.php`.

SECTION 13 במסמך = DEV NOTES לעמוד (לא תוכן אתר). לא לרנדר.

---

## 2. חי מול md

**HTTP:** `curl -sk` → **200** · canonical `/books/kushi-blantis/` · כותרת «כושי בלאנטיס». TLS סטייג'ינג פג בכוונה. `X-Robots-Tag: noindex, nofollow`.

**ערוץ החי = defaults.** גופי 02 / 04 / 08 / קטע 03 (עד «נשארו עשרים דקות!») וגוף FAQ **כן** מהמסמך — עם כרום צוות, סדר שגוי, רכישה שהומצאה, ומדיה בלי קובץ.

סדר המסמך (רצוי): **01 הירו → 02 תקציר → 03 קטע → 04 על הספר → 05 גלריה → 06 רכישה → 07 למי → 08 על אייל → 09 CTA ביניים → 10 FAQ → 11 כתבות → 12 CTA סופי → 14 עוד רגעים.** (13 = הערות מתכנת.)

סדר החי: 01 → 02 → 03 → 04 → **06 לפני 05** → 07 → 08 → 09 → 10 FAQ → **05 גלריה אחרי FAQ** → 11 → **14 לפני 12** → 12.

| בלוק חי (דסקטופ) | ב-defaults | במסמך |
|---|---|---|
| תג `הספר` | `phero.chap` | `# כושי בלאנטיס` (שורה 4). אין תג-פרק |
| H1 `כושי <em>בלאנטיס</em>` | `phero.title` | H1 בלי em: `כושי בלאנטיס` (שורה 4) |
| תת הירו | `phero.sub` | שורה 6 — **תואם** |
| הירו `kushi-blantis-cover.jpg` + CTA `לרכישת הספר · 69 ₪` → `mrng.to/MTUiO3vkIg` | `phero.media` + `cta_*` + `price=69` | SECTION 01: טקסט בלבד. **אין** תמונה, **אין** כפתור, **אין** 69 |
| H2+chap «תקציר הספר» | `sections[0]` | `## SECTION 02 – תקציר הספר`. ארבע פסקאות שורות 12–18 — **גוף תואם** |
| אקורדיון: H2 «קטע מתוך הספר – לקריאה» + `לחצו לקריאת הקטע המלא` | `collapsible` + `toggle_label` | כפתור שורה 30: `קטע מתוך הספר – לקריאה`. **אין** «לחצו…». חסר קו `___________________________________` (שורה 39). שאר הקטע עד שורה 85 — תואם. ברירת מחדל סגור — נכון |
| ספליט + `eyal-portrait-garden.jpg` | `split` | SECTION 04 שורות 94–106 — גוף תואם. **אין** תמונה |
| רכישה: «לרכישה דרך מורנינג» + «לרכישה מאובטחת (חשבונית ירוקה)» → אותו `mrng.to` | SECTION 06 prose | שורות 137–140: מודפס = `קישור יתווסף בהמשך` (בלי URL). דיגיטלי = `[לרכישה דרך מנדלי](https://www.mendele.co.il/product/kushibelantis/)` (**200**) |
| `.reveals` + 9 H3 שהומצאו + 9 תמונות `chapters/` | `reveals` | רשימה כתובה שורות 146–158. **אין** כותרות-כרטיס, **אין** תמונות |
| «לעמוד אייל עמית» → `https://www.eyalamit.co.il/about/` | SECTION 08 | שורה 178 ככתבה. בייצור **404**. בסטייג'ינג `/about/` (id 180) ו-`/eyal-amit/` (id 14) שניהם 200 — **לא** לבחור במקומו |
| CTA 09: «לרכישת הספר המודפס» + «ספר אלקטרוני» + `ea-pending-inline` GI | `cta` + `cta2` + `temp_note` | שורות 196–198: משפט + כפתור אחד `[לרכישת הספר הדיגיטלי](https://www.mendele.co.il/product/kushibelantis/)` |
| FAQ CPT `faqblock cats:kushi-blantis` — 6 שאלות, **סדר אחר** | `faqblock` | SECTION 10 שורות 205–221, סדר המסמך. הנוסחים כן מהמסמך (כולל «236 עמודים» שורה 215) |
| גלריה 5 תמונות + כיתובים שהומצאו (`עטיפה` / `סיני` / `תיעוד 2013`…) + `lead` «תמונות מהעולם של הספר.» + pending «בחירת ביניים של הצוות» | `gallery` + `pending-note` | SECTION 05 שורות 112–127: למשוך מהדף הקיים בלי להמציא קבצים — **בלי שם קובץ**. כיתובים/lead/pending **אינם** במסמך |
| כתבות: קופסת «ממתין לאישור» + קישור `/press/` | prose pending | SECTION 11 שורות 234–236: כתבות **אם קיימות**, בלי קובץ. `/press/` **הומצא** |
| «עוד רגעים מהדרך»: קופסת pending | prose pending | SECTION 14 שורות 277–279: תמונות נוספות **אם קיימות**, לא לשכפל את 05 — בלי קובץ |
| CTA 12: כפתור `לרכישת הספר` → Morning + `temp_note` GI | `cta` + `temp_note` | שורות 245–251: ארבע שורות + `[לרכישת הספר](https://www.mendele.co.il/product/kushibelantis/)` |
| JSON-LD `datePublished=2004` · `price=69` | ראש הקובץ + סכמה | **אין** 2004 במסמך הזה. **אין** 69. (S007 — לא נפתח סעיף) |

**קישורי המסמך שנבדקו:** `https://www.mendele.co.il/product/kushibelantis/` → **200**. `https://www.eyalamit.co.il/about/` → **404**. דף הייצור שצוין ב-DEV NOTES של 05 (`https://www.eyalamit.co.il/shop/books/כושי-בלאנטיס/`) → **200**, בלי שמות קבצים במסמך — לא נלקחו בייטים משם.

`https://mrng.to/MTUiO3vkIg` הוא קישור **חבילת שלושת הספרים** (R1-16 SECTION 10) — לא הבייטים של העמוד הזה. DEV NOTE שורה 135: «לא להמציא קישור מודפס».

**מחוץ לסקואופ:** `muzza-defaults.php` (הוגש) · defaults של אחים · `chapters-render.php` / `videoblk.php` / `block-faq-list.php` / `inc/data/*.json` · תפריט/פוטר · מובייל (סבב 3) · meta/JSON-LD (S007) · `WP-EI-01/02` · R1-18/R1-19 · R1-27 hub גלריות.

**מלכודת לבנאי:** לא לגעת ב-`ea-faq-seed.json` ולא ב-`block-faq-list.php`. לא להעתיק Morning מ-`muzza-defaults.php`. לא לפתוח `WP-EI-01/02`. לא לרנדר SECTION 13. לא לבחור בין `/about/` ל-`/eyal-amit/` (KSH-05). מדיה (KSH-01/02/03/04) לא בבנייה עד בחירת אייל.

---

## 3. סיווג (שער 2) — אמנה §3א

`ברור` רק אם (א) מקור מצוטט **ו** (ב) בייטים מילה-במילה **ו** (ג) פעולה אחת. אין סקירה → כל פער מול החי **בהיקף**. תוספת צוות שאינה במסמך יורדת עם ההדבקה. מדיה בלי קובץ = `לא ברור` לאייל. «236 עמודים» — **במסמך** (שורה 215) → מותר ב-FAQ. 69 / 2004 — **לא** במסמך → לא להדביק.

| # | הסעיף | (א) | (ב) | (ג) | סיווג | סטטוס |
|---|---|---|---|---|---|---|
| KSH-01 | תמונת הירו | SECTION 01 שורות 2–7 — אין קובץ | אין | בחירה | לא ברור | ממתין לאייל |
| KSH-02 | גלריה | SECTION 05 שורות 112–127 — בלי שם קובץ | אין | בחירה | לא ברור | ממתין לאייל |
| KSH-03 | כתבות עיתונות | SECTION 11 שורות 234–236 — «אם קיימות», בלי קובץ | אין | בחירה/מילוי | לא ברור | ממתין לאייל |
| KSH-04 | עוד רגעים מהדרך | SECTION 14 שורות 277–279 — «אם קיימות», בלי קובץ | אין | בחירה/מילוי | לא ברור | ממתין לאייל |
| KSH-05 | קישור אודות | SECTION 08 שורה 178 | ה-URL שבמסמך הוא 404 בייצור | בחירה/מילוי | לא ברור | ממתין לאייל |
| KSH-06 | סדר 01–04+07–08 + הירו בלי em + הסרת כרום צוות | SECTION 01–04 · 07–08 (שורות 4–106, 146–186) | כן, המסמך ככתבו | הדבקה אחת במקום הכרום | **ברור** | פתוח |
| KSH-07 | FAQ — 6 מהמסמך, `faq-inline`, סדר המסמך | SECTION 10 שורות 205–221 | 6 שאלות ככתבן | החלפה, בלי `block-faq-list.php` | **ברור** | פתוח |
| KSH-08 | רכישה + CTA מנדלי, בלי GI/Morning/69 | SECTION 06 · 09 · 12 (שורות 137–140, 196–198, 245–251) | מנדלי ככתבו; מודפס = «קישור יתווסף בהמשך» | כפתורים מהמסמך; כרום GI יורד (תקדים BK-03) | **ברור** | פתוח |

תשע תמונות ה-`.reveals` ותמונת הספליט ב-04 **אינן** במסמך — יורדות עם KSH-06 (אין תוכן = אין רכיב). לא נפתח להן סעיף. קופסאות «ממתין לאישור» ב-05/11/14 יורדות עם ההדבקה; החומר עצמו נשאר בטרקר (KSH-02/03/04).

אין סעיפים לנימרוד. המתנה למדיה מאייל **אינה** עצירה. `WP-EI-01/02` לא נפתחו: כרום ה-GI/Morning יורד כי אינו במסמך (BK-03).

---

## 4. פעולות הדבקה `ברור` (לשער 4, רק אחרי `GO BUILD R1-17`)

1. **KSH-06** — למחוק כרום שהומצא: `<em>` ב-H1 · תג `הספר` · `toggle_label` «לחצו לקריאת הקטע המלא» · ספליט+`eyal-portrait-garden.jpg` · `.reveals` + תשע תמונות `chapters/` + תשע כותרות-H3 · `lead`/כיתובי גלריה שהומצאו · קופסאות pending ב-05/11/14 · קישור `/press/` שהומצא. להדביק SECTION 01–04 ו-07–08 ככתבם, **בסדר המסמך** (05 אחרי 04 ולפני 06; 14 אחרי 12 או לא-מרונדר עד KSH-04). H1 = `כושי בלאנטיס` **בלי** `<em>`. אקורדיון 03: כפתור/summary = `קטע מתוך הספר – לקריאה`, כולל שורת `___________________________________`, סגור כברירת מחדל. 07 כרשימה כתובה (לא `.reveals`). 08: גוף ככתבו; **בלי** href עד KSH-05. Provenance על כל מחרוזת.
2. **KSH-07** — שש שאלות SECTION 10 כ-`faq-inline` **בסדר המסמך** (סיפור אמיתי → פנטזיה → למי → 236 עמודים → כבד/קל → קשר לעבודה). לא `faqblock` / CPT / `block-faq-list.php` / `inc/data/*.json`.
3. **KSH-08** — SECTION 06: משפט הזמינות + `לרכישת הספר המודפס – קישור יתווסף בהמשך` + `[לרכישה דרך מנדלי](https://www.mendele.co.il/product/kushibelantis/)`. SECTION 09: כותרת `רוצה להתחיל לקרוא כבר עכשיו?` + כפתור אחד `לרכישת הספר הדיגיטלי` → מנדלי; בלי כפתור מודפס, בלי `cta2`, בלי `temp_note`. SECTION 12: ארבע השורות ככתבן + `לרכישת הספר` → מנדלי; בלי GI. הירו: בלי כפתור 69 ₪ ובלי `mrng.to`. לא ממציאים מחיר/קישור מודפס. לא פותחים `WP-EI-01/02`.

מדיה (KSH-01/02/03/04) וקישור האודות (KSH-05) **לא** בבנייה עד בחירת אייל.

בנייה: רק `kushi-blantis-defaults.php` (+ חלק חדש ייעודי לעמוד אם חייבים). לא לגעת באב/אחים.

---

## 5. שאלות לנימרוד

**אין.**

---

## 6. מה לא נגענו בו

- PHP / FTP / git / xlsx / `tracker_page_tab.py` / `tracker_update.py`
- `muzza-defaults.php` `tsva-bekahol-defaults.php` `vekatavta-defaults.php`
- `chapters-render.php` `tpl-chapters-page.php` `videoblk.php` `block-faq-list.php` `product-cta.php` `chapters-commerce.php` `inc/data/*.json`
- R1-18 `/books/tsva-bekahol/` · R1-19 `/books/vekatavta/` · R1-27 `/galleries/`
- תפריט/פוטר משותפים · מובייל (סבב 3) · meta/JSON-LD (S007) · `WP-EI-01/02`
