RECOMMEND: BUILD — ברור **3** · לא-ברור נימרוד **0** · לא-ברור אייל **2**

# RESEARCH — R1-19 `/books/vekatavta/` · פאזה א׳ בלבד · 2026-08-18

**צוות:** 100 (S006 page researcher) · **לא בנאי · לא מאמת**
**סקואופ נעול:** `סקואופ אושר: R1-17, R1-18, R1-19, R1-25, R1-28 בלבד` + הקפאת `R1-07, R1-08, R1-09, R1-27`
**עמוד:** R1-19 `/books/vekatavta/` בלבד · http://eyalamit-co-il-2026.s887.upress.link/books/vekatavta/
**דין:** אמנה §2 «אין סקירה» · §3א נעילת סיווג · §3ג · מנדט `file:///Users/nimrod/Documents/AOS_V5/EyalAmit.co.il-2026/_COMMUNICATION/team_100/S006/MANDATE-R1-19-VEKATAVTA-BUILD-2026-08-18.md`
**שערים:** 1–2 (קליטה + סיווג). אין `GO BUILD R1-19`. אפס עריכות PHP / xlsx / git / FTP / אחים / `muzza-defaults.php`.
**טרקר סעיפים:** `file:///Users/nimrod/Documents/AOS_V5/EyalAmit.co.il-2026/_COMMUNICATION/team_100/S006/tracker/r1-19-items.json` · קידומת `VKT-`

---

## 1. חומר

SSOT (ה-md הוא הרצוי; אין קובץ סקירה → «אין הערה = מאושר» **אינו חל**):

`file:///Users/nimrod/Documents/AOS_V5/EyalAmit.co.il-2026/EyalAmit_Site_GoogleDrive_Sync/content 13.8.26/וכתבת/vekatavta.md`

| בדיקה | תוצאה |
|---|---|
| תיקיית `וכתבת/` | **כן** — קובץ יחיד `vekatavta.md` (19 336 בתים, 14 סקשנים, 355 שורות) |
| `סקירה וכתבת` / כל «סקירה …» לעמוד | **אין.** |
| מדיה בתיקייה (jpg/png/mp4/שם קובץ) | **אין** |
| DEV NOTES | בתוך אותו md (SECTION 01–03 מקומיים + SECTION 13 גלובלי) — לא תוכן אתר |
| `from-eyal/` | **לא נפתח** (אמנה §2 / מנדט) |
| אב `/books/` | **הוגש** — אל תגעו ב-`muzza-defaults.php` |
| `WP-EI-01` / `WP-EI-02` | ON_HOLD — **נדחים** למחזור הזה (מנדט) |

ערוץ כתיבה (פאזה ב׳ בלבד): `file:///Users/nimrod/Documents/AOS_V5/EyalAmit.co.il-2026/site/wp-content/themes/ea-eyalamit/inc/chapters/defaults/vekatavta-defaults.php`

**אחים בגל (לא לגעת):** `kushi-blantis-defaults.php` · `tsva-bekahol-defaults.php` · `muzza-defaults.php`.

---

## 2. חי מול md

**HTTP:** `curl -sk` → **200** · canonical `/books/vekatavta/` · כותרת מסמך «וכתבת - eyal amit». TLS סטייג'ינג פג בכוונה. `X-Robots-Tag: noindex, nofollow`.

**ערוץ החי = defaults.** גופים רבים קיימים, עם כרום צוות, סדר שגוי, כתיב ששונה, ו-Morning/GI במקום מנדלה.

סדר המסמך (רצוי): **01 הירו → 02 תקציר → 03 קטע → 04 על הספר → 05 גלריה → 06 רכישה → 07 למי מתאים → 08 על אייל → 09 CTA ביניים → 10 FAQ → 11 כתבות → 12 CTA סופי → 14 עוד רגעים.** SECTION 13 = DEV NOTES לעמוד (לא תוכן אתר).

סדר החי: 01 → 02 → 03 → 04 → **07 (+ זנב «בין השורות») → 08 → 09 → 10 → 06 → 05 → 11 → 14 → 12.**

| בלוק חי (דסקטופ) | ב-defaults | במסמך |
|---|---|---|
| תג `ספר` + H1 `ו<em>כתבת</em>` | `phero.chap` / `phero.title` | H1 בלי em, בלי תג: `וכתבת` (שורה 10) |
| תת מחובר בנקודה | `phero.sub` | שתי שורות 12–13, בלי נקודה מחברת |
| כפתור הירו `לרכישת הספר · 79 ₪` → `https://mrng.to/MTUiO3vkIg` | `phero.cta_*` + `price` 79 | `[לרכישת הספר](https://www.mendele.co.il/product/vekatavta/)` שורה 15. **אין מחיר** |
| הירו `vekatavt-cover.jpg` | `phero.media` | DEV NOTES שורות 3–8: תמונת כריכה **בלי קובץ** |
| H2 «ספר אישי מאוד…» + chap «תקציר הספר» | prose 02 | `##` = תקציר הספר. המשפט העבה הוא **גוף**, לא H2. `ל**וכתבת**` (ל מחוץ ל-bold) — החי `<strong>לוכתבת</strong>` |
| קטע מתקפל + «לחצו לקריאת הקטע המלא» + H2 «10 דברים…» | `collapsible` / `toggle_label` | שבירת שורות כפי שמוצג (שורות 46–50, 56–164). אין כפתור קיפול. H2 = `קטע מתוך הספר`. כתיב המסמך `היקוקומורי` / `היקוקמורי` — החי `היקיקומורי` |
| split + `eyal-portrait-garden.jpg` | SECTION 04 | פרוזה שורות 170–197. **אין** תמונה בסקשן |
| `.reveals` + 5 תמונות אתר + H3 שהומצאו + H2 «ולמי שפשוט…» / chap «בין השורות» | reveals + prose זנב | שורות 226–240 כרשימה כתובה. אין תמונות, אין H3 |
| CTA ביניים: «לרכישה מאובטחת (חשבונית ירוקה)» + «ספר אלקטרוני» + `temp_note` Morning — שניהם `mrng.to` | cta 09 | שורות 272–275: שתי שורות + `[לרכישת הספר](https://www.mendele.co.il/product/vekatavta/)`. אין GI, אין כפתור שני |
| FAQ CPT `faqblock cats:vekatavta` (7 שאלות, סדר אחר) | faqblock | 7 שאלות SECTION 10 שורות 281–300 **בסדר המסמך** |
| «רכישת הספר»: שתי גרסאות → שתיהן «לרכישה מאובטחת (חשבונית ירוקה)» / `mrng.to` | prose 06 | מודפסת: `(לפרטים והזמנה - צרו קשר)` בלי href. דיגיטלית: `[לרכישת הספר בגרסה דיגיטלית](https://www.mendele.co.il/product/vekatavta/)` |
| גלריה: עטיפה + 2 כרטיסי «ממתין לאישור» (April / Drive) | gallery pending | שורות 203–205 — טקסט בלבד, **אין** קבצים, **אין** מציינים |
| כתבות: קופסת «ממתין לאישור» + קישור `/press/` | pending-approval | שורה 306: `כתבות וראיונות על אייל עמית והספר (יתווספו בהמשך).` אין `/press/` |
| «עוד רגעים»: גוף + קופסת מדיה ממתינה | prose 14 + pending | שורות 347–354 — ארבע שורות, **אין** בקשת מדיה |
| CTA סופי: `לרכישת הספר` → `mrng.to` + `temp_note` GI; כותרת/גוף הפוכים | cta 12 | שורות 312–318 ככתבן + מנדלה |

**קישור המסמך שנבדק חי:** `https://www.mendele.co.il/product/vekatavta/` → **200**. אפס מופעי `mendele` ב-HTML החי. `mrng.to/MTUiO3vkIg` מופיע 8 פעמים (כפתורים + סכמה) — זה קישור **חבילת שלושת הספרים** (R1-16), לא הבייטים של העמוד הזה.

**מחוץ לסקואופ:** defaults של אחים · `chapters-render.php` / `videoblk.php` / `block-faq-list.php` / `inc/data/*.json` · JSON-LD `datePublished:2017` / `genre` / `price:79` (S007) · תפריט/פוטר משותפים · `WP-EI-01/02`.

**מלכודת לבנאי:** לא לגעת ב-`ea-faq-seed.json` ולא ב-`block-faq-list.php`. לא להעתיק `buy_*` / `mrng.to` מ-`kushi-blantis-defaults.php`. לא לרנדר SECTION 13. לא «לתקן» `היקוקמורי`. לא להמציא href על «צרו קשר». לא לפתוח WP-EI.

---

## 3. סיווג (שער 2) — אמנה §3א

`ברור` רק אם (א) מקור מצוטט **ו** (ב) בייטים מילה-במילה **ו** (ג) פעולה אחת. אין סקירה → כל פער מול החי **בהיקף**. תוספת צוות שאינה במסמך יורדת עם ההדבקה. מדיה בלי קובץ = `לא ברור` לאייל. כרום GI/Morning שאינו במסמך = תקדים BK-03 (הדבקת URL המסמך; הקופסה יורדת). אין סעיף לנימרוד: ל-06/09/12 יש בייטי מנדלה להדביק, בניגוד ל-BAG-06.

שנים/מספרים במסמך שמותר להדביק: **1998** · **46** · **252** · **כ־8 שנים** · **למעלה משני עשורים**. **79 ₪** ו-**2017** כתג/מחיר **אינם** במסמך — לא משאירים.

| # | הסעיף | (א) | (ב) | (ג) | סיווג | סטטוס |
|---|---|---|---|---|---|---|
| VKT-01 | תמונת הירו (כריכה) | SECTION 01 שורות 3–8 — תמונת כריכה בלי קובץ | אין קובץ/קישור | בחירה | **לא ברור** | ממתין לאייל |
| VKT-02 | גלריית SECTION 05 | SECTION 05 שורות 201–205 — טקסט בלי קובץ | אין קובץ. מצייני «ממתין» אינם במסמך | בחירה | **לא ברור** | ממתין לאייל |
| VKT-03 | סדר 01–14 + הירו בלי em/chap + הסרת כרום צוות + הדבקת 02–04 · 07–08 · 11 · 14 | SECTION 01–04 · 07–08 · 11 · 14 (שורות 10–13, 27–39, 56–164, 170–197, 226–266, 306, 347–354) | כן, המסמך ככתבו (כולל שבירת שורות ב-03 וכתיב `היקוקומורי`/`היקוקמורי`) | הדבקה אחת במקום הכרום. לא למחוק גלריה/CTA/FAQ כאן (VKT-02/04/05) | **ברור** | פתוח |
| VKT-04 | CTA רכישה מנדלה, בלי GI/Morning/79 | SECTION 01 שורה 15 · 06 שורות 211–220 · 09 שורות 272–275 · 12 שורות 312–318 | `[לרכישת הספר](https://www.mendele.co.il/product/vekatavta/)` ו-`[לרכישת הספר בגרסה דיגיטלית](…)` ככתבם. מודפסת = `צרו קשר` בלי href | כפתורי המסמך; `temp_note`/GI/79/`mrng.to` יורדים (תקדים BK-03) | **ברור** | פתוח |
| VKT-05 | שאלות נפוצות — 7 מהמסמך | SECTION 10 שורות 281–300 | שבע השאלות ככתבן, בסדר המסמך | `faq-inline`, בלי `block-faq-list.php` | **ברור** | פתוח |

**ברור: 3 · לא-ברור נימרוד: 0 · לא-ברור אייל: 2**

אין `ASK_NIMROD`. מדיה ממתינה לאייל אינה עצירת נימרוד.

---

## 4. פעולות הדבקה `ברור` (פאזה ב׳ אחרי `GO BUILD R1-19`)

רק `vekatavta-defaults.php`. Provenance על כל מחרוזת. לא לגעת ב-`muzza-defaults.php` / `kushi-blantis-defaults.php` / `tsva-bekahol-defaults.php` / `chapters-render.php` / `block-faq-list.php` / `videoblk.php`.

1. **VKT-03** — H1 = `וכתבת` **בלי** `<em>`, בלי chap `ספר`. תת: שתי השורות 12–13 ככתבן (לא חיבור בנקודה). סדר המסמך. H2 = השם העברי אחרי `## SECTION NN –` ל-02, 03, 04, 07, 08, 11, 14 (לא «Hero» / «תוכן» / «CTA סופי» / משפט ראשון שהועלה ל-H2). 02: `ל<strong>וכתבת</strong>` עם הלמ״ד מחוץ ל-strong. 03: כל הקטע עם שבירת השורות של המסמך; בלי `collapsible` / «לחצו לקריאת הקטע המלא»; כתיב `היקוקומורי`/`היקוקמורי` ככתבו — **לא** `היקיקומורי`. 04: פרוזה, בלי split/פורטרט גן. 07: שש הפסקאות כרשימה כתובה, בלי `.reveals` ובלי H3; בלי בלוק «בין השורות». 08 ככתבו. 11: המשפט שורה 306 בלבד — בלי קופסת ממתין ובלי `/press/`. 14: ארבע השורות, בלי קופסת מדיה. לא לגעת בגלריה/CTA/FAQ בסעיף הזה מעבר לשיבוץ בסדר.
2. **VKT-04** — הירו: `לרכישת הספר` → `https://www.mendele.co.il/product/vekatavta/` (`target=_blank` לפי DEV NOTES), **בלי** `· 79 ₪`. 06: שתי הגרסאות ככתבן; מודפסת בלי קישור קנייה; דיגיטלית = תווית+URL מנדלה. 09: שתי השורות + כפתור אחד `לרכישת הספר` → מנדלה; בלי כפתור «ספר אלקטרוני», בלי `temp_note`. 12: שורות 312–318 ככתבן + אותו כפתור. למחוק `price` 79 / `buy_*` ל-`mrng.to`. WP-EI-01 לא נפתח.
3. **VKT-05** — שבע השאלות בסדר המסמך כ-`faq-inline`. לא `faqblock` / CPT / `block-faq-list.php`.

מדיה (VKT-01/02) **לא** בבנייה עד בחירת אייל. לא ממציאים תמונת כריכה/גלריה. לא ממציאים מחיר. לא משאירים כרטיס ריק (אין תוכן = אין רכיב) — מצייני הגלריה יורדים ב-VKT-02 אחרי הבחירה; עד אז לא מרנדרים סלוט ריק ב-GO BUILD של הברור.

---

## 5. מה לא נגענו בו

- PHP חי · xlsx · git · FTP · `chapters-render.php` · `tpl-chapters-page.php` · `videoblk.php` · `block-faq-list.php` · `inc/data/*.json` · `muzza-defaults.php`
- `kushi-blantis-defaults.php` · `tsva-bekahol-defaults.php`
- תפריט/פוטר משותפים · מובייל (סבב 3) · meta/SEO / JSON-LD (S007)
- `WP-EI-01` / `WP-EI-02`
