# MANDATE — team_100 subagent · R1-06 `/learning/` · gates 1–4 (wave 1)

**סקואופ נעול (team_00):** `סקואופ אושר: R1-06, R1-10, R1-16, R1-20, R1-21, R1-22, R1-23, R1-24 בלבד`
**העמוד הזה בלבד:** R1-06 `/learning/` · http://eyalamit-co-il-2026.s887.upress.link/learning/
**בנאי:** Cursor Grok · **לא מאמת.** Iron Rule #1.
**דין:** `_COMMUNICATION/team_100/S006/S006-MILESTONE-CHARTER.md` §2 · §3א · §3ג · §8ד
**פקודות:** `_COMMUNICATION/team_100/S006/HANDOFF-TEMPLATE-GENERIC.md` v3.1.0
**דסקטופ בלבד.** TLS סטייג'ינג פג בכוונה — `curl -k` מותר כאן בלבד.

## חומר

`מקור החומר: אין` — אין תיקייה ייעודית תחת `EyalAmit_Site_GoogleDrive_Sync/content 13.8.26/` ואין `סקירה לימוד`.
אין «אין הערה = מאושר». לא ממציאים עותק. חי: 200. בתפריט החי «לימוד והכשרה» הוא כפתור דרופדאון בלי href ל-`/learning/`.

ערוץ כתיבה (רק אם יש `ברור` אחרי סיווג — לא צפוי בלי חומר):
`site/wp-content/themes/ea-eyalamit/inc/chapters/defaults/learning-defaults.php`

## פאזה א׳ — קליטה + סיווג בלבד (חובה עכשיו)

1. קראו את ה-defaults החיים + HTML הסטייג'ינג (`curl -sk`).
2. חפשו md/docx/xlsx בדרייב שעשויים להיות ל-hub הזה (כולל `דפים שלא אונדקסו`). אל תכתבו מ-`docs/project/eyal-ceo-submissions-and-responses/from-eyal/`.
3. סיווגו לפי אמנה §3א. מדיה בלי קובץ מאייל = `לא ברור`. תאריכים/שמות בלי מקור מצוטט = `לא ברור`.
4. כתבו:
   - `_COMMUNICATION/team_100/S006/RESEARCH-R1-06-LEARNING-2026-08-18.md`
   - `_COMMUNICATION/team_100/S006/tracker/r1-06-items.json`
5. שורת סיכום ראשונה ב-RESEARCH חייבת להיות אחת מ:
   - `RECOMMEND: FREEZE` + סיבה
   - `RECOMMEND: BUILD` + כמה `ברור` / כמה `לא ברור` לנימרוד / כמה לאייל
   - `RECOMMEND: ASK_NIMROD` + רשימת שאלות

סכימת JSON = כמו `_COMMUNICATION/team_100/S006/tracker/r1-04-items.json`. קידומת סעיפים: `LRN-`.
`לא ברור` לאייל: משפט אחד ב-`מה נדרש ממך` + `_picks`. אל תכתבו עמודות אנוש.

## אסור

טאב/xlsx · `chapters-render.php` · `tpl-chapters-page.php` · `videoblk.php` · `block-faq-list.php` · `inc/data/*.json` · defaults של עמוד אחר · FTP · `git commit` / `git push` · wp-admin.
אל תגעו ב-R1-01…R1-05 / R1-26 או בסעיפים ממתינים לאייל (H/T/MTH/LSN/SH/M-*).

## פאזה ב׳ — בנייה

רק אם האורקסטרטור כתב במפורש `GO BUILD R1-06` אחרי פאזה א׳. עד אז **אל תערכו PHP**.
Provenance על כל מחרוזת: `/* S006 · מקור: … */`. אין תוכן = אין רכיב.
