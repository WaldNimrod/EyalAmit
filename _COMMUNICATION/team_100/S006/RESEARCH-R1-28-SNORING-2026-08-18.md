RECOMMEND: BUILD · ברור 1 · לא-ברור נימרוד 0 · לא-ברור אייל 4

# RESEARCH — R1-28 `/snoring-sleep-apnea/` · פאזה א׳ בלבד · 2026-08-18

**צוות:** 100 (S006 page researcher) · **לא בנאי · לא מאמת**
**סקואופ נעול:** `סקואופ אושר: R1-17, R1-18, R1-19, R1-25, R1-28 בלבד` + הקפאת `R1-07, R1-08, R1-09, R1-27`
**עמוד:** R1-28 `/snoring-sleep-apnea/` בלבד · http://eyalamit-co-il-2026.s887.upress.link/snoring-sleep-apnea/
**דין:** אמנה §2 «אין סקירה» · §3א נעילת סיווג · §3ג · מנדט `file:///Users/nimrod/Documents/AOS_V5/EyalAmit.co.il-2026/_COMMUNICATION/team_100/S006/MANDATE-R1-28-SNORING-BUILD-2026-08-18.md`
**פאזה ב׳:** לא. אין `GO BUILD R1-28`. אפס עריכות PHP / xlsx / git / FTP / אחים.

סעיפי טרקר: `file:///Users/nimrod/Documents/AOS_V5/EyalAmit.co.il-2026/_COMMUNICATION/team_100/S006/tracker/r1-28-items.json` · קידומת `SNR-`.

`WP-EI-03` **נשאר ON_HOLD.** החבילה **לא נפתחה.** מכבי.jpg / יוני.jpg / אישור ניסוח רפואי = סעיפי המתנה לאייל כאן, לא המצאה.

---

## 1. חומר

SSOT (ה-md הוא הרצוי; אין קובץ סקירה → «אין הערה = מאושר» **אינו חל**):

`file:///Users/nimrod/Documents/AOS_V5/EyalAmit.co.il-2026/EyalAmit_Site_GoogleDrive_Sync/content 13.8.26/נחירות ודום נשימה/snoring-sleep-apnea-didgeridoo-CHECKED.md`

| בדיקה | תוצאה |
|---|---|
| תיקיית `נחירות ודום נשימה/` | **כן** — קובץ יחיד `snoring-sleep-apnea-didgeridoo-CHECKED.md` (41 046 בתים, 18 סקשנים, 751 שורות) |
| `סקירה נחירות` ליד הסנכרון | **אין.** קובץ סקירה יחיד בחבילה: `file:///Users/nimrod/Documents/AOS_V5/EyalAmit.co.il-2026/EyalAmit_Site_GoogleDrive_Sync/סקירה דף הבית.xlsx` |
| מדיה בתיקייה (jpg/png/mp4) | **אין** — כולל הקבצים שהמסמך קורא «קובץ שסופק»: `מכבי.jpg` · `יוני.jpg` |
| DEV NOTES | בתוך אותו md (לא קובץ נפרד) |
| `from-eyal/` | **לא נפתח** (אמנה §2) |
| `WP-EI-03` | ON_HOLD ב-`_aos/roadmap.yaml` (`hold_tracker_ref: R1-28`) — **לא נפתח** |

ערוץ כתיבה (פאזה ב׳ בלבד): `file:///Users/nimrod/Documents/AOS_V5/EyalAmit.co.il-2026/site/wp-content/themes/ea-eyalamit/inc/chapters/defaults/snoring-sleep-apnea-defaults.php`

כותרת ה-defaults עדיין מצטטת את נתיב הגיבוי `from-eyal/`. מקור הפאזה ב׳ = קובץ הדרייב למעלה, לא הגיבוי.

**אחים בגל (לא לגעת):** `treatment-defaults.php` · `method-defaults.php` · `about-defaults.php` · `contact-defaults.php` · וכל defaults אחר.

---

## 2. חי מול md

**HTTP:** `curl -sk` → **200** · canonical `/snoring-sleep-apnea/` · `<title>` «דיג׳רידו, נחירות ודום נשימה בשינה» (meta = S007, מחוץ לסקואופ). TLS סטייג'ינג פג בכוונה. `X-Robots-Tag: noindex, nofollow`.

**ערוץ החי = defaults.** גוף SECTION 01–17 **כבר מילה-במילה** מה-md (פורט WP-S4-01). הפער הוא כרום צוות + מדיה/אישור חסרים + שורת מקור ריקה.

סדר המסמך (רצוי): **01 הירו → 02 TOC → 03 הקשר → 04 מחקר → 05 מכבי → 06 נחירות → 07 פתרונות → 08 תרגול → 09 איך אני עובד → 10 יוני → 11 למי מתאים → 12 CPAP → 13 FAQ → 14 CTA → 15 אודות → 16 דיסקליימר → 17 מקורות.** SECTION 18 = הערות מתכנת (**לא תוכן אתר**).

סדר החי תואם. H1 תואם שורה 11, בלי `<em>`.

| בלוק חי (דסקטופ) | ב-defaults | במסמך |
|---|---|---|
| תגי `chap` ×18 (נחירות ודום נשימה בשינה / תוכן העניינים / הקשר / מחקר / …) | `phero.chap` + כל `sections[].chap` | כותרת-סקשן ב-DEV NOTES בלבד, לא ב-«התוכן» |
| H1 `נחירות ודום נשימה בשינה: גישה טיפולית באמצעות דיג'רידו` | `phero.title` | תואם שורה 11 |
| תת משלוש הפסקאות מחוברות | `phero.sub` | שורות 14–18. אין תמונת הירו בחי (אין `<img>` ב-`<main>`) |
| משפט מפתח מודגש + CTA `רוצה לדבר איתי` → `/contact/` | S01b + `phero.cta_*` | שורות 21–24 |
| H2 מומצא `מה יש בעמוד הזה` מעל TOC | `sections` S02 `title` | SECTION 02: רשימת עוגנים שורות 40–50 בלבד. אין H2 הזה |
| TOC 11 עוגנים | S02 `nav.toc` | תואם שורות 40–50 |
| גוף 03–09, 11–12, 15–16 | prose | תואם «התוכן» |
| FAQ 6 שאלות כ-`<details>` | `part=dd` | SECTION 13 שורות 525–571. סדר+ניסוח תואמים. DEV NOTES: אקורדיון — כבר כך |
| H2 כפול `אז איך יודעים אם זה יכול להתאים גם לכם?` (פרוזה + קופסת CTA) | S14 prose + S14b `cta` | H2 אחד (שורה 578) + גוף + כפתור שורות 610–611 |
| באנר `.ea-pending-approval` מעל סיפור יוני (מזכיר WP-EI-03) | S10 `body` prefix | **אין** במסמך. גוף הסיפור שורות 384–437 **כן** במסמך, ומוצג חי |
| כרטיס ממתינה `מכבי.jpg` + H2 `צילום מסך מאתר מכבי` + lead «יוטמע כאן…» | S05b `gallery` pending | IMAGE שורות 147–150: שם קובץ + ALT + CAPTION. **אין** H2/lead האלה. הקובץ **לא** בתיקייה |
| כרטיס ממתינה `יוני.jpg` | S10b `gallery` pending | IMAGE שורות 439–442. הקובץ **לא** בתיקייה |
| מקור שלישי «להשלמה לפני פרסום» | S17 `body` bullet 3 | שורה 671 — TODO, לא URL |
| SECTION 18 | לא רונדר (נכון) | הערות מתכנת שורות 676–751 |

**קישורי המסמך שנבדקו חי:** `/contact/` `/treatment/` `/method/` `/eyal-amit/` — כולם **200**. מכבי **200**. BMJ מה-curl **403** (חסימת בוט; ה-URL הוא הבייטים שורות 123 ו-669 — לא מחליפים). המסמך כותב `/contact` בלי סלאש סופי; החי עם `/contact/` — שניהם 200, לא המצאת נתיב.

**מדיה אופציונלית בלי קובץ (לא נפתח סעיף):** DEV NOTES שורה 8 «מומלץ… תמונה אותנטית של אייל עם דיג'רידו» · שורה 281 «אפשר לשלב וידאו» · שורה 320 «אפשר לשלב תמונה מהסטודיו» · שורה 623 «מומלץ… פורטרט». **לא רונדר.** אין תוכן = אין רכיב. תקדים R1-11 אייקונים.

**מחוץ לסקואופ:** defaults של אחים · `chapters-render.php` / `tpl-chapters-page.php` / `videoblk.php` / `block-faq-list.php` / `inc/data/*.json` · תפריט משותף · מובייל · meta/SEO/Schema (S007; SECTION 18 סעיפים 10–11, 14) · דפים עתידיים בהערות HTML (שורות 202, 267, 305, 697–704) · חבילת `WP-EI-03`.

**מלכודת לבנאי:** לא למחוק את סיפור יוני ואת שני כרטיסי ההמתנה. לא להמציא jpg. לא לפתוח את חבילת `WP-EI-03`. לא לגעת ב-`block-faq-list.php`. לא לרנדר SECTION 18. Provenance = נתיב הדרייב, לא `from-eyal/`.

---

## 3. סיווג (שער 2) — אמנה §3א

`ברור` רק אם (א) מקור מצוטט **ו** (ב) בייטים מילה-במילה **ו** (ג) פעולה אחת. אין סקירה → כל פער מול החי **בהיקף**. תוספת צוות שאינה במסמך יורדת עם ההדבקה **אלא אם** המנדט נועל אותה כהמתנה לאייל (יוני/מכבי + באנר האישור). מדיה בלי קובץ = `לא ברור` לאייל. תאריכים/מספרים **במסמך** (1999 · 2006 · 2014 · 250) → מותר להשאיר.

| # | הסעיף | (א) | (ב) | (ג) | סיווג | סטטוס |
|---|---|---|---|---|---|---|
| SNR-01 | צילום מסך מכבי | SECTION 05 שורות 147–150 · SECTION 18 שורות 710–715 | שם הקובץ במסמך; **הקובץ חסר** בתיקייה | בחירה/מילוי | לא ברור | ממתין לאייל |
| SNR-02 | תמונת ההתכתבות בסיפור יוני | SECTION 10 שורות 439–442 · SECTION 18 שורות 717–721 | שם הקובץ במסמך; **הקובץ חסר** | בחירה/מילוי | לא ברור | ממתין לאייל |
| SNR-03 | אישור פרסום סיפור יוני | SECTION 10 שורות 384–437 במסמך; באנר החי **אינו** במסמך; מנדט = אישור ניסוח רפואי | בייטי הסיפור כן; אישור הפרסום אין | בחירה | לא ברור | ממתין לאייל |
| SNR-04 | מקור רפואי שלישי | SECTION 17 שורה 671 | TODO בלי URL — אין בייטים להדביק כמקור | בחירה/מילוי | לא ברור | ממתין לאייל |
| SNR-05 | כרום צוות + H2 כפול + שורת TODO | SECTION 01–17 «התוכן» · S02 שורות 40–50 · S14 שורות 578–611 · S17 שורות 669–670 | כן, המסמך ככתבו (בלי תגי chap, בלי «מה יש בעמוד הזה», בלי H2 כפול, בלי שורת ההשלמה) | הדבקת כרום אחת; בלי לגעת ביוני/מכבי הממתינים | **ברור** | פתוח |

אין סעיפים לנימרוד. המתנת אייל ל-WP-EI-03 **אינה** עצירת נימרוד.

הסרת תגי `chap` / H2 המומצא ב-TOC / כותרת+lead של גלריית מכבי / H2 הכפול ב-CTA היא חלק מ-SNR-05: תוספת צוות שאינה במסמך. **לא** חלק מ-SNR-05: באנר יוני ושני כרטיסי ההמתנה (מנדט: לא מחיקה שקטה).

---

## 4. פעולות הדבקה `ברור` (לשער 4, רק אחרי `GO BUILD R1-28`)

1. **SNR-05** — גוף 01–17 כבר תואם; לא להחליף את הסיפור. להוריד כרום שהומצא: כל `chap` · `title` של S02 (`מה יש בעמוד הזה`) — להשאיר את רשימת העוגנים שורות 40–50 · `title`+`lead` של S05b (`צילום מסך מאתר מכבי` / «יוטמע כאן…») — **הכרטיס הממתין נשאר** עד SNR-01 · H2 הכפול ב-S14b (להשאיר H2 אחד + גוף + כפתור `[רוצה לדבר איתי](/contact)`) · שורת המקור השלישית «להשלמה לפני פרסום» (אין תוכן = אין רכיב; הרשומה = SNR-04). לא לרנדר SECTION 18. Provenance על כל מחרוזת שמשתנה: `content 13.8.26/נחירות ודום נשימה/snoring-sleep-apnea-didgeridoo-CHECKED.md`. FAQ כבר שש השאלות — לא לגעת ב-`block-faq-list.php`.

מדיה (SNR-01/02) ואישור יוני (SNR-03) **לא** בבנייה עד בחירת אייל. לא ממציאים jpg. לא פותחים `WP-EI-03`.

בנייה: רק `snoring-sleep-apnea-defaults.php` (+ חלק חדש ייעודי לעמוד אם חייבים). לא לגעת באחים.

---

## 5. שאלות לנימרוד

**אין.**

---

## 6. מה לא נגענו בו

- PHP / FTP / git / xlsx / `tracker_page_tab.py` / `tracker_update.py`
- `treatment-defaults.php` `method-defaults.php` `about-defaults.php` `contact-defaults.php` וכל defaults אחר
- `chapters-render.php` `tpl-chapters-page.php` `videoblk.php` `block-faq-list.php` `inc/data/*.json`
- חבילת `WP-EI-03` (`file:///Users/nimrod/Documents/AOS_V5/EyalAmit.co.il-2026/_COMMUNICATION/team_100/M-EYAL-INPUTS/WP-EI-03-LOD400-2026-07-15.md` — לא נקראה)
- תפריט/פוטר משותפים · מובייל · meta/SEO/Schema (S007) · דפים עתידיים מ-SECTION 18
- סעיפי אייל ממתינים בעמודים אחרים
