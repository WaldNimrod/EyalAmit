# S006 — אמנת אבן הדרך «דיוק תוכן מלא מול חומרי אייל»

**גרסה:** 1.0.0 · **נפתחה:** 2026-08-17 · **סמכות:** team_00 (נימרוד) · **מנהל:** team_100
**סטטוס:** ACTIVE — `_aos/roadmap.yaml` → `project.active_milestone: S006`

> המסמך הזה הוא **הבסיס המחייב הראשון** לכל עבודה תחת S006.
> סשן חדש של team_100 קורא אותו ואת הטרקר — ואינו זקוק לשום העברת ידע נוספת.
> אם משהו כאן סותר זיכרון סשן, הנחיה בעל־פה או הרגל קודם — **המסמך הזה גובר.**

---

## 1. למה אבן הדרך הזאת קיימת

עבודה רוחבית שרצה **במקביל** לפיתוח כבר דרסה תוכן מדויק יותר מפעם אחת. זו אינה השערה — זה מתועד
בקובץ הסקירה של אייל עצמו (דרייב: `Eyal Amit/סקירה דף הבית.xlsx`, 17.8.26):

> «הטקסט שמופיע באתר בכלל לא נכון»
> «קלוד המציא דברים ושכפל טקסטים»

לכן: **מקפיאים מצב, מתקנים במלואו, ולא נוגעים בשום נושא אחר** עד אישור אייל לכלל העמודים.

---

## 2. חוק התוכן — הסעיף החשוב ביותר במסמך

תוכן שנכנס לאתר יכול להיות **אחד משלושה בלבד**:

1. **מה שקיים כבר באתר** — כאשר אייל לא העיר עליו דבר.
2. **מה שהתקבל מאייל בקבצים** — דרייב `Eyal Amit/content 13.8.26/` וקבצי «סקירה …».
3. **מה שמתקבל מנימרוד ישירות במהלך העבודה** — כתוכן משלים.

**אין ניחוש. אין כתיבה עצמית. אין השלמת פערים. אין «שיפור» ניסוח.**

### ⚠ היעדר הערה הוא אישור — כלל מחייב (אייל, 17.8.26)

> «מה שאין לגביו הערות תקין. שים לב רק למצוי ורצוי.»

קובצי הסקירה של אייל בנויים משלוש עמודות: **מצוי** (מה שהוא ראה) · **רצוי** (מה שאמור להיות) ·
**הערות**. הכלל נגזר מהם ישירות:

| מצב בקובץ | המשמעות | מה מותר לנו |
|---|---|---|
| יש הערה בעמודת «הערות» | פגם שאייל סימן | לתקן לפי ההערה |
| **מצוי ≠ רצוי** | סדר או מבנה שגוי | לתקן לפי הרצוי |
| **מצוי = רצוי, אין הערה** | **אייל אישר. תקין.** | **לא לגעת. נקודה.** |

זה מחמיר על סעיף 2: לא די בכך שהתוכן מגיע ממקור לגיטימי — צריך גם **שאייל סימן שיש בעיה**.
שיפור עימוד, איחוד ניסוח או «התאמה ל-md» בבלוק שאייל לא העיר עליו הם **חריגה**, גם אם התוצאה
נראית טובה יותר.

**נגזרת לסיווג:** בלוק ללא הערה שבו מצוי = רצוי אינו יכול להיות `ברור` — הוא פשוט מחוץ להיקף.
לפני שמסווגים סעיף `ברור`, יש לאמת שהוא נשען על הערה מפורשת או על פער מצוי/רצוי.

חסר חומר, או שהחומר דו־משמעי → השורה בטרקר עוברת ל־**`הוקפא`** עם סיבה כתובה בעמודת
«הערות סוכן», והנושא עולה לנימרוד. **לא ממציאים.**

### Provenance — חובה על כל מחרוזת

כל מחרוזת שמשתנה מקבלת הערת מקור בקוד, בפורמט:

```php
/* S006 · מקור: content 13.8.26/דף הבית/homepage1-3 v2.md · שורה 42 */
/* S006 · מקור: סקירה דף הבית.xlsx · גיליון1!C8 */
/* S006 · מקור: נימרוד בסשן 2026-08-17 */
```

מחרוזת ללא הערת מקור = הפרה של חוק התוכן, ותיפסל בוולידציה.

---

## 3. הטרקר — מקור אמת יחיד למצב העמודים

**`EyalAmit_Site_GoogleDrive_Sync/EA-CONTENT-TRACKER.xlsx`**
עולה לדרייב מעצמו · [קישור](https://drive.google.com/file/d/1RHk2TwwZe9PNBE8BB_nl7yDJAdzZxK60/view)

טאבים: `README` · `מצב` · `סבב-1-ליבה` · `סבב-2` · `LOG`

### שתי הרשאות

| בעלים | עמודות | ערכי סטטוס שמותר לו לקבוע |
|---|---|---|
| **סוכן** | `#`, `סוג`, `נתיב`, `כותרת`, `קובץ תוכן`, `מקור חומר (אייל)`, `WP קשור`, `סטטוס מכונה`, `תאריך עבודה אחרון`, `ראיות QA`, `הערות סוכן` | `לא ידוע` → `טרם נבדק` → `בעבודה` → `הוגש לבדיקה` · ובכל שלב `הוקפא` **עם סיבה** |
| **אנוש** (נימרוד / אייל) | `סטטוס אישור`, `הערות נימרוד`, `הערות אייל`, `תאריך אישור` | `חזר לתיקונים` · `אושר ע״י נימרוד` · `אושר ע״י אייל` · `הוקפא` |

**סוכן לעולם אינו כותב בעמודת אנוש, ולעולם אינו קובע סטטוס אישור.** נקודה.

### האכיפה

- **UX:** dropdown-ים על שתי עמודות הסטטוס + נעילת עמודות הסוכן בממשק.
- **אכיפה אמיתית:** `scripts/tracker_guard.py` — משווה מול תצלום ה־CSV האחרון ופוסל:
  כתיבת סוכן לעמודת אנוש · סטטוס לא־חוקי · מעבר סטטוס אסור · `הוקפא` בלי סיבה ·
  מחיקת שורה · שינוי כותרות · עותק מתנגש של דרייב.
  בהפרה — עצירה + `TRACKER-GUARD-REJECT-<stamp>.md`, **ללא קומיט**.
- **רגרסיה:** `scripts/tracker_guard_selftest.py` — 11 מקרים, כולם חייבים לעבור.

### נוהל סשן — חובה

```bash
python3 scripts/tracker_guard.py --mode ingest    # בפתיחה: קליטת קלט אנושי חדש
# ... עבודה ...
python3 scripts/tracker_guard.py --mode verify    # לפני קומיט: אימות שלא נגענו באנוש
python3 scripts/tracker_snapshot.py               # תצלום ל-git
```

**מניעת התנגשות:** לפני חלון עבודה — לכתוב `agent <timestamp>` בתא `LOCK` בטאב «מצב»,
ולנקות בסיום. לפני כל כתיבה — קריאה מחדש + בדיקת `mtime`. אם דרייב ייצר עותק מתנגש —
ה־guard עוצר; **אין למזג לבד**, מגישים את ההפרש לנימרוד.

---

## 3א. שכבות העבודה הקבועות (החלטת team_00, 17.8.26)

זהו הנוהל המחייב לכל תיקון תוכן באבן הדרך, ללא יוצא מן הכלל.

**team_100 שומר על הנייטיב — אבל מנהל את כלל התהליכים בהרצת סוכני משנה לפי הקאנון.**
היות והתוכן הוא המוביל:

| מצב | סיווג | פעולה |
|---|---|---|
| ברור לגמרי **מה הכשל**, **מה התוכן הדרוש**, **ומה התיקון המתבקש** | `ברור` | **מבצעים באורקסטרציה — בלי לשאול.** |
| כל אחד מהשלושה אינו ברור | `לא ברור` | **אסקלציה לנימרוד.** הוא פוסק, או מעלה לאייל. |

**«מעלה לאייל» = רישום שורה בטאב של אותו עמוד** בקובץ המעקב, עם האפשרויות לבחירה.
לא הודעה בצ'אט שנעלמת — שורה שנשארת עד שמישהו בוחר.

הסיווג אינו הערה — הוא **ערך בעמודה `סיווג`**, והוא מה שקובע אם סוכן משנה מורץ או שהשורה
עוברת ל`ממתין להכרעת נימרוד` / `ממתין לאייל`.

---

## 3ב. טאב לכל עמוד בעבודה (החלטת team_00, 17.8.26)

**כל עמוד שנמצא בעבודה מקבל טאב משלו** בקובץ המעקב, ובו **כל הסעיפים של אותו עמוד** —
הכשל, התוכן הדרוש, התיקון, הסיווג, ומי צריך להכריע.

**עמוד שאושר — הטאב מוסתר** (`hidden`), **לא נמחק**: הרשומה של איך העמוד הגיע לאישור חייבת לשרוד.

טאבי הסבבים נשארים האינדקס ברמת העמוד; טאבי העמודים הם רמת הסעיף.

```bash
python3 scripts/tracker_page_tab.py --create R1-01 --items items.json
python3 scripts/tracker_page_tab.py --hide R1-01     # רק אחרי «אושר ע״י אייל»
python3 scripts/tracker_page_tab.py --list
```

ההסתרה מסרבת לפעול אם השורה אינה `אושר ע״י אייל` — אי אפשר להעלים עמוד מהעין בטעות.

---

## 4. סבבים

| סבב | היקף | מצב |
|---|---|---|
| **1 — ליבה** | 29 שורות: 21 פריטי התפריט הראשי החי + כל עמוד שאייל סיפק לו חומר | ACTIVE — `WP-S6-01` |
| **2** | כל שאר העמודים הזמינים לגולש שכבר קיימים: 54 פוסטים, 48 QR, legal, `/en`, כפילויות legacy (301 בלבד) | נפתח **רק** בסגירת סבב 1 — `WP-S6-02` |
| **3** | תוכן תומך: alt, meta, קידום, אופטימיזציה, עמודים חדשים | **אבן דרך נפרדת — S007**. ייתכן שנעלה לאוויר לפניה; ההחלטה תתקבל בגשר |

---

## 5. ערוץ כתיבת התוכן

**כותבים ל־** `site/wp-content/themes/ea-eyalamit/inc/chapters/defaults/<page>-defaults.php`

git-tracked · ניתן לשחזור לפרודקשן · ניתן לוולידציה חוצת־מנועים.

**לא כותבים דרך wp-admin/ACF.** אלה עריכות DB בסטייג'ינג בלבד, נמחקות ב-reseed,
ואין WP-CLI ב-uPress. ACF (WP-S4-05) היא שכבת override לשימוש **אייל**, לא ערוץ עבודה של סוכנים.

---

## 6. קוד — מתי מותר לגעת

תוכן בלבד. **כל ממצא קוד עובר את המסלול הזה, ללא יוצא מן הכלל:**

1. נרשם ב־`CODE-BLOCKED-REGISTER.md` — מה, איפה, איזו שורת טרקר נחסמת.
2. **שיפוט:** האם הוא באמת פוגע בהצגה נכונה של התוכן? רוב הממצאים — לא.
3. רק הפוגעים באמת מוצגים **לנימרוד** להחלטה סופית.
4. אחרי אישור — ליין **סוכן־משנה ייעודי**, לא בתוך סשן התוכן.

סוכן שמתקן קוד «תוך כדי» מפר את אבן הדרך.

---

## 7. מיון ההקפאה (החלטת team_00, 17.8.26)

| קטגוריה | טיפול |
|---|---|
| **תוכן** | `ON_HOLD` + `hold_reason` + `hold_tracker_ref`. נבחן **בשורת הטרקר שלו**, לא נפתח עצמאית. 9 חבילות: `S002-P001-WP001`, `S002-P001-WP002`, `WP-EI-01..07` |
| **קוד/באגים** | הנוהל בסעיף 6 |
| **SEO/meta** | `S002-P001-WP004` → הועברה ל־**S007**. לא הוקפאה — meta נכתב מול טקסט מאושר, ולכן אחרי S006 |
| **מעבר לפרודקשן** | `WP-S5-05` — `blocked_by: S006`. ירוץ בין סבב 2 לסבב 3, או אחרי סבב 3 |
| **בדיקת אנוש** | `WP-S5-04` — **תמיד אחרונה** לפני פרודקשן, חלק מאותו רצף סופי |

טרמינליים, לא נוגעים: 30 `COMPLETE`, 32 `DONE`, 8 `SUPERSEDED`.

---

## 8. מחזור יומי — חובה בסוף כל יום עבודה

```bash
python3 scripts/ftp_deploy_site_wp_content.py
```

```bash
node _aos/lean-kit/modules/validation-quality/scripts/qa/qa_probe.mjs --base http://eyalamit-co-il-2026.s887.upress.link --paths / --viewports desktop
```

### ⚠ FTP של uPress נעול לפי IP — זה תמיד כך

הפריסה תיפול ב-`TimeoutError: [Errno 60]` כשה-IP שלנו אינו פתוח. **זו אינה תקלה ואין מה לתחקר.**
סימן היכר: פורטים 21/22/990/2222 בטיימאאוט, בעוד HTTP לאותו IP מחזיר 403 והסטייג'ינג עצמו UP —
כלומר ההוסט נגיש והפורטים סגורים.

**מה עושים:** מציגים לנימרוד את הקישור הבא ומבקשים לפתוח את ה-IP הנוכחי. לא מנסים לעקוף.

https://my.upress.co.il/account/websites/eyalamit-co-il-2026.s887.upress.link?tab=development

- TLS של סטייג'ינג פג־תוקף **בכוונה** — `-k` מותר **כאן בלבד**, לעולם לא בפרודקשן.
### ⚠ כל פריסה עם שינוי CSS/JS — חובה להעלות `Version` ב-`style.css`

`wp_enqueue_style` שואב את הגרסה מ-`wp_get_theme()->get('Version')`. אם לא מעלים אותה,
כתובת ה-CSS לא משתנה ו**דפדפנים ממשיכים להגיש את הגרסה הישנה**. הבדיקות שלנו יעברו — אנחנו
מושכים עם cache-bust — בעוד שנימרוד ואייל יראו את הקוד הישן ויחשבו שזה באג שלנו.
קרה בפועל 17.8.26 (ראו C-09): 18 ציטוטים נראו חתוכים בעוד שהקוד היה תקין לגמרי.

- ⚠ **ה־harness-ים בפרויקט הזה נכשלים OPEN.** נדרשת **אסרציה חיובית** — «המחרוזת X נמצאת
  בעמוד» — ולא «לא נמצאה שגיאה». בדיקה שעברה בלי לאשר נוכחות טקסט אינה ראיה.
- E2E בדפדפן לכל עמוד שהוגש.
- ולידציה חוצת־מנועים (Iron Rule #1: builder ≠ validator) — **לכל אצווה מאושרת, לא לכל עמוד.**
- סגירה: `tracker_guard.py --mode verify` → `tracker_snapshot.py` → קומיט.

---

## 8-0. בחירת מנוע — לפי הקאנון, לא לפי הרגל. ואל תקימו תשתית

**החלטת team_00, 17.8.26** — אחרי ש-team_100 בנה עטיפות במקום להשתמש בקיים.

> «מה בדיוק אתם מוסיפים שחסר? כשצריך — מנוע שני ויותר לוגי — גרוק. גם קיים בקאנון שלנו.»

**בחירת מנוע אינה שיקול דעת.** הקאנון מחייב:
`agents-os/core/config/routing_policy.yaml` (dual-key team_00+team_100) הוא מפל
task-vector→engine מחייב, עם פותר מכני `core/modules/management/routing_policy.py::resolve()`.
**להשתמש בו — לא לגזור מחדש.** בניית argv ל-cursor-agent שייכת ל-`cursor_gateway.py`
(«NEVER hand-build cursor-agent argv»).

**המפל בפועל:** בנק Cursor (grok / composer / auto) לעבודה מכנית, בדיקות והרצות פשוטות ·
**Grok לוגיקה תחומה וממוקדת** · Claude בכיר שמור לאורקסטרציה, שיפוט וארכיטקטורה ·
**לעולם לא וריאנט `-fast` ללא בקשה נקודתית של team_00** (DEC-2026-08-14-1).

**דירוג מאמתים:** Grok / Composer / Gemini = **רשימת מועמדים בלבד**. GPT-5.x / Opus = verdict
מכריע. כלומר PASS מקומפוזר הוא ראיה — לא חותמת סופית לשער קריטי.

### ⚠ סטיות זמניות מהקאנון (17.8.26) — דווחו ל-team_120

סביבת AOS בשדרוג ואין פריסה חדשה צפויה; לכן פתרונות זמניים מתועדים במקום המתנה:

| הקאנון אומר | המצב בפועל | הפתרון הזמני |
|---|---|---|
| `cursor-grok-4.6-high` (hub CLAUDE.md) / `cursor-grok-4.5-high` (routing_policy) | **שניהם אינם ברשימה החיה.** `cursor-agent --list-models` מציע רק `cursor-grok-4.6-high-fast` | משתמשים ב-`-fast` תחת הוראת team_00 המפורשת «אם יש בעיות — תעברו לגרוק». חריגה מ-DEC-2026-08-14-1, מתועדת |
| `cursor_gateway.py` בונה את ה-argv | הספוק מריץ `scripts/run_cross_engine_validator.sh` שבונה ידנית | נשאר בשימוש (הוא ה-WIRED path של `/AOS_dispatch`), אבל `--mode` תוקן — ראו למטה |

**הקאנון מחייב לקרוא את מזהי המודלים חי** (`cursor-agent --list-models`) ולא לקודד אותם קשיח.
זה בדיוק מה שחשף את ההתיישנות.

---

## 8א. ⚠ אימות טכני = ליין קומפוזר. team_100 לא בודק בעצמו

**החלטת team_00, 17.8.26** — אחרי ש-team_100 הפר את הכלל הזה בסבב הראשון.

קומפוזר (`cursor-agent`, Cursor) הוא **המנוע הקאנוני לאימות** בפרויקט הזה. כל סבב בדיקה —
DOM, CSS, פריסה, ניגודיות, קישורים, רגרסיה — רץ כליין קומפוזר, **לא בסשן של team_100**.

```bash
scripts/run_cross_engine_validator.sh <prompt-file> composer-2.5 "$PWD" <out-file>
```

העטיפה מזהה פלט ריק / כשל / timeout ומריצה מחדש — cursor-agent מחזיר לעתים תשובה ריקה,
שאחרת הייתה נקראת כ«PASS ללא ממצאים».

**שלושה כללי הפעלה שנלמדו בשטח (17.8.26) — בלעדיהם הליין מחזיר ריק:**

1. **`--mode` היה מקודד קשיח ל-`plan` — וזה היה שגוי.** שני המצבים היחידים (`plan`, `ask`) הם
   קריאה־בלבד ו**אף אחד מהם אינו יכול להריץ shell**. מאמת שנדרש לאסוף ראיות בעצמו החזיר לכן
   פלט ריק. **תוקן 17.8.26:** הדגל הוסר, וברירת המחדל היא המצב הסוכני שכן מריץ shell.
   `AOS_VALIDATOR_MODE=plan|ask` רק כשהמנדט הוא ניתוח טהור על קבצים שכבר בדיסק.
   *(תקלה נלווית: macOS מריץ bash 3.2, שבו `set -u` נופל על הרחבת מערך ריק — הדגל מועבר
   כסקלרים ולא כמערך.)*
   כשכן משתמשים במצב קריאה־בלבד: team_100 **לוכד** ראיות ל-`_COMMUNICATION/team_90/evidence-<WP>/`
   והמאמת **מנתח**. לכידה אינה ניתוח — החלוקה נשמרת.
2. **ליין ממוקד לכל נושא.** מנדט שדרש קריאת ~400KB HTML מחמישה קבצים החזיר פלט ריק פעמיים.
   פיצול לשלושה ליינים — קובץ אחד או שניים לכל אחד — עבד מיד. עדיף שלושה ליינים קצרים
   מאחד גדול שנופל.
3. **תמיד לתת לו את מלכודות המדידה מראש.** לדוגמה: הקרוסלה מדפיסה כל כרטיס פעמיים, ולכן יש
   לספור אנשים ייחודיים ולא אלמנטים. בלי האזהרה הזו מאמת סביר ידווח על הכפלה שגויה.
   ⚠ **אבל לא לתת לו את המסקנה** — רק את המלכודת ואת מה שצריך לספור.

**למה זה חשוב פעמיים:**
1. **Iron Rule #1** — הבנאי אינו המאמת. כש-team_100 מאמת בנייה שהוא עצמו הזמין, אין כאן
   ולידציה חוצת-מנועים בכלל.
2. **כלכלת טוקנים** — בדיקה טכנית ישירה שורפת קונטקסט של המנהל. team_100 חייב להחזיק
   תהליך של עשרות עמודים; הוא לא יכול לבזבז קונטקסט על `getComputedStyle`.

**מה team_100 כן עושה:** מנסח את המנדט · מריץ את הליין · **קורא את ה-verdict בלבד** ·
רושם בטרקר · מנתב החלטות. הוא אינו טוען גופי עמודים, אינו מריץ JS בדפדפן, ואינו קורא CSS.

---

## 8ב. היקף מסך — סבבים 1 ו-2 הם דסקטופ בלבד

**החלטת team_00, 17.8.26, לייעול.** כל נושא **מובייל ורספונסיב** של האתר כולו עובר ל**סבב 3**.

- סבבים 1–2: אימות ואישור **בדסקטופ בלבד**. `qa_probe --viewports desktop`.
- ממצא מובייל שעולה תוך כדי — **נרשם לסבב 3 ולא מטופל**, אלא אם הוא שובר גם את הדסקטופ.
- אישור אייל בסבבים 1–2 הוא אישור דסקטופ. מובייל ייבדק כיחידה אחת בסבב 3.

---

## 8ג. 🔑 עקרון-על — כלכלת טוקנים ופירוק למשימות קטנות

**החלטת team_00, 17.8.26. חל על שלושת השלבים, ואינו נתון לשיקול דעת.**

> «יש לנו ליין ארוך מאוד וטכני מאוד שצריך מנהל טוב ששובר אותו להמון משימות מוגדרות וקטנות.»

לפנינו ~160 עמודים בשלושה סבבים. סשן team_100 שמבצע עבודה טכנית בעצמו נגמר אחרי חמישה עמודים.
לכן:

1. **team_100 מפרק, לא מבצע.** כל יחידה = משימה **אחת, מוגדרת, קטנה, בעלת קריטריון קבלה מדיד**.
   «לתקן את דף הבית» אינה משימה. «להחליף esc_html ב-ea_chapters_kses_e בארבעה שדות ולהזין את
   שתי שורות ה-trust» היא משימה.
2. **מנוע לפי המפל, לא לפי הרגל** — §8-0. עבודה מכנית לא הולכת ל-Claude בכיר.
3. **ליין ממוקד לכל נושא.** מנדט רחב מדי מחזיר פלט ריק (הוכח 17.8.26).
4. **team_100 קורא verdict, לא ראיות גולמיות.** לא לטעון HTML, לא להריץ JS בדפדפן, לא לקרוא CSS.
5. **מדד ההצלחה של המנהל אינו כמה בדק — אלא כמה עמודים עברו לאייל ליחידת קונטקסט.**

---

## 9. משמעת team_100 — אורקסטרציה בלבד

team_100 מחזיק **רק**: הטרקר · ה־roadmap · רגיסטר הקוד־החסום · ניתוב החלטות · שיפוט.
**team_100 אינו כותב תוכן עמודים.**

- לכל עמוד/אצווה → מנדט לליין בנייה (`team_10`/`team_110`).
- ולידציה → `team_90`/`team_50`, **במנוע שונה מהבנאי**.
- **חיסכון קונטקסט (קריטי לאבן דרך ארוכה):** team_100 אינו טוען גופי עמודים ואינו קורא
  קבצי `*-defaults.php` במלואם. הוא קורא את הטרקר ואת ה־verdict artifacts בלבד.
- **הנדאוף:** ב־~80% קונטקסט, עם **תוספת ספוק כתובה־ביד**. הנדאופים של `prompt-generate`
  אינם אמינים בספוק הזה (team_120) — לעולם לא להסתמך עליהם לבדם.
- נימרוד יתריע מראש לפני נקודת ההנדאוף.

---

## 10. הגדרת «סיום»

אבן הדרך נסגרת כאשר **כל שורה** בסבב 1 ובסבב 2 קוראת `אושר ע״י אייל` —
או `הוקפא` עם סיבה כתובה שנימרוד קיבל במפורש.

לא לפני. לא חלקית. לא «רוב העמודים».
