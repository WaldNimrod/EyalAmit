RECOMMEND: BUILD · ברור 3 · לא-ברור נימרוד 0 · לא-ברור אייל 2

# RESEARCH — R1-10 `/shop/` · פאזה א׳ בלבד · 2026-08-18

**צוות:** 100 (S006 page researcher) · **לא בנאי · לא מאמת**
**סקואופ נעול:** `סקואופ אושר: R1-06, R1-10, R1-16, R1-20, R1-21, R1-22, R1-23, R1-24 בלבד`
**עמוד:** R1-10 `/shop/` · http://eyalamit-co-il-2026.s887.upress.link/shop/
**דין:** אמנה §2 «אין סקירה» · §3א נעילת סיווג · מנדט `file:///Users/nimrod/Documents/AOS_V5/EyalAmit.co.il-2026/_COMMUNICATION/team_100/S006/MANDATE-R1-10-SHOP-BUILD-2026-08-18.md`
**פאזה ב׳:** לא. אין `GO BUILD R1-10`. אפס עריכות PHP / xlsx / git / FTP / ילדי תפריט.

סעיפי טרקר: `file:///Users/nimrod/Documents/AOS_V5/EyalAmit.co.il-2026/_COMMUNICATION/team_100/S006/tracker/r1-10-items.json` · קידומת `SHP-`.

---

## 1. חומר

SSOT (ה-md הוא הרצוי; אין קובץ סקירה → «אין הערה = מאושר» **אינו חל**):

`file:///Users/nimrod/Documents/AOS_V5/EyalAmit.co.il-2026/EyalAmit_Site_GoogleDrive_Sync/content 13.8.26/כלים למכירה/buy didgeridoo.md`

| בדיקה | תוצאה |
|---|---|
| תיקיית `כלים למכירה/` | **כן** — קובץ יחיד `buy didgeridoo.md` (16 048 בתים, 10 סקשנים) |
| `סקירה חנות` / `סקירה כלים` ליד הסנכרון | **אין.** קובץ סקירה יחיד: `file:///Users/nimrod/Documents/AOS_V5/EyalAmit.co.il-2026/EyalAmit_Site_GoogleDrive_Sync/סקירה דף הבית.xlsx` |
| מדיה בתיקייה (jpg/png/mp4/קישור קובץ) | **אין** |
| ילדי חומר נפרדים (תיקים / סטנדים / תיקון / סטנד רצפתי) | קיימים **כתיקיות אחרות** — שייכים ל-R1-11…R1-15, **מחוץ לסקואופ** |
| `from-eyal/` | **לא נפתח** (אמנה §2) |

**הערת מיפוי (לא סעיף טרקר):** אותה תיקייה `כלים למכירה` רשומה גם על R1-12 `/didgeridoos/` בטרקר. החי ב-`/didgeridoos/` כבר מכיל את 10 הסקשנים (עם תוספות צוות: `em`, bleed, `faqblock` CPT, גלריה ממתינה). המנדט נועל את ההדבקה ל-`/shop/` בלבד; ילדים לא נערכים. כפילות אפשרית תטופל ב-R1-12, לא כאן.

---

## 2. חי מול md

**HTTP:** `curl -sk` → **200** · canonical `/shop/` · כותרת מסמך «עמוד קטלוג ראשי». TLS סטייג'ינג פג בכוונה.

**ערוץ החי:** `file:///Users/nimrod/Documents/AOS_V5/EyalAmit.co.il-2026/site/wp-content/themes/ea-eyalamit/inc/chapters/defaults/shop-defaults.php`

החי = הקטלוג ב-defaults. **אפס** מ-10 הסקשנים של המסמך.

| בלוק חי (דסקטופ) | ב-defaults | במסמך |
|---|---|---|
| תג `חנות` | `phero.chap` | **אין** |
| H1 `כלים ואביזרים לדיג'רידו` | `phero.title` | H1 אחר: `כלי דיג'רידו למכירה - כלים בעבודת יד` (שורה 20) |
| תת `כלים בעבודת יד, תיקים, סטנדים, וכן שירות תיקון וחידוש.` | `phero.sub` | **אין** תת כזה |
| מדיה הירו | אין | DEV NOTES שורה 8 מבקשים תמונת רקע **בלי קובץ** |
| `המוצרים` / `כל המוצרים` + גריד `bookcard` | `sections[0]` | **אין** גריד איקומרס. DEV NOTES: «לא להיראות כמו חנות איקומרס» |
| 5 כרטיסים + `מחיר לפי התאמה` + `לעמוד המוצר ←` | `$ea_shop_defs` + postmeta | **אין** מחירים. סטנד רצפתי **אינו** ב-SECTION 07 |
| FAQ / המלצות / CTA-י המסמך | **אין** | SECTION 08–10 במסמך |

**כרטיסים חיים (כולם תוספת צוות):**

1. כלי דיג'רידו למכירה → `/didgeridoos/` — «כלים בעבודת יד, מותאמים לצליל ולנשימה.»
2. תיקים לדיג'רידו → `/bags/` — «הגנה ונשיאה נוחה לכלי, בעבודת יד.»
3. סטנדים לאחסון דיג'רידו → `/stands-storage/` — «תלייה או עמידה — אחסון יציב ובטוח.»
4. סטנד רצפתי לנגינה בישיבה נמוכה → `/stand-floor/` — «יציבות ונוחות לנגינה בישיבה על הרצפה.»
5. תיקון וחידוש דיג'רידו → `/repair/` — «שירות תיקון מקצועי לכלי דיג'רידו.»

**קישורי המסמך שנבדקו חי:** `/contact/` `/treatment/` `/method/` `/lessons/` `/sound-healing/` `/repair/` — כולם **200**.
`/instruments/` → **301** → `/tools-and-accessories/instruments/` (**200**, עמוד סבב-2 R2-025, לא `/bags/` ולא `/stands-storage/`). הבייטים במסמך הם `/instruments` — הדבקה מילה-במילה, בלי המרת נתיב (המרה ל-`/bags/` = המצאה).

**מחוץ לסקואופ:** defaults של `didgeridoos` / `bags` / `repair` / `stands-*` · תבנית משותפת · תפריט (`block-topnav.php` / `section-nav.php`) · meta/SEO (S007).

**מלכודת לבנאי:** לא להעתיק מ-`didgeridoos-defaults.php`. שם כבר הודבק המסמך **עם** `em` ב-H1, רצועת bleed, `faqblock cats:didgeridoos`, גלריית `pending`, ו-SECTION 06 בכפילות (steps+prose). זה R1-12, לא R1-10.

---

## 3. סיווג (שער 2) — אמנה §3א

`ברור` רק אם (א) מקור מצוטט **ו** (ב) בייטים מילה-במילה **ו** (ג) פעולה אחת. אין סקירה → כל פער מול החי **בהיקף**. תוספת צוות שאינה במסמך יורדת עם ההדבקה (כמו כרטיסי `.reveals` ב-R1-04). מדיה בלי קובץ = `לא ברור` לאייל. «26 שנים» / «מוקש דהימן» — **במסמך** (SECTION 02 שורות 60, 65) → מותר להדביק.

| # | הסעיף | (א) | (ב) | (ג) | סיווג | סטטוס |
|---|---|---|---|---|---|---|
| SHP-01 | תמונות הירו + «מי בונה» | SECTION 01 שורה 8 · SECTION 02 שורה 44 | אין קובץ/קישור | בחירה | לא ברור | ממתין לאייל |
| SHP-02 | תמונות קטנות ליד אביזרים | SECTION 07 שורה 275 | אין קובץ | בחירה | לא ברור | ממתין לאייל |
| SHP-03 | סדר 01–10 + הירו בלי em + הסרת גריד/מחירים | SECTION 01–10 (שורות 20–448) | כן, המסמך ככתבו | הדבקה אחת במקום הקטלוג | **ברור** | פתוח |
| SHP-04 | FAQ — 5 מהמסמך, `faq-inline` | SECTION 08 שורות 330–364 | 5 שאלות ככתבן | החלפה, בלי `block-faq-list.php` | **ברור** | פתוח |
| SHP-05 | שלוש המלצות + קישורי פייסבוק | SECTION 09 שורות 388–419 | שמות, ציטוטים, 3 URL | הדבקה + href | **ברור** | פתוח |

DEV NOTE שורה 382 («ההמלצות אינן ממוקדות ברכישת כלי — להשלים בהמשך») אינו בייטים חלופיים. לא ממציאים המלצות רכישה. מדביקים את השלוש שב-«התוכן». השלמה עתידית — לא סעיף עכשיו.

אין סעיפים לנימרוד. הסרת גריד ה-`bookcard` היא חלק מ-SHP-03: הגריד הוא תוספת צוות שאינה במסמך, לא בלוק מאושר של אייל.

---

## 4. פעולות הדבקה `ברור` (לשער 4, רק אחרי `GO BUILD R1-10`)

1. **SHP-03** — למחוק את `phero` המומצא (`חנות` / H1 «כלים ואביזרים…» / התת / גריד 5 כרטיסים / «מחיר לפי התאמה» / «לעמוד המוצר ←»). להדביק SECTION 01–07 ו-10 ככתבם, בסדר המסמך. H1 = `כלי דיג'רידו למכירה - כלים בעבודת יד` **בלי** `<em>`. בלי תג-פרק שהומצא. בלי מחירים. בלי כרטיס סטנד רצפתי. רשימות 03–05 ושלבי 1–4 ב-06 ככתבם. ארבעה CTA ל-`/contact` ככתבם: «לתיאום והתאמה אישית» · «לתיאום הגעה והתנסות בכלים» · «לבדיקת זמינות והתאמה» · «לתיאום הגעה ובחירת כלי». קישורי גוף: `/contact` `/treatment` `/method` `/lessons` `/sound-healing` `/repair` `/instruments` ככתבם. Provenance על כל מחרוזת.
2. **SHP-04** — חמש שאלות SECTION 08 כ-`faq-inline`. לא `faqblock` / CPT / `block-faq-list.php`.
3. **SHP-05** — שירי אלקבץ, רותי שליט, אלון גרזון רז + שלושת קישורי פייסבוק + פסקת הסיום עם `/treatment`. בלי שינוי מילה.

בנייה: רק `shop-defaults.php` (+ חלק חדש ייעודי לעמוד אם חייבים). לא לגעת בילדי defaults.

---

## 5. מה לא נגענו בו

- PHP / FTP / git / xlsx / `tracker_page_tab.py`
- `didgeridoos-defaults.php` `bags-defaults.php` `repair-defaults.php` `stands-*-defaults.php`
- `chapters-render.php` `videoblk.php` `block-faq-list.php` `inc/data/*.json`
- סעיפי אייל ממתינים בעמודים אחרים
- תפריט עליון / פוטר (קבצים משותפים)
