---
id: RESEARCH-WP-S5-03-GROUND-TRUTH-2026-07-16
from_team: team_100
date: 2026-07-16
type: research-preserved-for-handoff
wp: WP-S5-03
status: COMPLETE — ready to fold directly into WP-S5-03-LOD400 authoring
---

# Preserved ground-truth research — WP-S5-03 (legacy/301 completeness)

Produced by an Explore agent during this session, before the task was correctly redirected to a canonical
handoff (per team_00 instruction, 2026-07-16). This research is COMPLETE and should be used directly by the
LOD400-authoring session — no need to re-run it.

## Step 1 — Source data (GSC "not indexed" export, 2026-06-04)

Location: `docs/project/eyal-ceo-submissions-and-responses/from-eyal/תוכן לאתר 25.5.26/דפים שלא אונדקסו/` — 9 `.txt` files (mtime 2026-06-04 20:01), header row `כתובת אתר,נסרק לאחרונה`.

| Category (Hebrew filename) | English GSC label | URL count |
|---|---|---|
| `לא נמצא 404.txt` | 404 (Not found) | **67** |
| `הדף לא נוסף לאינדקס בגלל התג noindex.txt` | Excluded by 'noindex' tag | **127** |
| `נסרק - לא נכלל באינדקס כרגע.txt` | Crawled - currently not indexed | **126** |
| `נסרק אך לא נכלל באינדקס.txt` | Crawled but not indexed | **60** |
| `הדף מפנה לכתובת אתר אחרת.txt` | Page with redirect | **10** |
| `soft 404.txt` | Soft 404 | **5** |
| `שגיאת שרת.txt` | Server error (5xx) | **3** |
| `דף חלופי עם תג קנוני תקין.txt` | Alternate page with proper canonical | **1** |
| `עותק משוכפל בלי שנבחר דף קנוני.txt` | Duplicate, no canonical selected | **1** |
| **Total** | | **400** |

Concrete examples, largest categories:
- **noindex (127):** mostly legacy `/feed/` endpoints (e.g. `.../nukesh-dhiman.../feed/`, `/qr/qr10/feed/`, `/shop/cart/`) — non-content, low SEO risk.
- **crawled-not-indexed (126):** old gallery/shop pages, trailing-slash variants (e.g. `/פרדס-חנה/תיקון-דיגרידו/`, `/shop/books/וכתבת`, `http://www.eyalamit.co.il/qr/`).
- **404 (67, highest risk):** real ex-content, e.g. `/shop/books/צבע-בכחול-וזרוק-לים/`, `/shop/books/כושי-בלאנטיס/`, `/אייל-עמית/אודות/`, plus orphaned media (`/פסח/`, `?attachment_id=16321`).

## Step 2 — Live redirect mechanism

`site/wp-content/mu-plugins/ea-w209-legacy-301-redirects.php` (81 lines).

- Function `ea_w209_legacy_redirects()` on `template_redirect` priority 0 (before the site-tree canonical table, priority 1).
- **Exact-path matching only** (`trailingslashit($path)`, path-only, query stripped) — **except** one regex-prefix rule: `/Blog/(.+)$` → `/blog/$1` (301, case migration, line 31).
- Rule counts: **2× 410** (`/qr/פרק-א/`, `/thankyou/`), **32× exact-match 301** (lines 39-71), **1× regex-prefix** (`/Blog/*`). Total **34 explicit + 1 prefix rule**.
- **No category/wildcard rules for the old gallery system** — only the one `/Blog/` regex.
- Provenance (header docblock): generated by `scripts/gen_htaccess_301_from_decisions.py` from `hub/data/decisions/redirects-301-eyal-final-2026-05-27.json` (135 decisions). Explicit comment: `.htaccess is inert on this stack (nginx); PHP is the live mechanism. DO NOT hand-edit.`

## Step 3 — Cross-reference sample (21 of 400 URLs tested, direct + `-L` curl against staging)

| Verdict | Count in sample | Notes |
|---|---|---|
| covered (working) | 7 | incl. `/מפת-אתר-site-map/`→`/sitemap_index.xml`, `/shop/cart/`→`/shop/`, the `/Blog/` regex (2-hop: case-fold then canonical) |
| false-positive (not a real gap) | 2 | both `/wp-login.php?redirect_to=...` — legit login form, GSC mis-classified |
| **needs-301 (real gap)** | **9** | concentrated in: (a) a SECOND, unmapped legacy directory prefix `דיגרידו-סטודיו-נשימה-מעגלית-אייל-עמית-2/*` (map only covers the `דיגרידו-המרכז-לטיפול-בדיגרידו-סטודי/*` prefix — same content, different old URL family); (b) `/shop/books/*` paths (0 entries in the 32-rule map); (c) `/אייל-עמית/אודות/`, `/אייל-עמית-2/אודות/` |
| needs-content-check | 2 | one rule fires correctly but destination itself 404s (orphaned old blog post); one is root-domain www/scheme canonicalization — **not testable on staging, must verify on production domain separately** |
| needs-410 | 1 | `/פסח/` — orphaned media page, no clear new-site equivalent |

**Full table of the 21 tested URLs (path / category / in-map? / direct status / following status / verdict) is in the original agent transcript if line-level detail is needed — the summary above captures every distinct verdict class and root cause.**

## Step 4 — Stale/conflicting exports (confirmed quarantined, not live)

- `_COMMUNICATION/team_100/301-quarantine-stale-W2-06-exports/blog-301-rules.htaccess.QUARANTINED` + `blog-301-redirection-plugin.json.QUARANTINED` — W2-06 blog-migration exports (2026-05-28), **14 confirmed disagreements** with the live mu-plugin (e.g. `/צור-קשר/ → /` WRONG, correct is `/contact/`; 4× `/shop/*` wrongly → `/books/`, would break live `/shop/`). Quarantine reasons: wrong targets, `.htaccess` inert on nginx, superseded by the PHP mu-plugin. Reference: `_COMMUNICATION/team_100/301-quarantine-stale-W2-06-exports/README.md`, `_COMMUNICATION/team_100/301-MAP-RECONCILIATION-2026-06-20.md` §3.
- **SSoT chain (confirmed correct):** `hub/data/decisions/redirects-301-eyal-final-2026-05-27.json` (135 decisions) → `scripts/gen_htaccess_301_from_decisions.py` → `site/wp-content/mu-plugins/ea-w209-legacy-301-redirects.php` (LIVE) [+ `_COMMUNICATION/team_100/tools/htaccess_301_block.txt`, documentation-only mirror, byte-consistent, inert if ever deployed].
- **Adjacent-risk artifacts to flag in the LOD400 spec (not stale exports themselves, but confusion risk):** `_COMMUNICATION/team_100/tools/deploy_htaccess_301.py` (would FTP-upload `.htaccess` — misleading since nginx ignores it; mark "do not run"); `scripts/ftp_deploy_w2_06_blog.py` — confirmed ALREADY FIXED to point at the mu-plugin SSoT, not a hazard.
- **One pre-existing documented gap, still open:** the 54 per-slug blog-post redirects from the quarantined W2-06 exports were never folded into the 135-decision SSoT — only the `/Blog/` catch-all regex is live; individual old blog-post slugs are unverified. Documented in the reconciliation doc as an explicit pre-cutover TODO. **This should be an explicit line item in WP-S5-03's LOD400 spec.**
- Numerous `_COMMUNICATION/team_50/`, `_COMMUNICATION/team_190/` `evidence/*/redirect*.json|.txt` files are point-in-time QA logs from prior waves — NOT competing redirect-definition sources, do not cite as authoritative.

## Implication for WP-S5-03's LOD400 acceptance criteria (draft direction, not final)

- Full triage of all 400 URLs (not just the 21-sample) using the same method: check against the 34-rule map, curl direct-status.
- Explicit sub-tasks for the 2 concentrated gap areas found: (a) add rules for the `דיגרידו-סטודיו-נשימה-מעגלית-אייל-עמית-2/*` prefix (mirroring the already-covered `דיגרידו-המרכז-לטיפול-בדיגרידו-סטודי/*` targets), (b) add the missing `/shop/books/*` → `/books/*` mappings.
- Resolve the 54-per-slug-blog-redirect gap (either fold real ones into the map, or confirm via traffic/GSC data that none are worth preserving).
- `/פסח/`-class orphans → 410, not 301-to-nowhere.
- The 2 www/scheme + orphaned-blog-post-destination items are explicitly OUT of staging-testable scope — call this out so the spec doesn't demand an impossible staging verification.
