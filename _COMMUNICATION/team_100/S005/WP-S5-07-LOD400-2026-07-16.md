---
id: WP-S5-07-LOD400-2026-07-16
from_team: team_100
to_team: team_00
date: 2026-07-16
type: wp-build-spec
wp: WP-S5-07
next_wp: WP-S5-05
lod_status: LOD400
lod_target: LOD400
milestone: S005
authoring_team: team_100
phase_owner: team_100
consuming_team: team_10/110
assigned_validator: team_90
supersedes: WP-S4-07
parent_index: _COMMUNICATION/team_100/S005/S005-PACKAGE-1-3-INDEX-2026-07-16.md
authority:
  - _COMMUNICATION/team_110/DECISION-NAP-CANON-2026-07-16.md
  - _COMMUNICATION/team_110/ROUTING-REQUEST-TEAM100-S004-RECONCILIATION-2026-07-16.md
decision_ref: "team_00 (נמרוד) 2026-07-16 — «זה המידע המדוייק. לא לשאול יותר שוב!!! לתקן בפוטר בהתאם ובכל מקום שצריך.»"
blocked_by: []
blocks: [WP-S5-05]
---

# LOD400 — WP-S5-07: S004 residual — NAP consistency + FAQ-03 geo/price + rebirthing glow

## 0. ⛔ קרא ראשון — ה-NAP מאושר. זו לא שאלה.

**הכתובת והטלפון של אייל מאושרים** — ע"י team_00 שוב ושוב, וע"י אייל שוב ושוב.

> **«אני לא יודע כמה פעמים כבר אישרתי את הכתובת והטלפון האלו. גם אייל אישר - שוב ושוב ושוב זה חוזר. די.
> זה המידע המדוייק. לא לשאול יותר שוב!!! לתקן בפוטר בהתאם ובכל מקום שצריך.»** — team_00, 2026-07-16
>
> **«דיר באלק אם שוב אתם שואלים אותו מה הטלפון.»**

**אין ב-WP הזה שום פריט «להחליט NAP». אין לשאול את team_00. אין לשאול את אייל. אין לפתוח מחדש.**
ה-canon + צורות-הבייט המדויקות: `_COMMUNICATION/team_110/DECISION-NAP-CANON-2026-07-16.md`.
זה סוגר סופית את הפריט הדחוי של WP-S4-07 («decide D1 NAP footer line per BN-01») — **הפוטר נבנה.**

## 1. תקציר מנהלים

WP-S4-07 קיבל `PASS_WITH_FINDINGS` עם route_recommendation מפורש: *«לפני סגירת L-GATE_BUILD: תקן AC-10
(NAP FAQ↔פוטר) ו-AC-6 (glow ריברסינג); השלם AC-4 (FAQ-03 Q3 + Q1 geo)»* — **מעולם לא בוצע**. team_00 קבע
(2026-07-16): לא לפתוח מחדש את S004 ולא לפתוח מיילסטון חדש — לשאת את השארית קדימה כ-**WP-S5-07**.

**קבצי יעד:**
| # | קובץ | פעולה |
|---|------|-------|
| 1 | `site/wp-content/mu-plugins/ea-nap-ssot.php` | **חדש** — `ea_nap()` accessor (§4.A) |
| 2 | `site/wp-content/mu-plugins/ea-w2-seo-schema.php` | refactor L61/L65-66 → קורא מ-`ea_nap()` (§4.A) |
| 3 | `themes/ea-eyalamit/template-parts/chapters/section-footer.php` | **בונה NAP מלא** אחרי L34 (§4.B). ⚠ **לא** `block-footer-social.php` — הוא קוד מת; מסמכי הקלט טועים |
| 4 | `themes/ea-eyalamit/template-parts/chapters/parts/contact.php` | L87 **+** L88 — U+2011 → U+002D + nowrap (§4.C) |
| 5 | `themes/ea-eyalamit/inc/seo-head-fallbacks.php` | L50 — `רח׳`/`ב׳` U+05F3 → U+0027 (§4.D) |
| 6 | `themes/ea-eyalamit/page-templates/tpl-chapters-en.php` | L106/L112 — האחדה (§4.E) |
| 7 | `site/wp-content/mu-plugins/ea-w2-07-qr-content-data.php` | **L606 — טלפון שגוי** (§4.F) |
| 8 | `site/wp-content/mu-plugins/ea-s507-qr-reseed-once.php` | **חדש** — drop-in reseed ל-QR (§4.F) |
| 9 | `themes/ea-eyalamit/inc/data/ea-faq-seed.json` | L859 — `general-17` טלפון (§4.G) |
| 10 | `site/wp-content/mu-plugins/ea-s507-faq-update-once.php` | **חדש** — drop-in update ל-2 שורות FAQ (§4.G) |
| 11 | `scripts/qa/nap_canon_check.mjs` | **חדש** — שומר-הקאנון (§4.H) |

**מנוע:** בונה = team_10/110; ולידציה = team_90 (cross-engine, Iron Rule #1). `blocked_by: []` — ניתן להתחיל מיד.
**חוסם את WP-S5-05** (הפוטר בלי NAP + טלפון שגוי חי לא נשלחים לאייל).

---

## 2. הסיבה שזה חזר — שורש הבאג (זה מה שהופך את זה ל-WP ולא ל-typo)

ה-NAP **מעולם לא היה חסר**. הוא יושב ב-SSoT **מוצהר-בעצמו**: `ea-w2-seo-schema.php` L6
(*"BUSINESS-NAP-AND-HOURS SSoT"*) / L53 (*"Business: cbDIDG breath-therapy center (NAP SSoT)"*).
**אבל שום קובץ אחר לא קורא ממנו** — כל משטח מקודד עותק משלו. התוצאה (סריקה עצמאית של team_100, 2026-07-16):
**6 וריאנטים של טלפון + 3 של גרש** לאותו מידע מאושר.

**לכן `AC-10` של WP-S4-07 («NAP זהה-בייט FAQ ↔ פוטר») היה בלתי-אפשרי מלכתחילה:** לא הייתה צורת-בייט קאנונית
להשוות אליה, ולפוטר לא היה NAP בכלל. כל WP שנגע בזה «גילה» את העמימות והסלים אותה כשאלה, במקום שלקוד יהיה
מקור אחד. **§4.A + §4.H הם מה שסוגר את זה לתמיד** — אחרת ההחלטה הזו היא מסמך, לא קוד.

## 3. הקאנון (מ-`ea-w2-seo-schema.php`, אומת חי 2026-07-16)

| שדה | ערך קאנוני | קוד-תו קריטי | היכן |
|---|---|---|---|
| Street | `עמל 8 ב'` | גרש = **U+0027** (לא U+05F3 `׳`) | תצוגה + schema |
| Locality | `פרדס חנה-כרכור` | מקף = **U+002D** (לא U+2011) | תצוגה + schema |
| Address (display) | `רח' עמל 8 ב', פרדס חנה-כרכור` | U+0027 ×2, U+002D | פוטר, contact, FAQ |
| Phone — schema | `+972-52-482-2842` | — | `schema.org telephone` **בלבד** |
| Phone — `tel:` href | `+972524822842` | — | כל `<a href="tel:…">` |
| Phone — תצוגה | `052-4822842` | מקף = **U+002D** | פוטר, contact, FAQ, QR |

אומת במקור: L61 `'telephone' => '+972-52-482-2842'` · L65 `'streetAddress' => 'עמל 8 ב\''` ·
L66 `'addressLocality' => 'פרדס חנה-כרכור'`.

---

## 4. תת-משימות

### 4.A — `ea-nap-ssot.php` (חדש) + refactor ה-SSoT

**למה קובץ חדש ולא getter בתוך `ea-w2-seo-schema.php`:** שורש הבאג (§2) הוא ש-ה-SSoT הוא *literal בתוך תוסף
ה-schema שאיש לא יכול לצרוך*. הוספת getter **לאותו** קובץ משמרת את הצימוד. חילוץ הנתונים החוצה הופך את
`ea-w2-seo-schema.php` ל**צרכן ככל השאר** — וזו ההוכחה שה-accessor עובד.
mu-plugins נטענים לפני התמה ולפני `init`; כל הקריאות קורות בזמן רינדור → **סדר הטעינה אינו רלוונטי לנכונות.**

```php
<?php
/**
 * Plugin Name: EA — NAP SSoT accessor (WP-S5-07)
 * Description: The ONE canonical Name/Address/Phone source. Every display surface
 *   MUST read from ea_nap() instead of hard-coding a copy. Canon + rationale:
 *   _COMMUNICATION/team_110/DECISION-NAP-CANON-2026-07-16.md (RATIFIED — do not
 *   re-open, do not ask team_00/Eyal). Extracted out of ea-w2-seo-schema.php,
 *   whose NAP literal nothing could consume — the root cause of 6 phone variants
 *   and an unsatisfiable WP-S4-07 AC-10.
 * Version: 1.0.0
 */

defined( 'ABSPATH' ) || exit;

/**
 * Canonical NAP.
 *
 * @param string $key '' for the whole map, or one of the keys below.
 * @return string|array
 */
function ea_nap( $key = '' ) {
	$nap = array(
		'name'            => 'המרכז לטיפול בנשימה באמצעות דיג\'רידו',
		'alternate_name'  => 'cbDIDG',
		'street'          => 'עמל 8 ב\'',                  // U+0027
		'locality'        => 'פרדס חנה-כרכור',             // U+002D
		'country'         => 'IL',
		'address_display' => 'רח\' עמל 8 ב\', פרדס חנה-כרכור', // U+0027 x2, U+002D
		'phone_display'   => '052-4822842',                 // U+002D, no country code
		'phone_href'      => '+972524822842',               // tel: href
		'phone_schema'    => '+972-52-482-2842',            // schema.org telephone
	);
	if ( '' === $key ) {
		return $nap;
	}
	return isset( $nap[ $key ] ) ? $nap[ $key ] : '';
}
```

**refactor `ea-w2-seo-schema.php`** — החלף **ערכים בלבד**, ללא שינוי מבנה:
- L61 `'telephone' => '+972-52-482-2842',` → `'telephone' => ea_nap( 'phone_schema' ),`
- L65 `'streetAddress' => 'עמל 8 ב\'',` → `'streetAddress' => ea_nap( 'street' ),`
- L66 `'addressLocality' => 'פרדס חנה-כרכור',` → `'addressLocality' => ea_nap( 'locality' ),`
- L57 `'name' => 'המרכז...'` → `ea_nap( 'name' )`; `'alternateName' => 'cbDIDG'` → `ea_nap( 'alternate_name' )`

> 🔴 **AC-1 הוא שומר-הנסיגה כאן.** `ea-w2-seo-schema.php` הוא המנוע ש-WP-S5-02 זה עתה העביר ב-PASS צולב נקי.
> ה-refactor **חייב** להיות משמר-התנהגות: צומת `ProfessionalService` ב-JSON-LD החי **זהה-בייט** לפני/אחרי.
> אם הוא משתנה — עצור.

### 4.B — הפוטר: בנה את ה-NAP (זה AC-10)

> 🔴 **תיקון קריטי (cycle 1 / F-01) — מסמכי הקלט מכוונים לקובץ מת. אל תלך אחריהם.**
> ה-DECISION §4 שורה 1 וה-ROUTING-REQUEST §2 שניהם מכוונים ל-`template-parts/blocks/block-footer-social.php`.
> **הקובץ הזה אינו מרונדר באף מסלול חי.** אומת (team_100 + team_90, 2026-07-16): `ea-cfoot` = **0** על
> `/`, `/faq/`, `/contact/`, `/treatment/`, `/blog/`, `/blog/?cat=*`, `/qr/qr2/`. הוא מופנה **רק** ע"י תבניות
> Wave2 מתות (`tpl-blog-single.php`, `tpl-blog-archive.php`, `tpl-qr.php`) שה-Chapters router מנצח עליהן
> (`chapters-routing.php` @103 / blog @105). זהו חוב ה-dual-template Wave2→Chapters.
>
> **הפוטר החי בכל מסלול הוא `template-parts/chapters/section-footer.php`** — `foot__brand` = **1** בכל מסלול
> שנבדק. team_110 קרא את הקובץ המת וראה «פרדס חנה · ישראל», שדומה במקרה ל-«פרדס חנה, ישראל» של הפוטר החי —
> כך הטעות שרדה עד ה-cross-engine.

**היעד: `template-parts/chapters/section-footer.php`, בלוק `foot__brand` (L32-34):**
```php
<div class="foot__brand">
	<b>אייל עמית</b>
	<p>המרכז לטיפול בנשימה באמצעות דיג׳רידו · פרדס חנה, ישראל. שיטת cbDIDG, מאז 1999.</p>
```
**הוסף מיד אחרי L34** (שורת ה-tagline הקיימת נשארת **ללא שינוי** — היא tagline, לא NAP):
```php
			<p class="foot__nap"><?php echo esc_html( ea_nap( 'address_display' ) ); ?></p>
			<p class="foot__tel">
				<a href="tel:<?php echo esc_attr( ea_nap( 'phone_href' ) ); ?>" dir="ltr" style="white-space:nowrap"><?php echo esc_html( ea_nap( 'phone_display' ) ); ?></a>
			</p>
```
- `dir="ltr"` — מספר טלפון בעמוד RTL חייב כיוון LTR (תקדים: `contact.php` L88).
- `white-space:nowrap` — מונע שבירת-שורה באמצע המספר **בלי** תו-הומוגליף (זו הסיבה שה-U+2011 נולד; §4.C).
  אם התמה מעדיפה CSS על inline — הוסף `.foot__tel a { white-space: nowrap; }` ל-`chapters.css`.
  **בחר אחד. אל תעשה את שניהם.**

> **`block-footer-social.php` — אל תיגע בו.** אל תוסיף לו NAP «ליתר ביטחון»: הוא קוד מת, והוספת עותק
> **קשיח שני** של ה-NAP לקוד מת היא **בדיוק** דפוס הדריפט שה-WP הזה בא לסגור (§2). נרשם לגיבוי-חוב
> ה-dual-template, לא כאן. AC-2 מאמת שהוא **נשאר** מת (`ea-cfoot` = 0).

### 4.C — `contact.php`: **שתי** שורות, לא אחת

> ⚠ **מסמך הקלט מפספס אחת מהן.** ה-DECISION §4 שורה 4 אומר רק «`contact.php` **L88** | U+2011 → U+002D».
> team_100 בדק קודפוינטים: **גם L87 נושא U+2011** — ב-`פרדס חנה‑כרכור`. תיקון L88 בלבד משאיר את הכתובת
> עם מקף-הומוגליף, וה-address **עדיין לא יתאים-בייט** לקאנון. **תקן את שתיהן.**

| שורה | עכשיו | חייב להיות |
|---|---|---|
| **L87** | `"רח' עמל 8 ב', פרדס חנה‑כרכור"` ← **U+2011** | `<?php echo esc_html( ea_nap( 'address_display' ) ); ?>` |
| **L88** | `<a href="tel:+972524822842" dir="ltr">052‑4822842</a>` ← **U+2011** | href → `ea_nap('phone_href')`; טקסט → `ea_nap('phone_display')` + `style="white-space:nowrap"` |

ה-`href` ב-L88 כבר קאנוני — שנה אותו ל-`ea_nap()` בכל זאת (מקור אחד), אך זו אינה שגיאה כיום.

### 4.D — `seo-head-fallbacks.php` L50 — **רק אסימוני NAP**

השורה נושאת **שלושה** U+05F3: `דיג׳רידו` · `רח׳` · `ב׳`.

> 🔴 **החלף שניים בלבד: `רח׳` → `רח'` ו-`ב׳` → `ב'`.**
> **אל תיגע ב-`דיג׳רידו`.** זו מילת המותג, לא ה-NAP; הקאנון (§3) מגדיר Street/Locality/Phone בלבד, וקריטריון
> הקבלה של ה-DECISION אומר במפורש «zero occurrences of … U+05F3 **address**». `דיג׳רידו` נכתב ב-U+05F3 גם
> בפוטר (`__tag`) — החלפה עיוורת של U+05F3 בשורה הזו **תיצור** חוסר-עקביות חדש. **החלפה ממוקדת-אסימון בלבד.**

### 4.E — `tpl-chapters-en.php` — האחדה (החלטה §5.2)

העמוד **סותר את עצמו** היום: L106 מציג `+972-52-482-2842`, L112 מציג `052-4822842`. שני ה-hrefs כבר קאנוניים.
**החלטה:** שני המשטחים מציגים את **הצורה הבינלאומית** `ea_nap( 'phone_schema' )` — נימוק ב-§5.2.
- L106: `<a href="tel:<?php echo esc_attr( ea_nap('phone_href') ); ?>" dir="ltr"><?php echo esc_html( ea_nap('phone_schema') ); ?></a>`
- L112 (פוטר EN): אותו דבר — **שנה** מ-`052-4822842` לצורה הבינלאומית.

### 4.F — 🔴 `/qr/qr32/` — הטלפון השגוי (הפגם החמור ביותר כאן)

`mu-plugins/ea-w2-07-qr-content-data.php` **L606** (אומת חי — HTTP 200, מרנדר את המספר השגוי):
```html
<div><strong>לפרטים נוספים מוזמנים ליצור קשר</strong> <strong>052-482284</strong></div>
```
**9 ספרות — ה-`2` האחרון חסר.** קאנוני: `052-4822842` (10).

**למה זה לא קוסמטי:** לעמוד הזה מגיעים ע"י **סריקת קוד QR מודפס בספר של אייל**. קורא סורק, רוצה ליצור קשר,
ומקבל **מספר לא-קיים**. זה הטלפון **היחיד** בעמוד. זה חי **כל הזמן הזה**.

**שלב 1 — תקן את המקור:** L606 → `<strong>052-4822842</strong>` (U+002D).
**שלב 2 — reseed** (בלי זה ה-DB לא ישתנה; אין WP-CLI בסטייג'ינג). **אל תערוך את ה-DB ידנית.**
צור `mu-plugins/ea-s507-qr-reseed-once.php` — **העתק מדויק של דפוס `ea-w2-07b-qr-reseed-once.php`** (שדגלו
כבר נשרף), עם **דגל חדש**:
```php
add_action( 'init', function () {
	if ( 'done' === get_option( 'ea_s507_qr_reseed_done', '' ) ) { return; }
	if ( wp_installing() ) { return; }
	delete_option( 'ea_w2_07_qr_seeded_v3' );          // clears the W2-07 seeder guard
	update_option( 'ea_s507_qr_reseed_done', 'done' ); // single-fire
}, 27 );                                                // BEFORE the seeder at init@28
```
**זה עובד כי** `ea-w2-07-qr-seed-once.php` **מעדכן** `post_content` בהרצה חוזרת כשהוא שונה
(`if ( '' !== $content && $existing->post_content !== $content ) { $updates['post_content'] = $content; }`
→ `wp_update_post`). זהו בדיוק המנגנון שהוכח ב-WP-S5-02 §3.1.

> **תאימות ל-WP-S5-06 (חשוב — שני ה-WPs נוגעים ב-QR):** ה-reseed הזה **בטוח** מול ה-facade, ובדיוק **מפני
> ש**-WP-S5-06 הוא `the_content` render filter שאינו נוגע ב-`post_content`. ה-reseed מחזיר את אותו
> `post_content` מה-seed — כולל 60 ה-embeds — ולכן צמתי ה-`VideoObject` שורדים. **סדר ההרצה בין S5-06 ל-S5-07
> אינו משנה.** (אילו S5-06 היה בוחר במסלול ה-reseed האסור, השניים היו מתנגשים — עוד אישוש לאילוץ שם.)

### 4.G — FAQ: 🔴 המלכודת — ה-seeder הוא **insert-only**

**מצב מאומת (repo מול חי, 2026-07-16):**

| שורה | ב-repo | חי ב-`/faq/` | מסקנה |
|---|---|---|---|
| `general-18` (מחיר Q3 + glow) | קיים עם glow | **חי ✓** (2 מופעים) | **סגור — VERIFY בלבד** |
| `general-01` (geo Q1, «עמל 8 ב'») | קיים | **חי ✓** | **סגור — VERIFY בלבד** |
| `method-02` (glow ריברסינג) | **קיים עם glow** | **0** | **פתוח — צריך update ל-DB** |
| `general-17` (טלפון) | `052-482-2842` ✗ | `052-482-2842` ×2 | **פתוח — JSON + update ל-DB** |

> 🔴 **המלכודת:** `mu-plugins/ea-faq-seed-once.php` הוא **insert-only** —
> `if ( ! empty( $existing ) ) { continue; }` ואז `wp_insert_post` בלבד. **איפוס הדגל `ea_faq_seed_v2` לא
> יעדכן שום שורה קיימת.** בונה שיחקה את דפוס ה-QR (§4.F) יאפס את הדגל, יטען את העמוד, **לא יראה שינוי**,
> ויתקע. `method-02` ו-`general-17` נזרעו כבר — לכן ה-JSON המתוקן לעולם לא יגיע ל-DB בדרך הזו.
> **זו בדיוק הסיבה ש-AC-6 «נשאר פתוח» למרות שה-repo תקין.**

**אל תהפוך את `ea-faq-seed-once.php` ל-update-all + bump ל-v3.** זה ידרוס `post_content` של **כל 108** השורות
— ויימחק כל עריכה שאייל/team_00 עשו ב-wp-admin (שחזור העריכות ב-wp-admin היה כל מטרת WP-S4-05). **הרס נתונים.**

**הפתרון — drop-in ממוקד עם allow-list מפורש:** `mu-plugins/ea-s507-faq-update-once.php`
```php
add_action( 'init', function () {
	if ( 'done' === get_option( 'ea_s507_faq_update_done', '' ) ) { return; }
	if ( wp_installing() ) { return; }
	$keys = array( 'method-02', 'general-17' );   // EXPLICIT allow-list — never widen
	$file = get_stylesheet_directory() . '/inc/data/ea-faq-seed.json';
	$seed = json_decode( (string) file_get_contents( $file ), true );
	if ( ! is_array( $seed ) ) { return; }
	$by = array();
	foreach ( $seed as $row ) { if ( isset( $row['seed_key'] ) ) { $by[ $row['seed_key'] ] = $row; } }
	foreach ( $keys as $k ) {
		if ( empty( $by[ $k ] ) ) { continue; }
		$ids = get_posts( array(
			'post_type' => 'ea_faq', 'post_status' => 'any', 'numberposts' => 1, 'fields' => 'ids',
			'meta_key' => '_ea_faq_seed_key', 'meta_value' => $k,
		) );
		if ( empty( $ids ) ) { continue; }
		wp_update_post( array( 'ID' => (int) $ids[0], 'post_content' => $by[ $k ]['a'] ) );
	}
	update_option( 'ea_s507_faq_update_done', 'done' );
}, 41 );   // AFTER ea_faq_seed_once_maybe_run() at init@40
```
- ה-`post_content` נלקח **מה-JSON** — אין להקליד HTML מחדש (מקור אחד).
- allow-list של **2 מפתחות בלבד** → blast radius אפס.
- `init@41` — אחרי ה-seeder ב-40.

**גם תקן את המקור:** `ea-faq-seed.json` **L859** (`general-17`): `052-482-2842` → `052-4822842`.
(`ea-faq-seed.json` L11 / `general-01` כבר קאנוני — **אל תיגע**.)

**JSON לא יכול לקרוא PHP** — לכן שני ה-seeds (FAQ, QR) נשארים ליטרלים ומוגנים ע"י §4.H, לא ע"י `ea_nap()`.

### 4.H — `scripts/qa/nap_canon_check.mjs` (חדש) — שומר-הקאנון

בלי זה ההחלטה היא מסמך. הסקריפט נכשל (exit 1) אם קיים וריאנט לא-קאנוני **מחוץ** לקובץ העדויות:

```
סורק: site/wp-content/**  +  scripts/**   (מדלג node_modules, reports)
מחריג: site/wp-content/themes/ea-eyalamit/inc/data/ea-testimonials-fb.json   (§5.3)
נכשל אם נמצא:
  052-482284      כמספר עצמאי   → (?<![\d])052-482284(?![\d])      [הטלפון השגוי]
  052-482-2842                    → (?<![\d])052-482-2842(?![\d])   [מקף עודף]
  052‑4822842                     → U+2011 בטלפון
  עמל 8 ב׳ / רח׳ עמל             → U+05F3 בכתובת
```
> 🔴 **גבולות חובה.** `052-482284` הוא **תת-מחרוזת** של הקאנוני `052-4822842`. בתוך תחום-הסריקה שלמעלה
> (`site/wp-content` + `scripts`) חיפוש נאיבי מדווח **6 קבצים** «עם הטלפון השגוי» כשבפועל יש **אחד**
> (`ea-w2-07-qr-content-data.php:606`) — כלומר **5 false-positives**. team_100 נפל בזה בסריקה הראשונה.
> השתמש ב-lookarounds לעיל, אחרת ה-harness ישלח את הבונה לתקן קבצים תקינים לחלוטין.

---

## 5. החלטות שהוכרעו (אין TBD)

### 5.1 `ea_nap()` accessor — **כן, נבנה** (§5 של ה-DECISION השאיר זאת ל-team_100)
**מוכרע: לבנות.** ה-DECISION עצמו מאבחן שהסיבה שזה חזר היא ש-ה-SSoT «declarative but unreadable». בלי accessor,
ה-WP הזה מיישר 6 משטחים על קאנון ש**שום דבר לא אוכף** — והדריפט יחזור; זה בדיוק מה שקרה בין S002 ל-S004.
**היקף כן/לא (מדויק):**
- **כן** — משטחי PHP ש-WP זה כבר נוגע בהם: SSoT (§4.A), פוטר (§4.B), contact (§4.C), EN (§4.E).
- **לא** — `ea-faq-seed.json` / `ea-w2-07-qr-content-data.php`: **JSON/HTML-seed לא יכולים לקרוא PHP.** נשארים
  ליטרלים, נאכפים ע"י §4.H.
- **לא** — `privacy-defaults.php:81`, `accessibility-defaults.php:58`: **כבר קאנוניים** (אומת). refactor שלהם
  מוסיף סיכון בלי לתקן פגם חי. §4.H שומר עליהם. **נרשם כניקיון עתידי, לא כאן.**
- **לא** — `wave2-stage-b.php:19`: המספר שם הוא **בהערה בלבד** (`/** WhatsApp E.164 without + — 052-4822842 */`);
  הערך בפועל הוא `972524822842`. **אין מה לתקן — אל תיגע** (בונה עלול «לנרמל» הערה).

### 5.2 תצוגת הטלפון בעמוד ה-EN = **הצורה הבינלאומית**
הקאנון אומר «Display never carries the country code» — והמשטחים שהוא מונה (**פוטר, contact, FAQ, QR**) הם
כולם **עבריים**. עמוד ה-`/en/` מיועד לקהל **בינלאומי**, ו-`052-4822842` **אינו חייג מחו"ל**. לכן: EN מציג
`+972-52-482-2842` בשני המקומות. **זו הכרעת פורמט-לפי-משטח (engineering call, כמו §2 של ה-DECISION), לא שינוי
בנתוני אייל ולא סתירה לקאנון.** ממילא העמוד סותר את עצמו היום — חייבים להכריע לכיוון אחד.

### 5.3 `ea-testimonials-fb.json` — **מוחרג. אבל הנימוק במסמך הקלט שגוי.**
ה-DECISION §4 כותב: *«the other numbers there are testimonial authors', not Eyal's»*. **team_100 בדק — זה לא נכון.**
שני המופעים הם **הטלפון של אייל עצמו**, שצוטט ע"י הממליצים בתוך הטקסט שלהם:
- entry #1 (**נוית צוף שטראוס**): «… 0524822842 אייל»
- entry #10 (**אלון גרזון רז**): «… זה הטלפון שלו 052-4822842»

*(מיקום מדויק: `items[1]` ו-`items[10]` באינדוקס **0-based** של מערך ה-JSON — כלומר הרשומות ה-**2** וה-**11**
בספירה אנושית. הזיהוי המחייב הוא **שם המחבר**, לא המספר הסידורי.)*

**המסקנה (החרגה) נשארת — והיא חזקה יותר מהנימוק שנרשם:** אלה **ציטוטים מילוליים בתוך המלצות מיוחסות לאנשים
אמיתיים בשמם**. «נרמול» מספר בתוך טקסט של ממליץ = **שינוי מה שאדם אמיתי כתב**. אסור.
⚠ **חשוב לבונה:** אל תסיק מ«הנימוק שגוי» ש«ההחרגה שגויה». **אל תיגע בקובץ.** הוא מוחרג במפורש ב-§4.H.

### 5.4 האם לתקן את `דיג'רידו` (U+05F3 מול U+0027) site-wide? — **לא, מחוץ ל-scope**
המילה נכתבת בשתי הצורות באתר (פוטר `__tag` = U+05F3; `contact.php` L86 = U+0027). זו **מילת מותג, לא NAP**;
הקאנון (§3) מגדיר Street/Locality/Phone בלבד. **נרשם כנצפה-ומוחרג** כדי ש(א) הבונה לא «יתקן» אותה, ו-(ב)
ביקורת עתידית לא «תגלה» אותה כהפתעה. מועמד ל-WP היגייני נפרד.

---

## 6. קריטריוני קבלה (מדידים)

**Evidence root:** `_COMMUNICATION/team_110/evidence/s5-07/`

| AC | דרישה | סף מדיד | ראיה |
|----|-------|---------|------|
| **AC-1** | 🔴 **שומר-נסיגה — schema** | צומת `ProfessionalService` ב-JSON-LD החי **זהה-בייט** לפני/אחרי ה-refactor של §4.A (`telephone`, `address.streetAddress`, `address.addressLocality`, `name`, `alternateName`). WP-S5-02 העביר את המנוע הזה ב-PASS נקי — אסור לשבור. | `…/evidence/s5-07/schema-regression/` (before+after JSON) |
| **AC-2** | **הפוטר החי נושא NAP (זה AC-10 של S4-07)** | על **`/`, `/faq/`, `/contact/`, `/treatment/`** (כולם מרנדרים `section-footer.php`): הפוטר מכיל `רח' עמל 8 ב', פרדס חנה-כרכור` **וגם** `<a href="tel:+972524822842" …>052-4822842</a>`, בתוך בלוק `foot__brand`. מ-**0** מופעי טלפון היום. **התאמת-בייט FAQ↔פוטר:** `עמל 8 ב'` (U+0027) זהה-בייט ב-`/faq/` ובפוטר. **+ שומר קוד-מת:** `ea-cfoot` = **0** בכל 4 המסלולים (מוכיח ש-`block-footer-social.php` נשאר מת ולא נוצר עותק NAP שני). | `…/evidence/s5-07/footer-nap/` |
| **AC-3** | 🔴 **`/qr/qr32/` — הטלפון הנכון, חי** | `curl -k /qr/qr32/` → HTTP 200 **וגם** מכיל `052-4822842` **וגם** **0** מופעים של `052-482284` כמספר עצמאי. 47 עמודי ה-QR האחרים — `post_content` ללא שינוי. | `…/evidence/s5-07/qr32/` (before+after) |
| **AC-4** | **FAQ חי — 2 השורות הפתוחות** | `/faq/` מכיל `ניסוח בידול` (glow של `method-02`) ≥1 — מ-**0** היום. `/faq/` מכיל `052-4822842` ו-**0** מופעים של `052-482-2842` — מ-**2** היום. | `…/evidence/s5-07/faq-update/` |
| **AC-5** | **VERIFY-בלבד — כבר סגור, אסור לשבור** | `/faq/` עדיין מכיל: `כמה עולה טיפול / שיעור` ×1 · `מדיניות מחיר` (glow) ×2 · `עמל 8 ב` ×1. סה"כ `.ea-pending-approval` ב-`/faq/` **≥10** (היום 10; ה-glow של method-02 מוסיף → צפוי 11). | `…/evidence/s5-07/faq-update/` |
| **AC-6** | **נרמול — אפס וריאנטים** | `node scripts/qa/nap_canon_check.mjs` → **exit 0**. אפס מופעים של: `052-482284` (עצמאי) · `052-482-2842` · `052‑4822842` (U+2011) · `עמל 8 ב׳`/`רח׳ עמל` (U+05F3) — **מחוץ ל-`ea-testimonials-fb.json`**. | `…/evidence/s5-07/canon-guard/` |
| **AC-7** | **ההחרגות מכובדות** | `ea-testimonials-fb.json` — **0 שינויים** (`git diff --stat` ריק). `דיג׳רידו` — ספירת U+05F3 בפוטר `__tag` **ללא שינוי**. `wave2-stage-b.php` — ללא שינוי. | `…/evidence/s5-07/scope/` |
| **AC-8** | **היגיינה + אין נסיגה** | `php -l` נקי על כל קובץ PHP שנגעת בו. **a11y:** `node scripts/qa/http-qa-axe.cjs / /faq/ /contact/` → **exit 0** (docblock: *"the reusable S5 a11y gate"* — 0 critical + 0 serious). **RTL/overflow:** `node _aos/lean-kit/modules/validation-quality/scripts/qa/qa_probe.mjs` → PASS mobile+desktop על `/` ו-`/contact/`, 0 גלישה אופקית (הטלפון עם `nowrap` בעמוד RTL הוא סיכון גלישה אמיתי — זו הסיבה שה-AC קיים). | `…/evidence/s5-07/a11y-rtl/` |

**הגדרת סיום:** AC-1..AC-8 כולם PASS מול הסטייג'ינג עם ראיות.
**AC-1 FAIL ⇒ עצור** (שברת את מנוע ה-schema של WP-S5-02). **AC-7 FAIL ⇒ עצור** (חרגת מה-scope).

**Guardrails למודד (שמרו — מנעו false-FAIL ב-S5-01/02/06):**
- TLS פג / סטייג'ינג HTTP-only — **מתוכנן**. `curl -k`. **לא ממצא.**
- `x-robots-tag: noindex` באתר כולו — host-conditional. **לא ממצא.**
- `curl 000` באצווה — timeout תעבורה, **לא** redirect. בדוק סדרתית לפני FAIL.
- **תת-מחרוזת:** `052-482284` ⊂ `052-4822842`. השתמש ב-lookarounds (§4.H) — אחרת 6 false-positives.
- ה-drop-ins הם single-fire: אחרי ההרצה הראשונה הם no-op. «לא קרה כלום בטעינה השנייה» = **תקין**, לא כשל.

---

## 7. ולידציה (Iron Rule #1)
בונה = team_10/110 (Claude/Anthropic); מוודא = **team_90** (`composer-2.5`, Cursor) — ספקים נבדלים.
מנדט: שקף את `MANDATE-TEAM90-WP-S5-0{1,2}-*` **כולל סעיף ה-Guardrails**.
מריץ: `bash scripts/run_cross_engine_validation.sh team_90 . <mandate>` (`--dry-run` תחילה); דורש אישור
team_00 ל-`cursor-agent -p --force --trust` **בניסוח מילולי**.

---

## 8. תיקונים למסמכי הקלט (נמצאו באימות מול קוד חי)

| # | מקור | הטענה | המצב בפועל |
|---|------|-------|-----------|
| 1 | DECISION §4 שורה 4 | «`contact.php` **L88**» בלבד | **גם L87** נושא U+2011 (`פרדס חנה‑כרכור`). תיקון L88 בלבד משאיר את הכתובת לא-קאנונית. §4.C. |
| 2 | DECISION §4 (`ea-testimonials-fb.json`) | «the numbers … are **testimonial authors'**, not Eyal's» | **שגוי** — שניהם **הטלפון של אייל**, מצוטט ע"י ממליצים בשמם. **ההחרגה נשארת**, מנימוק חזק יותר: ציטוט מיוחס. §5.3. |
| 3 | ROUTING-REQUEST §2 | «**AC-6 gap** — `method-02` אין `.ea-pending-approval`» | ב-**repo** ה-glow **קיים**. פתוח **רק חי** — כי ה-seeder insert-only. הפגם אינו «חסר glow» אלא «ה-DB לא עודכן». §4.G. |
| 4 | ROUTING-REQUEST §2 / verdict S4-07 | «**AC-4** — FAQ-03 Q3 (price) **absent**; Q1 geo missing» | **שניהם חיים** (`general-18` עם glow ×2; `general-01` «עמל 8 ב'» ×1). **סגורים — VERIFY בלבד** (AC-5). team_110 ביקש במפורש «verify per-AC before scoping» — בוצע. |
| 5 | ROUTING-REQUEST §1 (S4-06 F-01) | «F-01 = placeholder **לא-מסומן**… נסגר כש-WP-S5-07 ינחת» | ה-verdict של S4-06 אומר ההפך: F-01 = נוכחות של glow **לגיטימי** («correct behavior, not a false positive»). הוא ייסגר כאן — אך המסגור «לא-מסומן» שגוי. |
| 6 | DECISION §1 (טבלת וריאנטים) | «`052-482284` … 1 קובץ» | **נכון — אומת: קובץ אחד בדיוק** (`ea-w2-07-qr-content-data.php:606`). סריקה נאיבית בתחום `site/wp-content`+`scripts` מדווחת **6** (5 false-positives) בגלל תת-מחרוזת. §4.H. |
| 7 | **DECISION §4 שורה 1 · ROUTING-REQUEST §2** | «`block-footer-social.php` L64-65 → **ADD** full NAP» — הפוטר לתיקון | **הקובץ מת.** `ea-cfoot` = **0** בכל מסלול חי שנבדק (`/`, `/faq/`, `/contact/`, `/treatment/`, `/blog/`, `/blog/?cat=*`, `/qr/qr2/`); מופנה רק ע"י תבניות Wave2 מתות. הפוטר החי הוא **`template-parts/chapters/section-footer.php`** (`foot__brand`=1 בכל מסלול). נתפס ע"י team_90 ב-cycle 1 (F-01) — **team_100 ירש את הטעות ממסמכי הקלט ולא אימת את מסלול הרינדור.** §4.B. |

---

## 9. היסטוריית ולידציה חוצת-מנוע

**מחזור 1 — 2026-07-16, team_90 (`composer-2.5`, Cursor) — `PASS_WITH_FINDINGS`: 0 blockers · **1 major** · 3 minor.**
מנדט: `MANDATE-TEAM90-WP-S5-07-LOD400-2026-07-16.md` · verdict: `VERDICT-WP-S5-07-LOD400-2026-07-16.md`.
ה-validator אימת עצמאית: שתי המלכודות (FAQ insert-only · תת-מחרוזת), ששת תיקוני §8 למסמכי הקלט, והחלטות §5.

**כל 4 הממצאים נסגרו ב-rev-2:**

| ID | חומרה | ממצא | תיקון |
|----|-------|------|-------|
| **F-01** | **major** | §4.B/AC-2 כיוונו ל-`block-footer-social.php`, אך מסלולי ה-Chapters החיים מרנדרים `section-footer.php` — בונה «היה מצליח» בקובץ הלא-נכון ו-AC-2 היה נכשל | §4.B **נכתב מחדש** ליעד `section-footer.php` L32-34 (`foot__brand`), עם אזהרה מפורשת שהקובץ הישן מת + הוראה **לא** להוסיף לו NAP. AC-2 מאמת על 4 מסלולים + שומר `ea-cfoot`=0. §1 (טבלת קבצים) + §8#7 עודכנו. **team_100 ירש את הטעות ממסמכי הקלט** — ראה §8#7 |
| **F-02** | minor | AC-8 ציין `qa_probe.mjs` ללא נתיב | נתיב מלא `_aos/lean-kit/modules/validation-quality/scripts/qa/qa_probe.mjs` + נימוק ה-RTL/nowrap |
| **F-03** | minor | «entry #1/#10» דו-משמעי (0-based מול 1-based) | §5.3 — צוין 0-based מפורש + «הזיהוי המחייב הוא שם המחבר» |
| **F-04** | minor | «נאיבי = 7 קבצים» | **6** בתוך תחום-הסריקה (ה-7 היה `docs/`, מחוץ ל-scope). תוקן ב-§4.H וב-§8#6; bounded=1 ללא שינוי |

**מחזור 2 — 2026-07-16, team_90 (`composer-2.5`, Cursor) — ✅ `PASS` נקי: 0 blockers · 0 major · 0 minor.**
מנדט: `MANDATE-TEAM90-WP-S5-07-LOD400-CYCLE2-2026-07-16.md` · verdict: `VERDICT-WP-S5-07-LOD400-CYCLE2-2026-07-16.md`.
כל F-01..F-04 **CLOSED**; אנטי-נסיגה **שלמה** (מלכודות §4.G/§4.H, AC-1/3/4/5/7, guardrails); **0 ממצאים חדשים**.
**המלצת ניתוב: `L-GATE_SPEC PASS` — team_110 רשאי להתחיל `L-GATE_BUILD` מיד (`blocked_by: []`).**

**מה F-01 מלמד (נרשם בכוונה):** זהו **חוב ה-dual-template Wave2→Chapters** — אותה תבנית שגרמה ל-WP-CANON.
team_100 אימת ש-`block-footer-social.php` אכן חסר NAP, אבל **לא אימת שהוא הקובץ שמרנדר**. שני מסמכי הקלט
נקבו בו, והמחרוזת שלו («פרדס חנה · ישראל») דומה במקרה לזו של הפוטר החי («פרדס חנה, ישראל») — כך הטעות
שרדה שלושה מסמכים. **זה בדיוק מה ש-Iron Rule #1 קיים בשבילו.**

---

<!-- HANDOFF-FOOTER v1 -->

## הנדאוף לחבילה הבאה (חובה — team_110)

- **חבילה נוכחית:** WP-S5-07 · **הבאה (`next_wp`):** **WP-S5-05** (WP-S5-07 ∈ `blocked_by` שלו)
- בסיום `L-GATE_BUILD PASS`: הפק הנדאוף קאנוני דרך hub prompt-generate, כתוב `artifact_markdown` verbatim
  ל-`_COMMUNICATION/team_110/`, **הוסף spoke supplement** (השלד יחזור ריק-תוכן — ראה HANDOFF של WP-S5-06),
  הצג `activation_block` ל-team_00.
- **אל תתחיל את WP-S5-05 אוטומטית** — שער cutover חסום; המתן לניתוב team_00.
- **רישום הוא סמכות team_100/team_00** — אל תערוך `_aos/` (Iron Rule #4); הגש routing request.
