---
id: WP-S5-06-LOD400-2026-07-16
from_team: team_100
to_team: team_00
date: 2026-07-16
type: wp-build-spec
wp: WP-S5-06
next_wp: WP-S5-05
lod_status: LOD400
lod_target: LOD400
milestone: S005
authoring_team: team_100
phase_owner: team_100
consuming_team: team_10/110
assigned_validator: team_90
parent_index: _COMMUNICATION/team_100/S005/S005-PACKAGE-1-3-INDEX-2026-07-16.md
authority: _COMMUNICATION/team_110/ADDENDUM-TEAM100-WP-S5-06-FACADE-MANDATORY-2026-07-16.md
decision_ref: "team_00 (נמרוד) 2026-07-16 — «משנה את ההגדרה שלי בהתאם להמלצה - ליזי מחוייב... ולחייב לממש את כל ההשלכות»; scope: WP נפרד (לא ממוזג ל-S5-03); transcripts EXCLUDED"
blocked_by: []
blocks: [WP-S5-05]
---

# LOD400 — WP-S5-06: QR embed facade (click-to-load)

## 0. תקציר מנהלים

**המטרה:** לממש בפועל את ההתחייבות ש-`loading="lazy"` **לא** מקיימת — 42 עמודי QR טוענים כיום ~1.06 MB
של נגן YouTube בטעינת העמוד, לפני שהמשתמש ביקש לצפות. הפתרון: **facade** (תמונת-כרזה + כפתור נגן) שמחליף
את ה-iframe ברינדור; הנגן האמיתי נטען **רק בלחיצה**.

**קהל היעד:** אדם שסרק קוד QR **מודפס בספר** → נייד, לעיתים קרובות סלולרי. ~1 MB הוא עלות אמיתית.

**קבצי יעד (מדויק):**
| # | קובץ | פעולה |
|---|------|-------|
| 1 | `site/wp-content/themes/ea-eyalamit/inc/chapters/chapters-qr-facade.php` | **חדש** — gate + `the_content` filter + enqueue |
| 2 | `site/wp-content/themes/ea-eyalamit/inc/chapters/chapters-bootstrap.php` | עריכה — `require_once` אחד (אחרי L18) |
| 3 | `site/wp-content/themes/ea-eyalamit/assets/css/chapters.css` | עריכה — כללי facade אחרי L790 |
| 4 | `site/wp-content/themes/ea-eyalamit/assets/js/ea-qr-facade.js` | **חדש** — click-to-load |
| 5 | `site/wp-content/themes/ea-eyalamit/style.css` | עריכה — `Version: 1.5.6` → `1.5.7` (cache-bust) |
| 6 | `scripts/qa/qr_facade_probe.mjs` | **חדש** — harness ה-AC (קידום מתיקיית evidence) |

**מה **לא** נוגעים בו:** `ea-w2-07-qr-content-data.php` (seed) · `post_content` ב-DB · `ea-w2-seo-schema.php` ·
`ea-w2-07-qr-seed-once.php` · `ea-w2-07b-qr-reseed-once.php`. **אין שינוי DB בכלל** — הבנייה היא file-only
(deploy FTP בלבד; אין WP-CLI בסטייג'ינג, ואין בו צורך).

**מנוע:** בונה = team_10/110; ולידציה = team_90 (cross-engine, Iron Rule #1). **`blocked_by: []`** — ניתן להתחיל מיד.

---

## 1. הבסיס המדוד (אומת עצמאית ע"י team_100, 2026-07-16)

הרצה חוזרת של `qr_cwv_probe.mjs` (headless Chrome/CDP, mobile 375×812, cold cache, staging) **משחזרת** את
ממצא team_110:

| עמוד | iframes | 1st iframe מול fold | LCP | YouTube reqs / bytes |
|------|---------|---------------------|-----|----------------------|
| `/qr/qr48/` (0 וידאו — baseline) | 0 | — | 1688 ms | **0 / 0 KB** |
| `/qr/qr2/` (1 וידאו) | 1 (lazy) | 958px מול 812px → מתחת לקיפול | 1108 ms | **9 / 1061 KB** |
| `/qr/qr10/` (3 וידאו) | 3 (lazy) | 848px מול 812px → מתחת לקיפול | 1532 ms | **9 / 1063 KB** |

**המסקנה המחייבת:** ה-iframe **מתחת לקיפול** ובכל זאת ~1.06 MB נטען — סף ה-lazy של Chrome (≈1250px) בולע אותו.
ההצדקה שנרשמה בעבר («native lazy-load already meets the CWV bar») **שגויה כמנגנון** ואינה נסחבת קדימה.

**גודל התועלת:** lazy כן עובד לעומק — qr10 (3 וידאו) מושך את **אותם** 9 requests / ~1.06 MB כמו qr2 (1 וידאו).
כלומר העלות היא **הווידאו הראשון בלבד, × 42 עמודים**.

**מפת ה-scope (נספרה מה-seed, לא הוערכה):**
```
48 עמודי QR · 60 embeds · 60 <iframe> · 60 </iframe> · 60 מזהים ייחודיים
  6 עמודים × 0 וידאו   (מחוץ ל-scope — לא נוגעים)
 27 עמודים × 1 וידאו
 12 עמודים × 2 וידאו
  3 עמודים × 3 וידאו
 => 42 עמודים ב-scope · dedup לא משנה אף ספירה (raw == unique בכל 48)
```

> **⚠ תיקון למסמכי הקלט:** האדנדום §2 (וגם WP-S5-02 LOD400 §3) כותבים **«46 iframes»**. זה **שגוי**.
> הספירה בפועל: **60**. אומת: `grep -c '<iframe'` = 60, `</iframe>` = 60, ו-`preg_match_all` על ה-seed
> מחזיר 60 (6×0 + 27×1 + 12×2 + 3×3 = 60). המספר **42** (עמודים ב-scope) שבאדנדום **נכון**.

**LCP רועש — לא AC:** בשתי ההרצות qr10 נע 1076 ms → 1532 ms ו-qr48 1680 ms → 1688 ms. השוואת LCP בין
עמודים שונים חלשה מיסודה (אלמנט LCP שונה). **המספר היציב וההחלטתי הוא ה-payload** (9 requests, 1061–1063 KB
בשתי ההרצות). AC-4 מנוסח בהתאם — סף-נסיגה רחב, לא יעד.

---

## 2. 🔴 האילוץ הקריטי (נורמטיבי — אי-ציות = הרס שקט של WP-S5-02)

> ### ה-facade **חייב** להיות `the_content` render filter. מסלול ה-reseed **אסור**.

`site/wp-content/mu-plugins/ea-w2-seo-schema.php` **L266** פולט את צמתי ה-`VideoObject` של QR ע"י regex על
שדה ה-DB הגולמי `post_content` — **לא** על הפלט המרונדר. הקוד המדויק (נקרא חי):

```php
if ( preg_match_all( '#youtube(?:-nocookie)?\.com/embed/([A-Za-z0-9_-]{6,})#', (string) $qr_obj->post_content, $qr_m ) ) {
```

60 ה-iframes חיים ב-seed (`ea-w2-07-qr-content-data.php`) → נזרעים ל-`post_content` ב-DB.

| מסלול | תוצאה |
|-------|-------|
| ✅ **`the_content` filter** | `post_content` לא נגע → ה-regex עדיין מוצא → **צמתי VideoObject שורדים** |
| ❌ **שינוי seed + reseed** | ה-URL המילולי `youtube-nocookie.com/embed/<id>` עוזב את `post_content` → `preg_match_all` מחזיר **0** → **כל צומת VideoObject נעלם משקט מ-42 עמודי QR** |

**למה זו מלכודת אמיתית ולא תיאוריה:** מסלול ה-reseed הוא הדרך המתבקשת — ו**בדיוק כך בוצע §3.1 של WP-S5-02
לפני ימים** (`delete_option('ea_w2_07_qr_seeded_v3')` → reseed). בונה שיחזור על התבנית המוצלחת האחרונה יהרוס את
ה-build שזה עתה קיבל PASS צולב נקי מ-team_90 — **ללא שגיאה, ללא אזהרה, ללא כשל בבנייה**. הנסיגה תתגלה רק
ב-GSC, אחרי ה-cutover.

**AC-2 הוא שומר-הנסיגה שמוכיח שההתנגשות נמנעה.** הוא אינו אופציונלי.

---

## 3. ארכיטקטורה

### 3.1 מסלול הרינדור (אומת חי — לא הונח)

```
/qr/qrN/  → template_include @103  ea_chapters_template_include()   [inc/chapters/chapters-routing.php:53]
          → pattern route 'qr'                                      [inc/chapters/chapters-render.php:75]
          → page-templates/tpl-chapters-qr.php
          → L38: <div class="intro-body r r2"><?php the_content(); ?></div>   ← נקודת האחיזה
```
`tpl-chapters-qr.php` L33-34 מריץ `while ( have_posts() ) : the_post();` על ה-main query →
`is_main_query()` **וגם** `in_the_loop()` אמת. ה-Wave2 `tpl-qr.php` הוא **קוד מת** (Chapters מנצח ב-103).

### 3.2 מדוע priority 20 (ולא אחר)

חמשת פילטרי ה-`the_content` הקיימים — **כולם ממודרים מחוץ ל-QR** (אומת פונקציה-פונקציה):

| Pri | פונקציה | קובץ | ה-gate שלו | נוגע ב-QR? |
|-----|---------|------|-----------|-----------|
| 1 | `ea_strip_legacy_blog_shortcodes_content` | `mu-plugins/ea-blog-shortcode-cleanup.php:13` | `is_singular('post')` בלבד | ❌ (QR = page) |
| 8 | `ea_wave2_suppress_legacy_editorial_content` | `themes/.../inc/wave2-w2-07.php:522` | `ea_wave2_editorial_ctx()` בלבד | ❌ |
| 9 | `ea_w2_07_inject_press` | `themes/.../inc/wave2-w2-07.php:226` | `ea_w2_07_is_press()` בלבד | ❌ |
| 9 | `ea_w2_08_inject_content` | `themes/.../inc/wave2-w2-08.php:176` | `ea_w2_08_is_en_page()` בלבד | ❌ |
| 12 | `ea_m2_contact_fluent_the_content_fix` | `mu-plugins/ea-m2-seed-shell-once.php:113` | `post_name === 'contact'` בלבד | ❌ |

ליבת WP על `the_content`: `do_blocks`@9 · `wpautop`/`wptexturize`/`wp_filter_content_tags`@10 · `do_shortcode`@11 · `convert_smilies`@20.

**priority 20** ⇒ רץ **אחרי** `wpautop` (המרקאפ כבר סופי) ואחרי `wp_filter_content_tags` (ליבת WP כבר לא תוסיף
`loading` אוטומטי לפלט שלנו — לכן **אנחנו** נכתוב `loading="lazy"` על ה-`<img>` במפורש). `convert_smilies`@20
נרשם מוקדם יותר ולכן ירוץ ראשון באותו priority — חסר-משמעות (סמיילים לא נוגעים ב-iframes).

### 3.3 המרקאפ שאותו מחליפים (מה-seed, verbatim)

```html
<div class="ea-qr-embed ea-qr-embed--video"><iframe src="https://www.youtube-nocookie.com/embed/ng9q5-xkNmE" title="YouTube video" loading="lazy" frameborder="0" allow="accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture" allowfullscreen></iframe></div>
```
60/60 מהם `youtube-nocookie` · **0** `www.youtube.com` · לכל אחד עוטף `.ea-qr-embed--video` קיים.

**קופסת ה-CLS כבר קיימת** (`assets/css/chapters.css` L777-790):
```css
.ea-qr-embed--video { position: relative; width: 100%; aspect-ratio: 16 / 9; border-radius: var(--ea-radius-img); overflow: hidden; }
.ea-qr-embed--video iframe { position: absolute; inset: 0; width: 100%; height: 100%; border: 0; }
```
⇒ ה-facade יושב באותה קופסה `position:absolute; inset:0` → **CLS = 0 by construction**. אין לשנות את L777-790.

---

## 4. תת-משימות

### 4.A — `inc/chapters/chapters-qr-facade.php` (חדש)

מודול עצמאי: gate + filter + enqueue. תקדים לקובץ-פיצ'ר שרושם את ה-enqueue של עצמו:
`inc/chapters/chapters-commerce.php:83,102`.

```php
<?php
/**
 * Chapters — QR embed facade (WP-S5-06).
 *
 * Replaces the rendered YouTube iframe on /qr/ pages with a click-to-load
 * facade. RENDER-FILTER ONLY: post_content is never modified, because
 * mu-plugins/ea-w2-seo-schema.php L266 regexes the RAW post_content to emit the
 * QR VideoObject nodes — a seed-change/reseed would silently delete every one of
 * them (WP-S5-06 LOD400 §2).
 *
 * @package ea_eyalamit
 */

defined( 'ABSPATH' ) || exit;

/**
 * True on the /qr/ hub or any /qr/{child}/ page.
 * Mirrors the schema gate at mu-plugins/ea-w2-seo-schema.php L238-246.
 *
 * @return bool
 */
function ea_chapters_qr_facade_is_view() {
	if ( ! is_page() ) {
		return false;
	}
	$obj = get_queried_object();
	if ( ! $obj instanceof WP_Post ) {
		return false;
	}
	if ( 'qr' === $obj->post_name && 0 === (int) $obj->post_parent ) {
		return true; // hub
	}
	return $obj->post_parent
		&& 'qr' === (string) get_post_field( 'post_name', (int) $obj->post_parent );
}

/**
 * Swap every YouTube iframe for a facade button. Non-YouTube iframes untouched.
 *
 * @param string $content
 * @return string
 */
function ea_chapters_qr_facade_content( $content ) {
	if ( is_admin() || ! is_main_query() || ! in_the_loop() || ! ea_chapters_qr_facade_is_view() ) {
		return $content;
	}
	if ( false === stripos( $content, '/embed/' ) ) {
		return $content;
	}

	$title = get_the_title( get_queried_object_id() );
	$total = preg_match_all(
		'#youtube(?:-nocookie)?\.com/embed/([A-Za-z0-9_-]{6,})#i',
		$content,
		$ignored
	);
	$index = 0;

	return preg_replace_callback(
		'#<iframe\b[^>]*\bsrc=(["\'])https?://(?:www\.)?youtube(?:-nocookie)?\.com/embed/([A-Za-z0-9_-]{6,})[^"\']*\1[^>]*>\s*</iframe>#i',
		static function ( $m ) use ( $title, $total, &$index ) {
			$index++;
			$vid = $m[2];

			// Accessible name. The INDEX RULE mirrors the VideoObject `name` at
			// ea-w2-seo-schema.php L275-278 (append " — N" only when >1) so the two
			// stay in lockstep. The "נגן וידאו:" prefix is DELIBERATE and has no
			// counterpart in VideoObject.name: this is a <button>, so its accessible
			// name must state the ACTION (WCAG 2.4.6 / 4.1.2), not just the title.
			// Do not "align" them by dropping the prefix.
			$label = $total > 1
				? sprintf( 'נגן וידאו: %s — %d', $title, $index )
				: sprintf( 'נגן וידאו: %s', $title );

			return sprintf(
				'<button type="button" class="ea-qr-facade" data-ea-qr-video="%1$s" aria-label="%2$s">' .
					'<img class="ea-qr-facade__thumb" src="https://i.ytimg.com/vi/%1$s/hqdefault.jpg" alt="" ' .
						'width="480" height="360" loading="lazy" decoding="async">' .
					'<span class="ea-qr-facade__play" aria-hidden="true"></span>' .
				'</button>',
				esc_attr( $vid ),
				esc_attr( $label )
			);
		},
		$content
	);
}
add_filter( 'the_content', 'ea_chapters_qr_facade_content', 20 );

/**
 * Facade runtime — QR views only. CSS ships inside chapters.css (already loaded
 * here, and it already owns .ea-qr-embed--video) so this adds no extra request.
 */
function ea_chapters_qr_facade_assets() {
	if ( is_admin() || ! ea_chapters_qr_facade_is_view() ) {
		return;
	}
	wp_enqueue_script(
		'ea-qr-facade',
		get_stylesheet_directory_uri() . '/assets/js/ea-qr-facade.js',
		array(),
		wp_get_theme()->get( 'Version' ),
		true
	);
}
add_action( 'wp_enqueue_scripts', 'ea_chapters_qr_facade_assets', 30 );
```

**חובה — כללי דיוק:**
1. **אל תשנה** את ה-regex ב-`ea-w2-seo-schema.php` L266 ואל תיגע ב-seed/DB.
2. ה-regex כאן תופס **רק** iframes של YouTube. iframe אחר (אם ייכנס אי-פעם) עובר כמו שהוא.
3. `data-ea-qr-video` נושא **מזהה בלבד** — ה-JS בונה את ה-URL. אין להזריק URL מלא ל-DOM.
4. `$total` נספר על אותו `$content` לפני ההחלפה — כדי שה-label יידע אם להוסיף אינדקס.

### 4.B — `chapters-bootstrap.php` (עריכה)

הוסף **שורה אחת** אחרי L18 (`chapters-enqueue.php`):
```php
require_once $ea_chapters_dir . 'chapters-qr-facade.php';
```

### 4.C — `assets/css/chapters.css` (עריכה — אחרי L790)

הוסף מיד אחרי בלוק `.ea-qr-embed--video iframe` הקיים. **אין לשנות** את L777-790.

```css
/* ── QR facade (WP-S5-06) — click-to-load poster ───────────────── */
.ea-qr-embed--video .ea-qr-facade {
	position: absolute;
	inset: 0;
	width: 100%;
	height: 100%;
	padding: 0;
	border: 0;
	background: #000;
	cursor: pointer;
	display: flex;
	align-items: center;
	justify-content: center;
}
.ea-qr-facade__thumb {
	position: absolute;
	inset: 0;
	width: 100%;
	height: 100%;
	/* hqdefault is 480x360 (4:3) with letterbox bars; cover crops them to 16:9. */
	object-fit: cover;
	display: block;
}
.ea-qr-facade__play {
	position: relative;
	width: 68px;
	height: 48px;
	border-radius: 14px;
	background: rgba(0, 0, 0, 0.72);
	transition: background-color 160ms ease, transform 160ms ease;
}
.ea-qr-facade__play::before {
	content: "";
	position: absolute;
	top: 50%;
	left: 50%;
	transform: translate(-50%, -50%);
	border-style: solid;
	border-width: 10px 0 10px 17px;
	border-color: transparent transparent transparent #fff;
}
.ea-qr-facade:hover .ea-qr-facade__play,
.ea-qr-facade:focus-visible .ea-qr-facade__play {
	background: #c00;
	transform: scale(1.06);
}
.ea-qr-facade:focus-visible {
	outline: 3px solid var(--ea-focus, #fff);
	outline-offset: -3px;
}
@media (prefers-reduced-motion: reduce) {
	.ea-qr-facade__play { transition: none; }
	.ea-qr-facade:hover .ea-qr-facade__play,
	.ea-qr-facade:focus-visible .ea-qr-facade__play { transform: none; }
}
```

**RTL:** הכפתור ממורכז ב-flex; משולש ההפעלה מרוכז ב-`translate(-50%,-50%)`. אין `left`/`right` תלויי-כיוון
(ה-`left:50%` הוא מרכוז, לא יישור) ⇒ נכון ב-RTL ללא כלל נפרד. **אין להוסיף** `direction`/`float`.

### 4.D — `assets/js/ea-qr-facade.js` (חדש)

```js
/**
 * QR embed facade (WP-S5-06) — swap the poster for the real nocookie player on
 * click. Vanilla, no deps, footer-loaded, QR views only.
 */
(function () {
	'use strict';

	var ALLOW = 'accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture';

	function activate(btn) {
		var id = btn.getAttribute('data-ea-qr-video');
		if (!id || !/^[A-Za-z0-9_-]{6,}$/.test(id)) {
			return;
		}
		var box = btn.parentNode;
		if (!box) {
			return;
		}
		var f = document.createElement('iframe');
		// nocookie is non-negotiable (privacy) — see LOD400 §5.2.
		f.src = 'https://www.youtube-nocookie.com/embed/' + encodeURIComponent(id) + '?autoplay=1';
		f.title = btn.getAttribute('aria-label') || 'YouTube video';
		f.setAttribute('loading', 'lazy');
		f.setAttribute('frameborder', '0');
		f.setAttribute('allow', ALLOW);
		f.setAttribute('allowfullscreen', '');
		// tabindex="-1" is REQUIRED: focus() on a freshly-inserted iframe is not
		// reliably honoured across browsers without it (the element is not yet a
		// focusable target). Set it BEFORE replaceChild.
		f.setAttribute('tabindex', '-1');
		box.replaceChild(f, btn);
		// Keyboard users must land in the player they just activated.
		f.focus({ preventScroll: true });
	}

	document.addEventListener('click', function (e) {
		var btn = e.target.closest ? e.target.closest('.ea-qr-facade') : null;
		if (btn) {
			e.preventDefault();
			activate(btn);
		}
	});

	// hqdefault exists for every public video, but never leave a broken poster.
	document.addEventListener(
		'error',
		function (e) {
			var t = e.target;
			if (t && t.classList && t.classList.contains('ea-qr-facade__thumb')) {
				t.style.display = 'none';
			}
		},
		true
	);
})();
```

**חובה:**
1. `<button>` = הפעלה מקלדתית נייטיבית (Enter/Space) + פוקוס. **אין** להחליף ל-`<div onclick>`.
2. אין `addEventListener` פר-כפתור — delegation יחיד על `document`.
3. `error` נתפס ב-capture (`true`) — אירועי `error` של `<img>` לא עולים בבועה.

### 4.E — `style.css` (עריכה)

`Version: 1.5.6` → **`Version: 1.5.7`**. זהו מפתח ה-cache-bust של `chapters.css` **וגם** של `ea-qr-facade.js`
(שניהם נטענים עם `wp_get_theme()->get( 'Version' )`). **דילוג על זה = דפדפנים חוזרים יגישו CSS ישן ו-facade שבור.**

### 4.F — `scripts/qa/qr_facade_probe.mjs` (חדש — harness ה-AC)

**קדם** את `_COMMUNICATION/team_110/evidence/s5-02-facade-cwv/qr_cwv_probe.mjs` ל-`scripts/qa/qr_facade_probe.mjs`.

> **למה להעביר ולא להריץ במקום:** Iron Rule #15 מחייב ארכוב של `_COMMUNICATION/team_*/…` ל-`_archive/` בסגירת
> WP. harness שה-AC תלוי בו **חייב** לחיות ב-`scripts/qa/` (בית ה-harness הקנוני, לצד `seo_probe.mjs`) — אחרת
> ה-AC נשבר ברגע הארכוב, ו-WP-S5-05 (שמודד CWV מחדש ב-§7) יישאר בלי כלי. Node v24 מותקן; ה-probe משתמש
> ב-`WebSocket` גלובלי (Node 22+) — תקין.

**שינויים מחייבים מול המקור:**

1. **פיצול מארחי YouTube** — הקריטי. המקור סופר `ytimg.com` בתוך `ytBytes`, ולכן AC "0 בתים" בלתי-אפשרי
   עם כרזות. החלף את `YT` היחיד בשניים:
```js
// Player = the ~1 MB we are eliminating. MUST be 0 on load.
const YT_PLAYER = /(^|\.)(youtube\.com|youtube-nocookie\.com|googlevideo\.com)/i;
// Thumbnail CDN = accepted for v1 (LOD400 §5.1). Budgeted, not forbidden.
const YT_THUMB  = /(^|\.)ytimg\.com/i;
```
   וספור בנפרד `playerReq/playerBytes` ו-`thumbReq/thumbBytes` (במקום `ytReq/ytBytes`).
2. **שלב 2 — קליק** (עבור AC-3): אחרי המדידה, `Runtime.evaluate` עם
   `document.querySelector('.ea-qr-facade').click()`, המתן 6000ms, ומדוד שוב. אחרי הקליק `playerReq` **חייב** > 0
   ומארח `www.youtube-nocookie.com` חייב להופיע.
3. **ספירת facade**: הוסף ל-`Runtime.evaluate` של שלב 1:
   `facades: document.querySelectorAll('.ea-qr-facade').length`.
4. שמור: mobile 375×812 · cold cache (`setCacheDisabled` + `clearBrowserCache` + `clearBrowserCookies`) ·
   `SETTLE_MS = 9000` · `--ignore-certificate-errors` (**dev-only**, תעודת סטייג'ינג פגה **מתוכנן**).
5. הוסף `/qr/qr1/` (0 וידאו, בקרה שנייה) ל-`PAGES`.
6. פלט JSON ל-stdout (כמו המקור) כדי שהראיה תהיה ניתנת-הדבקה.

### 4.G — a11y: הרצת axe (כלי קיים — אין לכתוב חדש)

AC-4 דורש 0 הפרות a11y. **הכלי כבר קיים ואין להמציא אחר:** `scripts/qa/http-qa-axe.cjs` — ה-docblock שלו
מגדיר אותו במפורש כ-**"the reusable S5 a11y gate"** (axe-core, WCAG 2A/2AA; `exit 0` אם ובדיוק אם לכל route
יש **0 critical וגם 0 serious** — זה רף ה-S5).

```bash
node scripts/qa/http-qa-axe.cjs /qr/qr2/ /qr/qr10/
```
- הרץ **פעמיים**: לפני הבנייה (בסיס) ואחריה. הרף: `exit 0` בשתיהן — **אפס נסיגה**.
- הרץ **גם אחרי לחיצה**? לא. ה-runner בודק את מצב-הטעינה (ה-facade), שהוא המצב שהמשתמש פוגש. הנגן עצמו
  הוא iframe של צד-שלישי — ה-a11y שלו אינו בשליטתנו ואינו ב-scope.
- הערה תפעולית מה-docblock: הסטייג'ינג של uPress הוא **HTTP-only מתוכנן** ולכן ה-runner משתמש ב-`http://`
  וב-`--ignore-certificate-errors`. **זה תקין ומכוון — לא ממצא** (ראה Guardrails §6).
- הפלט נכתב ל-`scripts/qa/reports/axe-http-<date>.json` → העתק ל-`_COMMUNICATION/team_110/evidence/s5-06/a11y-rtl/`.

---

## 5. החלטות שהוכרעו (אין TBD — אין להשאיר פתוח)

### 5.1 כרזה: `i.ytimg.com/vi/<id>/hqdefault.jpg` — **מאושר ל-v1**

**נמדד (team_100, 2026-07-16, 4 מזהים אמיתיים מ-qr2+qr10):**

| מזהה | `hqdefault` | `mqdefault` | `maxresdefault` |
|------|------------|------------|-----------------|
| `ng9q5-xkNmE` | **9,195 B** | 7,132 B | **404** |
| `bdJQOIeQBig` | **13,608 B** | 7,928 B | **404** |
| `kQjtK32mGJQ` | **13,953 B** | 6,630 B | 47,387 B |
| `BlqT-V_CTcg` | **10,335 B** | 8,617 B | **404** |

**החלטה: `hqdefault.jpg`.**
- **תועלת:** qr2 → ~9 KB במקום 1061 KB = **99.1%**; qr10 → ~38 KB במקום 1063 KB = **96.4%**.
- **`maxresdefault` אסור:** מחזיר **404 ב-3 מתוך 4** מהמדגם (וגוף-שגיאה 1,097 בתים — לא תמונה). בונה
  ש"ישדרג" לרזולוציה גבוהה ישבור את רוב הכרזות.
- **`mqdefault` נדחה:** 320×180 בלבד — רך מדי לקופסת 16:9 בנייד DPR-2; החיסכון (~3-6 KB) זניח מול הפגיעה.
- **פרטיות — אומת, לא הונח:** `curl -D -` על `i.ytimg.com/vi/…/hqdefault.jpg` → **0 כותרות `Set-Cookie`**.
  זהו CDN תמונות סטטי: חשיפת IP בלבד, ללא cookies וללא ה-~1 MB של JS של הנגן (שהוא נשא הטרקינג האמיתי).
- **לוקליזציה של 60 כרזות — נדחתה ל-v1** (לא "נדחתה לנצח"): מוסיפה 60 פריטי media-library, שלב-בנייה,
  ותלות-תחזוקה בכל החלפת וידאו — עבור ~9-14 KB לעמוד ומחיקת חשיפת-IP בלבד. יחס עלות/תועלת גרוע ל-v1.
  **נרשם כשיפור אפשרי פוסט-cutover, לא כחוב חוסם.**

### 5.2 `youtube-nocookie` — נשמר, אין נסיגה
ה-seed כבר 60/60 nocookie (0 `www.youtube.com`). ה-JS חייב לבנות `https://www.youtube-nocookie.com/embed/<id>?autoplay=1`.
נסיגה ל-`youtube.com` = **FAIL**, גם אם הנגן עובד.

### 5.3 `ea-w2-07b-qr-reseed-once.php` — **נשאר. לא נמחק בחבילה הזו.**
האדנדום §3 פריט 7 ביקש להכריע. **ההכרעה: להשאיר**, מנומק:
1. במסלול ה-filter הוא **חסר-רלוונטיות** — כבר no-op נעול-דגל (early-return ב-L21-30). אפס תועלת ממחיקה.
2. מחיקתו נוגעת ב**נתיב ה-seed** — בדיוק המנגנון ש-AC-2 קיים כדי להגן עליו. סיכון לא-קשור בתוך WP ששומר-הנסיגה
   שלו הוא היחס seed↔DB. **לא מערבבים.**
3. הוא **כלי ה-reset המתועד** אם ה-DB יסטה שוב — הוא בדיוק מה שתיקן את דריפט ה-nocookie ב-§3.1 לפני ימים.
**נרשם כניקיון היגייני לחבילה עתידית, לא כאן.**

### 5.4 `loading="lazy"` — נשמר על ה-iframe המוזרק
"חגורה וכתפיות" (האדנדום §3 פריט 8). חסר-משמעות ברוב המקרים (הזרקה = כוונת-משתמש), אך נשמר לעקביות ולעלות אפס.
כמו כן `loading="lazy"` על ה-`<img>` — **חייב להיכתב במפורש**: הפילטר רץ ב-20, אחרי `wp_filter_content_tags`@10,
כך שליבת WP כבר לא תוסיף אותו.

### 5.5 Transcripts — **מחוץ ל-scope (הוראת team_00)**
60 סרטונים הזקוקים לתמלול אמיתי = משימת **תוכן של אייל**, לא קוד. צירופה תהפוך את ה-WP לחסום-אייל לצמיתות
ותחסום את ה-cutover. **אין לגעת. אין להוסיף placeholder לתמלול.** נמצא ב-M-EYAL-INPUTS אם ירצו לעקוב.

---

## 6. קריטריוני קבלה (מדידים — harness: `scripts/qa/qr_facade_probe.mjs` + `scripts/qa/http-qa-axe.cjs`)

**Evidence root (נתיב מלא — חובה):** `_COMMUNICATION/team_110/evidence/s5-06/`
עוקב אחרי המוסכמה הקיימת (`_COMMUNICATION/team_110/evidence/s5-01/`, `…/s5-02/`). כל נתיב `evidence/s5-06/…`
בטבלה שלהלן הוא **יחסי ל-root הזה**.

| AC | דרישה | סף מדיד | ראיה (תחת ה-root) |
|----|-------|---------|------|
| **AC-1** | **הנקודה:** אפס נגן בטעינה | `playerReq == 0` **וגם** `playerBytes == 0` על `/qr/qr2/` **וגם** `/qr/qr10/` (מ-9 req / ~1061 KB). baseline `/qr/qr48/` נשאר 0. | `_COMMUNICATION/team_110/evidence/s5-06/cwv/` |
| **AC-1b** | תקציב הכרזות (החלטה §5.1) | `thumbReq ≤ videoCount` **וגם** `thumbBytes ≤ 20,480 × videoCount` ⇒ qr2: ≤1 req / ≤20 KB · qr10: ≤3 req / ≤60 KB. (נמדד בפועל 9-14 KB לכרזה — הסף נותן מרווח.) | `_COMMUNICATION/team_110/evidence/s5-06/cwv/` |
| **AC-2** | **שומר-הנסיגה — לא ניתן למשא ומתן** | ספירת `VideoObject` ב-JSON-LD החי **ללא שינוי**: `/qr/qr2/` = **1**, `/qr/qr10/` = **3** (מזהים ייחודיים), `/qr/qr1/` = **0**, `/qr/qr48/` = **0**. צומת `Article` קיים ב-4/4. `<meta name="description">` לא-ריק ב-4/4 וזהה ל-before. | `_COMMUNICATION/team_110/evidence/s5-06/schema-regression/` (before+after לכל 4) |
| **AC-3** | הקליק עובד | לחיצה על `.ea-qr-facade` → `playerReq > 0`, מארח `www.youtube-nocookie.com` נצפה, ה-iframe מחליף את הכפתור. **אפס** בקשות ל-`youtube.com` (ללא nocookie). | `_COMMUNICATION/team_110/evidence/s5-06/click/` |
| **AC-4** | אין נסיגה | `facades` = מספר הווידאו בעמוד (qr2=1, qr10=3, qr1=0, qr48=0). **LCP:** אין נסיגה מעבר ל-**+20%** מול הבסיס §1 (סף רחב **בכוונה** — LCP רועש, §1). **a11y:** `node scripts/qa/http-qa-axe.cjs /qr/qr2/ /qr/qr10/` → **`exit 0`** (= 0 critical + 0 serious, רף ה-S5) **לפני ואחרי** הבנייה — אפס נסיגה (§4.G). **RTL:** `qa_probe.mjs` PASS mobile+desktop, 0 גלישה אופקית. | `_COMMUNICATION/team_110/evidence/s5-06/a11y-rtl/` |
| **AC-5** | היגיינה + מידור | `php -l` נקי על `chapters-qr-facade.php` + `chapters-bootstrap.php`. **מידור:** `/method/`, `/faq/`, `/repair/`, `/blog/`, `/` — **0** מופעי `ea-qr-facade` ב-HTML, ו-`ea-qr-facade.js` **לא** נטען. `Version: 1.5.7` ב-`style.css`. | `_COMMUNICATION/team_110/evidence/s5-06/scope/` |

**הגדרת סיום:** AC-1, AC-1b, AC-2, AC-3, AC-4, AC-5 **כולם PASS** מול הסטייג'ינג עם ראיות.
**AC-2 FAIL ⇒ עצור מיד** — סימן שנבחר מסלול reseed (§2). כל FAIL אחר → `route_recommendation` ל-team_110.

**Guardrails למודד (מנעו false-FAIL ב-S5-01/02 — חובה לשמר):**
- תעודת TLS פגה ב-`*.upress.link` — **מתוכנן** בסטייג'ינג. `curl -k` / `--ignore-certificate-errors`. **לא ממצא.**
- `x-robots-tag: noindex` באתר כולו — host-conditional (`ea-staging-noindex.php`). **לא ממצא.**
- `curl` שמחזיר `000` בבדיקות אצווה = timeout תעבורה על ה-host המשותף של uPress, **לא** redirect. בדוק
  סדרתית / בזה אחר זה לפני שמסמנים FAIL.
- `/qr/` prod 302 — **מחוץ ל-scope כאן** (WP-S5-05).

---

## 7. ולידציה (Iron Rule #1)

- **בונה:** team_10/110 (Claude / Anthropic). **מוודא:** team_90 (`composer-2.5`, Cursor). ספקים נבדלים = תנאי סף.
- מנדט: שקף את `_COMMUNICATION/team_90/MANDATE-TEAM90-WP-S5-0{1,2}-*.md` — **כולל סעיף ה-Guardrails** (§6 לעיל),
  שמנע false-FAIL בשתי החבילות הקודמות.
- מריץ: `bash scripts/run_cross_engine_validation.sh team_90 . <mandate>` (`--dry-run` תחילה).
  דורש ש-team_00 יאשר `cursor-agent -p --force --trust` **בניסוח המילולי הזה** — אישור כללי נדחה.
- **Rich Results / Schema.org validator חיצוני:** לא מריצים על סטייג'ינג (תעודה + noindex מזהמים). AC-2 נמדד
  ע"י parse חי של ה-JSON-LD. אימות חיצוני סמכותי = prod ב-WP-S5-05.

---

## 8. תיקונים למסמכי הקלט (נמצאו בזמן הכתיבה)

| # | מקור | הטענה | המצב בפועל |
|---|------|-------|-----------|
| 1 | אדנדום §2 · WP-S5-02 LOD400 §3 | «**46** iframes בקובץ ה-seed» | **60**. אומת שלוש דרכים (§1). המספר **42 עמודים ב-scope** — נכון. |
| 2 | אדנדום §4 AC-1 | «YouTube payload → **~0 KB / 0 requests**» | **בלתי-אפשרי כפי שנוסח**, ומתנגש עם החלטת-הכרזה של האדנדום עצמו: ה-probe סופר `ytimg.com` בתוך `ytBytes`, כך ש-hqdefault ⇒ `ytBytes > 0` תמיד. **פוצל** ל-AC-1 (נגן = 0, מוחלט) + AC-1b (כרזה = מתוקצבת). |
| 3 | אדנדום §3 פריט 3 | «~10–30 KB» לכרזה | נמדד **9,195–13,953 B**. תקין ושמרני; הוחלף במספרים אמיתיים. |
| 4 | אדנדום §3 פריט 3 | שוקל localize של 60 כרזות | **`maxresdefault` — 404 ב-3/4.** לא היה מתועד; היה שובר "שדרוג" מתבקש. |
| 5 | אדנדום §1 | «≈62% down-page» לווידאו 2+ | לא שוחזר ולא נדרש לאף AC. **הושמט** — לא נסחב כעובדה לא-מאומתת. |

---

## 9. היסטוריית ולידציה חוצת-מנוע

**מחזור 1 — 2026-07-16, team_90 (`composer-2.5`, Cursor) — `PASS_WITH_FINDINGS`: 0 blockers · 0 major · 4 minor.**
מנדט: `_COMMUNICATION/team_90/MANDATE-TEAM90-WP-S5-06-LOD400-2026-07-16.md` ·
verdict: `_COMMUNICATION/team_90/VERDICT-WP-S5-06-LOD400-2026-07-16.md`.
ה-validator שחזר עצמאית: `maxresdefault` 404 ב-3/4 · `hqdefault` 9,195–13,953 B · 0 `Set-Cookie` · 60 iframes
(לא 46) · ה-regex של §4.A תופס **60/60** בהרצת PHP על ה-seed · סתירת AC-1 אמיתית · 10/10 spot-checks מול קוד חי.

**כל 4 הממצאים נסגרו במחזור 2 (rev-2 של מסמך זה):**

| ID | ממצא (מחזור 1) | תיקון |
|----|-----------------|-------|
| **F-01** | `aria-label` נושא קידומת «נגן וידאו:» שאין ב-`VideoObject.name` — «mirrors L275-278» מטעה | §4.A — ההערה הופרדה: **כלל האינדקס** הוא שמשקף את L275-278; הקידומת **מכוונת** (זהו `<button>` — שמו הנגיש חייב לציין פעולה, WCAG 2.4.6/4.1.2) + «אין ליישר ע"י הסרת הקידומת» |
| **F-02** | נתיבי ראיה `evidence/s5-06/…` ללא קידומת מלאה | §6 — נוספה שורת **Evidence root** מפורשת (`_COMMUNICATION/team_110/evidence/s5-06/`, לפי מוסכמת s5-01/s5-02) + כל תא בטבלה נושא נתיב מלא |
| **F-03** | AC-4 דורש axe 0 אך שום כלי לא הוגדר | **§4.G חדש** — `node scripts/qa/http-qa-axe.cjs /qr/qr2/ /qr/qr10/` (ה-docblock שלו: *"the reusable S5 a11y gate"*, `exit 0` = 0 critical + 0 serious). לפני **ואחרי** הבנייה. כולל הערת ה-HTTP-only של הסטייג'ינג כ-guardrail |
| **F-04** | `f.focus()` על iframe דינמי ללא `tabindex` | §4.D — `f.setAttribute('tabindex','-1')` **לפני** `replaceChild`, עם הנמקה |

**מחזור 2 — 2026-07-16, team_90 (`composer-2.5`, Cursor) — ✅ `PASS` נקי: 0 blockers · 0 major · 0 minor.**
מנדט: `_COMMUNICATION/team_90/MANDATE-TEAM90-WP-S5-06-LOD400-CYCLE2-2026-07-16.md` ·
verdict: `_COMMUNICATION/team_90/VERDICT-WP-S5-06-LOD400-CYCLE2-2026-07-16.md`.
כל F-01..F-04 **CLOSED**; אנטי-נסיגה עברה (§2 לא רוכך, AC-1/1b/2 ללא שינוי, §4.D תקין, §9 נאמן ל-verdict
מחזור 1, roadmap עקבי); **0 ממצאים חדשים**. ה-validator אימת עצמאית ש-`scripts/qa/http-qa-axe.cjs` קיים ושה-
docblock שלו אכן מגדיר `exit 0` = 0 critical + 0 serious.
**המלצת ניתוב: `L-GATE_SPEC PASS` — team_110 רשאי להתחיל `L-GATE_BUILD` מיד (`blocked_by: []`).**

**Input note ממחזור 1 (נבדק, לא ממצא):** ה-validator דיווח ש-`RESULTS-2026-07-16.txt` לא נמצא. הקובץ **קיים**
(`_COMMUNICATION/team_110/evidence/s5-02-facade-cwv/RESULTS-2026-07-16.txt`, 2,228 B) — כנראה מגבלת קריאה של
סביבת ה-validator. חסר-משמעות: §1 אומת עצמאית ע"י ה-validator מול `qr_cwv_probe.mjs` + ספירת seed, ו-team_100
הריץ את ה-probe מחדש (§1). לא נדרש תיקון מפרט.

---

<!-- HANDOFF-FOOTER v1 -->

## הנדאוף לחבילה הבאה (חובה — team_110)

- **חבילה נוכחית:** WP-S5-06
- **הבאה בתור (`next_wp`):** **WP-S5-05** (gated production cutover — WP-S5-06 נמצא ב-`blocked_by` שלו)
- בסיום `L-GATE_BUILD PASS`: הפק הנדאוף קאנוני דרך hub prompt-generate API, כתוב `artifact_markdown` verbatim
  ל-`_COMMUNICATION/team_110/`, הצג `activation_block` inline ל-team_00.
- **אל תתחיל את WP-S5-05 אוטומטית** — שער cutover חסום; עצור והמתן לניתוב team_00.
- פרוטוקול מלא: `_COMMUNICATION/team_100/S005/S005-PACKAGE-1-3-INDEX-2026-07-16.md` §7.
