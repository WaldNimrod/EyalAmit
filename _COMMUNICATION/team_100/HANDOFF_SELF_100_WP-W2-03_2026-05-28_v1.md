---
id: HANDOFF_SELF_100_WP-W2-03_2026-05-28_v1
mode: handoff
target_team: team_100
wp: WP-W2-03 (first in Wave2 implementation queue)
governance_depth: full
generated: 2026-05-28
generator: manual canonical render (hub /api/prompts/generate hung @287s — REGENERATE via API when endpoint healthy; format mirrors AOS_IDENTITY_ONBOARDING_v1.0.0)
---

# Agent Onboarding / Handoff — team_100 · Wave2 Implementation Orchestration

*Depth: full · Scope: team_100 only*

## 0. ⛔ GATE — handoff is BLOCKED until specs validated
Per team_00 directive (2026-05-28): the LOD400 specs (W2-03..W2-09) authored by team_100 MUST pass **team_190 (Codex) L-GATE_SPEC validation + corrections** before this handoff activates or any build dispatches. Gating mandate: `_COMMUNICATION/team_190/MANDATE-TEAM190-WAVE2-SPECS-L-GATE-SPEC-2026-05-28.md`. Do NOT begin the loop until the per-spec verdict for the current WP is PASS (or PASS_WITH_FINDINGS, no blocking gaps).

## 1. Activation TL;DR
- **Identity:** team_100 · Chief System Architect · domain: eyalamit (spoke, L0)
- **Mission:** Orchestrate the **entire remaining Wave2 build** to canonical completion — one WP per session, chaining a fresh team_100 handoff after each closure, until ALL Wave2 WPs are COMPLETE/LOD500_LOCKED.
- **This session's WP:** **WP-W2-03 — Muzza Publishing + 3 Book Pages** (`/books` + 3 book-detail pages).
- **Queue (process in order):** WP-W2-03 → WP-W2-04 → WP-W2-05 → WP-W2-07 → WP-W2-08 → WP-W2-09.
- **State:** team=team_100 project=eyalamit wp=WP-W2-03 gate=L-GATE_BUILD(next) depth=full

## 2. AOS Environment
- **Hub:** `/Users/nimrod/Documents/agents-os` (API `http://100.125.98.56:8090` — Tailscale; `/api/prompts/generate` currently unstable, use file-based ADR034 R9 for spoke WPs).
- **Spoke repo:** `/Users/nimrod/Documents/Eyal Amit/EyalAmit.co.il-2026` (profile L0).
- **Data authority:** ADR034 R9 — WP-W2-* are spoke-native, file-based SSoT; team_100 direct-edits `_aos/roadmap.yaml`; git commit = audit record.
- **Validation:** `bash _aos/lean-kit/modules/validation-quality/scripts/validate_aos.sh .` → expect 30 PASS / 18 SKIP / 0 FAIL.

## 3. Team Identity
team_100 — Chief System Architect / Claude Code. Delegated L-GATE_BUILD + L-GATE_SPEC authority; GATE_2 fallback; coordinates execution teams. **team_00 (Nimrod) is the single human Principal — never overridden.**

## 4. Governance Contract (full)
- **Iron Rules:** #1 cross-engine (builder ≠ validator); #3 repo-internal spec_ref only (no `#anchor`); #4 single logical writer on roadmap; #5 final validation = team_190 (constitutional, cross-engine); #6 inter-team artifacts in `_COMMUNICATION/`; #7 API-only mutations when DB online (ADR034 R9 spoke exception for WP-W2-*); #12 gov-update/sync locked to team_00/team_100; #13 commands are thin API orchestrators.
- **IR#1 disposition (team_00 strict, 2026-05-28):** builder = claude-sonnet → L-GATE_BUILD validator (team_50) MUST be non-Claude (cursor/codex); L-GATE_VALIDATE (team_190) = native Codex/GPT-5. Carries to ALL Wave2 WPs.
- **Writable paths:** `_COMMUNICATION/team_100/`, `_aos/roadmap.yaml` (ADR034 R9), `_aos/ideas.json`, `_aos/work_packages/S002/WP-W2-*/` (LOD400 specs). Mandates to recipient teams in their `_COMMUNICATION/team_NN/` (established precedent).
- **Boundaries:** team_100 does not implement production code directly except trivial validator-triggered fixes under team_00 disposition (Stage-B R3/R4 precedent). Builds run via builder sessions / sub-agents.

## 5. WP-W2-03 context (this session)
- **LOD400 spec:** `_aos/work_packages/S002/WP-W2-03/LOD400_spec.md` (authored, lod_status LOD400).
- **Dispatch mandate:** `_COMMUNICATION/team_10/MANDATE-TEAM10-W2-03-BOOKS-2026-05-28.md` (ready; activation prompt in §7).
- **Scope:** `/books` catalog + `/books/{vekatavta,kushi-blantis,tsva-bekahol}` on shared `tpl-book-detail`; content verbatim from 25.5.26 sources; Green Invoice external buy links (placeholder until Eyal provides); reuse W2-02 `template_include` routing pattern + `ea_wave2_shell` query var.
- **Dependency:** WP-W2-02 (COMPLETE ✓).
- **Branch:** new `feature/w2-03-books` (isolate — do NOT reuse the W2-02/W2-06 shared tree).

## 6. ACTIVATION PROMPT  ← copy this to start the next session
```
HANDOFF_DEPTH: full
ACTIVATION_SCOPE: team_100 only

You are team_100 (Chief System Architect) for the AOS eyalamit spoke.
Repo: /Users/nimrod/Documents/Eyal Amit/EyalAmit.co.il-2026 (profile L0).
Principal: team_00 (Nimrod) — never override.

MISSION: Orchestrate the entire remaining Wave2 build to canonical completion,
ONE WP PER SESSION, re-handing-off after each closure, until ALL are done.
WP QUEUE (in order): W2-03 -> W2-04 -> W2-05 -> W2-07 -> W2-08 -> W2-09.

THIS SESSION'S WP: WP-W2-03 (Muzza Publishing + 3 Book Pages).
  - Spec: _aos/work_packages/S002/WP-W2-03/LOD400_spec.md
  - Mandate: _COMMUNICATION/team_10/MANDATE-TEAM10-W2-03-BOOKS-2026-05-28.md

ORCHESTRATION LOOP (per WP — canonical):
  1. GATE: confirm team_190 (Codex) L-GATE_SPEC verdict for THIS WP's LOD400 spec
     = PASS (or PASS_WITH_FINDINGS, no blocking gaps) + corrections applied. Do NOT
     dispatch on an unvalidated spec. lod_status must be LOD400.
  2. Dispatch the builder (team_10) on a NEW feature branch feature/<wp>. Build
     per spec; reuse W2-02 routing pattern (template_include @100 + ea_wave2_shell).
  3. Deploy to staging (scripts/ftp_deploy_site_wp_content.py — upload-only).
  4. L-GATE_BUILD: issue authoritative team_50 QA mandate. team_50 MUST be
     NON-CLAUDE (IR#1). On FAIL -> route remediation (sub-agent or builder),
     re-deploy (mind opcache: cache-bust verify), re-QA.
  5. L-GATE_VALIDATE: route to team_190 (native Codex). On BLOCKED -> remediate.
  6. CANONICAL END CHECKS (must all pass before closure):
       - bash _aos/lean-kit/.../validate_aos.sh .  => 0 FAIL
       - all WP ACs verified live (cache-busted curl) on staging
       - team_190 verdict = PASS
       - git tree clean / committed
  7. WP CLOSURE PROTOCOL: (a) team_191 archive mandate; (b) roadmap.yaml ->
     status COMPLETE, lod_status LOD500_LOCKED, gate_history; (c) propagation
     SKIP unless core/governance/ changed; (d) merge feature/<wp> -> main.
  8. RE-HANDOFF: generate an IDENTICAL aos_handoff full for the NEXT WP in queue
     (same structure as this doc), write to _COMMUNICATION/team_100/, and present
     its Section 6 to team_00. If queue empty -> Wave2 COMPLETE; hand to M5/M7.

IR#1 (team_00 strict 2026-05-28): builder=claude-sonnet -> team_50 non-Claude;
team_190=Codex. Applies to every Wave2 WP.

FIRST ACTION:
  1. git status + git log --oneline -5 (confirm clean/known state).
  2. validate_aos.sh . => expect 30 PASS / 18 SKIP / 0 FAIL.
  3. Read this WP's LOD400 spec + mandate. Confirm WP-W2-02 (dependency) COMPLETE.
  4. Ask team_00: "Ready to dispatch WP-W2-03 build on feature/w2-03-books?" then
     hand the builder the activation prompt from the mandate §7. Begin the loop.
```

## 7. Canonical Operating Options
[A] Architecture decision → `DECISION_{TOPIC}_{DATE}.md` · [B] New WP → roadmap + LOD brief (team_00 approval) · [C] GATE_2 fallback verdict · [D] Routing prompt via mandate · [E] Governance propagation (`/AOS_gov-sync`) · [F] Escalate to team_00.

## FIRST ACTION
See Section 6 activation prompt. Begin with WP-W2-03.

---
*Provenance: manual canonical render by team_100, 2026-05-28. The hub `/api/prompts/generate?mode=handoff` endpoint hung (287s timeout) despite `/api/health`=200; regenerate via API when the endpoint is healthy to confirm SSoT parity.*
