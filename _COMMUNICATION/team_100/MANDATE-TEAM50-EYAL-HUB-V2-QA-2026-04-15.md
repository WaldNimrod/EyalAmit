# מנדט צוות 50 — QA מקיף ומלא — Eyal Client Hub V2

**תאריך:** 2026-04-15  
**עדכון מהותי:** 2026-04-16 — הגדרת משימה: **בקרת איכות מקיפה ומלאה** (ראו §1).  
**מוציא:** צוות 100  
**נמען:** צוות 50 (QA)  
**סביבת עבודה:** Cursor — workspace שורש `EyalAmit.co.il-2026`

---

## 1. מטרה — QA מקיף ומלא למערכת Hub

**משימת צוות 50 במנדט זה היא אחת:** לבצע **בקרת איכות מקיפה ומלאה** על **מערכת** Eyal Client Hub V2 (אתר סטטי בנתיב Hub), מול [`EYAL-CLIENT-HUB-V2-LOD400-2026-04-15.md`](./EYAL-CLIENT-HUB-V2-LOD400-2026-04-15.md) **§9 — כל** קריטריון קבלה **AC-01 עד AC-15**, ובהתאם ל־[`docs/CLIENT_HUB_APPENDIX_EYAL.md`](../../docs/CLIENT_HUB_APPENDIX_EYAL.md).

| מונח | משמעות |
|------|--------|
| **מקיף** | אין לדלג על AC או על דף ליבה מ־LOD400 §2 בשל נוחות; כל פריט בטבלת §9 מקבל שורה בדוח (תוצאה + ראיה או `PENDING`/`UNVERIFIED` מנומק). |
| **מלא** | בדיקות פונקציונליות, תוכן גלוי (כולל AC-03), נגישות מינימום (AC-14), ייצואי JSON (AC-07–AC-10), רגרסיית Standard (AC-15), ו־**אימות שרת** (AC-12) **כאשר** קיים URL ציבורי — לא «דגימה חופשית» במקום חובה. |
| **מערכת** | כל דפי הליבה ב§2 + נכסים תחת `files/` ו־`mockups/` לפי אפיון; קישורים פנימיים וחיצוניים לפי סיכון. |

**סביבת ביצוע:** כשיש **כתובת Hub ציבורית חיה** — הבדיקה חייבת לכלול אימות **בשרת** (HTTP/HTTPS, מטמון, קבצים סטטיים). כשאין URL — עדיין חובה **מעבר מלא** על כל ה-AC, עם תיעוד מפורש מה נבדק מקומית ומה נשאר **PENDING DEPLOY** עד לפריסה.

---

## 2. קריאה חובה לפני התחלה

1. [`_communication/team_50/onboard_team50.md`](../team_50/onboard_team50.md) — אונבורד מלא.
2. [`EYAL-CLIENT-HUB-V2-LOD400-2026-04-15.md`](./EYAL-CLIENT-HUB-V2-LOD400-2026-04-15.md) — טבלת AC §9 (**v1.1**, כולל AC-15).
3. [`docs/CLIENT_HUB_APPENDIX_EYAL.md`](../../docs/CLIENT_HUB_APPENDIX_EYAL.md) — סטייג'ינג מול פרודקשן, Varnish.
4. **מסמך מסירה של צוות 10** — מומלץ `EYAL-HUB-V2-DELIVERY-TEAM10-*.md` תחת `_communication/team_10/`: שורש URL (אם קיים), purge, מיפוי AC.
5. אם קיים דוח QA Hub קודם — השוואת רגרסיה.

---

## 3. תוצר (Done) — דוח QA מקיף

קובץ דוח יחיד: `_communication/team_50/EYAL-HUB-V2-QA-REPORT-TEAM50-<תאריך>.md`

**חובה — מבנה מינימלי:**

1. **הצהרת היקף:** האם הבדיקה כללה שרת ציבורי (כן/לא) ו־URL בסיס.
2. **טבלת AC מלאה:** שורה לכל **AC-01…AC-15** — עמודות: תוצאה (PASS / FAIL / PENDING / UNVERIFIED) · ראיה (URL, צילום, ציטוט, פקודה) · הערה.
3. **דפי ליבה §2:** לכל דף — טעינה, אקורדיון (אם חל), ייצואים רלוונטיים.
4. **קישורים:** סריקת `href` פנימיים (מלאה); חיצוניים — לפי הערכת סיכון (מלא או דגימה מנומקת).
5. **קבצים סטטיים:** `files/` ו־`mockups/` — HTTP 200 בשרת כשזמין; אחרת סימון PENDING.
6. **תוצאה כללית:** PASS / PASS WITH NOTES / FAIL.
7. **מטמון:** האם נדרש `?nocache=` או purge לצורך אימות אמיתי.

---

## 4. גבולות

- לא לתקן קוד — רק דוח ושיוך באגים לצוות 10.
- לא לשנות אפיון — פערים מדווחים לצוות 100.

---

## 5. פרומט אקטיבציה (זהות + קונטקסט מלא)

**מסמך מרכזי (העתקה לסשן Cursor חדש):**  
[`EYAL-HUB-V2-ACTIVATION-PROMPTS-ALL-TEAMS-2026-04-15.md`](./EYAL-HUB-V2-ACTIVATION-PROMPTS-ALL-TEAMS-2026-04-15.md) — **סעיף «צוות 50 — QA (Cursor)»**.

---

**חתימת צוות 100:** מנדט פעיל לאחר מסירת צוות 10; **QA מלא בשרת** — לאחר פריסה ו־URL.
