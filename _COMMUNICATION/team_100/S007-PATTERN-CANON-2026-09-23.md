# קאנון דפוסי עמוד — 2026-09-23

סטטוס: נעול. ולידציה חוצת מנועים: PASS, אפס ממצאים, מנוע GPT, מזהה `00a7a30e-117c-4fc1-87db-41f93b047f53`. הבונה (Cursor / Grok) אינו החותם.

מקורות: סקיצת `/repair/` שאושרה («יפה מאושר»), עמוד הבית ועמוד דום נשימה שעוצבו ישירות, ו־[`S007-TYPOGRAPHY-CANON.md`](file:///Users/nimrod/Documents/AOS_V5/EyalAmit.co.il-2026/_COMMUNICATION/team_100/S007-TYPOGRAPHY-CANON.md). אין גיליון לפי סוג פרק. עמוד בוחר מחלקות הצטרפות ב־`chapters.css`. דיוק של טיפוס אחד לא מזיז טיפוס אחר. `:root` ו־[`EYAL-SITE-COLOR-PALETTE.md`](file:///Users/nimrod/Documents/AOS_V5/EyalAmit.co.il-2026/docs/project/EYAL-SITE-COLOR-PALETTE.md) לא נערכים.

## 1. הכרעות שננעלו עם אישור התוכנית

התוכנית קבעה שערך הסקיצה הוא ברירת המחדל אלא אם סומן אחרת. לא סומן אחרת. אלה הערכים החיים:

| פריט | נעול |
|---|---|
| RT-01 גובה | `phero--compact` = 78vh. הירו מדיה רגיל נשאר 88vh |
| RT-01 שכבה | כהות הסקיצה, לא מוגברת |
| RT-01 כפתור | `btn--gw` (מתאר). חול רק על `btn--sand` בתוך פס שוקולד |
| RT-03 | רדיוס הסקיצה. במובייל התמונה נשארת בסדר הסקיצה |
| RT-04 | רק הריווח האנכי שכבר בסקיצה |
| RT-05 | שתי עמודות גם ב־390. כרטיס חמישי לבד. הרדיוס שחי |
| RT-06 | רק השאלה הראשונה פתוחה. המסגרת והאייקון שחיים |
| RT-07 | שכבה וכיוון בלוק הטקסט כמו בסקיצה (`margin-inline-end: 8vw`) |
| RT-08 | יחס 3/4, הגאפ שחי, ארבע עמודות ב־1440, שתיים ב־390 |
| קישור גוף | נשאר טרקוטה (`--terra`). שוקולד לקישור לא הוחל |
| RT-02 | 82ch כבר על `.intro-body`. אין מידה חדשה |

סגור ולא נפתח: פס `#5C3A2E` רק דרך `cta-band--choc`, כפתור `#D8C7B5` עם טקסט שוקולד רק דרך `btn--sand`, פוטר `#0E0905`, דיו על שנהב, `DEAD-01` עד `DEAD-05`. טרקוטה אינה מילוי פס.

## 2. איך מרכיבים עמוד

שמונה דפוסי התיקון, כולם הצטרפות:

| דפוס | מחלקה | מתי |
|---|---|---|
| הירו קומפקטי | `phero--compact` | עמוד פנימי עם תמונת רקע וכפתור מתאר |
| ציפה עומדת | `pfloat--standing` | תמונת אורך ליד טקסט ארוך |
| כיסוי | `split2--cover` | תמונה שממלאת את עמודת הגובה |
| פס קריאה | `cta-band--choc` + `cta-band--stack` + `btn--sand` | יש כותרת, פסקת משנה וכפתור. בלי כפתור יתום |
| כרטיסי נקודה | `point-cards` | כותרת קצרה וגוף שכבר קיימים |
| שאלות | `ea-faq-list--cards` | רק כשביקשו כרטיס |
| פס תמונה | `photo-band` | רקע מלא ובלוק טקסט |
| גלריית דיוקנאות | `gallery--portraits` | לפחות שלוש תמונות שכבר משויכות |

`.pfloat` ב־260px ו־`.gfig` ב־4/3 נשארים לדפים שלא הצטרפו. שמות הסקיצה (`.split`, `.cta`, `.band`) לא נכנסים לתמה.

כפתור בלי כותרת ובלי פסקת משנה לא הופך לפס שוקולד. אין המצאת משפט.

## 3. מפקד בית ודום נשימה

שני העמודים הם מקור לדפוסים, לא חריגה קפואה ולא מוחלפים בסקיצת התיקון. המראה שאושר נשמר. לא שונה CSS שלהם בסבב הזה: כל שורה שונה מהותית נרשמה כדפוס משלה.

### בית

| שורה | הכרעה |
|---|---|
| הירו וידאו | דפוס `hero--video`. לא `phero--compact` |
| «מה זה טיפול» | פרוזה קיימת, 82ch |
| וידאו מוטמע | דפוס `videoblk` |
| שלושה דיוקנאות בפרק אייל | דפוס `portrait-trio`. לא `gallery--portraits` |
| «למי מתאים» עם תמונה בכל פריט | דפוס `whom-cards`. לא `point-cards` |
| מפגש | פרוזה |
| ציטוט על תמונה | דפוס `bleed` הקיים |
| סטודיו | פיצול קיים |
| עדויות | דפוס `ea-testi-cards` |
| השוואת טיפול מול סאונד | דפוס `cmp-pair` |
| «איך מתחילים» | דפוס `.start`. לא `photo-band` |

### דום נשימה

| שורה | הכרעה |
|---|---|
| הירו מדיה | `phero--media` ב־88vh. לא הקומפקטי |
| תוכן עניינים | דפוס `toc` |
| צילום מסך ליד טקסט + lightbox | דפוס `split--doc`. לא ציפה עומדת ולא כיסוי |
| כפתור «רוצה לדבר איתי» בלי כותרת | דפוס הפעולה של העמוד הזה, כי כך עוצב. לא נצבע שוקולד ולא מקבל משפט מומצא |
| צילום וואטסאפ צף | `.pfloat` הישן. לא `pfloat--standing` — זה לא דיוקן אורך |
| כרטיסי עדות | `ea-testi-cards` |
| רשימת הגדרות | דפוס `deflist` |

## 4. מה מורכב בסבב הזה, ומה נשאר

פס עם כותרת, פסקת משנה וכפתור עובר ל־`cta-band--choc` בלי המצאת טקסט:

- `/method/`
- `/sound-healing/`
- `/learning/`
- `/learning/lectures/`
- `/learning/workshops/`
- `/learning/therapist-training/`

נשארים כמו שהם, כי אין כותרת ופסקה שכבר נכתבו, או כי העמוד מוחזק:

- שלושת הספרים: קיפול ארבע שורות עולה. פס הרכישה נשאר כפתור בלי משפט חדש
- טיפול, חנות, כלים, תיקים: אין פס מלא להצטרפות
- צור קשר, מוקש, סטנדים, סטנד רצפתי, מבנה התפריט, כותרות cbDIDG: מוחזקים
- 179 תמונות בלי שיוך: לא נכנסות

## 5. מה עולה עם הגל ולא מחכה לאייל

- עוגיות: שלוש המחרוזות וכפתור «אישור» מלא, «דחייה» מתאר
- קיפול הספרים: ה־PHP כבר על השרת; ה־CSS של הקיפול עולה עם ברירות המחדל
- פוטר: תנועה כמו באתר הישן, צבע `#0E0905`. הסוגר המוקדם בתוך `prefers-reduced-motion` תוקן לפני ההעלאה. `prefers-reduced-motion: reduce` מכבה את התנועה
