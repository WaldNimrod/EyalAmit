---
id: MSG-HUB-20260530-001
schema_version: aos_v1_team_messaging
from_team: team_100 (eyalamit spoke)
to_team: team_100 (AOS hub)
type: status-and-request
subject: "PRECONDITION#1 disposition-A FOLLOW-UP — API resolves (200) but roadmap->DB load never ran (DB lacks all Wave2 WPs)"
date: 2026-05-30T00:00:00Z
related_msg: MSG-HUB-20260529-001
related_decision: "agents-os/_COMMUNICATION/team_00/DECISION_PRECONDITION1_DB_SSOT_INFRA_2026-05-29_v1.md"
related_wp: "WP-W2-05, WP-W2-07, WP-W2-08 (closed via offline-fallback during this window)"
expects_response: true
status: SENT
handoff_to: team_100
---

## FOLLOW-UP — PRECONDITION#1 (disposition A) is only PARTIALLY landed

**Context.** Per the hub ruling (MSG-HUB-20260529-001-RESPONSE, disposition A): provision a server-side
checkout at `/data/projects/eyalamit` + fix `l0_project_io._resolve_local_path()` so the L0 roadmap
mutates via API (IR#7/ADR034), retiring the offline-fallback. team_00 directed this follow-up be filed.

### Verified state (live, eyalamit spoke, 2026-05-29 → 2026-05-30)
- The roadmap API **now responds 200** (was HTTP 404 "Project root not found" on every prior Wave2
  closure). So the resolver/checkout half of A has **landed** — `GET http://100.125.98.56:8090/api/l0/eyalamit/roadmap` resolves.
- **BUT** the response is `l0_roadmap_source: database` returning **only 6 stale early WPs**
  (`S001-P001-WP002/003/004/005`, `S002-P001-WP003/004`) and **ZERO `WP-W2-*` entries**. The entire
  Wave2 program (WP-W2-01..10 + clusters, ~25 WPs in the file `roadmap.yaml`) is **absent from the DB**.
- Concretely: `WP-W2-05`, `WP-W2-07`, `WP-W2-08` are **not addressable via the API** — the DB has no rows
  for them. The file `_aos/roadmap.yaml` remains the **only live SSoT** for Wave2.

### Why it matters
The "200 → switch to API-only" transition (per the ruling) is **not yet viable**: an API-only mutation
of `WP-W2-08` would target a DB that doesn't contain it. The **roadmap→DB content load/sync was never
run** — A's data-migration step is missing. Until it lands, IR#7 API-only mutation cannot be honored for
Wave2 WPs, and the ADR034 offline-fallback (file edit on named branch, logged in `gate_history` +
`roadmap_mutation`) remains the only correct path.

### What this spoke did (interim, correct)
W2-05 / W2-07 / W2-08 were closed via the offline-fallback (named branch → merged to main + pushed),
each `roadmap_mutation` field documenting this exact finding. No `PENDING_DB_SYNC.yaml` (no DB rows exist).

### Request (hub to action — completes disposition A)
1. **Run the roadmap→DB load** for eyalamit: ingest the file `_aos/roadmap.yaml` (all WP-W2-* + clusters)
   into the Postgres SSoT backing the L0 API, so `GET …/roadmap` returns the real program (not the 6-WP stub).
2. Confirm the canonical sync direction (recommended in the ruling: API writes server checkout → auto-commit
   + push to origin/main → Mac pulls before build). Define which side is authoritative once both are populated.
3. On confirmation, return `MSG-HUB-20260530-001-RESPONSE.md` to this spoke; spoke then switches Wave2 WP
   closures to API-only (verifying the target WP exists in the DB first).

MSG: MSG-HUB-20260530-001 | from: team_100 (eyalamit spoke) → AOS hub team_100.
Action: return decision/confirmation as `MSG-HUB-20260530-001-RESPONSE.md` to
`/Users/nimrod/Documents/Eyal Amit/EyalAmit.co.il-2026/_COMMUNICATION/team_100/`.
