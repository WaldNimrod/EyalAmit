# Session Handoff — team_100 · WP-W2-16 post-content completion (full)

**Depth:** full · **Engine:** Claude Code · **Generated:** 2026-06-16 (AOS_handoff→AOS_mail; hand-authored).
**Repo @ handoff:** `origin/main` HEAD `7c3598e`. **Goal (Principal):** deliver Eyal an **as-accurate-as-possible site ASAP**, prepare a version to present live.

## 1. SESSION ACCOMPLISHED
- **WP-W2-15 CR1–CR4 — DONE, triple-PASS, merged to main, pushed.** Site content **33.6% → 96.7% simple / 98.4% weighted; 16/17 sourced pages pass** the content-accuracy gate (only `/mokesh` = CR5). team_50 + team_190 dual-PASS + CR-FINAL clean; all findings closed (incl. legacy `/muzza` & `/muzeh` → single 301 → `/books`; nav/footer chrome → `/books`; `hikikomori → היקיקומורי` unified in source+render).
- **Contact form (C1):** CF7 form **#392** created via `mu-plugins/ea-w2-15-cf7-contact-form-once.php` + wired (`ea_wave2_cf7_form_id`) + **styled to match the site** (`ea-atoms.css` `.ea-contact-form--cf7 …`, tokens only). Recipient = `admin_email` → **verify post-cutover** (`_COMMUNICATION/team_100/CUTOVER-NOTES-WP-W2-15-CONTACT-FORM_2026-06-06.md`).
- **C3** closed (form + WhatsApp, no PII block). **H5/mokesh redirect** → single hop to `/about/moksha`.
- **Eyal info round 2026-06-16** ingested (`INFO-ROUND-EYAL-2026-06-16.md`): homepage video + 9 feedback points + Drive docs (= the same `25.5.26` set; pts 4/5/7/9 already built by CR1–4 → re-check, not rebuild). Mapping done. **Eyal update message drafted** (`MSG-TO-EYAL-UPDATE-2026-06-16.md`) — for team_00 to send.
- **Homepage video compressed** → `local/video-work/` (gitignored): `ea-home-hero-720-muted.mp4` (6.1 MB), `…-1080-muted.mp4` (13.7 MB), `ea-home-hero-poster.jpg`. Source was HEVC 1080p/42.9s/57.9 MB at `~/Downloads/סרטון לאתר של אבא .mov`.
- **WP-W2-16 defined** (`WP-W2-16-POST-CONTENT-COMPLETION-PROGRAM-2026-06-16.md`) + roadmap (parent + 16-A..16-F).
- **4 decisions RESOLVED** (hub/data/decisions.json, status approved; live on hub):
  - **D-EYAL-VIDEO-13 = ב׳** — hero video: **muted background loop, FULL length** (42.9s), no sound control (muted already embedded in the UI).
  - **D-EYAL-TESTIMONIALS-14 = א׳** — carousel from the **existing FB testimonials dataset NOW** (`ea_w2_07_fb_testimonials`); more content from Eyal later.
  - **D-EYAL-ABOUT-URL-15 = ב׳** — **`/eyal-amit` canonical** (SEO/GEO + approved site-tree slug). Flip the redirect.
  - **D-EYAL-MOKESH-16** — **build from `ומה היום.docx`** (drift fix — it WAS mislabeled "background, not a page"), then ask Eyal for final edit.
- Materials hub updated for the next round (G1 received; new group I: I1 more testimonials, I2 re-check). FTP open via IP **79.177.137.188**.

## 2. MISSION (this handoff)
Implement all now-unblocked WP-W2-16 batches, deploy, and prep the Eyal-ready live version. All 4 decisions are resolved → 16-A/B/C/D/E are READY; 16-F verifies.

## 3. EXECUTION — per batch (branch wp-w2-16 off main; ADR034 surgical commits)
- **16-A Hero video** (D-13=ב): copy `local/video-work/ea-home-hero-720-muted.mp4` (+ `-1080` for desktop, optional) + poster into the theme (`assets/video/` or `assets/images/`); deploy them; wire the home hero (`template-parts/blocks/block-hero.php` + `inc/wave2-stage-b.php` hero ctx) to render a **muted autoplay loop `<video>` bg (full length) with the poster**, replacing the gradient. Keep `prefers-reduced-motion` → poster. NB: theme deploy ships only the listed files — add any new asset paths to `scripts/ftp_deploy_site_wp_content.py` if it doesn't sweep the whole theme dir (it uploads the whole `themes/ea-eyalamit/` tree, so assets under it ship automatically).
- **16-B Testimonials carousel** (D-14=א): build a moving (RTL↔LTR auto-scroll) carousel **atom** (new atom + reuse tokens; NOT in ea-tokens.css), replace the static 3-card band on service pages + home, feed from `ea_w2_07_fb_testimonials()` (>3). Accessible (pause, aria). Eyal pt 2.
- **16-C FAQ topic-nav (TOC)** (no decision): sticky topic menu at the top of `/faq` (block-faq-list categories) → anchor jump-to-section + clearer section separation. Eyal pt 3.
- **16-D `/eyal-amit` canonical + nav** (D-15=ב): **flip** the redirect in `inc/wave2-w2-02.php` (`/about` → `/eyal-amit`, remove the reverse), make the editorial render under `/eyal-amit` (`inc/wave2-w2-07.php` `ea_wave2_editorial_route` — it keys on slug), update nav/footer + `<link rel=canonical>`. Also **expose shop pages** (didgeridoos/bags/stands-storage/stand-floor) in the top nav (Eyal pt 9; currently only /repair + /tools-and-accessories shown). Re-point content-diff PAGE_MAP `/about/`→`/eyal-amit/` if the gate should follow.
- **16-E Mokesh** (D-16): extract `ומה היום.docx` (the gate already parses docx: `content-diff.mjs parseDocxSections` / `unzip -p … word/document.xml`), build `/about/moksha` editorial from it **verbatim** (it's currently non-source/mockup). Check older content rounds (`from-eyal/CONTENT*`) for any extra mokesh text. Add the bio intro + dates 1950–2020 as placeholders pending Eyal. Then route to Eyal for final edit.
- **16-F Verification**: bust staging cache (theme version bump in `style.css`); re-verify blog pagination (Eyal pt 6); content-diff (mokesh should now pass); 0-overflow @360/390/414/768 + axe 0/0; per-batch route team_50 → team_190; merge/lock roadmap.

## 4. Context / gotchas
- **Deploy:** `python3 scripts/ftp_deploy_site_wp_content.py` (uploads whole child theme + listed mu-plugins; **add new mu-plugins to its file list**). **Hub:** `python3 scripts/build_eyal_client_hub.py` then **targeted FTP** of root `*.html` + `data/*.json` (the full `ftp_publish_eyal_client_hub.py` hits huge `files/team40/` legacy media and times out before reaching the pages — upload changed pages directly, see this session's snippets).
- **Cache:** assets are `?ver={theme Version}` (`style.css`) — **bump it** to bust CSS/JS cache after edits.
- **Gate:** `scripts/qa/content-diff.mjs` is the calibrated/ratified CONTENT-ACCURACY gate; `content-selfcheck.mjs` is the WP-free proxy for worktree agents (live run is authoritative).
- **APIs:** uPress FTP open via IP 79.177.137.188; **AOS hub API now reachable (HTTP 422, was down)** → reconcile the roadmap DB + decisions via API when convenient (this session wrote them file-fallback).
- **Parallel build pattern (proven):** dispatch 1 page/feature = 1 worktree sub-agent; they fork from the **pre-branch base** (not current HEAD) — verify integration with `git diff main..branch` and pull only the intended files; re-measure with the calibrated gate after integration.
- **HARD RULE:** no "ready"/sign-off to Eyal until each batch's dual-PASS; the Eyal update message (drafted) is for team_00 to send, not us.

## 5. Standing rules
ADR034 named branch per batch (merge on team_00 go); verbatim content; locked atoms / ea-tokens.css untouched (new atoms only where a batch needs one — carousel); `php -l` + `validate_aos .` clean; roadmap single-writer = team_100; surgical commits `Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>`.

## 6. ACTIVATION PROMPT
```
HANDOFF_DEPTH: full
ACTIVATION_SCOPE: team_100

You are team_100 (Chief Architect), Claude Code, on EyalAmit.co.il-2026.
MISSION: implement WP-W2-16 (post-content completion) and prepare an accurate
Eyal-ready live site ASAP. All 4 decisions are resolved.

READ FIRST:
- _COMMUNICATION/team_100/HANDOFF_SELF_100_WP-W2-16_2026-06-16_v1.md (this handoff)
- _COMMUNICATION/team_100/WP-W2-16-POST-CONTENT-COMPLETION-PROGRAM-2026-06-16.md
- _COMMUNICATION/team_100/INFO-ROUND-EYAL-2026-06-16.md + MSG-TO-EYAL-UPDATE-2026-06-16.md
- hub/data/decisions.json (D-EYAL-13/14/15/16 = approved + resolutions)
- _aos/roadmap.yaml WP-W2-16*

DO (branch wp-w2-16 off main):
1. 16-A: integrate local/video-work/ea-home-hero-720-muted.mp4 (+poster) as the home
   hero — muted autoplay loop, FULL length, replace gradient (D-13=ב).
2. 16-C: FAQ topic-nav (TOC) on /faq (no decision).
3. 16-D: flip /eyal-amit canonical (D-15=ב) + expose shop pages in nav (#9).
4. 16-B: testimonials carousel from the FB dataset, >3, moving (D-14=א).
5. 16-E: build /about/moksha from ומה היום.docx verbatim (D-16 drift fix); bio+dates
   as placeholders; then route Eyal for final edit.
6. 16-F: cache-bust, re-verify blog pagination (#6), content-diff + overflow + axe,
   route team_50→team_190 per batch, merge/lock.

Deploy = scripts/ftp_deploy_site_wp_content.py (FTP open via 79.177.137.188).
Hub = build_eyal_client_hub.py + targeted upload of changed pages (avoid team40 media timeout).
HARD: no "ready" to Eyal until per-batch dual-PASS. Content verbatim — zero invention.
FIRST ACTION: branch wp-w2-16; start 16-C (FAQ TOC) + 16-A (video) in parallel.
```
