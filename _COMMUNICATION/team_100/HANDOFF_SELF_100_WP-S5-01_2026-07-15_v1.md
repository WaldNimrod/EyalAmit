# Session Handoff — 100 | S004 (8 WPs, staging final-review) built+cross-engine-validated+delivered. Client-hub fully audited+reconciled against site+full Eyal correspondence (key correction: 48 testimonials already supplied+live, not still-needed). S003 milestone formally closed (40 WPs classified, team_00 sign-off). S005 'Path to Production Cutover' opened, WP-S5-01..05 registered at LOD200 in _aos/roadmap.yaml. Everything committed+pushed to GitHub.


## 1. SESSION ACCOMPLISHED
- S003 closed with per-WP evidence notes; S005 milestone opened; WP-S5-01..05 registered (scope-level LOD200); ground-truth research for WP-S5-03 (legacy/301) fully completed and preserved at _COMMUNICATION/team_100/RESEARCH-WP-S5-03-GROUND-TRUTH-2026-07-16.md — ready to fold directly into LOD400 authoring, no need to re-research.

## 2. IDENTITY SNAPSHOT
## Team Identity
- **Team ID:** 100
- *(team details unavailable)*


## 3. CONTEXT SNAPSHOT
## Work Package — WP-S5-01
*(work package details unavailable — verify wp_id is correct)*


## 4. MANDATORY READS
- `_aos/governance/100.md`
- `_aos/roadmap.yaml`
- `methodology/AOS_IDENTITY_ONBOARDING_v1.0.0.md` (first AOS session only)


## 5. BLOCKERS / OPEN ITEMS
- None. Next task: elevate WP-S5-01 (quick-verify residuals), WP-S5-02 (SEO/GEO ratified-execution completion), WP-S5-03 (legacy/301 completeness) from LOD200 to full LOD400 canonical specs (one index doc + one per-WP LOD400 doc, matching the S004-PACKAGE-SEQUENCE-INDEX pattern), covering dependencies, exact execution sequence, and precise measurable acceptance criteria -- then run the full sequence through cross-engine validation (team_90, Path A / run_cross_engine_validation.sh, composer-2.5) with a correction cycle until clean PASS -- then generate the canonical /AOS_handoff to team_110 for the actual build, and present that handoff inline to team_00 (Nimrod) for routing. Do NOT start the WP-S5 build itself in this handoff's session -- LOD400 authoring + validation only, ending at the team_110 handoff.

## 6. ACTIVATION PROMPT
```
HANDOFF_DEPTH: full
ACTIVATION_SCOPE: 100 only

# Agent Onboarding — 100

*Generated 2026-07-15T22:30:02.448556Z  ·  Depth: full*

## Session bootstrap (paste FIRST — sets identity, base, registered session)

```bash
export AOS_SESSION_TEAM_ID=100 AOS_PROJECT_ID=agents-os
export AOS_API_BASE="${AOS_API_BASE:-http://100.125.98.56:8092}"
bash scripts/aos_session_ctl.sh start WP-S5-01
/AOS_mail check
```

## Activation TL;DR
- **Identity:** 100 · engine: — · role: —
- **Domain:** — · profile: —
- **Assignment:** WP=WP-S5-01 —  · gate=—
- **Task:** —
- **Writes to (first 3):** `_COMMUNICATION/100/`
- **First reads:** `CLAUDE.md` · `_aos/governance/100.md` · `_aos/roadmap.yaml`
- **State:** team=100 project=— wp=WP-S5-01 gate=— depth=full

## AOS Environment
- **Hub:** agents-os (AOS platform — methodology engine + Lean Kit)
- **Platform:** AOS v3.1.2 dashboard / Lean Kit 3.1.10+
- **Universal Iron Rules:** CLAUDE.md §Iron Rules (1–9) — cross-engine, lean-kit snapshots, project roadmap authority, inter-team artifacts, activation prompts, gate authority split, routing display (ADR032), data authority (ADR034), port canon
- **Data authority:** ADR034 — DB-as-SSoT when online (API-only mutations for canonical fields); files retain gate_history + prose
- **Directory canon:** methodology/AOS_DIRECTORY_CANON_v1.0.0.md
- **Agent guide:** `AGENTS.md` (engine-neutral agent onboarding reference)

## Team Identity
- **Team ID:** 100
- *(team details unavailable)*

## Governance Contract
*No governance file found for 100.*

## Work Package — WP-S5-01
*(work package details unavailable — verify wp_id is correct)*

## Session Task
*No task was set when this session was generated.*

**First action:** Before doing any substantive work, ask the user:
> *"What task should I focus on in this session?"*

Present these intuitive options (team-appropriate) so the user can pick quickly or describe a custom task:

- **[A] Continue current work** — resume where session left off
- **[B] Submit work product** — write output artifact to _COMMUNICATION/team_{ID}/
- **[C] Escalate to Team 00** — route decision or blocker to principal

**Completion criteria:** Once the user confirms a task, restate it back in one sentence and proceed. Report the deliverable path + a one-line summary to Team 00 via `_COMMUNICATION/100/` when done.

## Instructions
You are being onboarded as an AOS agent. Read the sections below carefully.

1. **Confirm your identity** — verify your team ID, engine, and role match the Team Identity section.
2. **Read the Governance Contract** — these are your Iron Rules and authority boundaries.
3. **Understand the project** — review the Project Context and Active Modules.
4. **Locate your working directories:**
   - Deliverables: `_COMMUNICATION/100/`
   - Onboarding: `_COMMUNICATION/100/__ONBOARDING_TEAM_*.md`
   - Governance: `_aos/governance/100.md`
5. **Confirm readiness** — respond with a brief summary of your role and current assignment.


FIRST ACTION:
Gate N/A is complete. Check _aos/roadmap.yaml for next gate (L-GATE_SPEC) and _COMMUNICATION/team_100/ for pending actions.
```


## 7. CANONICAL OPTIONS
- **[A] Continue current work** — resume where session left off
- **[B] Submit work product** — write output artifact to _COMMUNICATION/team_{ID}/
- **[C] Escalate to Team 00** — route decision or blocker to principal

## 8. CONTEXT CHECKPOINT (aos_handoff)
```aos-context-checkpoint
{
  "team_id": "100",
  "wp_id": "WP-S5-01",
  "domain": "agents-os",
  "profile": {
    "depth": "FULL",
    "target": "RICH",
    "lifecycle": "NEW",
    "mission_source": "WP_REF"
  },
  "source_versions": {
    "base": "7b9153f29ac936bd",
    "team": "d359ed74ce9a7093",
    "domain": "08555fd886d2438b",
    "wp": "ae165a9134fb4870"
  }
}
```

---

<!-- team_100 SPOKE SUPPLEMENT (hub DB lacks L0 eyalamit spoke → file-SSoT; not part of API 7-section assembly) -->

## נספח אורקסטרציה — team_100 → team_100 (ספוק EyalAmit L0, file-SSoT, סשן חדש)

**קונטקסט:** ה-hub prompt-generate הרכיב את ההנדאוף לעיל, אך מסד ה-hub אינו מכיל את ספוק ה-L0 `eyalamit`, ולכן פרטי המשימה/WP מגיעים מ-SSoT הקובץ במאגר זה. נספח זה משלים את «Session Task» ו-«Work Package» החסרים.

**זהות ותחום:** engine — Claude/Sonnet (מומלץ — עבודת אפיון/LOD400, לא בנייה); domain — `eyalamit` (L0); profile — L0; team — **100 (Chief Architect)**, לא team_110 (הבונה). **סשן זה הוא סשן-אפיון חדש, קונטקסט נקי — לא המשך הסשן שהפיק את ההנדאוף.**

### המשימה המלאה (מהוראת team_00, 2026-07-15/16)
**מטרה:** השלמת חבילות S005 מס' 1+2+3 (WP-S5-01, WP-S5-02, WP-S5-03) באופן מלא וקאנוני — עד רמת LOD400, מאומת חוצה-מנועים, ומוגש כהנדאוף בנייה קאנוני לצוות 110.

**4 השלבים (בסדר, כל שלב תלוי בקודמו):**

1. **בחינה מעמיקה של כלל המהלך** — **שלושת מסמכי מחקר קרקע-אמת כבר מוכנים ומחכים לך** (ר' למטה), אחד לכל חבילה (WP-S5-01, WP-S5-02, WP-S5-03). כולם השתמשו בקריאת קוד ישירה + curl חי מול הסטייג'ינג — לא הנחות. **אתה לא צריך לחקור מחדש** — קרא את שלושתם ועבור ישר לשלב 2.

2. **אפיון LOD400 מלא לכלל הרצף** — לכתוב:
   - מסמך-אינדקס אחד (בדומה ל-`_COMMUNICATION/team_100/S004/S004-PACKAGE-SEQUENCE-INDEX-2026-07-15.md`): `_COMMUNICATION/team_100/S005/S005-PACKAGE-1-3-INDEX-2026-07-16.md` — כולל: תלויות בין 3 החבילות, רצף ביצוע מדויק, גרף תלויות, ומטריצת קריטריוני-הצלחה כוללת.
   - 3 מסמכי LOD400 נפרדים, אחד לכל חבילה: `_COMMUNICATION/team_100/S005/WP-S5-01-LOD400-2026-07-16.md`, `WP-S5-02-LOD400-2026-07-16.md`, `WP-S5-03-LOD400-2026-07-16.md`. **הרף** (זהה לכל LOD400 קודם בפרויקט): כל מפתח ג'וניור/agent טרי חייב להצליח לממש בלי פערים, ניחושים או הנחות — קבצים, פונקציות, שורות, קריטריוני-קבלה מדידים.
   - עדכן את `_aos/roadmap.yaml`: `lod_status: LOD200 → LOD400` לכל אחת מ-3 החבילות, עם `spec_ref` מצביע למסמך ה-LOD400 החדש שלה.

3. **ולידציה קאנונית מלאה חוצת-מנועים + מעגל תיקון עד PASS מלא** — הרץ דרך `scripts/run_cross_engine_validation.sh team_90 <repo> <mandate_path>` (Path A, מנוע composer-2.5 — cursor vendor; ה-builder של ה-LOD400 עצמו הוא Claude/Sonnet, כך ש-Iron Rule #1 מתקיים אוטומטית ללא בדיקה נוספת). אם הוולידציה מחזירה ממצאים (findings) — תקן את מסמכי ה-LOD400 בהתאם והרץ שוב. **חזור על המעגל עד PASS נקי (0 ממצאים, בדומה ל-S004's cycle3)** — בדיוק כמו שנעשה ל-S004 המקורי. שמור verdict סופי ב-`_COMMUNICATION/team_90/VERDICT-S005-LOD400-COVERAGE-2026-07-16.md`.

4. **הגשה קאנונית לצוות 110** — לאחר PASS נקי: הפק הנדאוף אמיתי דרך `/AOS_handoff` (hub prompt-generate API — `type=onboard_agent`, `mode=handoff`, `team_id=110`, `wp_id=WP-S5-01`, `gate_state=gate_done`, `next_gate=L-GATE_BUILD`), כתוב את `artifact_markdown` verbatim ל-`_COMMUNICATION/team_110/HANDOFF_SELF_{NUM}_WP-S5-01_2026-07-16_v1.md` (מספור רץ: ספרו קבצי `HANDOFF_SELF_*` קיימים ב-`_COMMUNICATION/team_110/` + 1), והצג את ה-`activation_block` **inline בצ'אט** ל-team_00 (נמרוד) לניתוב. **אל תתחיל את הבנייה בעצמך** — זו אך ורק חבילת אפיון+ולידציה; הבנייה בפועל מתחילה רק אחרי שנמרוד מנתב את ההנדאוף.

### מחקר קרקע-אמת שכבר בוצע (השתמש ישירות, אל תחקור מחדש)
- **WP-S5-01** (5 פריטים: באג pagination בבלוג, TOC ב-FAQ, ניווט חנות, אימות QR direct-200, בדיקת-מדגם schema/meta) — `_COMMUNICATION/team_100/RESEARCH-WP-S5-01-GROUND-TRUTH-2026-07-16.md`. **ממצא מרכזי:** 3 מתוך 5 פריטים כבר תקינים לגמרי (הבאג בבלוג תוקן, TOC ב-FAQ בנוי, חנות בתפריט) — הספק שלהם ב-LOD400 צריך להיות "אימות/regression-test", לא "בנייה". פריט QR — עובר בסטייג'ינג אך הסיכון המתועד הוא production-only, לא ניתן לאימות מלא כאן. פריט schema/meta — 2 פערים אמיתיים נמצאו (`/press/` ו-`/qr/qrN/` חסרים meta description + schema ייעודי) עם הצעת-תיקון מדויקת (file:line) כלולה במסמך.
- **WP-S5-03** (400 URL-ים לגאסי מייצוא GSC, מיפוי 301 חי) — `_COMMUNICATION/team_100/RESEARCH-WP-S5-03-GROUND-TRUTH-2026-07-16.md`. **ממצא מרכזי:** 34 חוקי-301 חיים בלבד (`ea-w209-legacy-301-redirects.php`) מול 400 URL-ים; מדגם של 21 גילה 9 פערים אמיתיים המרוכזים בשתי סיבות-שורש ברורות (תחילית-URL ישנה שנייה שלא ממופה כלל + נתיבי `/shop/books/*` חסרים) — לא 400 בעיות נפרדות, אלא כמה תבניות חוזרות שניתן לתקן בבת-אחת. גם זוהה TODO מתועד-מראש (54 הפניות-בלוג לפי slug שמעולם לא קופלו ל-SSoT).
- **WP-S5-02** (sitemap, schema route-completeness, QR-iframe CWV, כלל מחיר-Offer) — `_COMMUNICATION/team_100/RESEARCH-WP-S5-02-GROUND-TRUTH-2026-07-16.md`. **ממצא מרכזי:** sitemap כבר מיושר לחלוטין (אין פעולה — עולה חי רק ב-cutover); QR-iframe lazy-loading כבר חי (46/46), החסר הוא רק החלטת facade/תמלול + reset ל-flag `ea_w2_07_qr_seeded_v3` לתפיסת תיקון קוסמטי; כלל Offer-price תקין ורדום (יהפוך לרלוונטי כשמחירים ייכנסו). **הפער האמיתי היחיד**: `/press/`, `/shows-heritage/` (השם `/shows/` בתוכנית הישנה לא מדויק — ה-slug החי הוא `/shows-heritage/`), וכל 48 עמודי `/qr/qrN/` + hub — חסרי schema ייעודי וחסרי noindex מפורש כאחד. **שים לב לחפיפה עם WP-S5-01 פריט 5** (schema/meta route-completeness — אותו ממצא §8 item 7, אותה רשימת routes בדיוק) — אל תכפול עבודה; מומלץ ש-WP-S5-02 יבצע את התיקון בפועל, ו-WP-S5-01 יישאר "אימות בלבד".

### מקורות רקע חיוניים (קרא לפני כתיבה)
- `_aos/roadmap.yaml` — חפש `milestone_ref: S005` לרשומות הקיימות של WP-S5-01..05 (LOD200) + הערת סגירת S003 בכותרת הפרויקט (הקשר מלא).
- `_COMMUNICATION/team_100/SEO-GEO-VALIDATION-DEPLOYMENT-PLAN-2026-06-20.md` — התוכנית הקיימת ל-cutover (§6.3+§7+§8) שממנה WP-S5-02/03 שואבות; **אל תמציא מחדש** — רק תרגם ל-LOD400 מדויק.
- `_COMMUNICATION/team_100/WP-W2-17-CRFINAL-SEO-REMEDIATION-PROGRAM-2026-07-03.md` — הרקע ההיסטורי המלא ל-WP-S5-02.
- `_COMMUNICATION/team_100/S004/S004-PACKAGE-SEQUENCE-INDEX-2026-07-15.md` + `WP-S4-01-LOD400-2026-07-15.md` — **תבנית מבנית לחיקוי** (מבנה מסמך אינדקס + מסמך LOD400 בודד) — כבר עברו את אותו תהליך (אפיון → ולידציה חוצת-מנועים → cycle עד PASS) בהצלחה.

### פרוטוקול ולידציה קאנוני (Iron Rule #1)
- **Builder** של ה-LOD400 עצמו: Claude/Sonnet (team_100, סשן זה). **Validator**: team_90 דרך `run_cross_engine_validation.sh` → composer-2.5 (cursor vendor) — cross-vendor אוטומטי, ללא צורך בבדיקה נוספת.
- אל תבלבל בין ולידציית ה-**LOD400 עצמו** (buildability/coverage — מה שנדרש כאן) לבין ולידציית ה-**קוד שייבנה בפועל** (זה יקרה בסשן team_110 הבא, אחרי ההנדאוף).

### First action (מחליף את ה-FIRST ACTION הגנרי לספוק זה)
1. קרא את שלושת מסמכי המחקר השמורים (WP-S5-01, WP-S5-02, WP-S5-03) + את שתי התוכניות הקיימות (SEO-GEO plan + WP-W2-17).
2. אמת health probe (`$AOS_API_BASE/api/system/health` → `db.status=online`).
3. כתוב את מסמך-האינדקס + 3 מסמכי ה-LOD400 (שים לב לחפיפה בין WP-S5-01 פריט 5 ל-WP-S5-02 item 2 — אותו ממצא, אל תכפול).
4. הרץ ולידציה חוצת-מנועים; תקן במעגל עד PASS נקי.
5. הפק הנדאוף קאנוני ל-team_110, הצג inline לנמרוד, **עצור**.
