---
id: WP-S4-03-LOD400-2026-07-15
from_team: team_100
to_team: team_00
date: 2026-07-15
type: wp-build-spec
wp: WP-S4-03
next_wp: WP-S4-07
lod_status: LOD400
lod_target: LOD400
authoring_team: team_100
phase_owner: team_100
consuming_team: team_50
validation_team: team_90
milestone_ref: S004
parent_index: _COMMUNICATION/team_100/S004/S004-PACKAGE-SEQUENCE-INDEX-2026-07-15.md
authorized_by: "team_00 2026-07-15 — plan «רצף חבילות למסירה סופית לאייל»"
---

# LOD400 — WP-S4-03: סגירת Content-QA 12.7 בדפדפן (ראיות staging)

## 0. מטרת המסמך, סוג, ומתודולוגיה

באצווה של 2026-07-12 (12 פריטי תוכן + quick-fix) הגיעו ל-**L-GATE_BUILD `PASS_WITH_FINDINGS`** דרך ולידציה חוצת-מנוע (`team_90` / cursor-composer-2, מנוע ≠ הבנאי Claude — Iron Rule #1). אבל אותה ולידציה הייתה **סטטית בלבד** (`git diff` + `file:line` + `php -l` + `yaml.safe_load`) — **אין** סגירת QA ייעודית בדפדפן/staging. מסמך זה מפרט את סגירת ה-QA הזו עם **ראיות רינדור אמיתיות**.

- **Builder של WP זה:** `team_50` / `team_90` (בודקים, לא בונים — read-only למעט קבצי ראיה/verdict).
- **חוצת-מנוע:** ה-QA כאן חייב לרוץ במנוע **שונה** מהבנאי המקורי (Claude) של אצוות 12.7 — Iron Rule #1.
- **מקורות שנחקרו:** `_aos/roadmap.yaml:39-46` · `_COMMUNICATION/team_90/MANDATE_CONTENT-QA-2026-07-12_L-GATE_BUILD.md` · `_COMMUNICATION/team_90/VERDICT_CONTENT-QA-2026-07-12_L-GATE_BUILD.md` · `_COMMUNICATION/team_90/EVIDENCE-CLOSE-ITEM10c-METADESC-PROVENANCE-2026-07-14.md`.

**דיסציפלינת TLS/dev (קנון):** staging מגיש cert לא-תקין ו-`noindex` **בכוונה** — זה ארטיפקט dev, **לא** פגם. `curl -k`/`--ignore-certificate-errors` = DEV-ONLY. **לעולם לא `curl` לבד לאימות layout** — לבדיקת box-model/overflow/computed-style משתמשים ב-`_aos/lean-kit/modules/validation-quality/scripts/qa/qa_probe.mjs` (Node 18+, אפס תלויות) או ב-CDP `Runtime.evaluate`. קנון: `_aos/lean-kit/modules/validation-quality/docs/BROWSER_QA_HARNESS_CANON_v1.0.0.md`.

**staging base:** `http://eyalamit-co-il-2026.s887.upress.link`

**ארטיפקטים:** `mkdir -p _COMMUNICATION/team_90/evidence/wp-s4-03-2026-07-15` — כל צעד שומר לשם.

---

## 1. הרמה: qa_probe בסיסי לכל העמודים הרלוונטיים (overflow + absent)

לפני הבדיקות הפרטניות, ריצת-רוחב אחת שמכסה overflow + מונחים-אסורים על כל 12 המסלולים:

```bash
cat > tmp/qa/wp-s4-03-content.json <<'JSON'
{ "base": "http://eyalamit-co-il-2026.s887.upress.link",
  "viewports": [{"name":"mobile","w":375,"h":812},{"name":"desktop","w":1440,"h":900}],
  "pages": [
    {"name":"home","path":"/"},
    {"name":"method","path":"/method/"},
    {"name":"faq","path":"/faq/"},
    {"name":"repair","path":"/repair/"},
    {"name":"kushi","path":"/books/kushi-blantis/"},
    {"name":"vekatavta","path":"/books/vekatavta/"},
    {"name":"tsva","path":"/books/tsva-bekahol/"},
    {"name":"treatment","path":"/treatment/"},
    {"name":"sound-healing","path":"/sound-healing/"},
    {"name":"lessons","path":"/lessons/"},
    {"name":"didgeridoos","path":"/didgeridoos/"}
  ],
  "absent": ["TBD","CDIP","Lorem","undefined","&lt;br&gt;"], "shots": true }
JSON
node _aos/lean-kit/modules/validation-quality/scripts/qa/qa_probe.mjs \
  --config tmp/qa/wp-s4-03-content.json \
  --out _COMMUNICATION/team_90/evidence/wp-s4-03-2026-07-15/qa_probe
```
**צפוי:** `verdict: PASS` (אפס overflow, אף מונח-אסור, `title` לא-ריק בכל 22 הצירופים). `&lt;br&gt;` נבדק במפורש כי item 1 תיקן `<br>` שהוצג כטקסט.

> **CDP helper (לבדיקות computed-style/DOM ידניות שלמטה):** אפשר להשתמש ב-MCP `cursor-ide-browser` (`browser_navigate` → `browser_cdp` `Runtime.evaluate`) או ב-snippet Node קצר מעל `chrome-headless-shell` (אותו binary ש-qa_probe מאתר ב-`~/.cache/puppeteer`). כל ביטוי `EXPR` שלמטה מיועד ל-`Runtime.evaluate({expression: EXPR, returnByValue:true})` על העמוד הטעון. שמור כל פלט כ-`.json`/`.txt` תחת תיקיית הראיות.

---

## 2. מטריצת אימות 12 פריטים (per-item)

לכל פריט: **route** · **DOM/computed/behaviour לאימות** · **צעד qa_probe/CDP/curl** · **תוצאה צפויה** · **ארטיפקט**. עוגני-קוד מ-`git HEAD` נועדו לאימות-צולב מול הרינדור החי, לא כתחליף לו.

### פריט 1 — Homepage Trust Line + escaping
- **route:** `/`
- **אימות:** `.hero__trust` קיים **מעל** ה-`<h1>` בתוך `.hero__c` (מקור: `section-hero.php:28-31`, `home-defaults.php:21`). `hero_subtitle` מרונדר עם `ea_chapters_kses_e()` — כלומר `<em>/<br>` מופיעים כ-HTML, **לא** כטקסט גולמי.
- **CDP EXPR:**
```js
JSON.stringify({
  trust: !!document.querySelector('.hero__c .hero__trust'),
  trustBeforeH1: (()=>{const c=document.querySelector('.hero__c');if(!c)return false;
    const t=c.querySelector('.hero__trust'),h=c.querySelector('h1');
    return !!(t&&h)&&(t.compareDocumentPosition(h)&Node.DOCUMENT_POSITION_FOLLOWING)>0;})(),
  literalBr: document.querySelector('.hero__c')?.innerHTML.includes('&lt;br&gt;')||false
})
```
- **צפוי:** `{"trust":true,"trustBeforeH1":true,"literalBr":false}`
- **ארטיפקט:** `.../item01-trustline.json` + screenshot `home_desktop.png`

### פריט 2 — Testimonial names clickable (homepage)
- **route:** `/`
- **אימות:** בקרוסלת העדויות בבית, שמות עם `href` עטופים ב-`<a class="tmq__nl" target="_blank" rel="noopener noreferrer">` (מקור: `section-05-testimonials.php:45-46`). שם ללא `href` = `figcaption` רגיל (fallback תקין).
- **CDP EXPR:**
```js
JSON.stringify({
  links: [...document.querySelectorAll('.tmq__nl')].map(a=>({
    href: a.getAttribute('href'), tgt: a.getAttribute('target'), rel: a.getAttribute('rel')
  }))
})
```
- **צפוי:** לפחות קישור אחד; כל אחד `target="_blank"` + `rel` כולל `noopener noreferrer` + `href` לא-ריק. **finding לא-חוסם** אם 0 קישורים (כל השמות ללא `href` חי) — לרשום, לא להכשיל.
- **ארטיפקט:** `.../item02-testimonial-links.json`

### פריט 3 — `/method` 8-card carousel + ציטוטים verbatim
- **route:** `/method/`
- **אימות:** `parts/testimonials.php` מרונדר עם **8** כרטיסים (מקור: `method-defaults.php:171-181`, `tpl-chapters-method.php:81-85`). ה-VERDICT כבר אישר סטטית 8/8 byte-identical מול `git HEAD`; כאן מאמתים שהרינדור החי מציג 8.
- **CDP EXPR:**
```js
JSON.stringify({cards: document.querySelectorAll('#method .tmq, .method-testi .tmq, [data-ea-testi] .tmq').length,
  textLens: [...document.querySelectorAll('.tmq')].slice(0,8).map(n=>n.innerText.trim().length)})
```
- **צפוי:** `cards` ≥ 8 (או ספירת הסלקטור הנכון לפי snapshot); כל `textLens` > 0. אימות verbatim: השוואה מול `git show HEAD:site/wp-content/themes/ea-eyalamit/inc/chapters/defaults/method-defaults.php` (כבר PASS ב-VERDICT §item3).
- **ארטיפקט:** `.../item03-method-carousel.json` + screenshot `method_mobile.png`

### פריט 4 — Book reading-excerpt accordions + scroll-reveal fix
- **route:** `/books/kushi-blantis/`, `/books/vekatavta/`, `/books/tsva-bekahol/`
- **אימות:** section «קטע מתוך הספר» הוא accordion (`collapsible`). באג שתוקן: הגוף בתוך `<details>` סגור לא קיבל class `r`/`r2` של scroll-reveal (אחרת נשאר `opacity:0` אחרי פתיחה) — הגוף משתמש רק ב-`intro-body` (מקור: `prose.php:19-24,31-35`). **behaviour קריטי:** אחרי פתיחת האקורדיון הטקסט **גלוי** (`opacity` סופי ≈ 1), וגם לאחר סגירה+פתיחה חוזרת.
- **CDP EXPR (לכל עמוד; פתיחה תכנותית ובדיקת opacity):**
```js
(()=>{const d=document.querySelector('details .intro-body, .prose-acc details')?.closest('details')
   ||document.querySelector('details');
 if(!d)return JSON.stringify({found:false});
 d.open=true; const b=d.querySelector('.intro-body')||d;
 const op=getComputedStyle(b).opacity;
 d.open=false; d.open=true; const op2=getComputedStyle(b).opacity;
 return JSON.stringify({found:true, hasRevealClass:/\br\b|\br2\b/.test(b.className),
   opacityAfterOpen:op, opacityAfterReopen:op2});})()
```
- **צפוי:** `{"found":true,"hasRevealClass":false,"opacityAfterOpen":"1","opacityAfterReopen":"1"}` בכל 3 העמודים.
- **ארטיפקט:** `.../item04-<slug>-accordion.json` (×3)

### פריט 5 — Homepage hero video CWV (timing only)
- **route:** `/`
- **אימות:** `<video>` ב-hero **ללא** `autoplay`, `preload="none"` (מקור: `section-hero.php:21`); הניגון נדחה ל-`window.load` וכבוי תחת `prefers-reduced-motion` (מקור: `ea-chapters.js:72-78`). קובץ הווידאו עצמו לא שונה.
- **CDP EXPR:**
```js
(()=>{const v=document.querySelector('.hero video, video');
 return JSON.stringify({found:!!v, autoplay:v?.hasAttribute('autoplay')??null,
   preload:v?.getAttribute('preload')??null, src:v?.currentSrc||v?.querySelector('source')?.src||''});})()
```
- **צפוי:** `{"found":true,"autoplay":false,"preload":"none","src":"...ea-home-hero-720-muted.mp4"}`. אימות reduced-motion: הרצת qa_probe/CDP עם `Emulation.setEmulatedMedia({features:[{name:'prefers-reduced-motion',value:'reduce'}]})` → הווידאו לא מתנגן (`v.paused===true`).
- **ארטיפקט:** `.../item05-hero-video.json`

### פריט 6 — FAQ font-size
- **route:** `/faq/`
- **אימות:** `.ea-faq-item__answer p` בגודל `1rem` (16px), לא `0.9rem` (מקור: `ea-atoms.css:1109`).
- **CDP EXPR:**
```js
(()=>{const p=document.querySelector('.ea-faq-item__answer p');
 return JSON.stringify({found:!!p, fontSize:p?getComputedStyle(p).fontSize:null});})()
```
- **צפוי:** `{"found":true,"fontSize":"16px"}` (בהנחת root 16px). אם ה-`<details>` סגור — לפתוח תחילה או לבדוק את ה-CSS rule ישירות.
- **ארטיפקט:** `.../item06-faq-fontsize.json`

### פריט 7 — "לימוד והכשרה" nav links
- **route:** כל עמוד (nav גלובלי); בדיקה מ-`/`
- **אימות:** 3 מ-4 תת-קישורים → `/learning/therapist-training/`, `/learning/lectures/`, `/learning/workshops/`; «קורסים» נשאר `#` בכוונה (URL חיצוני ממתין) (מקור: `section-nav.php:29,31-33`).
- **צעד (DOM + HTTP 200):**
```js
// CDP: אסוף hrefs
JSON.stringify([...document.querySelectorAll('nav a')].map(a=>a.getAttribute('href'))
  .filter(h=>h&&(h.includes('/learning/')||h==='#')))
```
```bash
for p in therapist-training lectures workshops; do
  code=$(curl -s -o /dev/null -w '%{http_code}' "http://eyalamit-co-il-2026.s887.upress.link/learning/$p/")
  echo "$p -> $code"
done | tee _COMMUNICATION/team_90/evidence/wp-s4-03-2026-07-15/item07-learning-http.txt
```
- **צפוי:** 3 קישורי `/learning/*` נוכחים; «קורסים» = `#` (לא-חוסם, מתועד); שלושת המסלולים מחזירים **200**.
- **ארטיפקט:** `.../item07-learning-http.txt` + `.../item07-nav-hrefs.json`

### פריט 8 — Footer disclaimer + a11y/privacy links
- **route:** `/` (footer גלובלי)
- **אימות:** בלוק `.foot__legal` עם טקסט disclaimer זהה-תו-לתו ל-`block-disclaimer.php:25`, + קישורי `/accessibility/` ו-`/privacy/` (מקור: `section-footer.php:43-45`).
- **CDP EXPR:**
```js
(()=>{const f=document.querySelector('.foot__legal');
 return JSON.stringify({found:!!f,
   disc:f?.querySelector('.foot__disc')?.innerText.trim().slice(0,60)||'',
   a11y:!!document.querySelector('.foot__legal a[href*="/accessibility/"]'),
   priv:!!document.querySelector('.foot__legal a[href*="/privacy/"]')});})()
```
- **צפוי:** `found:true`, `a11y:true`, `priv:true`, ו-`disc` תואם את תחילת המחרוזת הקנונית. אימות תו-לתו מלא: `diff` מול `git show HEAD:.../template-parts/blocks/block-disclaimer.php` (כבר PASS סטטית).
- **ארטיפקט:** `.../item08-footer-legal.json`

### פריט 9 — `/repair` electronics-engineer sentence
- **route:** `/repair/`
- **אימות:** משפט חדש (זווית ניתוח הנדסת-אלקטרוניקה) קיים בגוף, קורא טבעי, לא כפול (מקור: `repair-defaults.php:34`).
- **צעד:**
```bash
curl -fsS "http://eyalamit-co-il-2026.s887.upress.link/repair/" \
  | rg -o 'מהנדס אלקטרוניקה[^<]{0,120}' \
  | tee _COMMUNICATION/team_90/evidence/wp-s4-03-2026-07-15/item09-repair-sentence.txt
```
- **צפוי:** התאמה אחת (לא כפולה). קריאה אנושית לאישור שהמשפט משתלב בהקשר.
- **ארטיפקט:** `.../item09-repair-sentence.txt`

### פריט 10 — 8 meta descriptions (mu-plugin) + read-back חי
- **route:** front page + `/treatment/`, `/method/`, `/sound-healing/`, `/lessons/`, `/faq/`, `/repair/`, `/didgeridoos/`
- **אימות:** `<meta name="description">` בכל אחד מ-8 המסלולים תואם את המחרוזת שנכתבה ב-`ea-content-eyal-seo-metadesc-2026-07-12-once.php` (מקור: mu-plugin `:63-101`; provenance נסגר ב-`EVIDENCE-CLOSE-ITEM10c-...` על בסיס תעתיק — ה-**read-back החי** הוא בדיוק מה ש-WP-S4-03 סוגר).
- **צעד (read-back מ-staging + השוואה למקור):**
```bash
for path in / /treatment/ /method/ /sound-healing/ /lessons/ /faq/ /repair/ /didgeridoos/; do
  desc=$(curl -fsS "http://eyalamit-co-il-2026.s887.upress.link${path}" \
    | rg -o '<meta[^>]*name="description"[^>]*content="[^"]*"' | head -1)
  printf '%s\t%s\n' "$path" "$desc"
done | tee _COMMUNICATION/team_90/evidence/wp-s4-03-2026-07-15/item10-metadesc-readback.txt
# מקור להשוואה:
rg -n "update_post_meta|_yoast_wpseo_metadesc|content" \
  site/wp-content/mu-plugins/ea-content-eyal-seo-metadesc-2026-07-12-once.php \
  | tee _COMMUNICATION/team_90/evidence/wp-s4-03-2026-07-15/item10-metadesc-source.txt
```
- **צפוי:** 8 מסלולים מחזירים `meta name="description"` לא-ריק; כל מחרוזת חיה **זהה** למחרוזת ב-mu-plugin (זהו ה-read-back שה-VERDICT המליץ עליו לפני production). אי-התאמה = **finding** (לא בהכרח חוסם — יכול לנבוע מ-cache/Yoast; לתעד ולתאם עם team_10).
- **ארטיפקט:** `.../item10-metadesc-readback.txt` + `.../item10-metadesc-source.txt`

### פריט 11 — `_aos/roadmap.yaml` תקין (רגרסיה)
- **אימות:** ה-YAML נטען, אין שכפול/שחיתות (כבר PASS ב-VERDICT; נשמר כאן כ-regression guard).
- **צעד:**
```bash
python3 -c "import yaml,sys; yaml.safe_load(open('_aos/roadmap.yaml')); print('YAML OK')" \
  | tee _COMMUNICATION/team_90/evidence/wp-s4-03-2026-07-15/item11-yaml.txt
```
- **צפוי:** `YAML OK`.
- **ארטיפקט:** `.../item11-yaml.txt`

### פריט 12 — FTP deploy list כולל את mu-plugin
- **אימות:** `ea-content-eyal-seo-metadesc-2026-07-12-once.php` ברשימת הפריסה (מקור: `scripts/ftp_deploy_site_wp_content.py:144-146`) — כך שה-meta descriptions באמת מגיעים ל-staging (מה שמאפשר את read-back של פריט 10).
- **צעד:**
```bash
rg -n "ea-content-eyal-seo-metadesc-2026-07-12-once.php" scripts/ftp_deploy_site_wp_content.py \
  | tee _COMMUNICATION/team_90/evidence/wp-s4-03-2026-07-15/item12-deploy-list.txt
```
- **צפוי:** התאמה אחת. אם הקובץ עדיין untracked ב-git — לוודא שנפרס בפועל (העדות המכרעת = פריט 10 read-back מחזיר את המחרוזות).
- **ארטיפקט:** `.../item12-deploy-list.txt`

---

## 3. קריטריוני קבלה מדידים (ACs)

| # | קריטריון | מדידה | חוסם? |
|---|----------|-------|-------|
| AC-1 | כל 12 הפריטים אומתו בדפדפן על staging עם ראיה שמורה | 12 ארטיפקטים ב-`_COMMUNICATION/team_90/evidence/wp-s4-03-2026-07-15/` | כן |
| AC-2 | אפס horizontal-overflow בכל 22 צירופי page×viewport | §1 qa_probe `verdict: PASS` | כן |
| AC-3 | אפס `&lt;br&gt;`/טקסט-גולמי בבית (item 1) | פריט 1 `literalBr:false` + qa_probe absent | כן |
| AC-4 | accordion ספרים גלוי אחרי פתיחה+פתיחה-חוזרת (item 4) | פריט 4 `opacity` = `1` ×3 | כן |
| AC-5 | 8 meta descriptions חיים ותואמים מקור (item 10 read-back) | פריט 10 — 8/8 לא-ריק ותואם | לא (finding אם אי-התאמה) |
| AC-6 | 3 קישורי `/learning/*` → HTTP 200 (item 7) | פריט 7 — 3×200 | כן |
| AC-7 | 0 blocking; findings מתועדים בפירוט | verdict `PASS`/`PASS_WITH_FINDINGS` | — |

**0 blocking** נדרש למעבר. Findings לא-חוסמים (למשל item 2 ללא קישורים חיים, item 10 אי-התאמת cache) נרשמים בטבלת ה-verdict עם `evidence-by-path` + `route_recommendation`.

---

## 4. תוצר ולידציה + בעלות צעד-הבא

- **team_50 / team_90** (בנאי-QA, מנוע ≠ Claude): מריצים §1–§2, שומרים 12 ארטיפקטים + qa_probe summary + screenshots.
- **team_90** (חתימה חוצת-מנוע): כותב verdict ל-`_COMMUNICATION/team_90/VERDICT-WP-S4-03-CONTENT-QA-BROWSER-2026-07-15.md` — טבלת 12 פריטים (`PASS`/`FINDING`/`FAIL` + `evidence-by-path`), overall verdict, ו-`route_recommendation` לכל finding.
- **team_00**: אישור סגירת אצוות 12.7 כ-QA-closed (משחרר את WP-S4-08 sher-hasgira).

**שאריות ידועות (לא-חוסמות):**
- item 2 — קישורי FB חיים בעדויות הבית תלויים ב-`ea_fb_testimonials_all()` (מקור-נתונים חי); אם ריק, זולג ל-`WP-EI-07` (אצירת 48 עדויות + GA4/Clarity).
- item 10 — provenance התעתיק נסגר; ה-read-back החי כאן הוא האישור הסופי לפני production.

*Filed by team_100 · LOD400 build-spec · 2026-07-15*


<!-- HANDOFF-FOOTER v1 -->

## הנדאוף לחבילה הבאה (חובה — הצוות המבצע / team_110)

- **חבילה נוכחית:** WP-S4-03
- **הבאה בתור (`next_wp`):** WP-S4-07
- בסיום `L-GATE_BUILD PASS`: הפק הנדאוף קאנוני דרך `/AOS_handoff` (hub prompt-generate API — `type=onboard_agent`, `mode=handoff`, `team_id=110`, `wp_id` של הבאה בתור, `gate_state=gate_done`), כתוב את `artifact_markdown` verbatim ל-`_COMMUNICATION/team_110/HANDOFF_SELF_*_2026-07-XX_v1.md`, והצג את `activation_block` **inline בצ'אט** ל-team_00 (נמרוד) לניתוב ויצירת הסשן הבא.
- **אל תתחיל את החבילה הבאה אוטומטית** — עצור והמתן לניתוב team_00.
- פרוטוקול מלא + שרשרת הביצוע: `_COMMUNICATION/team_100/S004/S004-PACKAGE-SEQUENCE-INDEX-2026-07-15.md` §8.
