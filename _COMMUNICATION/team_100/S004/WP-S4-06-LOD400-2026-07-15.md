---
id: WP-S4-06-LOD400-2026-07-15
from_team: team_100
to_team: team_00
date: 2026-07-15
type: wp-build-spec
wp: WP-S4-06
next_wp: WP-S4-08
lod_status: LOD400
lod_target: LOD400
authoring_team: team_100
phase_owner: team_100
consuming_team: team_10
validation_team: team_50
milestone_ref: S004
blocked_by: [WP-S4-01, WP-S4-04]
parent_index: _COMMUNICATION/team_100/S004/S004-PACKAGE-SEQUENCE-INDEX-2026-07-15.md
authorized_by: "team_00 2026-07-15 — plan «רצף חבילות למסירה סופית לאייל»"
roadmap_mutation: "ADR034 R8 file-SSoT"
---

# LOD400 — WP-S4-06: ביקורת placeholder זוהר כלל-אתרית (glow-marker audit)

## 0. מטרת המסמך, מתודולוגיה, וממצא-על

### 0.1 הדרישה (מ-team_00)

> **כל** פיסת תוכן שעדיין חסרה מאייל חייבת להיות מסומנת בבירור באתר בסמן **זוהר** (placeholder זוהר). אין פריט חסר-מאייל שמרונדר כאילו הוא סופי.

זהו WP של **ביקורת כלל-אתרית + הקשחת רכיבים**, לא בניית תוכן. המטרה הכפולה:
1. **ביקורת (audit):** למפות כל פריט חסום-אייל (OPEN-* ב-`hub/data/eyal-pending.json`) למיקומו הפיזי באתר ולמצב ה-placeholder הנוכחי שלו.
2. **הקשחה (hardening):** לסגור כל פער שבו פריט חסר-מאייל מרונדר **ללא** סמן זוהר — ע"י הרחבת מנגנון ה-placeholder לתמוך גם ב**סקשני פרוזה/CTA** (לא רק בסלוטי מדיה בגלריה).

### 0.2 מתודולוגיה (code-verified)

כל שורה במטריצה §2 אומתה מול הקוד החי בריפו (grep מדויק + קריאת קובץ מלאה):
- מנגנון ה-pending: `site/wp-content/themes/ea-eyalamit/template-parts/chapters/parts/gallery.php` + `assets/css/chapters.css:714-734`.
- מקור החוסמים: `hub/data/eyal-pending.json` (13 פריטי OPEN-*).
- מיקום כל פריט: `inc/chapters/defaults/*-defaults.php` + `inc/chapters/chapters-commerce.php` + `inc/chapters/chapters-render.php` (מפת ראוטים).

**הרף (זהה ל-WP-CANON-LOD400):** כל spec מפורט מספיק כדי שמפתח ג'וניור/agent טרי יממש בלי פערים, ניחושים או הנחות — קבצים, פונקציות, שורות, מחלקות CSS, ו-AC מדידים.

### 0.3 ממצא-על מחייב (code-verified): המנגנון תומך היום רק במדיה, לא בפרוזה/CTA

מנגנון ה-placeholder הזוהר הקיים (`.ea-pending-approval` + `.gfig--pending`) בנוי **אך ורק** לתוך `gallery.php` — הוא מופעל כשפריט גלריה מסומן `pending=true`. אין לו וריאנט לסקשן פרוזה, לכפתור CTA, או ל-hero. בפועל, סוכנים "עקפו" את המגבלה ע"י **הזרקת ה-HTML של `.ea-pending-approval` ידנית לתוך שדה `body` של סקשן פרוזה** (ר' `vekatavta-defaults.php:181`, `tsva-bekahol-defaults.php:151`, `snoring-sleep-apnea-defaults.php:95`). זה עובד אבל:
1. **לא אחיד** — כל אחד כותב HTML גולמי משלו, סיכון escaping/דריפט מבנה.
2. **לא מכסה CTA/כפתורים** — כפתורי הרכישה הזמניים (Green Invoice / Morning) מרונדרים **ללא שום סמן** שהקישור זמני → נראים סופיים. זהו הפער החמור ביותר מול דרישת team_00.

**מסקנה:** נדרשת הרחבת-רכיב אחת (§3): (א) partial חדש `pending-note.php` (סקשן-פרוזה זוהר אחיד, מחליף את ה-HTML הגולמי הידני); (ב) מחלקת CSS inline חדשה `.ea-pending-inline` + hook ב-`cta.php`/`product-cta.php` לסמן קישורי-רכישה זמניים.

---

## 1. מנגנון ה-placeholder הקיים (code-verified, לא משתנה בבסיסו)

### 1.1 השכבה החזותית (CSS) — `assets/css/chapters.css:714-734`

```714:734:site/wp-content/themes/ea-eyalamit/assets/css/chapters.css
/* ── Pending approval / missing media (team_00 2026-07-15) ── */
.ea-pending-approval{
  display:flex;flex-direction:column;align-items:center;justify-content:center;gap:8px;
  margin:24px 0;padding:28px 20px;text-align:center;
  border:3px solid #e8a317;border-radius:12px;
  background:linear-gradient(135deg,#fff8e1 0%,#ffe082 55%,#ffd54f 100%);
  box-shadow:0 0 0 4px rgba(232,163,23,.25),0 8px 24px rgba(120,80,0,.12);
  color:#5d4037;font-family:var(--bf);
}
.ea-pending-approval__badge{
  display:inline-block;padding:4px 12px;border-radius:999px;
  background:#e65100;color:#fff;font-size:.72rem;font-weight:700;letter-spacing:.04em;
}
.ea-pending-approval__title{margin:0;font-size:1.05rem;font-weight:700;color:#bf360c}
.ea-pending-approval__note{margin:0;font-size:.88rem;line-height:1.55;max-width:36em}
.gfig--pending{
  aspect-ratio:4/3;display:flex;align-items:center;justify-content:center;
  border:3px solid #e8a317;background:linear-gradient(135deg,#fff8e1,#ffe082);
  box-shadow:inset 0 0 0 3px rgba(255,255,255,.5);
}
.gfig--pending .ea-pending-approval{margin:12px;padding:16px;box-shadow:none;border-width:2px;background:transparent}
```

הזוהר = `border:3px solid #e8a317` + `box-shadow:0 0 0 4px rgba(232,163,23,.25)` + gradient כתום-ענבר. זה ה"glow" הקנוני — **כל** סמן חדש חייב לרשת את אותה שפה חזותית.

### 1.2 השכבה הלוגית (PHP) — `gallery.php:25-53`

`gallery.php` מרנדר סלוט `.ea-pending-approval` כשפריט מסומן `pending=true` (עם/בלי image). התמיכה בשדות: `pending` (bool), `pending_label` (כותרת), `alt` (הערה). זהו החוזה היחיד שקיים היום.

### 1.3 חוזה-נתונים קיים (מה שכבר עובד)

| שכבה | טריגר | פלט |
|---|---|---|
| גלריה — מדיה חסרה | `items[].pending = true` | `figure.gfig--pending > .ea-pending-approval` |
| פרוזה — hack ידני | HTML גולמי בשדה `body` | `.ea-pending-approval` (לא אחיד, סיכון escaping) |

---

## 2. מטריצת מלאי מלאה — כל פריט חסום-אייל → מיקום → מצב נוכחי → סמן נדרש

מקור: `hub/data/eyal-pending.json`. עמודת «מצב סמן נוכחי» אומתה בקוד. עמודת «פעולת WP-S4-06» היא ההוראה המחייבת.

| # | פריט (OPEN-*) | route(ים) | קובץ defaults / accessor | מצב סמן נוכחי (code-verified) | פעולת WP-S4-06 |
|---|---|---|---|---|---|
| 1 | `OPEN-GI-BOOK-URLS` — קישורי GI ל-3 ספרים | `/books/vekatavta/` · `/books/kushi-blantis/` · `/books/tsva-bekahol/` · `/books/` (hub) | `vekatavta-defaults.php` · `kushi-blantis-defaults.php` · `tsva-bekahol-defaults.php` · `muzza-defaults.php` | **חסר** — כפתורי `cta_url => https://mrng.to/MTUiO3vkIg` (temp Morning) מרונדרים כסופיים, אין סמן זמני | **P0** — inline chip `.ea-pending-inline` תחת כל CTA רכישה + section `pending-note` אחד ליד בלוק הרכישה (§3.2, §4) |
| 2 | `OPEN-GI-CHECKOUT-URLS` — קישורי סליקה ל-5 מוצרים | `/didgeridoos/` · `/bags/` · `/stands-storage/` · `/stand-floor/` · `/repair/` | `chapters-commerce.php:17-25` (`ea_w2_05_gi_url_map`) + `product-cta.php` | **חסר** — `ea_w2_05_gi_url()` מחזיר את אותו temp Morning ל-5 המוצרים; `product-cta.php` מרנדר כסופי | **P0** — hook `gi_temp` ב-`product-cta.php` → chip `.ea-pending-inline` (§3.3, §4) |
| 3 | `OPEN-PRODUCT-GALLERIES` — תמונות מוצר אמיתיות | 5 עמודי המוצר (כנ"ל) | `didgeridoos/bags/stands-storage/stand-floor/repair-defaults.php` | **חסר** — אין סקשן גלריה כלל (הוחלט «בלי placeholders אפורים»), אין סמן זוהר | **P1** — section חדש `part=>'gallery'` עם 2 סלוטי `pending=true` לכל עמוד מוצר (§4) |
| 4 | `OPEN-BOOK-GALLERY-IMAGES` — גלריות ספרים | 3 עמודי ספרים | `vekatavta`(149-166) · `tsva-bekahol`(119-165) · `kushi-blantis`(140-172) | **חלקי** — vekatavta+tsva: סלוטי `pending` קיימים ✓ ; **kushi**: תמונות משותפות (`kushi-02/04`) מוצגות כתוכן ללא סמן זמני ✗ | **P1** — הוספת section `pending-note` לעמוד kushi המסמן שהתמונות זמניות (§4) |
| 5 | `OPEN-LEGAL-COPY` — פרטיות/נגישות/תקנון | `/privacy/` · `/accessibility/` · `/terms/` | `privacy-defaults.php:26` · `accessibility-defaults.php:27` · `terms-defaults.php:26` | **חסר** — טקסט טיוטה בסוגריים `⟨…⟩` בשדה `body`, ללא סמן זוהר | **P0** — המרת סקשן ה-`prose` ל-`pending-note` (§3.1, §4) |
| 6 | `OPEN-EN-BODY` — גוף /en | `/en/` | `tpl-chapters-en.php` (inline), `en-defaults.php` ריק | **חסר** — גוף אנגלי inline ללא סמן טיוטה | **P1** — banner `.ea-pending-inline--wide` בראש `tpl-chapters-en.php` (§3.4, §4) |
| 7 | `OPEN-MATERIALS-MEDIA` — hero + מדיה 4 עמודי שירות | `/treatment/` · `/method/` · `/sound-healing/` · `/lessons/` | `*-defaults.php` (`phero` media) | **לא-חוסם** — hero משתמש בתצלומי סטודיו קיימים (stand-in איכותי, לא תוכן חסר-אייל) | **P3 / אין-פעולה** — תוכן העמוד מלא; media polish בלבד. **לא** מסמנים (ר' §2.1 החרגה) |
| 8 | `OPEN-CHAPTERS-TESTIMONIALS` — 48 עדויות + תצוגה | עמודי שירות + בית | `*-defaults.php` (`testimonials`) | **לא-חוסם** — עדויות מקור **חיות** במרקיזה (verbatim) | **אין-פעולה** — התוכן חי, לא «חסר». אצירת ה-48 = M-EYAL-INPUTS WP-EI-07 |
| 9 | `OPEN-MOKESH-FINAL` — אישור סופי + מדיה | `/eyal-amit/mokesh-dahiman/` | `mokesh-defaults.php` | **לא-חוסם** — תוכן חי verbatim; מדיה (טריילר/גלריה/fb) בטיפול WP-CANON T1 | **אין-פעולה ב-S4-06** — אישור סופי = WP-EI-04 |
| 10 | `OPEN-FAQ-COPY` | עמודי שירות | CPT `ea_faq` | **סגור** (2026-07-14) | אין-פעולה |
| 11 | `OPEN-HOME-MARKETING-COPY` | `/` | `home-defaults.php` | **owner=team** — לא חסום-אייל | אין-פעולה (לא בתחום «חסר-אייל») |
| 12 | `OPEN-BRAND-LOGO` | כלל-אתר | — | **owner=team** | אין-פעולה |
| 13 | `OPEN-BRAND-RETIRED` | כלל-אתר | — | **locked** (השמטה) | אין-פעולה |
| 14 | עוגן — מדיית מכבי/יוני + Case Study | `/snoring-sleep-apnea/` | `snoring-sleep-apnea-defaults.php:95,108-109` | **קיים ✓** — סלוט `pending` (מכבי.jpg) + `.ea-pending-approval` (יוני) | **אימות בלבד** — לוודא שנשמר אחרי WP-S4-01 |

### 2.1 החרגות מפורשות (עם נימוק)

- **hero של 4 עמודי שירות (#7):** תצלומי הסטודיו הם stand-in איכותי, לא «תוכן חסר-אייל» — סימונם כזוהר יזהם 4 עמודים מלאים ותקינים. גבול: מסמנים רק פריט ש**אייל חייב לספק/לאשר** לפני עלייה, לא כל שיפור-מדיה אופציונלי.
- **`/galleries/`, `/media/` (data-blocked):** עמודי קטלוג עם `⟨כיתוב להשלמה⟩`. אם בהיקף המסירה (S004-INDEX §2.1 מסמן אותם «מונעי-קטלוג») — מומלץ **להסתירם מהתפריט** עד תוכן אמיתי (החלטה ל-team_10/WP-S4-08) ולא לסמן. אם נשארים חשופים → מקבלים `pending-note` (P2). ברירת-מחדל: **P2, מומלץ הסתרה**.
- **`/qr/`, `/shop/`:** מונעי-דאטה/פורט; מחוץ להיקף חסר-אייל.

---

## 3. הרחבת רכיבים (component hardening) — קבצים, קוד, CSS מדויקים

> עיקרון: **לא לשכפל** את שפת הזוהר — לרשת אותה. כל תוספת CSS יורדת בסוף בלוק ה-pending הקיים ב-`chapters.css`.

### 3.1 רכיב חדש #1 — `pending-note.php` (סקשן-פרוזה זוהר אחיד)

**קובץ חדש:** `site/wp-content/themes/ea-eyalamit/template-parts/chapters/parts/pending-note.php`

מחליף את ה-HACK הידני (הזרקת HTML גולמי לשדה `body`) ב-`part` ילידי. חוזה `$args`: `chap` (אופ'), `title` (חובה — כותרת הפריט החסר), `note` (חובה — מה בדיוק ממתין ולמי), `id` (אופ'), `alt` (אופ' — sec--alt).

```php
<?php
/**
 * Chapters part — pending-approval note (section-level glow marker).
 * Canonical replacement for hand-injected .ea-pending-approval HTML in prose bodies
 * (WP-S4-06). Use for prose/legal/EN/temp-CTA sections that have no media slot.
 * $args: chap, title (REQUIRED), note (REQUIRED), id, alt
 *
 * @package ea_eyalamit
 */
defined( 'ABSPATH' ) || exit;
$a = isset( $args ) && is_array( $args ) ? $args : array();
if ( empty( $a['title'] ) ) { return; }
?>
<section class="sec<?php echo ! empty( $a['alt'] ) ? ' sec--alt' : ''; ?>"<?php echo ! empty( $a['id'] ) ? ' id="' . esc_attr( $a['id'] ) . '"' : ''; ?>>
	<div class="wrap center">
		<?php if ( ! empty( $a['chap'] ) ) : ?><span class="chap chap--c r"><?php echo esc_html( $a['chap'] ); ?></span><?php endif; ?>
		<div class="ea-pending-approval r" role="status" aria-live="polite">
			<span class="ea-pending-approval__badge">ממתין לאישור</span>
			<p class="ea-pending-approval__title"><?php echo esc_html( $a['title'] ); ?></p>
			<?php if ( ! empty( $a['note'] ) ) : ?>
				<p class="ea-pending-approval__note"><?php echo wp_kses_post( $a['note'] ); ?></p>
			<?php endif; ?>
		</div>
	</div>
</section>
```

**רישום ה-part:** יש לוודא שהראוטר של הסקשנים (`template-parts/chapters/parts/` dispatch ב-`tpl-chapters-page.php` / `chapters-render.php`) טוען `part` לפי שם — הוא כבר עושה זאת גנרית (`gallery`, `prose`, `cta`, `product-cta` כולם נטענים כך). `pending-note` יעבוד ללא שינוי ראוטר. **AC-בנייה:** `locate_template('template-parts/chapters/parts/pending-note.php')` לא ריק.

### 3.2 רכיב חדש #2 — `.ea-pending-inline` (chip inline לצד CTA) — CSS

**קובץ:** `assets/css/chapters.css` — להוסיף מיד אחרי שורה 734 (סוף בלוק ה-pending):

```css
/* ── Inline pending marker (temp CTA / temp link) — WP-S4-06 ── */
.ea-pending-inline{
  display:inline-flex;align-items:center;gap:8px;margin-top:12px;
  padding:6px 14px;border-radius:999px;
  border:2px solid #e8a317;
  background:linear-gradient(135deg,#fff8e1,#ffe082);
  box-shadow:0 0 0 3px rgba(232,163,23,.22);
  color:#8a5a00;font-family:var(--bf);font-size:.8rem;font-weight:600;line-height:1.3;
}
.ea-pending-inline::before{
  content:"";width:9px;height:9px;border-radius:50%;
  background:#e65100;box-shadow:0 0 0 3px rgba(230,81,0,.25);
}
.ea-pending-inline--wide{display:flex;width:100%;max-width:none;justify-content:center;text-align:center;margin-inline:auto}
@media(prefers-reduced-motion:no-preference){
  .ea-pending-inline{animation:eaPendingPulse 2.4s var(--e,ease) infinite}
  @keyframes eaPendingPulse{0%,100%{box-shadow:0 0 0 3px rgba(232,163,23,.18)}50%{box-shadow:0 0 0 5px rgba(232,163,23,.34)}}
}
```

### 3.3 hook בכפתורי CTA — `product-cta.php` + `cta.php`

**`product-cta.php`** — להוסיף תמיכה ב-`$args['gi_temp']` (bool) + `$args['gi_temp_note']`. אחרי בלוק ה-`ea-cta-ab` של Green-Invoice (הקוד הקיים בשורות 52-57), להוסיף:

```php
<?php if ( '' !== $gi && ! empty( $a['gi_temp'] ) ) : ?>
	<p class="ea-pending-inline r" role="status">
		<span><?php echo esc_html( ! empty( $a['gi_temp_note'] ) ? $a['gi_temp_note'] : 'קישור רכישה זמני — ממתין לקישור הסופי מאייל' ); ?></span>
	</p>
<?php endif; ?>
```

**`cta.php`** (משמש את עמודי הספרים דרך `cta_slug`) — להוסיף תמיכה ב-`$args['temp_note']`. אחרי בלוק ה-`.cta-band__act` (סוף שורה 29, בתוך `.cta-band__in`), להוסיף:

```php
<?php if ( ! empty( $a['temp_note'] ) ) : ?>
	<p class="ea-pending-inline" role="status" style="margin-top:10px"><span><?php echo esc_html( $a['temp_note'] ); ?></span></p>
<?php endif; ?>
```

### 3.4 banner ל-`/en` — `tpl-chapters-en.php`

בראש גוף התוכן (מיד אחרי פתיחת ה-`<main>`/ה-hero), להזריק:

```php
<div class="wrap"><p class="ea-pending-inline ea-pending-inline--wide" role="status">
	<span>Draft — English copy is a team draft pending Eyal's sign-off before launch · טיוטה צוותית ממתינה לאישור אייל</span>
</p></div>
```

---

## 4. ההוראות המדויקות פר-קובץ (מה team_10 עורך)

> כל שינוי נתונים בלבד בקבצי `*-defaults.php`; שינויי הרכיבים (§3) מבוצעים פעם אחת. אין שינוי לוגיקת render מעבר ל-§3.

### 4.1 עמודי מוצר (5) — `didgeridoos/bags/stands-storage/stand-floor/repair-defaults.php`

לכל אחד:
1. **בסקשן `product-cta`** (למשל `didgeridoos-defaults.php:171-180`) — להוסיף `'gi_temp' => true` ל-`args`.
2. **סקשן גלריה חדש** לפני ה-`product-cta`, עם 2 סלוטי pending:
```php
array(
	'part' => 'gallery',
	'args' => array(
		'chap'  => 'גלריית מוצר',
		'title' => 'תמונות המוצר',
		'items' => array(
			array( 'pending' => true, 'pending_label' => 'תמונת מוצר 1 — ממתין לאישור / תמונות מ-Eyal', 'alt' => 'תמונת מוצר אמיתית תוטען מ-Drive' ),
			array( 'pending' => true, 'pending_label' => 'תמונת מוצר 2 — ממתין לאישור / תמונות מ-Eyal', 'alt' => 'תמונת מוצר אמיתית תוטען מ-Drive' ),
		),
	),
),
```

### 4.2 עמודי ספרים (3) — `vekatavta/kushi-blantis/tsva-bekahol-defaults.php`

1. **בסקשן ה-`cta`** של הרכישה (זה שנושא `cta_slug`) — להוסיף `'temp_note' => 'קישור רכישה זמני (דוגמת Morning) — ממתין לקישור GI ייעודי לספר מאייל'`.
2. **kushi-blantis בלבד:** להוסיף section `pending-note` מיד אחרי סקשן הגלריה (`kushi-blantis-defaults.php:140`), המסמן שהתמונות המשותפות זמניות:
```php
array(
	'part' => 'pending-note',
	'args' => array(
		'chap'  => 'מדיה',
		'title' => 'תמונות הגלריה — בחירת ביניים של הצוות',
		'note'  => 'התמונות בגלריה זו הן בחירה זמנית של הצוות ולא אישור תוכן. ממתין: 2–3 תמונות ייעודיות לספר מ-Eyal, או אישור להשאיר כפי שהוא.',
		'alt'   => true,
	),
),
```
3. **hub הספרים** — `muzza-defaults.php` סקשן ה-CTA (שורות 115-131) — להוסיף `pending-note` אחרי בלוק הרכישה: `title => 'קישור רכישת החבילה — זמני'`, `note => 'קישור זמני (דוגמת Morning). ממתין לקישור GI ייעודי לחבילת 3 הספרים מ-Eyal.'`.

### 4.3 עמודים משפטיים (3) — `privacy/accessibility/terms-defaults.php`

להמיר את סקשן ה-`prose` היחיד (המכיל `⟨…⟩`) ל-`pending-note`:
```php
array(
	'part' => 'pending-note',
	'args' => array(
		'chap'  => 'טיוטה משפטית',
		'title' => 'נוסח משפטי בהשלמה — ממתין לאישור אייל / ייעוץ משפטי',
		'note'  => 'הטקסט המוצג הוא טיוטת-בסיס של הצוות בלבד ואינו נוסח משפטי סופי. הצהרת נגישות נדרשת בחוק בישראל. יוחלף verbatim לאחר אישור.',
	),
),
```
> נשמרת פסקת ה-הסבר הקצרה הקיימת כסקשן `prose` **נוסף** אחרי ה-`pending-note` (כדי שהעמוד לא יהיה ריק מתוכן שימושי).

### 4.4 `/en` — `tpl-chapters-en.php`

להזריק את ה-banner מ-§3.4 בראש הגוף. אין שינוי `en-defaults.php`.

---

## 5. קריטריוני קבלה מדידים (measurable ACs)

> העיקרון המחייב: **אפס פריט חסר-אייל מרונדר ללא סמן זוהר, מאומת פר-route.**

| AC | תיאור | מדידה (פר-route) | סף |
|---|---|---|---|
| AC-1 | רכיב `pending-note.php` קיים ונטען | `locate_template()` לא ריק; render-smoke לא זורק PHP error | PASS/FAIL |
| AC-2 | CSS: `.ea-pending-inline` קיים ב-`chapters.css` | grep + נטען ב-DOM (computed `border-color` = rgb(232,163,23) / דומה) | קיים |
| AC-3 | **5 עמודי מוצר** — כל אחד נושא ≥1 סמן זוהר | qa_probe: `.ea-pending-inline` (chip GI זמני) **וגם** `.gfig--pending` (גלריה) קיימים | ≥1 מכל סוג בכל route |
| AC-4 | **3 עמודי ספרים + hub** — סמן זמני על קישור הרכישה | qa_probe: `.ea-pending-inline` או `.ea-pending-approval` קיים בקרבת ה-CTA | ≥1 בכל route |
| AC-5 | **3 עמודים משפטיים** — `pending-note` זוהר | qa_probe: `.ea-pending-approval` קיים; אין טקסט `⟨` גלוי כתוכן סופי | קיים בכל 3 |
| AC-6 | **/en** — banner טיוטה זוהר | qa_probe: `.ea-pending-inline--wide` קיים בראש הגוף | קיים |
| AC-7 | **עוגן /snoring-sleep-apnea/** — סמני מכבי/יוני נשמרו | qa_probe: `.gfig--pending` + `.ea-pending-approval` קיימים | ללא רגרסיה |
| AC-8 | **אין רגרסיה** — עמודים מלאים (home/treatment/method/…) **לא** קיבלו סמן שגוי | qa_probe: אפס `.ea-pending-*` בעמודי שירות מלאים (למעט טסטימוניאלס חיים) | 0 שגויים |
| AC-9 | ניגודיות + נגישות הסמן | `role="status"`; ניגודיות טקסט הצ'יפ ≥ 4.5:1 (בדיקת qa_probe/axe) | עובר |
| AC-10 | **כיסוי-על:** כל פריט P0/P1 ב-§2 → סמן קיים | טבלת manifest §6.2 — 0 שורות «חסר» | 0 חסר |

---

## 6. ולידציה (qa_probe / browser — team_50)

> **לא curl בלבד** (curl לא רואה box-model/glow). חובה browser-QA runner: `_aos/lean-kit/modules/validation-quality/scripts/qa/qa_probe.mjs` (Node 18+).

### 6.1 שיטת המדידה

לכל route בטבלה §6.2, qa_probe (CDP) סופר את הסלקטורים ומחזיר JSON `{route, pending_approval, pending_inline, gfig_pending, has_raw_placeholder_text}`. `has_raw_placeholder_text` = האם `⟨` מופיע ב-textContent גלוי (חייב `false`).

פקודת בסיס (staging, TLS-bypass DEV-only מותר):
```
node _aos/lean-kit/modules/validation-quality/scripts/qa/qa_probe.mjs \
  --url "https://<staging>/didgeridoos/" \
  --count ".ea-pending-inline,.gfig--pending,.ea-pending-approval"
```

### 6.2 manifest אימות (SSOT ל-team_50) — `tmp/qa/wp-s4-06-pending-manifest.json`

team_10 מספק manifest שמצפה לסמן פר-route; team_50 מריץ qa_probe ומשווה. שורות מחייבות (route → סמנים נדרשים):

| route | `.ea-pending-inline` | `.gfig--pending` | `.ea-pending-approval` |
|---|---|---|---|
| /didgeridoos/ /bags/ /stands-storage/ /stand-floor/ /repair/ | ≥1 | ≥2 | 0+ |
| /books/vekatavta/ /books/tsva-bekahol/ | ≥1 | ≥1 | 0+ |
| /books/kushi-blantis/ | ≥1 | 0+ | ≥1 |
| /books/ | ≥1 | 0 | ≥1 |
| /privacy/ /accessibility/ /terms/ | 0+ | 0 | ≥1 |
| /en/ | ≥1 (`--wide`) | 0 | 0+ |
| /snoring-sleep-apnea/ | 0+ | ≥1 | ≥1 |
| /treatment/ /method/ /sound-healing/ /lessons/ /eyal-amit/ / (home) | 0 | 0 | 0 (למעט testimonials חיים — לא סמן) |

### 6.3 ולידציה חוצת-מנוע (Iron Rule #1)

בונה = team_10 (Cursor/Composer). ולידטור = team_50 במנוע **שונה** (gpt-5.2/Codex או Claude Code), מריץ qa_probe עצמאית + השוואת manifest. פלט: `_COMMUNICATION/team_50/QA-REPORT-WP-S4-06-<date>.md` עם evidence-by-path (JSON per route) + verdict PASS/FAIL/PASS_WITH_FINDINGS.

---

## 7. רישום roadmap + בעלות צעד-הבא

- **roadmap (`_aos/roadmap.yaml`, ADR034 R8 file-SSoT):** WP-S4-06 — `status: PLANNED` → `IN_PROGRESS` בתחילת בנייה; `current_lean_gate: L-GATE_SPEC`; `lod_status: LOD400`; `milestone_ref: S004`; `blocked_by: [WP-S4-01, WP-S4-04]`; `spec_ref: _COMMUNICATION/team_100/S004/WP-S4-06-LOD400-2026-07-15.md`.
- **תלות:** WP-S4-01 (עוגן verbatim — לוודא שסמני מכבי/יוני נשמרים, AC-7) ו-WP-S4-04 (טיוטות legal/en — כדי שהסמנים ישבו על התוכן הצוותי הנכון).
- **בעלות הצעד הבא:**
  1. **team_10** — מבצע §3 (הרחבת רכיבים, פעם אחת) + §4 (עריכות defaults) + מספק manifest §6.2.
  2. **team_50** — מריץ ולידציה §6 חוצת-מנוע, כותב QA-REPORT.
  3. **team_100** — סוקר כיסוי מול §2 (AC-10); מעביר ל-WP-S4-08 (שער סגירה).
- **הערת דיוק:** אם team_00 יחליט להסתיר `/galleries/` `/media/` מהתפריט (§2.1) — זו החלטת-סקופ שלא חוסמת WP-S4-06; ברירת-המחדל היא P2 (סמן אם נשארים חשופים).


<!-- HANDOFF-FOOTER v1 -->

## הנדאוף לחבילה הבאה (חובה — הצוות המבצע / team_110)

- **חבילה נוכחית:** WP-S4-06
- **הבאה בתור (`next_wp`):** WP-S4-08
- בסיום `L-GATE_BUILD PASS`: הפק הנדאוף קאנוני דרך `/AOS_handoff` (hub prompt-generate API — `type=onboard_agent`, `mode=handoff`, `team_id=110`, `wp_id` של הבאה בתור, `gate_state=gate_done`), כתוב את `artifact_markdown` verbatim ל-`_COMMUNICATION/team_110/HANDOFF_SELF_*_2026-07-XX_v1.md`, והצג את `activation_block` **inline בצ'אט** ל-team_00 (נמרוד) לניתוב ויצירת הסשן הבא.
- **אל תתחיל את החבילה הבאה אוטומטית** — עצור והמתן לניתוב team_00.
- פרוטוקול מלא + שרשרת הביצוע: `_COMMUNICATION/team_100/S004/S004-PACKAGE-SEQUENCE-INDEX-2026-07-15.md` §8.
