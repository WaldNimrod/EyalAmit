---
id: WP-S4-08-LOD400-2026-07-15
from_team: team_100
to_team: team_00
date: 2026-07-15
type: wp-build-spec
wp: WP-S4-08
lod_status: LOD400
lod_target: LOD400
milestone: S004
authoring_team: team_100
phase_owner: team_100
consuming_team: team_50
assigned_validator: team_190
next_wp: M-EYAL-INPUTS
authorized_by: "team_00 (נמרוד) 2026-07-15 — «בסיום חייבת להיות חבילת עבודה של בקרה ובדיקה סופית ומלאה ומעמיקה של כלל התיקונים וכלל העמודים והממשקים באתר»."
---

# LOD400 — WP-S4-08: בקרה ובדיקה סופית מקיפה + פריסה + חבילת מסירה לאייל

## 0. תקציר מנהלים

חבילת שער-הסגירה של S004. **אין** תיקוני-תוכן חדשים כאן — זו בקרה חוצת-מנוע, מלאה ומעמיקה, של **כלל התיקונים (WP-S4-01..07), כלל העמודים, וכלל ממשקי העריכה** באתר, לפני מסירה לאייל. סיום מוצלח = שער `L-GATE_VALIDATE` PASS (team_190, חוקתי, cross-engine) + פריסה + חבילת מסירה (docx/PDF) שמציגה בגאווה מה מוכן ומה ממתין לאייל (M-EYAL-INPUTS).

**תנאי-כניסה (חוסם):** WP-S4-01..07 כולם ב-`L-GATE_BUILD PASS` מתועד. אם חבילה כלשהי לא PASS — WP-S4-08 לא מתחיל.

**מנוע:** הבנייה/הרצה = team_50; ה-VALIDATE החוקתי = team_190. שניהם ≠ מנוע-הבנאי של החבילות שנבדקות (Iron Rule #1 / #5).

---

## 1. מטרה

לוודא, בראיות, שכל עמוד באתר הבדיקה (uPress staging) זמין, מרונדר נכון, מכיל תוכן מדויק, ניתן לעריכה ב-wp-admin (היכן שהוגדר), וכל חוסר-מאייל מסומן בסימון זוהר — ואז לפרוס ולארוז חבילת מסירה לאייל.

---

## 2. חלק A — בקרת תיקונים (re-check של WP-S4-01..07)

לכל חבילה, אמת מחדש את ה-ACs המדידים שלה מול הסטייג'ינג החי (לא מול המסמך). טבלת בקרה:

| WP | מה נבדק מחדש | שיטת ראיה | ראיה נשמרת ב |
|----|--------------|-----------|---------------|
| WP-S4-01 | 18 סקשני עוגן verbatim; 0 סקשן חסר; content-diff מול MD; סלוטי מכבי/יוני זוהרים | content-diff script + qa_probe `/snoring-sleep-apnea/` | `evidence/s4-08/anchor/` |
| WP-S4-02 | גלריות (kushi 5 / וכתבת+צבע pending); 0 קישורי Mendele; GI זמני על 3+5; schema | grep + curl JSON-LD + qa_probe | `evidence/s4-08/books/` |
| WP-S4-03 | 12 פריטי Content-QA 12.7 בדפדפן | qa_probe + CDP per-item | `evidence/s4-08/content-qa/` |
| WP-S4-04 | learning (תוכן/placeholder), משפטי (טיוטה+באנר), /en (טיוטה) | qa_probe per-route | `evidence/s4-08/content/` |
| WP-S4-05 | **עריכת wp-admin — ראה חלק C (הבדיקה הקריטית)** | ראה §4 | `evidence/s4-08/admin/` |
| WP-S4-06 | 0 פריטי חוסר-אייל ללא סימון זוהר | inventory walk + qa_probe | `evidence/s4-08/glow/` |
| WP-S4-07 | AF-01..04 answer-first blocks; FAQ-02/03; claims מסומנים | qa_probe + grep | `evidence/s4-08/seo/` |

**AC-A:** כל 7 השורות = PASS מול הסטייג'ינג, עם ראיה שמורה. כל FAIL = חוזר לחבילה המקורית (route_recommendation), לא נעקף.

---

## 3. חלק B — בקרת עמודים מלאה (כל עמוד באתר)

מקור-אמת לרשימת העמודים: `ea_chapters_route_map()` + `ea_chapters_pattern_routes()` ב-[chapters-render.php](site/wp-content/themes/ea-eyalamit/inc/chapters/chapters-render.php) + `/learning/*` + עמודי QR ילדים + בלוג + `/en`. הבנאי חייב לגזור את הרשימה מהקוד החי (לא להעתיק את הטבלה למטה — היא ייחוס).

עמודים לבדיקה (ייחוס, לא ממצה): `/` (home), `/method/`, `/treatment/`, `/snoring-sleep-apnea/`, `/sound-healing/`, `/lessons/`, `/eyal-amit/`, `/eyal-amit/mokesh-dahiman/`, `/faq/`, `/didgeridoos/`, `/bags/`, `/stands-storage/`, `/stand-floor/`, `/repair/`, `/shop/`, `/books/`, `/books/vekatavta/`, `/books/kushi-blantis/`, `/books/tsva-bekahol/`, `/contact/`, `/galleries/`, `/media/`, `/qr/` + 48 ילדי `/qr/qrN/`, `/privacy/`, `/accessibility/`, `/terms/`, `/en/`, `/learning/therapist-training/`, `/learning/lectures/`, `/learning/workshops/`, בלוג + פוסטים.

לכל עמוד, אמת (per-route checklist):
1. **זמינות** — HTTP 200 (curl); לא 404/500.
2. **רינדור** — qa_probe: אין horizontal overflow, RTL תקין, H1 יחיד, אין console errors.
3. **דיוק תוכן** — התאמה למקור המאושר (content-diff לעמודים בעלי מקור; eyeball לשאר).
4. **ניווט/קישורים פנימיים** — אין קישורים שבורים; QR permalinks שמורים (48/48).
5. **SEO** — seo_probe: title/meta-description/schema לפי canon (ציוני dev = artifacts).

**AC-B:** 0 עמודים ב-404/500; 0 overflow/RTL blocking; 48/48 QR permalinks; ראיה per-route ב-`evidence/s4-08/pages/`.

---

## 4. חלק C — בקרת ממשקי עריכה (wp-admin) — הבדיקה הקריטית

זו הבדיקה שנוגעת ישירות בדריפט שזוהה (ראה master index §1). לכל «עמוד חשוב» (master §2.1 שאושר, למעט מוחרגים באישור אייל):

1. היכנס ל-wp-admin → ערוך את העמוד → אמת ששדות ה-ACF (מ-WP-S4-05) **מופיעים** בעורך.
2. שנה שדה בדיקה (למשל כותרת סקשן) → שמור → טען את העמוד החי → אמת ש-**ה-override תפס** (השדה גובר על ה-default).
3. רוקן את השדה → אמת **נפילה חזרה ל-default** (אין white-screen, אין ריק).
4. עמודי מוצר: אמת meta-box מחיר (`ea_product_price`) עורך את המחיר המוצג.
5. FAQ: אמת ששאלות ה-CPT `ea_faq` נערכות; QR ילדים ובלוג — עורך רגיל.

**AC-C (חוסם מסירה):** כל «עמוד חשוב» עובר את מחזור override→fallback; אף עמוד לא קורס כש-ACF ריק; מחיר מוצר נערך. תיעוד: צילומי מסך + before/after ב-`evidence/s4-08/admin/`. **אם AC-C נכשל — האתר לא נמסר לאייל** (זו בדיוק הבטחת «אייל יכול לערוך את האתר»).

---

## 5. חלק D — בקרת סימון זוהר (placeholder) כלל-אתרית

הליכה על מלוא מצאי חוסרי-אייל (master §3 + WP-S4-06 inventory). **AC-D:** 0 פריטי חוסר-אייל מרונדרים ללא סימון «ממתין לאישור» זוהר גלוי; כל סימון מפנה ל-WP-EI המתאים.

---

## 5.5 חלק G — בקרת קבלת ליד (טפסי יצירת קשר) [VERDICT team_90 C90-01]

מאחר ש-S004 היא «מסירה סופית לפני עלייה לאוויר», חובה לאמת שהאתר **קולט לידים** בפועל — לא רק מרנדר עמוד `/contact/`.

1. **טופס CF7** (`ea-w2-15-cf7-contact-form-once.php`): שלח טופס-בדיקה מלא בסטייג'ינג → אמת (א) הודעת-הצלחה מוצגת; (ב) המייל הגיע ליעד/למערכת (או נרשם ב-Flamingo/לוג SMTP); (ג) אין שגיאת validation שקטה.
2. **Fallback דטרמיניסטי** — אם CF7/SMTP כבוי: אמת שכפתורי `wa.me/972524822842` ו-`tel:` חיים ולחיצים בכל route רלוונטי (WhatsApp צף + עמוד יצירת קשר).
3. **מדידה** — אמת שאירוע ליד (`generate_lead`/submit) נורה ל-GA4 (`G-MRXESK7QJF`) בשליחה מוצלחת (CDP Network / GA4 DebugView).

**AC-G (חוסם מסירה):** שליחת טופס-בדיקה אחת מקצה-לקצה מצליחה עם ראיה שמורה (`evidence/s4-08/lead/`), או — אם CF7 כבוי בכוונה — fallback `wa.me`/`tel:` מאומת חי בכל route. אחרת האתר לא נמסר.

---

## 6. חלק E — פריסה

לאחר A–D PASS:
1. פריסת theme + mu-plugins: `python3 scripts/ftp_deploy_site_wp_content.py` (כולל `ea-snoring-anchor-seed-once.php`).
2. בניית + פריסת Hub: `python3 scripts/build_eyal_client_hub.py` → `python3 scripts/ftp_publish_eyal_client_hub.py`.
3. אימות פוסט-פריסה: qa_probe smoke על 5 עמודי-מפתח + `generatedAt` מ-`hub/dist/metadata.json`.

**AC-E:** פריסה הצליחה; smoke PASS; `generatedAt` מתועד.

---

## 7. חלק F — חבילת מסירה לאייל (docx/PDF בלבד — לא MD)

לפי כלל הפרויקט: כל מה שיוצא מול אייל = **Word או PDF בלבד**.
1. הפק מסמך מסירה: `python3 scripts/build_eyal_ceo_deliverables.py` (תלות `scripts/requirements-docx.txt`).
2. תוכן: (א) מה מוכן — כל עמוד + יכולת עריכה; (ב) מה ממתין לך — רצף M-EYAL-INPUTS (7 פריטים) עם בדיוק מה נדרש; (ג) איך לספק (Hub materials-intake / content-intake).
3. יעד: `docs/project/eyal-ceo-submissions-and-responses/to-eyal/2026-07-15--final-review-package/`.

**AC-F:** docx+PDF קיימים; אין קובץ MD מוגש לאייל; רצף M-EYAL-INPUTS מוצג במלואו.

---

## 8. שער סופי + מסירה

- **L-GATE_VALIDATE** (team_190, חוקתי, cross-engine ≠ בנאי): מאמת A–F, מפיק verdict עם `evidence-by-path` + `route_recommendation`. ארטיפקט: `_COMMUNICATION/team_190/VERDICT-S004-FINAL-CONTROL-2026-07-XX.md`.
- על PASS: team_100 מסמן S004 מוכן-לסגירה, ומציג ל-team_00 (נמרוד) **go/no-go למסירה לאייל** + פתיחת רצף M-EYAL-INPUTS.
- הנדאוף קאנוני (ראה master index §8) — WP-S4-08 הוא הסוף של שרשרת S004; ה-next הוא פתיחת M-EYAL-INPUTS עם קבלת חומרים מאייל.

## 9. קריטריוני קבלה (סיכום מדיד)

AC-A..F לעיל, כולם PASS מול הסטייג'ינג עם ראיות שמורות + verdict team_190 PASS. אף שער לא נעקף; כל FAIL מנותב חזרה לחבילת-המקור.


<!-- HANDOFF-FOOTER v1 -->

## הנדאוף לחבילה הבאה (חובה — הצוות המבצע / team_110)

- **חבילה נוכחית:** WP-S4-08
- **הבאה בתור (`next_wp`):** פתיחת M-EYAL-INPUTS (עם קלט אייל)
- בסיום `L-GATE_BUILD PASS`: הפק הנדאוף קאנוני דרך `/AOS_handoff` (hub prompt-generate API — `type=onboard_agent`, `mode=handoff`, `team_id=110`, `wp_id` של הבאה בתור, `gate_state=gate_done`), כתוב את `artifact_markdown` verbatim ל-`_COMMUNICATION/team_110/HANDOFF_SELF_*_2026-07-XX_v1.md`, והצג את `activation_block` **inline בצ'אט** ל-team_00 (נמרוד) לניתוב ויצירת הסשן הבא.
- **אל תתחיל את החבילה הבאה אוטומטית** — עצור והמתן לניתוב team_00.
- פרוטוקול מלא + שרשרת הביצוע: `_COMMUNICATION/team_100/S004/S004-PACKAGE-SEQUENCE-INDEX-2026-07-15.md` §8.
