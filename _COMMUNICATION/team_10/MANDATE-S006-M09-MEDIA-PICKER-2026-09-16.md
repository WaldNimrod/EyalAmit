---
id: MANDATE-S006-M09-MEDIA-PICKER-2026-09-16
from_team: team_100
to_team: team_10
cc: [team_00, team_110]
date: 2026-09-16
type: build-mandate
milestone: S006
disposition: בנייה · team_10 בונה · team_100 מוודא בסוף
---

# מנדט M-09 · ספריית מדיה מקומית לסינון — 943 תמונות

## מה נדרש ולמה

נימרוד צריך לעבור על **כל** המדיה שלנו ולסמן מה נחוץ ומה לא. אין היום כלי לזה:
בוררת הסלוטים הקיימת מציגה 202 תמונות בלבד ושואלת שאלה הפוכה — «איזו תמונה לסלוט
הזה» ולא «לאן שייכת התמונה הזו».

**אותו מסך יעבור בהמשך לאייל**, אחרי הסינון של נימרoד ואחרי העשרה מהאתר הישן. בנה
אותו כך שהשלב השני לא ידרוש כתיבה מחדש.

## המספרים — מדדתי אותם, הם יעד ולא הערכה

- **1188** קובצי תמונה על הדיסק בשלושה אוספים
- **943** תמונות **ייחודיות לפי תוכן** (sha256)
- **222** מופיעות ביותר מאוסף אחד

**הדדופ חייב להיות לפי sha256 של תוכן הקובץ, לא לפי שם.** שמות חוזרים בין האוספים
ואינם מזהה. אם קיבלת מספר אחר מ-943 — עצור ודווח, אל תתקן לכיוון המספר שלי.

שלושת השורשים, יחסית לשורש הריפו:

    hub/dist/files/team40/ea-legacy-curated/media      315 קבצים
    hub/dist/files/team40/ea-dok-web-thumbs            582 קבצים
    hub/dist/files/team40/site-theme-images            291 קבצים

## מה לבנות

**קובץ HTML מקומי יחיד** שנפתח בדאבל-קליק — בלי שרת, בלי התקנה, בלי CDN. הוא ייפרס
בהמשך לשרת כדי שאייל יעבוד מרחוק, ולכן **כל נתיב חייב להיות יחסי.**

### א. רשת כל התמונות
כל 943, **כל תמונה פעם אחת בלבד**. לכל אחת:
- **מזהה יציב** — 12 התווים הראשונים של ה-sha256. יציב בין ריצות ובין מכונות.
- **סימון מהיר** — נחוץ · לא נחוץ · אולי. מקלדת, לא רק עכבר; מעבר לתמונה הבאה אוטומטי.
- **שדה הערה חופשית.**
- מאיזה אוסף הגיעה, ובאילו אוספים נוספים היא מופיעה (יש 222 כאלה).

### ב. סקשן «נבחרו» — לפי עמוד, נפרד מהרשת
**זו הדרישה המרכזית של נימרוד ואינה אופציונלית.** למעלה, מופרד לגמרי משאר הגלריה:
לכל עמוד — התמונות **המוגדרות לו כרגע בממשק**, ואלה שנימרוד סימן שיצטרפו אליו.
- **המקור לשיבוץ הקיים** הוא בוררת הסלוטים, בקובץ של בוררת התמונות תחת תיקיית ההפצה.
  שם יש **49 סלוטים ייחודיים**, **424** רשומות מועמדים, ו-**69** מסומנות `__current__`
  שהן התמונה שמוצגת היום. הקיבוץ לעמודים קיים שם ככותרות («בית (Home)», «אודות
  (collage)») ולא כשדה נתונים — **תצטרך לחלץ אותו, ולדווח כמה עמודים יצאו לך.**
- **שדה הערות לכל עמוד**, בנוסף להערה שלכל תמונה.

### ג. ייצוא JSON
כפתור שמייצר קובץ ובו **רק** מה שסומן או מה שיש לו הערה — לא 943 שורות ריקות.
לכל רשומה: מזהה · שם קובץ · נתיב יחסי · סימון · הערה · העמוד שאליו שויכה אם שויכה.
ובנוסף הערות העמודים. **שמירה מקומית בדפדפן** כדי שרענון לא ימחק עבודה.

## מה לא בונים עכשיו
העשרה מהאתר הישן — באיזה עמוד הופיעה ומה היה ה-alt — **אינה בסקואופ הזה.** ה-API
של האתר הישן פתוח ומחזיק 1192 פריטים, ובדקתי 5 מהנתיבים שלנו ו-5 נפתרו שם. אבל
**תרגום מזהה העמוד לשם קריא לא עמד בבדיקה שלי** — שני מזהים שונים החזירו אותה
כותרת, כלומר השאילתה לא סיננה. אל תסמוך על זה ואל תבנה עליו. שלב נפרד.

**השאר מקום במבנה הנתונים** לשדות האלה כדי שההעשרה לא תדרוש כתיבה מחדש.

## קריטריון קבלה — מדוד, ואני מוודא בסוף
- **943 תמונות בדיוק**, ואף תמונה לא מופיעה פעמיים. הוכח בספירה בקוד, לא בעין.
- הקובץ נפתח מקומית בדאבל-קליק ועובד במלואו בלי שרת.
- סקשן «נבחרו» מציג את השיבוצים הקיימים מקובצים לפי עמוד.
- ייצוא מייצר JSON תקין שנפתח, ובו רק פריטים מסומנים או עם הערה.
- רענון הדף לא מאבד עבודה.
- **כל הנתיבים יחסיים** — אמת זאת מפורשות, זה מה שיאפשר פריסה לשרת.

## 🪤 מלכודות מהשבוע
הפאנל לא גולל · ויופורט `0x0` פוסל מדידה · תמונות `lazy` נותנות `NaN` והשוואה מחזירה
«תקין» מהיעדר נתונים · `curl` ≠ DOM כשיש JS · «לא ראיתי שגיאה» אינו ראיה.
**ותוסיף אחת משלך:** 943 תמונות ברשת אחת — אם לא תטען אותן עצלות המסך ייתקע.

## אסור
פריסה · דחיפה · נגיעה בטרקר או בטופס · שינוי בוררת הסלוטים הקיימת · תלות חיצונית.
