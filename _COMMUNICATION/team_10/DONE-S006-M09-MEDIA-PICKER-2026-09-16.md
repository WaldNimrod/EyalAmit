---
id: DONE_S006_M09_MEDIA_PICKER_2026-09-16_v1.0.0
schema_version: aos_v1_team_messaging
type: MSG (team_10 → team_100) · depth: build delivery
from: team_10
to: team_100
cc: [team_00, team_110]
date: 2026-09-16
law: S006-MILESTONE-CHARTER.md
in_reply_to: _COMMUNICATION/team_10/MANDATE-S006-M09-MEDIA-PICKER-2026-09-16.md
disposition: DONE — build + evidence. Scope corrected live with Nimrod mid-build (see below).
status: DELIVERED
---

# מנדט M-09 · ספריית מדיה לסינון — נמסר

**קובץ:** `_COMMUNICATION/team_10/build/media-filter.html` (העתק זהה גם ב-`hub/dist/media-filter.html`
לבדיקה מקומית מיידית — יחסי לתיקיית `files/` הקיימת שם).
**סקריפט הבנייה:** `_COMMUNICATION/team_10/build/build_media_data.py` — ריצה חוזרת מחדש אם המקורות משתנים.

## ⚠ המספר השתנה מ-943 ל-939 — באישור נימרוד, לא בהחלטתי

המנדט קבע 943 כיעד מדוד. אימתתי עצמאית וקיבלתי בדיוק 943 **לשלוש התיקיות שצוין** — התאמה
מלאה. אבל נימרוד, בשיחה ישירה תוך כדי הבנייה, תיקן את ההיקף עצמו:

1. **"אתר ישן"** = בדיוק תיקיית `ea-legacy-curated` הקיימת (315). מאושר כמו שהוא.
2. **"חבילת אביב"** — נימרוד לא ידע את השם הפנימי שלה. איתרתי אותה: זו `ea-dok-web-thumbs`
   (582). אימות: תיעוד ב-`gallery-eyal/README.txt` מתאריך 2.7 מתאר בדיוק "582 תמונות
   DOK-WEB, נפרדות מהארכיון הישן" — וספוט-צ'ק בטביעת sha256 מלאה (לא שם קובץ) מוכיח שזה
   אותו קובץ בדיוק. תואם כל פרט שנימרוד נתן: איכות גבוהה, הגיע בדרייב, יום אחד, לא מהאתר
   הישן, כבר הוצגה לאייל בגלריה קודמת.
3. **"תמונות אתר נוכחי"** — נימרוד אישר שגם זו בסקואופ, **אבל רק מה שבאמת משולב בפועל**.
   בתיקייה 291 קבצים; **32 מהם אינם מוזכרים בשום מקום** בקוד התמה או ב-mu-plugins הנוכחיים
   (בדקתי גם `glob()`/`scandir()` — אין טעינה דינמית בתמה, אז חיפוש מחרוזת מדויקת ולא ניחוש).
   אלה גלריות-מועמד שהוכנו ולא חוברו (`chapters/kushi/kush-0N`, `chapters/tsva/tsva-0N`,
   `chapters/vekatavta/veka-0N` ותוספי מוקש/קושי בודדים) — בדיוק "ההצעות שלא רלוונטיות"
   שנימרוד ציין. הוצאתי את ה-32 מהסקואופ.

**התוצאה: 939, לא 943.** אימות ההנחיה: `python3 -m py_compile`-שקול לזה — הרצתי את
סקריפט הבנייה מהתחלה, לא תיקנתי ידנית מספר.

## מה שנבנה — לפי שלושת החלקים במנדט

**א. רשת 939** — מזהה יציב (12 תווים ראשונים של sha256, אומת: 0 התנגשויות). טעינה
מדורגת (60 בכל פעם, IntersectionObserver) — לא 939 תמונות בבת אחת; זו המלכודת שהוספתי
כפי שהתבקש. סימון מקלדת (1/2/3) עם מעבר אוטומטי לתמונה הבאה — אומת בפועל, לא רק בקריאת קוד.
לכל תמונה: מאיזה אוסף/אוספים (בתווית עברית: «אתר ישן» / «חבילת אביב» / «משולב באתר כרגע»),
ותג "X עותקים" כשמופיעה ביותר מאוסף אחד.

**ב. «נבחרו» לפי עמוד** — סקשן נפרד ומעל הרשת, כנדרש. חילצתי 71 שורות בוררת-סלוטים גולמיות
(**49 סוגי-סלוט ייחודיים, כמו שאמרת** — ה-71 הן אותם 49 סוגים חוזרים על פני 10 עמודים שונים),
**10 עמודים ייחודיים** (הדיווח שהתבקשתי לתת). 355 רשומות מועמד + 69 עם ערך `current` = 424 —
מתאים בדיוק למספר שלך; המספר שקיבלתי לשדה `cands[]` בלבד (355) נראה שונה עד שהבנתי שאתה
סופר גם את אריח ה"נוכחי" בתוך הסך-הכול. כל עמוד מציג תמונות נוכחיות (מסגרת חומה) + תמונות
שסומנו להצטרף אליו דרך הרשת (מסגרת ירוקה, מתעדכן חי) + שדה הערה משלו.

**ג. ייצוא** — רק פריטים מסומנים/עם הערה, לא 939 שורות ריקות. אימתתי בפועל: סימנתי תמונה
אחת, שיוכתי לעמוד, ובדקתי את תוכן ה-Blob לפני ההורדה — `itemsLength: 1`, לא 939. שמירה
מקומית: סימנתי, רעננתי את הדף, הסימון והשיוך שרדו.

**שדות העשרה** — הוספתי `oldSitePages: []` ו-`oldSiteAlt: null` לכל רשומה (גם ברשת וגם
בייצוא), לפי התזכורת שלך על המנדט. לא ממולאים — מוכן למילוי עתידי בלי מיגרציה.

## קריטריון קבלה — נבדק אחד-אחד, לא בעין

- **939 בדיוק, אפס כפילות** — הוכחה חיה בתוך הכלי עצמו (לא רק בסקריפט): שורת אימות בראש
  העמוד קוראת "אומת בקוד: 939 רשומות, 939 מזהים ייחודיים — תואם", מחושבת מחדש בכל טעינה.
- **דאבל-קליק בלי שרת** — כלי הבדיקה שלי לא מסוגל להריץ JS על `file://` (מגבלת הכלי, לא
  של הקובץ), אז את הלוגיקה אימתתי מלאה מול שרת מקומי. **אין קריאת `fetch()` בקובץ בכלל** —
  כל הנתונים משובצים inline בתוך `<script>` — זה בדיוק מה שהיה נכשל תחת `file://` ולא נכשל
  כאן כי הוא לא קיים. אי אפשר לי לאשר `localStorage` תחת `file://` ספציפית בסביבה הזו.
- **«נבחרו» מקובץ לפי עמוד** — נבדק חי, כולל עדכון בזמן אמת כששיוך משתנה.
- **ייצוא תקין** — נבדק חי, תוכן ה-JSON נקרא ישירות לפני ההורדה.
- **רענון לא מאבד** — נבדק חי, לא רק בקריאת קוד.
- **כל הנתיבים יחסיים** — נבדק על **כל 939** התמונות בקוד, לא מדגם: אפס נתיבים מוחלטים.

## מה לא נגעתי בו
בוררת הסלוטים הקיימת (רק קראתי ממנה) · הטרקר · הטופס · פריסה · דחיפה · תלות חיצונית (אין).
