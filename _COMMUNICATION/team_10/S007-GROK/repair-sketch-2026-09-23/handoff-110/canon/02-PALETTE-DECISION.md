# החלטת פלטה — עמוד תיקון וטיפוסי השורה

מזהה: `PAL-REPAIR-2026-09-23`  
תאריך: 2026-09-23  
אישור: נימרוד, על הסקיצה בסיבוב השני («יפה מאושר»)  
מקור המספרים: פלטת המותג `file:///Users/nimrod/Documents/AOS_V5/EyalAmit.co.il-2026-repair-sketch/docs/project/EYAL-SITE-COLOR-PALETTE.md`  
המסמך הזה לא משנה את פלטת המותג ולא את `:root` ב־`chapters.css`.

## ההחלטה

פס קריאה לפעולה בעמוד התיקון, ובכל טיפוס שורה שמשתמש בפס הזה (`RT-04`), נצבע **שוקולד `#5C3A2E`**.

הכפתור על הפס הזה הוא **חול `#D8C7B5`** עם טקסט שוקולד.

הפוטר נשאר **`#0E0905`**. צבע הפוטר לא משמש פס תוכן.

**טרקוטה `#A44E2B` פסולה כמילוי פס.** היא נשארת צבע במותג. היא לא חוזרת כרקע של קריאה לפעולה בעמוד הזה.

## למה זו ההחלטה, לא העדפה

בסיבוב הראשון הפס היה טרקוטה `#A44E2B`. נימרוד פסל את הגוון: בהיר מדי, והגוון עצמו לא יפה. זו פסילה של הצבע, לא רק של יחס הניגוד. לבן על טרקוטה הוא 5.67:1 — עובר סף טקסט — ובכל זאת נפסל.

במקביל, פס כמעט־שחור (ה־`--dark-grad` של `.cta-band` החי, קרוב לפוטר) נפסל קודם לכן כי הוא נראה כמו הפוטר. לכן הפס החדש חייב להיות חום, כהה מהטרקוטה, ושונה מהפוטר.

שוקולד `#5C3A2E` הוא הצבע מהפלטה שעומד בשלושת התנאים. לבן עליו: 10:1.

## תפקידים סגורים

| תפקיד | HEX | טקסט עליו | ניגוד | סטטוס |
|---|---|---|---|---|
| מילוי פס קריאה (`RT-04`) | `#5C3A2E` שוקולד | לבן `#FFFFFF` | 10.00:1 | סגור |
| כפתור על הפס, ועל פס תמונה (`RT-07`) | `#D8C7B5` חול | `#5C3A2E` | 6.08:1 | סגור |
| פוטר | `#0E0905` | לבן | 19.81:1 | סגור. לא פס תוכן |
| טקסט גוף וכותרות על שנהב | `#2E2B28` דיו | על `#fffffa` | 14.03:1 | סגור. כמו החי |
| קישור בגוף על שנהב | `#5C3A2E` | על `#fffffa` | 9.97:1 | סגור לסקיצה |
| שורת רקע רכה (`.sec--alt`) | `#efeae1` | דיו `#2E2B28` | 11.74:1 | סגור כרקע המתחלף |
| רקע עמוד | `#fffffa` | | | סגור. כמו החי |

חול הוא מילוי הכפתור. חול אינו רקע של שורה מתחלפת. בסיבוב הראשון רקע חול מלא הכביד על העמוד, והסיבוב המאושר החליף אותו בשנהב־2 `#efeae1`, שכבר קיים כ־`.sec--alt`.

## מה לא בשימוש בעמוד הזה

| HEX | שם | למה לא כאן |
|---|---|---|
| `#A44E2B` | טרקוטה | פסול כמילוי פס. נימרוד, 23.9.2026 |
| `#B5663D` | ה־`--terra` החי ב־`chapters.css` | לבן עליו 4.26:1, מתחת לסף טקסט גוף. לא צבע מהפלטה הנעולה |
| `#0E0905` כפס | פוטר | נראה כמו סוף העמוד |
| `#AB3A2B` | בריק | לא נבחר. לבן עליו 6.23:1, אבל הסקיצה המאושרת לא משתמשת בו |
| `#8A5A44` | אדמה | טקסט משני בלוח הסקיצה בלבד. לא מילוי פס |
| `#6E6F4A` | זית | לא בשימוש בסקיצה המאושרת |

## היקף

ההחלטה מחייבת את עמוד התיקון ואת טיפוסי השורה בחבילה הזו.

היא לא מחליפה את `--terra` הגלובלי, לא צובעת מחדש כפתורי טרקוטה בעמוד הבית או בדום נשימה, ולא מעדכנת את מסמך פלטת המותג. הרחבה לעמודים אחרים היא סבב נפרד, עם אישור נימרוד, ורשומה חדשה במסמך הזה — לא תיקון שקט של השורה הסגורה.

## יישום

הצבעים נכנסים לקובץ ה־CSS של עמוד התיקון (נטען רק כשסוג הפרק הוא `repair`), כמשתנים מקומיים של הקובץ או כערכים על המחלקות `cta-band--choc` ו־`btn--sand`. לא כשינוי של `:root` ב־`chapters.css`.
