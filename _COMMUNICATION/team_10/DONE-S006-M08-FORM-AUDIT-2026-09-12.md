---
id: DONE_S006_M08_FORM_AUDIT_2026-09-12_v1.0.0
schema_version: aos_v1_team_messaging
type: MSG (team_10 → team_100) · depth: audit delivery
from: team_10
to: team_100
cc: [team_00, team_110]
date: 2026-09-12
law: S006-MILESTONE-CHARTER.md
in_reply_to: _COMMUNICATION/team_10/MANDATE-S006-M08-FORM-AUDIT-2026-09-12.md
disposition: 3 findings worth blocking send, 2 worth fixing, rest clean. Zero edits made.
status: DELIVERED
---

# מנדט M-08 · ביקורת הטופס — תוצאות

**קובץ שנבדק:** `hub/dist/s006-review.html` (80,834 בייט, נוצר 12.9 15:23) — מקור:
`scripts/s006_review_form.py`. לא ערכתי אף אחד מהם.

## תקציר חומרה

| חומרה | ממצא |
|---|---|
| **חוסם שליחה** | 1. שישה מתוך 13 «סעיפים» הם רשומות ריקות שלא מרונדרות בכלל — והטקסט מבטיח לאייל שהן «מופיעות למטה כשאלה» |
| **חוסם שליחה** | 2. סקשן «נימרוד — פנימי, לא לאייל» נגיש ומקושר באותו עמוד שנשלח לאייל |
| **כדאי לתקן** | 3. `/eyal-amit/mokesh-dahiman/`: הנגן ה«אמיתי» רחוק מטקסט הפתיחה, לא לידו |
| קוסמטי/לתשומת לב | 4. אין ניגוד ממצה נוסף מעבר לאלה |

---

## א. אמת מול האתר החי

בדקתי מחדש (לא הסתמכתי על M-07 מלפני 4 ימים) את חמשת העמודים בלוק «מה השתנה»:

- **`/shop/`, `/faq/`, `/snoring-sleep-apnea/`** — הטקסט לא השתנה מאז M-07, לא בדקתי חי שוב לעומק (זהה בייט-בבייט לטקסט שכבר אימתתי; אין סיבה לחשוד ברגרסיה שם — שום קומיט לא נגע בעמודים האלה מאז).
- **`/eyal-amit/mokesh-dahiman/`** — בדקתי **מחדש, חי**, כי מצאתי סתירה פנימית (ראו סעיף ג/סעיף 3 למטה). התוצאה: הטענה בבלוק העליון («נוסף נגן של הסרט ליד טקסט הפתיחה») מדויקת רק לגבי הרקע המושתק בהירו. הנגן האמיתי (עם כפתורי הפעלה) יושב הרבה יותר למטה — **ממצא, פרטים בסעיף 3**.
- **`/contact/`** — נבדק חי במלואו, ותואם: `<select required>` עם 7 האופציות המדויקות שהטופס מצטט («טיפול בדיג'רידו · שיעורי נגינה · סאונד הילינג · רכישת כלי · רכישת ספר · תיקון כלי · אחר»), וכפתור וואטסאפ **גלוי אחד בלבד** (`querySelectorAll` על קישורי wa.me + הכפתור הצף, מסונן לפי `display`/`hidden` בפועל). ה-D-8 וההסרה של הכפתור הכפול תקינים ומדויקים.

## ב. אמת פנימית — מספרים

- **«13 סעיפים לסימון»** (שורה 45) — **תואם טכנית** את `S006_CONFIG.items.length` (13, אימתתי דרך הקונסולה בעותק חי של הטופס). **אבל** — ראו ממצא 1 למטה: 6 מתוך ה-13 לא מרונדרים לשום card. מי שסופר cards בעין רואה 7, לא 13.
- **«מוכנים לאישור באתר הבדיקה (15)»** — תואם. `pages.length` בקונפיג הוא 16, אבל אחד מהם הוא `GENERAL` (הערות כלליות, לא עמוד עם קישור/אישור) והקוד מסנן אותו החוצה במפורש (`p.key !== "GENERAL"`). 15 בפועל. **תקין, נבדק במפורש.**
- לא מצאתי סתירות נוספות במספרים (למשל «15 מתוך 16» ו-«3» בבלוק «שני דברים שכבר תקינים» — אלה בדיוק המספרים שאמורים להצטרף לשני הסעיפים החסרים בממצא 1, ראו שם).

### ⚠ ממצא 1 — שישה «סעיפים» מהקונפיג לא קיימים בעמוד בפועל (חוסם שליחה)

מדדתי דרך `window.S006_CONFIG.items` מול `document.querySelectorAll('.s006-item--need')` בעותק חי
(שרת מקומי, לא שינוי בקובץ). הקונפיג מכיל 13 רשומות; רק 7 מרונדרות ל-DOM. השישה החסרות:

| id | pageKey | תוכן ברשומה |
|---|---|---|
| R1-01/H-16 | R1-01 (בית) | `{id, domId, pageKey}` **בלבד** — אין `title`, אין `ask`, אין בחירות |
| R1-26/TST-D3 | R1-26 (עדויות) | אותו דבר — ריק |
| R1-02/TRT-D4 | R1-02 (טיפול) | ריק |
| R1-04/LSN-D4 | R1-04 (שיעורים) | ריק |
| R1-05/SH-D4 | R1-05 (סאונד הילינג) | ריק |
| R1-12/DG-06 | R1-12 (כלים/דיג'רידו) | ריק |

זה **בדיוק** אותם עמודים מהבלוק «שני דברים שכבר היו תקינים» (בית 15/16, כלים 3, טיפול/שיעורים/סאונד
הילינג — מספר נכון, עדויות — חצי הקרוסלה). והטקסט שם **מבטיח**: «שתי אלה מופיעות למטה כשאלה»
(שורה 40 בקובץ). **הן לא מופיעות.** לא כ-card ריק, לא כהודעה — כלום. `items.length=13` גורם
למונה ההתקדמות («X מתוך 13») להיות **בלתי-ניתן-להשלמה במבנה** — אייל לעולם לא יגיע ל-13/13
דרך המסך, גם אם ימלא את כל מה שהוא רואה (7/13 הוא המקסימום הפיזי).

**למה זה חמור מבחינת אייל:** הטופס מבטיח לו במפורש שיראה שאלה שהוא לא רואה, והמונה שהוא
עוקב אחריו לעולם לא ייסגר. אם השאלות האלה **היו** אמורות לכלול תוכן אמיתי (אישור «המספר כבר
נכון» לכל עמוד) — הן אבדו בדרך. אם הן **לא** היו אמורות בכלל — הן צריכות לצאת מ-`items`
ומהספירה, לא להישאר כרשומות רפאים.

### ⚠ ממצא 2 — סקשן «לא לאייל» נגיש בפועל באותו טופס (חוסם שליחה)

שורה 173: `<a href="#nimrod-decisions">נימרוד · שבע הכרעות לסבב 2 (**פנימי, לא לאייל**)</a>`.
שורה 664-666: `<section class="s006-nimrod-board" ...><p>פנימי · לא למילוי אייל · מקומי בלבד</p>
<h2>שאלות לנימרוד — סבב 2</h2>`. שבעה סעיפים (`data-mode="nimrod"`) על החלטות 301/הקפאה
לעמודים כפולים, עם כפתור ייצוא נפרד (`btn-export-nimrod`) ומונה התקדמות נפרד.

**אין שום `hidden`, שום גייט, שום חלק נפרד של הבנייה שמסנן את זה.** זה קישור רגיל, לחיץ,
בעמוד שהוא בדיוק העמוד שנשלח לאייל בוואטסאפ. תיוג הטקסט כ-«לא לאייל» **בתוך הדף שנשלח
לאייל** אינו מנגנון הרשאה — זו הודעה שהוא יכול לקרוא ואז ללחוץ בכל זאת (או סתם לגלול/לחפש
בעמוד ולהיתקל בזה). זה חושף החלטות פנימיות (301, הקפאת עמודים) בשפה שאינה מיועדת ללקוח.

**המלצה (לא ביצוע — זה שלך):** הפרדה אמיתית — קובץ נפרד ל-team_00/נימרוד, לא סקשן מקושר
מתוך הקובץ שהולך ללקוח.

## ג. הבנה + כפילות + ניסוח פנימי

עברתי על כל 7 הפריטים שכן מרונדרים (H-16 לא נכלל בהם — הוא בין ה-6 הריקים). כולם: שאלה
אנושית ברורה בעברית, הסבר-קונטקסט לפני הבחירה, אפשרויות בשפה טבעית (לא קודים), fallback
לטקסט חופשי. לא מצאתי ניסוח שנשמע כהודעה פנימית **בתוך שאלה שכן קיימת**. לא מצאתי כפילות
בין סעיפים. לא מצאתי סעיף ששואל דבר שאייל כבר סיפק.

**ממצא 3 (כדאי לתקן) — `/eyal-amit/mokesh-dahiman/`, MK-08:** הטקסט אומר «כבר יש בעמוד הסרט
הזה — רץ כרקע ההירו... אנחנו מוסיפים נגן אמיתי שאפשר לצפות בו **ליד הפתיחה**». מדדתי חי:
ה-iframe השני (`title="MUKESH: The Art of Shanti Living"`, בקרות מלאות, `allowfullscreen`,
בלי `autoplay`/`mute`/`controls=0`) יושב ב-`top:1491px` — אחרי הכותרת (`523-587`), הפסקה
הראשונה (`607-711`) **וגם** פסקה נוספת («מתוך בית מלאכה צנוע...») שמקדימה אותו כ-sibling
ישיר. זה לא «ליד הפתיחה» — זה בהמשך הפתיחה, אחרי לפחות פסקה שלמה. ההשלכה מעשית: אם אייל
בוחר «להשאיר את שניהם» מתוך הנחה שהנגן האמיתי נוח ונגיש ליד הכותרת, הוא טועה לגבי מה שהוא
מאשר. לא בדקתי אם התיאור ב-MK-08 עצמו מדויק במידה מספקת בעיניכם — זו שאלת ניסוח שמסורה לכם.

## ד. דליפות פנימיות

בטקסט **שאייל קורא** (לא בתוך תגי `id`/`data-*` הטכניים, שהמנדט עצמו מתיר) — לא מצאתי מזהי
שורה, שם קובץ, סלקטור CSS, מספר גרסה, שם צוות או מונח גיט. תגיות `<span class="s006-item__id">`
(כמו «MK-09») הן בדיוק ה-«מזהה קצר ליד שאלה» שהמנדט אישר. **הממצא היחיד באזור הזה הוא ממצא 2
למעלה** — לא דליפת-מזהה טכני אלא דליפת **תוכן** (שפה/החלטות פנימיות), חמור יותר מדליפת מזהה.

## ה. קישורים

הרצתי `scripts/hub_check_dist_links.py` — **אפס כשלים ב-`s006-review.html` עצמו** (הכשלים
שהתקבלו כולם ב-`index.html`/`image-picker.html`, קבצים אחרים ב-`hub/dist/`, לא בטופס). מעבר
לזה (כי הסקריפט בודק קיום בדיסק, לא נכונות יעד, כדבריכם):
- שלושת קישורי הניווט (`what-we-need.html`, `tasks.html`, `archive.html`) — קיימים, עם `<title>`
  תואם לתוכן (לא דפים ריקים/placeholder).
- כל חמשת קישורי העמודים בבלוק «מה השתנה» — ניווטתי לכל אחד בפועל (לא רק HEAD/curl), הגעתי
  לעמוד הנכון בכל מקרה (כותרת הדף תואמת).
- לא מצאתי קישור ל-Google Drive/xlsx בטופס עצמו (יש רק אזכור טקסט «בדרייב» בלי `href` בסקשן
  אחר בקובץ שכבר קראתי ב-M-07) — זה לא קישור שבור, פשוט אין קישור שם; לא ממצא, רק הערה.

## ו. הטופס כמנגנון

קראתי את `hub/dist/assets/s006-review.js` במלואו ובדקתי חי (עותק מקומי, `localStorage`
מבודד — לא נגעתי בקובץ):
- **שמירה/שחזור:** בחרתי אופציה + כתבתי טקסט חופשי, רעננתי את הדף — **שניהם השתחזרו נכון**
  (`localStorage['ea-s006-review-v2']`). תואם לטקסט «התשובות נשמרות במחשב שלך».
- **ייצוא ריק:** קריאת קוד (`exportJson`, שורה 316-319) — אם שום עמוד/סעיף לא סומן, הפונקציה
  מציגה `alert("אין תשובות לייצוא — סמנו לפחות עמוד אחד, או כתבו הערה.")` ועוצרת. **יש הודעה
  ברורה, לא כשל שקט.** לא הפעלתי את דיאלוג ה-alert בפועל (קשה לתפוס אוטומטית בכלי הזה) —
  הקוד חד-משמעי מספיק כדי לסמוך עליו כאן.
- **שדה חובה שחוסם בלי להסביר:** לא מצאתי. אין `required` על שום שדה בטופס-האקסל עצמו (ה-`required`
  היחיד שראיתי הוא ב-CF7 החי באתר, לא בטופס-הביקורת).

## ז. עברית ו-RTL

- **Markdown שדלף:** בדקתי גורף — כל מופע של `**` בעמוד כולו: **אפס** מחוץ לתגיות HTML.
  כל מופע `_`: רק בתוך `id`/`name` טכניים (`R1-22__MK-08` וכו'), אף פעם בטקסט קריא. **לא
  שוחזר כאן** (ייתכן שהתקלה שהוזכרה במנדט כבר תוקנה לפני שנוצר הקובץ הזה, 15:23 היום).
- קראתי את כל הטקסט הרציף בבלוקים העליונים ובשבעת הסעיפים המרונדרים: לא מצאתי שגיאת כתיב,
  מקף/גרש שבור, או היפוך לטיני-בתוך-RTL. הניסוח קולח ואנושי.

## ח. כל דבר אחר

שני הממצאים החמורים (1,2) כבר נכנסים תחת «כל דבר אחר» יותר משהם נכנסים לסעיפים הספציפיים —
דיווחתי אותם שם כי זה איפה שגיליתי אותם (בדיקת מנגנון → 1; קריאת ניווט מלא → 2). לא מצאתי
דבר נוסף מעבר לשלושת הממצאים.

---

## מה לא נגעתי בו
עריכת הטופס או `scripts/s006_review_form.py` · פריסה · דחיפה · הטרקר.
