---
id: DONE_S006_M02_R1-23_CONTACT_2026-09-06_v1.0.0
schema_version: aos_v1_team_messaging
type: MSG (team_10 → team_100) · depth: mandate delivery
from: team_10
to: team_100
cc: [team_00]
date: 2026-09-06
law: S006-MILESTONE-CHARTER.md
in_reply_to: _COMMUNICATION/team_10/MANDATE-S006-M02-R1-23-CONTACT-2026-09-06.md
disposition: DONE — code + evidence. Closes at build; not deployed, not pushed.
status: DELIVERED
---

# מנדט M-02 · R1-23 `/contact/` — הושלם

## קובץ שנגע

`site/wp-content/themes/ea-eyalamit/template-parts/chapters/parts/contact.php`
(יחיד. שום קובץ אחר לא שונה — אומת ב-`git status --short -- site/`.)

**הערת גבול:** זהו קובץ ב-`template-parts/chapters/parts/`, לא `<page>-defaults.php`. לפי ההכרעה
הקודמת שלך (ב.2.1) הנתיב הזה סומן כ-team_110; לפי אותה הכרעה, "קובץ קיים, אחד, של עמוד אחד → אתה"
(ב.2.3). המנדט הזה קרא לי לקובץ קיים בנתיב הזה במפורש, אז ביצעתי — אבל מציין את המתח כדי שהכלל
הכללי יתעדכן אם צריך.

## מקור מצוטט

`EA-CONTENT-TRACKER.xlsx` · `סבב-1-ליבה` · `R1-23` · «הערות אייל»:
> «בטופס עצמו - תמחק את השדה "נושא הפניי". זה מיותר.»

## הפעולה

מחיקה בלבד — אין תוכן חדש, אין ניסוח. הוסר לגמרי (שורות 48–58 במקור): ה-`label` עם
`for="ea-cf-topic"`, ה-`select` עם `id="ea-cf-topic"` ושש ה-`option` שבתוכו, והעטיפה
`div.ea-contact-form__field` שהייתה נשארת ריקה. `git diff` מצורף למטה — diff נטו, בלי תזוזה
של שום שורה אחרת.

```diff
-					<div class="ea-contact-form__field">
-						<label class="ea-contact-form__label" for="ea-cf-topic"><?php esc_html_e( 'נושא הפנייה', 'ea-eyalamit' ); ?></label>
-						<select class="ea-contact-form__select" id="ea-cf-topic" name="topic">
-							<option value="intro">...</option> (וחמישה נוספים)
-						</select>
-					</div>
-
```

## מטפל השליחה — נבדק

חיפוש גורף (`grep -rn "ea-cf-topic"` + `grep -rn "'topic'"`) בכל `site/wp-content/themes/`
ו-`site/wp-content/mu-plugins/`: **אין שום מקום נוסף** — לא PHP (`$_POST`), לא JS — שקורא לשדה
הזה לפי `id` או לפי `name="topic"`. הטופס הגולמי (`action="#"`, `novalidate`) אינו מטופל ע"י
`ea-ab-testing.js` ברמת submit כלל (ה-JS נוגע רק ב-attribute `data-wa-variant` על העטיפה, לא
בשדות). מסקנה: אין הפניה לתקן — אין מה לשבור.

## הוכחה — לא הצהרה

אי אפשר לפרוס (שער 4 מושהה), אז בניתי אימות מקומי מהקובץ האמיתי, לא מהעתקה ידנית:

1. רינדור בפועל של `contact.php` **הערוך** דרך stub PHP מינימלי (מגדיר רק את הפונקציות
   שהקובץ קורא להן — `esc_html_e` וכו' — ומריץ `include` אמיתי על הקובץ בדיסק).
2. הפלט נטען בדפדפן יחד עם **הקובץ האמיתי** `ea-ab-testing.js` (ללא שינוי), ולחיצה אמיתית
   על «שליחת פנייה».
3. אסרציות חיוביות שנבדקו בפועל (לא "לא נמצאה שגיאה"):
   - `document.getElementById('ea-cf-topic')` → `null`.
   - הטקסט «נושא הפנייה» לא קיים בתוך `section.ea-wave2-contact` (מוגבל לרכיב עצמו,
     לא ל-`body` כולו — הבדיקה הראשונה שלי נפלה על false positive כי מחרוזת החיפוש
     בקוד-הבדיקה שלי עצמו נספרה בטעות; תוקן וסומן).
   - `fieldCount` בטופס = 4 (שם מלא, טלפון, דוא"ל, הודעה) — בדיוק המצופה.
   - `window.__jsErrors` אחרי הלחיצה על submit = `[]` — אפס שגיאות JS.

## php -l

```
No syntax errors detected in site/wp-content/themes/ea-eyalamit/template-parts/chapters/parts/contact.php
```

---

# דיווח בלבד — לא בוצע

## שני כפתורי וואטסאפ ב-`/contact/`

אותרו **שני מקורות נפרדים**, לא כפילות של אותו רכיב:

1. **CTA בתוך העמוד** — `contact.php` שורות 76–79 (`div.ea-cta-ab`, וריאנט A גלוי תמיד
   בפועל: `ea-ab-testing.js` שורה 62 עושה `querySelector` יחיד שתופס רק את הראשון מבין
   שני ה-`[data-ea-ab-wa]`; הווריאנט השני נשאר עם `hidden` הסטטי מה-markup ולעולם לא
   נחשף). זה חלק מתוכן העמוד עצמו.
2. **הכפתור הצף הגלובלי** — `ea_wave2_render_whatsapp_float()`,
   `inc/wave2-stage-b.php` שורות 379–396, מחובר ל-`add_action('wp_footer', ..., 15)`
   (שורה 396) — **נטען בכל עמוד באתר**, לא רק ב-`/contact/`.

**זה בדיוק המקרה שציינת** — רכיב גלובלי (כפתור צף). לא נגעתי בו. לפי ההכרעה ב.2.3 — גל-רכיב
נפרד אצל team_110, לא אצלי.

## מה שלא נגעתי בו

טקסט אחר בעמוד · שדות אחרים · CSS גלובלי · פריסה · דחיפה · הטרקר.
