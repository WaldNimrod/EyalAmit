---
id: DONE_S007_M11_2026-09-20
schema_version: aos_v1_team_messaging
type: DONE (team_10 -> team_100)
from: team_10
to: team_100 (eyalamit-co-il-2026-76)
cc: [team_00, team_50]
date: 2026-09-20
mandate: MANDATE-S007-M11-COMPOSER-VISUAL-PROOF-2026-09-20.md
theme_under_test: 1.5.83 (unchanged — no fix, no deploy, per the mandate)
status: Both Composer lines complete. Every file verified to exist. Every specifically-named
  check independently re-opened and viewed by me, not just relayed from Composer's own text.
  Two of Composer's four flagged "issues" do not survive that check; both are documented below
  with the exact evidence, not just asserted.
---

# M-11 — screenshots, on Composer, of everything that shipped tonight

Two Composer lines ran, per team_00's instruction. Report only — nothing fixed, nothing
deployed, live stays at 1.5.83.

## What I did beyond running the two lines

Per this mandate's own point ("send me the paths — I will look at the images myself"), I did
not just forward Composer's prose. I opened a specific, deliberately chosen set of the actual
PNG files myself — one per named check in the mandate, plus every image behind a "FOUND AN
ISSUE" verdict — and for two of those four issues, went one step further and checked the live
DOM/source directly, because the screenshot alone did not settle what was actually causing what
Composer described. Both are written up below with the evidence, not just a verdict.

## Line A — reading-measure risk (65ch→82ch), 10 pages/posts, both widths

Full per-file report: `tmp/qa/xval-2026-09-18/M11A.out.md`. All ~40 files confirmed on disk.

**Result: clean, with one correction.** Composer flagged `treatment-390-mid.png` as "a single
orphaned word `מהבסיס.` alone on its own line." I opened that exact file myself: the line in
question is `העבודה מתחילה מהבסיס.` — three words, a normal paragraph-closing line, not an
orphan. I also opened `treatment-1440-body.png` (the primary 1440 prose evidence) directly —
centered column, ~equal side gutters, no collisions, Heebo throughout, matches the CONFIRMED
CLEAN verdict.

Everything else in Line A — `/faq/`, `/services/`, one blog post, and all six M-10-cleaned posts
(194, 195, 190, 188, 203, 193) at both widths — reads CONFIRMED CLEAN, with three URLs from my
own prompt 404ing (a typo of mine, "סאוון"; a missing letter in post 193's slug; post 203's slug
not resolving directly but working via `/?p=239`). Composer reported all three transparently and
worked around two of them rather than silently skipping — flagged here for completeness, not as
a defect in anything live.

## Line B — features shipped tonight (TOC-in-hero, Yoni rebuild, WhatsApp icon, fonts)

Full per-file report: `tmp/qa/xval-2026-09-18/M11B.out.md`. All 19 files confirmed on disk. I
personally opened 9 of the 19 — every image tied to one of the mandate's named checks, plus both
pages behind a FOUND-AN-ISSUE verdict:

- **TOC in hero, `/snoring-sleep-apnea/` (item 2):** confirmed on `snoring-hero-1440.png` —
  full numbered list 01–11, inline-end side, vertically centered, legible on the dark hero.
  Confirmed on `snoring-hero-390.png` that this is genuinely different at mobile width: no
  inline list, a collapsed "בעמוד הזה" pill bottom-right instead.
- **Yoni rebuild, `/snoring-sleep-apnea/` (item 3):** confirmed on `snoring-yoni-1440.png` — the
  WhatsApp screenshot floats right, Hebrew paragraphs wrap beside it. Confirmed on
  `snoring-yoni-cards-1440.png` — the two quote cards sit side-by-side as a full-width band
  directly under "מיד אחר כך הוא כתב:". Confirmed on `snoring-yoni-390.png` that the float
  becomes full-width-stacked-above at mobile width, no wrap.
- **Drawer hides WhatsApp, `/about/` (item 4):** confirmed on `about-menu-open-390.png` — drawer
  open, full nav visible, no WhatsApp icon anywhere in frame.
- **Fonts (item 5):** Heebo throughout every image I opened; nothing in a raw fallback face.

**Two open items, accurately observed, not yet resolved as bug-vs-design:**
1. The `/snoring-sleep-apnea/` TOC collapsing to a bottom-right pill at 390px instead of showing
   the inline list.
2. The Yoni WhatsApp screenshot stacking full-width above the text at 390px instead of floating
   with wrap.

I didn't find anything in the CSS naming these as a deliberate breakpoint decision, but I also
didn't find anything calling them a bug — the mandate describes the desktop behavior in detail
and doesn't state a mobile-specific expectation either way, and floats commonly get dropped
below a width where there is no room left for text to wrap beside them. Flagging both as real
and worth a call, not asserting either way.

**Two of Composer's four flagged issues do not hold up:**

1. **`/contact/` "FAB not visible" — not a defect. Pre-existing, deliberate, dated 2026-09-12.**
   Composer accurately saw no floating WhatsApp icon on `contact-1440.png` / `contact-390.png` —
   I confirmed the same directly (the raw page source has zero `ea-whatsapp-float` occurrences
   at all, not a CSS-hidden element, genuinely absent from the markup). But this isn't new and
   isn't a bug: `inc/wave2-stage-b.php:391-393` has an explicit `is_page('contact')` early return
   with a comment dated **12.9.2026**, quoting Eyal directly — «יש בדף שני כפתורים המובילים
   לוואצאפ. צריך למחוק כפתור אחד.» ("there are two buttons on the page leading to WhatsApp, one
   needs to be removed") — the float was deliberately suppressed on this one page specifically
   because it already carries an in-content WhatsApp CTA and a form. Nothing from tonight touched
   this. I'm flagging it only so it doesn't get chased as a new regression.

2. **`/en/` mobile "FAB has a text label" — misattributed to the wrong element.** I checked the
   live DOM at 390px directly rather than trust the screenshot alone, because the claim
   contradicted the CSS. Result: the real `.ea-whatsapp-float` on `/en/` is `position:fixed`,
   `54×54px`, bottom-left, exactly like every other page, and its label span computes
   `display:none` — genuinely icon-only, correct. The visible "Talk on WhatsApp" text Composer
   saw belongs to a completely different element: an ordinary in-page CTA button
   (`tpl-chapters-en.php:104`, class `btn btn--terra`) sitting in normal document flow ~3,470px
   down the page, in the "Get in touch" section — not fixed, not floating, not the component this
   check is about. English copy on an English in-content button is correct as-is. No fix needed
   here.

## Files

All under `tmp/qa/m11-composer-shots/` — full per-file tables with verdicts are in
`tmp/qa/xval-2026-09-18/M11A.out.md` and `M11B.out.md` (both committed). Open any file directly:

```
open /Users/nimrod/Documents/AOS_V5/EyalAmit.co.il-2026/tmp/qa/m11-composer-shots/<filename>
```

## Status

No fix, no deploy — as instructed. Two real open questions (TOC pill / Yoni stack at 390) and
one already-corrected overstatement (the "orphan") stand from Line A+B combined; the other two
of Composer's four flagged issues are resolved above and should not go into the mobile-phase
queue as regressions. Available and waiting for the next mandate.
