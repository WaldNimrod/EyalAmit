---
id: REQA-SIGNAL-W2-02-2026-05-28
from_team: team_100 (Chief System Architect)
to_team: team_50 (QA / L-GATE_BUILD)
wp: WP-W2-02 — Core Content
date: 2026-05-28
gate: L-GATE_BUILD (re-run)
fix_commit: ebb6101
status: RE-QA REQUESTED
---

# RE-QA SIGNAL — WP-W2-02 L-GATE_BUILD (AC-05 remediated)

Your FAIL verdict (`QA-VERDICT-WP-W2-02-L-GATE-BUILD-2026-05-28.md`, AC-05 P1) has been remediated (commit `ebb6101`) and redeployed to staging.

## What was fixed (root cause)
`ea_wave2_dequeue_unused_styles()` was inert because `ea_wave2_is_active_view()` used `is_page_template()`, but W2-02 force-routes templates via `template_include` without assigning page-template meta. Fix:
1. `wave2-w2-02.php` — set `ea_wave2_shell` query var on force-routed Wave2 pages (makes `is_active_view()` true).
2. `wave2-stage-b.php` — detect contact via `is_page('contact')`; add CF7's native `wpcf7_load_css`/`wpcf7_load_js` filters to block CF7 assets off the contact page.

## team_100 live verification (re-verify independently)
- `/method/`: CF7 asset tags = **0**, `wp-block-library` = **0** ✓
- `/contact/`: CF7 asset tags = **5** (kept) ✓
- (Note: no page currently renders a CF7 form — form ID is an Eyal-dependent open item; CF7 assets load only on `/contact/`.)

## Request
Re-run L-GATE_BUILD on WP-W2-02. AC-05 should now PASS; confirm the other 9 ACs are unaffected. P3 items (hero H1 separator, FAQ count 50 vs mandate's stale "42", intermittent staging timeouts) remain non-blocking.

Engine constraint unchanged: non-Claude only (builder = claude-sonnet-4-6).
Deliverable: updated verdict file → `_COMMUNICATION/team_50/`.

*team_100 — 2026-05-28*
