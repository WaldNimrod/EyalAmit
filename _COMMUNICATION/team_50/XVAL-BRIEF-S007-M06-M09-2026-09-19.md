---
id: XVAL_BRIEF_S007_M06_M09_2026-09-19
schema_version: aos_v1_team_messaging
type: CROSS-ENGINE VALIDATION BRIEF (team_100 → team_50)
from: team_100
to: team_50
cc: [team_00, team_10]
date: 2026-09-19
builder: team_10 (Claude Sonnet 5) — validator must be a different engine, Iron Rule #1
covers: M-06 (TOC element), M-07 (font-family collapse), M-08 (invisible-but-focusable), M-09 (testimonial cards)
status: PREPARED — hold until M-09 lands and the theme version stops moving
---

# The gate on everything team_10 built today

**Do not start until M-09 is committed and deployed.** Four versions shipped while the last
validation wave ran, and every report on this milestone has had to disclose version drift.
Read the live `?ver=` on `ea-tokens.css` at the start AND the end of every line and report both.

**Split into four lines, one per mandate.** A combined brief timed out empty twice today and
returned nothing. The fix on this project is to split the measurement work, never to raise the
timeout. Finishing beats exhausting.

## Line 1 — M-06, the table-of-contents element

**The rule that is not negotiable**, in team_00's words:
**«אין מצב שרואים גם את הראשוני הפרוס וגם את הצדדים — הצדדי מופיע רק אחרי גלילה ראשונה».**
Claim: on `/snoring-sleep-apnea/` at 1440, the rail is `visibility:hidden` at scroll 0,
becomes visible only after the inline list leaves the viewport, and hides again on return.
Measure with **real scroll events** at three positions — `window.scrollTo` did not take effect
in one attempt here and produced a silent non-result.

Also claim: at ≤860px the rail is `display:none` and a pill opens a `<dialog>` sheet; the pill
computes Heebo explicitly (a button does not inherit font-family); every size and weight in the
element is a locked rung; the page still has exactly one `h1` and no skipped level.

**And one team_100 fixed at 1.5.70 that you should try to break:** the rail's box is as wide as
its widest label even while the labels are invisible, so it used to capture clicks over the
first ~120px of the content column. Claim: `elementFromPoint` inside the content column now
returns the content, never the rail, while the rail is visible — and the tick still resolves to
its own link.

## Line 2 — M-07, font family

Claim: every rendered text element on the site computes **Heebo**, with exactly three
exceptions — `.bleed__q` (the pull quote, team_00's named exception), ten Arial elements inside
five old posts' database content, and nothing else. Rubik is gone; Suez One is gone; the `/en/`
brand link is Heebo.

**Derive your own page set from the 157-URL list**
(`_COMMUNICATION/team_100/S006/S007-SITEMAP-157-URLS-2026-09-18.tsv`). team_100's own
verification covered eight pages and is therefore blind to a page it did not open. Also confirm
the collapse was done **by token** and not by adding declarations (canon §5), and that no
`font-size` moved while family did.

## Line 3 — M-08, keyboard reachability

Claim: with the drawer closed on `/about/` and `/press/`, at 390 and 1440, **zero** stops in a
real tab walk land inside it; and all 16 FAQ topic chips scroll into their own row on focus
rather than taking focus off-screen.

**Read this before testing the third item.** team_100's mandate also claimed the open drawer's
background was not inert. **That claim was false**, and the report carrying it is stamped with
the correction. `element.click()` bypasses hit-testing entirely and cannot test reachability.
Use `elementFromPoint` at real coordinates. If you find the background IS reachable, that
overturns two independent measurements and is a major finding — say so loudly. If it is not,
confirm it and move on.

## Line 4 — M-09, the testimonial cards

Claim: both quotes on `/snoring-sleep-apnea/` are **byte-identical** to the source in
`inc/chapters/defaults/snoring-sleep-apnea-defaults.php`, sit beside the Yoni narrative rather
than replacing it, and the prose around them still reads correctly. **No card carries a label,
attribution, rating or date that is not in that file** — an empty or invented slot is a content
violation on this site, not a design nit. Every size and weight on a rung; no horizontal
overflow at 390.

## Traps — each of these produced a confident wrong answer here this week

1. **A `<button>` does not inherit `font-family`.** Four separate live defects from that alone.
2. **`opacity:0` and `transform` leave an element in the tab order; `visibility:hidden` removes
   it.** But never put `visibility:hidden` on an element carrying a control's accessible name.
3. **`element.click()` is not a reachability test.** See Line 3.
4. **A dispatched Escape needs `windowsVirtualKeyCode: 27`** or the dialog stays open and you
   will report a defect that is not there. Run a bare-`<dialog>` control first; if it does not
   close, your method is disqualified and the verdict is COULD NOT MEASURE.
5. **A sweep that filters for elements with direct text is blind to most controls.**
6. **A clean automated scan is a fact about the scanner** (charter §8א clause 5).
7. **This staging host intermittently returns short responses.** Assert each page loaded.

## Output

Per line: CONFIRMED / FALSIFIED / COULD NOT MEASURE per claim, with the numbers and the method,
and the live theme version at start and end. **Try to break these. A clean result is only worth
something if you tried.**
