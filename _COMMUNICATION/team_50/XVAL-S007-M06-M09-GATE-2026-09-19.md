---
id: XVAL_S007_M06_M09_GATE_2026-09-19
schema_version: aos_v1_team_messaging
type: CROSS-ENGINE VALIDATION RESULT (team_50 → team_100)
from: team_50 (four lines, cursor-grok-4.6-high)
to: team_100
cc: [team_00, team_10]
date: 2026-09-19
builder: team_10 + team_100 (Claude) — validator Grok, Iron Rule #1 at engine level
theme_under_test: 1.5.76 (start and end on G1–G3; G4 saw the 1.5.77 fix land mid-run and says so)
brief: _COMMUNICATION/team_50/XVAL-BRIEF-S007-M06-M09-2026-09-19.md
prompts: tmp/qa/xval-2026-09-19/G{1,2,3,4}-*.txt
status: 5 claims falsified · 3 fixed at 1.5.77 and verified · 2 open for team_00
---

# The gate on M-06 through M-09

**Falsified and already fixed at 1.5.77** (measured after deploy, not claimed):

1. **The TOC rule broke scrolling UP.** A symmetric .28s transition kept the rail painting
   while the inline list was back on screen — six samples over ~167ms with both navigations
   visible. team_100's own verification scrolled, settled and measured, which is the one shape
   of test that cannot see it. Hiding is now instant; only appearing fades. Re-measured: zero
   samples with both visible.
2. **The current-section label still captured the content column** — 199.8px wide, ~131px over
   the wrap on its own row, the narrow survivor of the 1.5.70 fix. `pointer-events` removed
   from `.on`; it is a marker, the tick is the target. Re-measured: x=121–249 all return the
   content.
3. **The WhatsApp float sat above the drawer scrim and took clicks** while the drawer was open
   — neither behind nor hidden, against team_00's explicit ruling. Now `visibility:hidden`
   under both drawer flags. Re-measured: the coordinate lands on the scrim.

**Confirmed under adversarial re-test:** closed drawer takes zero tab stops at all four
page/width combinations (was 22 of 28 and 10 of 16); all 16 FAQ chips focus inside their row,
nothing off-screen (worst was 69px out); every size and weight in the TOC and the cards is on a
rung; one h1 and no skipped level; zero Rubik and zero Suez One computed on 157 URLs; zero
non-Heebo controls across 36 CDP pages; the `/en/` brand link is Heebo; and **team_100's
correction to its own false inertness finding held** — `main` sits under the scrim, and the
line was explicitly told to shout if it found otherwise.

**Two things that are NOT fixed and are team_00's to decide** — see the report body: the
testimonial cards do not achieve «ליד הסיפור» at either width, and two lead-in sentences in the
Yoni narrative now point at quotes that are no longer there.

**One finding for a later round:** Rubik is still enqueued on 157/157 pages and Suez One on
149/157, with **zero computed consumers** — two font families downloading on every page load
for nothing. Removing them touches the enqueue path site-wide, so it is not a drive-by.

**One correction to our own records:** post «(27) חכמת הפרצוף» has zero Arial. The fifth
Arial-carrying post is «(24) ילד אסור ילד מותר», which `deviations.csv` already had right.

---



# ── Line G1 ──────────────────────────────────────────────

Independent verification only: I’ll measure the TOC on `/snoring-sleep-apnea/` against those four claims, change nothing, and report what the live page actually does. First I’ll lock theme `?ver=` on `ea-tokens.css`, then run the measurements.Start version is `ea-tokens.css?ver=1.5.76`. Next I’ll load the live TOC in a real browser and measure Claims A–D with scroll events and `elementFromPoint`, not CSS source.I’ll measure in Chrome with real wheel events and `elementFromPoint`, and I’ll verify `scrollY` actually moved before treating a reading as a result.Chrome 153 needed a user-data dir and a longer startup; I’ll switch to this project’s chrome-headless-shell 149 and wait until CDP is actually listening.The first Claim D grid sampled a lead column that had already left the viewport, so that zero is not a result. I’ll re-hit-test the in-view wrap under the rail and sample the 0.28s fade on the way back, where both lists can paint at once.Independent verifier, **chrome-headless-shell 149.0.7827.22**, CDP. Nothing in the repo or on the host was changed. Live `ea-tokens.css` was `?ver=1.5.76` at the start of the run and `?ver=1.5.76` at the end (CDP + a separate curl of `/snoring-sleep-apnea/` after the last measurement).

Load was asserted before any number: CDP `outerHTML` **80415–80420** bytes, `.ea-toc`, `.toc-inline`, `#main`/`main`/`.chapters-main`, and a non-empty title. Curl of the same URL was **97621** bytes and contained `ea-toc`. Short responses were not used.

Scroll was **CDP `mouseWheel`**, not a trusted `window.scrollTo`. `window.scrollY` was read after every wheel; it moved (`0 → 1075 → 1179 → 0` in run 1; `0 → 1500 → 1060…` in run 2). Hit tests were **`document.elementFromPoint(x,y)`** at viewport coordinates. The pill was opened with a coordinate `mousePressed`/`mouseReleased`, not `element.click()`.

---

### Claim A — **FALSIFIED**

The three *settled* samples match the narrow wording. The owner’s non-negotiable rule does not.

| position | how | `scrollY` | `.toc-inline` | `.rail` |
|---|---|---|---|---|
| 1 | top, after a verified wheel | **0** | in view (`top` 785 / `bottom` 1099 on return; at start `inView: true`) | `visibility: hidden`, `opacity: 0`, `.is-on` false |
| 2 | wheel until `inline.bottom < 0` | **1179** (run 1) / **1500** (run 2) | fully gone (`bottom` −401 in run 2) | `visibility: visible`, `.is-on` true |
| 3 | wheel back to top | **0** | in view again | `visibility: hidden`, `opacity: 0`, `.is-on` false |

Downward, a leftover **23.75px** of the inline list still in view (`scrollY` 1075) kept the rail `visibility: hidden`. That part of the claim holds.

On the way **back**, the first frame after the inline list re-entered the viewport (`inline.bottom = 38.75px`, `.is-on` already false) the rail was still `visibility: visible` at **`opacity: 1`**. Six consecutive samples over **~167ms** had both painted (`opacity` 1 → 0.75 → 0.37 → 0.17 → 0.073 → 0.029). Only after that did `opacity` drop under 0.02; `visibility: hidden` landed ~100ms later.

That is both lists on screen at once. The 0.28s `opacity`/`visibility` transition on `.rail` is what does it. A 500ms settle after `scrollY === 0` hides this; the owner’s sentence does not.

---

### Claim B — **CONFIRMED** (this page, these widths)

I could not break it.

- **860×900:** `.rail` `display: none` (box 0×0). `.toc-fab` `display: flex`. Coordinate click at **(786, 861)** (fab center) opened a real `<DIALOG>` (`sheet.open === true`). Computed on the button: `font-family: Heebo, -apple-system, Arial, sans-serif`, `font-size: 13.6px` (`--fs-xs`), `font-weight: 500` (`--fw-medium`). Close button also Heebo.
- **375×812:** same: rail `display: none`; fab Heebo / 13.6px / 500; click **(301, 773)** opened `<DIALOG>`.
- **861×900** (one pixel above the query): rail `display: flex`, fab `display: none`. The 860 cutoff is real.

---

### Claim C — **CONFIRMED** (this element, this page)

113 computed `font-size`/`font-weight` reads inside `.ea-toc` (elements + `::before`/`::after` with `content` not `none`). Each size was compared to the **live** token px on that document, not to the canon’s printed table:

`34 / 44.2 / 24.65 / 21.25 / 19.55 / 18.7 / 18.36 / 17 / 15.3 / 13.6 / 12.24 / 11.05`  
weights `100–800` in steps of 100.

Off-rung count: **0** (tolerance 0.08px). Same scan at 860: still 0.

Headings in DOM order: **one `h1`**, then `h2`s, then `h3`s under a later `h2`. No skipped level. This is a scan of this page’s heading tags, not the accessibility tree.

---

### Claim D — **FALSIFIED**

The opacity-0 capture the 1.5.70 comment describes is gone. The claim as written (“inside the content column … **never** the rail”) is not.

First grid was a non-result: it sampled the lead `.intro-body` at `left: 659` which had already left the viewport (`top: −394`). `contentGridCount: 0` from that pass is discarded.

Second grid, rail on (`scrollY` 1500), in-view **content `.wrap`**: `left: 120`, `right: 1320`. Rail box still `16–251` × `289–611` (width **235px**, as wide as the current label).

- **`a:not(.on) span` at `opacity: 0`:** every `elementFromPoint` on the label (left / mid / right) returned the section or wrap, **never** the rail. `invRailHits: []`.
- **Historic x=130/160/200/235 at y=450** (the old trap, not on the current-label row): all `div.wrap`, not the rail.
- **Ticks:** all 11 centers returned `i` (the tick), each still on its own `a[href="#…"]`.
- **Current `a.on span`:** `opacity: 1`, `pointer-events: auto`, box **x=51–251** (`w=199.8`), **`overlapsWrap: true`**. 100/100 points on that box hit `span`. Inside the wrap itself, **x=121, 137, 153, 169, 185, 201, 217, 233, 249 at y=290** all returned the rail `span`. That is **~131px** of the content column (wrap starts at 120; label runs to 251) still taking hits — painted, not invisible, one row (~21px), not the old 322px-tall dead zone.

So: invisible labels no longer steal the column; the **current** label still does, over the wrap, while the rail is visible. Ticks still hit-test as themselves.

---

**Theme version:** start `ea-tokens.css?ver=1.5.76` · end `ea-tokens.css?ver=1.5.76`.The second probe did finish: it printed the Claim D grid and the return-fade samples, then the Node process hung instead of exiting, which is why the task shows as an error. I killed the leftover Chrome and already used that output in the report.

The verdicts stand: **A falsified** (both lists paint on the way back), **B and C confirmed** on this page, **D falsified** (the current label still hit-tests over the content wrap). Theme stayed **1.5.76** start to end.

# ── Line G2 ──────────────────────────────────────────────

I'll falsify the font-family claims independently: first the live `ea-tokens.css` version and the 157-URL sitemap, then computed families across CSS/template families — including buttons and single-glyph text.Theme at start is 1.5.76; Rubik and Suez One stylesheets are still enqueued. Next I’ll derive the 157-URL family map and check computed families, not just loads.There's an existing type-scan harness and an M-07 mandate. I'll use those to pick one URL per CSS/template family, then measure computed families on the live host.M-09 added Yoni quote cards after the family collapse — that page is a likely third exception. I'll diff the collapse commit and scan every CSS/template family, including that page.I'll cluster the 157 URLs by live stylesheet fingerprint, then run a family-only CDP scan that includes buttons, single-glyph text, and `::before`/`::after`.Scan is through 33 of 34 pages at 1.5.76. Rubik is still linked everywhere; I’ll inspect the non-Heebo hits as soon as the last page finishes.Independent verifier (this engine ≠ builder). Nothing in the repo was changed. Live `ea-tokens.css?ver=` was **1.5.76 at start** and **1.5.76 at end**. No version drift in this run.

---

## Method (one campaign)

- Sitemap: 157 URLs from `file:///Users/nimrod/Documents/AOS_V5/EyalAmit.co.il-2026/_COMMUNICATION/team_100/S006/S007-SITEMAP-157-URLS-2026-09-18.tsv`.
- HTTP fingerprint of all **157/157** (byte length + `<title>` + `ea-tokens.css` + a heading/nav). Grouped by stylesheet set + `page-template-*` / `single-post` / `blog`. **15 CSS+template families**, not 16.
- Computed family at 1440×900 on **36 pages**, **36/36 loaded** (HTML length ≥ 4 000, title, known heading/nav/`ea-tokens.css`). Every family had at least one page. Traps applied: direct text **length > 0** (including one glyph), **zero-size rect excluded**, **every `button/input/select/textarea`**, `::before`/`::after` with `content`.
- Plus an HTML inline-`font-family` pass over all **54 posts**.
- Source: live CSS + git commit `d99cad8` (M-07, 1.5.73) and original `.bleed__q` in `69347c2`.

Elements inspected on the 36 CDP pages: **4 545** visible text/control/pseudo samples. First computed family **Heebo: 4 360** on the first 34 pages; remaining pages the same pattern.

---

## Verdicts

| Claim | Verdict |
|---|---|
| Every rendered text node computes Heebo, except `.bleed__q` and the Arial paste residue | **CONFIRMED** on 36/157 pages covering **15/15 families**. **0 computed items outside those two buckets.** |
| `.bleed__q` is the owner’s serif exception, weight 400, **not italic** | **CONFIRMED** (3 live nodes). Matches `69347c2`: `font-family:var(--serif);font-weight:400;` and no `font-style`. |
| Ten `Arial`/`arial` nodes in five old posts, DB paste, not a stylesheet | **CONFIRMED as a count (10 computed nodes / 5 posts).** The **prose list of which five is wrong** (see below). |
| Rubik is gone | **FALSIFIED** as a live asset. **CONFIRMED** as a computed family (0 Rubik nodes). |
| Suez One is gone | **FALSIFIED** as a live asset. **CONFIRMED** as a computed family (0 Suez One nodes). |
| `/en/` brand link is Heebo | **CONFIRMED** |
| Collapse was done by changing **tokens**, not component declarations (canon §5) | **FALSIFIED** |
| No `font-size` moved while family did | **CONFIRMED** for the family-collapse commit |

---

## Complete list outside the two named exceptions

**Empty.** No third computed family on the 36 pages.

Not in that list, because they **are** the two named exceptions:

**A. `.bleed__q` — Frank Ruhl Libre, 400, `normal`, 34px (`--fs-display`)**

| Page | Text |
|---|---|
| `http://eyalamit-co-il-2026.s887.upress.link/` | נשימה היא הבסיס להכל |
| `http://eyalamit-co-il-2026.s887.upress.link/treatment/` | הנשימה היא לכולם. |
| `http://eyalamit-co-il-2026.s887.upress.link/eyal-amit/mokesh-dahiman/` | shanti play mantra inside – shanti coming home |

**B. Computed Arial/arial — 10 nodes, 5 posts, all inline/Word-paste (`MsoNormal` / `font-family` in `style=`). Child theme sheets do not set Arial on these.**

| n | Post | Notes |
|--:|---|---|
| 5 | `http://eyalamit-co-il-2026.s887.upress.link/40-הטור-של-אייל-עמית-פרסומת-אחת-וחזרנו/` | Word `MsoNormal` spans |
| 2 | `http://eyalamit-co-il-2026.s887.upress.link/29-הטור-של-אייל-עמית-רייב-שבוע-הספר/` | nested spans; includes the single glyph `\|` |
| 1 | `http://eyalamit-co-il-2026.s887.upress.link/41-הטור-של-אייל-עמית-חארטה-בארטה/` | one visible span; HTML has 23 Arial wrappers |
| 1 | `http://eyalamit-co-il-2026.s887.upress.link/הטור-של-אייל-עמית-24-ילד-אסור-ילד-מותר/` | single glyph `.` — a length>1 filter would miss it |
| 1 | 2012 studio post (`…/דיגרידו-פרדס-חנה-סטודיו-לבנייה-ונגינ/`) | lowercase `arial` on an `<em>` span |

The mandate/prose named **(27) חכמת הפרצוף** as one of the five. That post has **zero** inline Arial and **zero** computed Arial. The fifth computed post is **(24) ילד אסור ילד מותר**, which is what their own `deviations.csv` already had.

**HTML-only, not a rendered-text hit:** post **(36) שיטת השקשוקה** has 3 inline Arial tags and **0** computed Arial (no direct text / zero box). Not a third family.

**`/en/` brand:** `a.ea-en-head__b` → `Heebo, -apple-system, Arial, sans-serif`, weight 300, style `normal`, text “Eyal Amit”.

**Controls:** 0 non-Heebo `button/input/select/textarea` on the 36 pages. Contact (CF7 + Fluent Forms) included.

**Yoni quote cards** on `/snoring-sleep-apnea/` (added at 1.5.76): family **Heebo**. Out of family scope; they compute `italic` because they are `<blockquote>`s.

---

## Rubik / Suez One — still on the wire

Computed family is not Rubik or Suez One. The **files are still requested**:

- **Rubik** Google Fonts: `id='ea-eyalamit-fonts-rubik-css'` on **157/157** HTTP pages. Source: `file:///Users/nimrod/Documents/AOS_V5/EyalAmit.co.il-2026/site/wp-content/themes/ea-eyalamit/functions.php` (still enqueued, still a dependency of the unused fallback sheet). Owner: **child theme**, not a third party.
- **Suez One**: bundled in `ea-chapters-fonts` (`family=Heebo+…+Frank+Ruhl+Libre+Suez+One`) on **149/157** pages (every Chapters view). Absent on the six GP-shell pages (`/services/`, `/shows-heritage/`, `/historical-articles/`, `/thank-you/`, `/courses-soon/`, `/learning/courses-external/`). Owner: `file:///Users/nimrod/Documents/AOS_V5/EyalAmit.co.il-2026/site/wp-content/themes/ea-eyalamit/inc/chapters/chapters-enqueue.php`.
- Live `--display` **is** aliased to Heebo (`:root{--display:var(--bf)}`). The `@font-face` request is leftover, not a computed consumer.
- `theme-shell-fallback.css` still contains `font-family: "Rubik"`. **Not enqueued** on this host (GeneratePress is present). Dead on staging; would fire if the parent sheet disappeared.
- Third-party sheets that *are* live (`wpa-style.css`, Fluent Forms, CF7): no computed family deviation traced to them on these pages.

---

## “Changed tokens, not component rules” — FALSIFIED

M-07 commit `d99cad8` (1.5.73) did **two** token edits **and** edited component/template declarations:

**Tokens (real):**
- `chapters.css` `--display: 'Suez One','Frank Ruhl Libre',serif` → `var(--bf)`
- `style.css` `body.ea-m4-polish { --ea-font-sans: "Rubik", sans-serif }` → `var(--ea-font)`

**Component/template `font-family` values changed in place (canon §5 violation):**
- `.tl__y`, `.st3::after`, `.shstep__dot span`, `.bookcard__cover .ph`, `.bookcard__t`: `var(--serif)` → `var(--bf)`
- `tpl-chapters-en.php` `.ea-en-head__b`: `var(--serif,'Frank Ruhl Libre',serif)` → `var(--bf,'Heebo',sans-serif)`

Those six `font-family` declarations were not left pointing at a retargeted token; the selector rules themselves were rewritten. `.bleed__q` was left on `var(--serif)`.

**`font-size` on those same rules did not move** (still `--fs-h2` / `--fs-display` / `--fs-sm` / `--fs-h4` / `1.2rem` on the EN brand). Searched live `ea-tokens.css` for `font-size`, `font:`, and `font-family`: the file holds **13** `font-size`, **9** composite `font:` shorthands, **2** `font-family` floors (`html body`, `html button,input,select,textarea`). No size tokens were rewritten in M-07.

Live computed check of the collapsed consumers that actually render: `.bookcard__t` and `.tl__y` are Heebo.

---

## Coverage

| | n |
|---|--:|
| URLs considered | 157 |
| HTTP loaded for family map | 157 |
| Distinct CSS+template families | **15** |
| Families with a CDP page | **15/15** |
| CDP pages (load-asserted) | **36** |
| Theme `ea-tokens.css?ver=` start | **1.5.76** |
| Theme `ea-tokens.css?ver=` end | **1.5.76** |

The 121 URLs not opened in Chrome sit inside a family that was opened. All 54 posts were HTML-grepped for inline Arial; every post that had it was then CDP’d.

This is not a 157-URL computed sweep. A unique paste on an unscanned **page** (not post) remains possible; none showed up in the family representatives, including the six GP-shell pages that used to be Rubik.Those background jobs already fed the report you have.

The 157-URL cluster and the 34-page computed scan both finished clean. The later all-URL Arial HTML sweep died on a 502; the 54-post retry after that is what established the 10 computed Arial nodes on five posts (and that (27) is not one of them).

Nothing new to add to the verdicts.

# ── Line G3 ──────────────────────────────────────────────

I'll treat this as a falsification pass only: measure `/about/`, `/press/`, and `/faq/` with real tab events, and I will not change anything. First I am locking the live theme/`ea-tokens.css` version and confirming the pages actually loaded.Health is online. Next I am capturing the live `ea-tokens.css` version and how the drawer actually hides, before any tab walk.The first fetch died on a broken PATH, not the site. I’m restoring a usable environment and reading the existing tab-walk method so the new run uses real key events.I have the prior method and the live CSS rules. Next I’ll write a throwaway CDP probe that uses real Tab keys and `elementFromPoint`, then run it against the three pages only.I have the live CSS and the prior trap (`element.click()`). Writing a one-shot CDP probe that sends real Tab keys and samples `elementFromPoint` at actual coordinates.Claim A and B walks finished; Claim C never opened the drawer. I’m reading the burger hit-test and the chip rectangles before I trust those zeros.Claim C never opened: the selector hit a 0×0 GeneratePress `menu-toggle`, not the live 44×44 `.ea-mnav-burger`. I’m re-running only the open-drawer hit-test with a real click on that control.Independent verifier. Nothing was changed. I tried to break all three claims with real Tab keys and `elementFromPoint`. I did not find a closed-drawer Tab stop, an off-screen FAQ chip, or a path through the open scrim into `main`.

**Theme `ea-tokens.css?ver=`:** start **1.5.76** · end **1.5.76** (no drift). Dispatch said 1.5.76; that is what was live.

Pages asserted before any number was used:

| Page | HTTP | Bytes (HTTP / DOM) | Known element |
|---|---|---|---|
| `/about/` | 200 | 73 439 / 71 887–71 892 | `#ea-mnav-drawer` + `main` |
| `/press/` | 200 | 99 514 / 96 874–96 877 | `#ea-mnav-drawer` + `main` |
| `/faq/` | 200 | 162 807 / 159 029 | 16 × `.ea-faq-toc__link` + `main` |

**Method:** Chrome (`chrome-headless-shell`), CDP. Tab = `Input.dispatchKeyEvent` `rawKeyDown`+`keyUp`, `key:Tab`, `windowsVirtualKeyCode:9`. No `element.focus()` loop. 480 ms settle after each Tab, then `activeElement` + `getBoundingClientRect`. Claim C open = real mouse down/up at the burger centre after `elementFromPoint` confirmed `.ea-mnav-burger` (44×44 at x=24,y=6). Enter-on-burger did **not** set `ea-mnav-open` (burger was focused; drawer stayed `visibility:hidden` at x=−335.4) — that open path is discarded, not treated as a product finding on this line.

Raw: [file:///tmp/ea_m08_kb_probe/result.json](file:///tmp/ea_m08_kb_probe/result.json) · [file:///tmp/ea_m08_kb_probe/claimc.json](file:///tmp/ea_m08_kb_probe/claimc.json)

---

### Claim A — closed drawer, zero Tab stops — **not falsified**

| Walk | Stops | Cycled | Inside `#ea-mnav-drawer` |
|---|---|---|---|
| `/about/` 390×844 | 39 | yes | **0** (was 22 of 28) |
| `/press/` 390×844 | 71 | yes | **0** (was 10 of 16) |
| `/about/` 1440×900 | 59 | yes | **0** |
| `/press/` 1440×900 | 80 | **no — hit cap** | **0** of the 80 walked |

Closed computed state at 390, both pages, before the walk: `display:flex`, **`visibility:hidden`**, `opacity:1`, `transform:matrix(1,0,0,1,-335.391,0)`, `inert` absent, `aria-hidden` null, rect `{x:−335.39, y:0, w:335.39, h:844}`, 35 controls still in the node. At 1440 the same node is `display:none` (0×0). The transform is still there; Tab does not enter it.

First six stops at 390, both pages: skip → GP leftover skip → brand → burger → sound → EN, then page content. No `.ea-mnav-link` / `.ea-mnav-acc__btn` / drawer brand. Offending-stop rectangles: **none**.

`/press/` @1440 did not finish the document. Remaining unwalked stops cannot be in a `display:none` drawer; I am not claiming the rest of that page’s tab order.

---

### Claim B — 16 FAQ chips on `/faq/` @390 — **not falsified**

Real Tab stops **6–21** are the 16 `.ea-faq-toc__link` nodes (inventory matches the HTML, including the live label `וכתבת`). After each focus:

| Chip (was worst) | After-focus `left` | In viewport | In `.ea-faq-toc` row |
|---|---|---|---|
| סאונד הילינג בדיג'רידו (was **−69**) | **0.97** | full | yes |
| סטנדים לאחסון דיג'רידו (was −32.7) | 1.28 | full | yes |
| תיקון וחידוש דיג'רידו (was −26.6) | 1.38 | full | yes |
| כושי בלאנטיס (was −60.9) | 1.09 | full | yes |
| סדנאות דיג'רידו (was −64.7) | 1.33 | full | yes |
| other 11 | all `left ≥ 0`, `right ≤ 390` | full | yes |

**0 / 16** off-screen. Max px outside left: **0** (was 69). No offending rectangle.

Walk on `/faq/` stopped at 40 after the chips (cap). I did not walk the summaries.

---

### Claim C — open-drawer background reachable? — **did not overturn the correction**

I did **not** find `main` (or any `main` descendant) as the top hit. That is the loud finding this line was asked to shout if it appeared. It did not.

Opened with a real coordinate click on `/about/` and `/press/` @390. `html.ea-mnav-open` true. Drawer `visibility:visible`, rect `{x:0, w:335.39}`. Scrim `visibility:visible`, `opacity:1`, `pointer-events:auto`, `z-index:40`, 390×844. **`html` and `body` both compute `overflow:hidden` / `overflow-y:hidden`.**

`elementFromPoint` (click-open, both pages identical pattern):

| Coord | Hit | `inMain` |
|---|---|---|
| (168, 422) drawer-centre | `BUTTON.ea-mnav-acc__btn` | no |
| (363, 422) uncovered mid | `DIV.ea-mnav-scrim` | no |
| (363, 80) uncovered top | `DIV.ea-mnav-scrim` | no |
| (382, 295) far-right | `DIV.ea-mnav-scrim` | no |
| (8, 8) top-left | `DIV.ea-mnav-drawer__head` | no |
| **(363, 780) uncovered bottom** | **`A.ea-whatsapp-float`** «שלח הודעה» | **no** |

`elementsFromPoint` at the uncovered strip: top = scrim, then a `main` descendant, then `MAIN#main`. Main is under the scrim, not on top. A programmatic `main.click()` would still fire. Hit-testing does not reach it.

The WhatsApp float is above the scrim at that one coordinate. That is not the page background and does not meet the “background is reachable” overturn. It is a control outside the drawer that `elementFromPoint` can still land on while the drawer is open. Previous “only drawer links and the scrim” statements missed it.

---

### Hidden-but-reachable (these walks only)

No focused stop had own or ancestor `opacity:0` with `visibility` still visible. No focused stop had `visibility:hidden`. No `aria-labelledby` target was `visibility:hidden`. The GP `.screen-reader-text.skip-link` paints a real box when focused (`{x:4,y:4,w:110.6,h:50}`).

Not walked: `/faq/` past stop 40; `/press/` @1440 past stop 80.

**Theme at start: 1.5.76. Theme at end: 1.5.76.**

# ── Line G4 ──────────────────────────────────────────────

I'll treat this as a falsification pass only: measure the live theme `?ver=` first, then check the two cards against the PHP source and the live page — no edits.Source quotes and Yoni prose are in the PHP file. Next I’ll capture live `ea-tokens.css?ver=` and inspect the two cards on the staging page.Start `?ver=` is `1.5.76`. Page loaded (97621 bytes, Yoni + cards present). Next I’ll extract the live card strings and measure layout at 390.Repo `style.css` is already `1.5.77`; live start is still `1.5.76`. Next I’ll byte-diff the card strings and measure layout in the browser at 390.Original sequence is confirmed: two quotes sat after those two lead-ins. Next I’ll measure live layout, computed type, and overflow at 390 via CDP.Writing a CDP probe that asserts a full page load, then measures card geometry, type, and overflow at 390.Hero screenshots miss the cards. I’ll scroll that block into view and capture placement at 390 and 1440.Independent verifier. Nothing changed. Live host moved under this run.

**Theme `ea-tokens.css?ver=`**  
- Start: **1.5.76** (HTTP 200, 97621 bytes, `הסיפור של יוני` + `.ea-testi-cards` present)  
- End: **1.5.77** (HTTP 200, 97621 bytes, same two markers, still 2 cards)  
CDP in the middle also read `1.5.77`. Quotes did not change across the bump.

---

## Claim A — content integrity — **not falsified**

Attempted byte-diff of live card `innerHTML` against the `quotes` entries in `file:///Users/nimrod/Documents/AOS_V5/EyalAmit.co.il-2026/site/wp-content/themes/ea-eyalamit/inc/chapters/defaults/snoring-sleep-apnea-defaults.php`. Both match. Two cards, no empty slot. `::before`/`::after` `content: none`. No `h*`, `cite`, `footer`, `figcaption`, name, rating, or extra label in the card DOM. `innerText` has no added wording.

| | Source string | Live card `innerHTML` | `==` |
|---|---|---|---|
| 1 | `<strong>"בדיקת השינה לא הדגימה תסמונת הפסקות נשימה בשינה."</strong>` | `<strong>"בדיקת השינה לא הדגימה תסמונת הפסקות נשימה בשינה."</strong>` | yes |
| 2 | `<strong>"ב-2014 היו לי 250 הפסקות נשימה בלילה."</strong>` | `<strong>"ב-2014 היו לי 250 הפסקות נשימה בלילה."</strong>` | yes |

Template wrap is `<blockquote class="ea-testi-cards__card">` only. `2014` in card 2 is inside that source string, not an added date field. Clip at 390 shows those two strings and no other card copy.

---

## Claim B — placement — **falsified** (as stated)

The Yoni block is still there (`#הסיפור-של-יוני`, heading `הסיפור של יוני`, body still starts `יוני, שם בדוי…`). Cards do not replace it.

**«Beside» is false at 390.** Cards stack under the whole Yoni split, then a CTA. Wraps: Yoni bottom 14933.5 / cards top 14995.5; `yOverlap -62`; `verticallyStacked true`. Card boxes `left 48 / right 342` on a 390 viewport — same column, not a side column.

**«Beside» at 1440 is only section-to-section, not quote-to-lead-in.** Cards wrap `161–621`, Yoni wrap `659–1279`, gap 38px, `yOverlap 1959`. Cards sit at y≈9237; `מיד אחר כך הוא כתב:` is at y≈10272 in the other column (~1kb lower). `display:contents` zeroes both `<section>` boxes.

**«Narrative still surrounds them» is false at both widths.** Quotes were pulled out of the prose. Cards are a sibling after the entire split. At 390 a CTA follows the cards before the next chapter. The story does not wrap the cards.

---

## Claim C — the seam — **builder flag is true on the live page**

Live sequence in `#הסיפור-של-יוני` (asserted loaded; 25 `<p>`s):

1. `וכשהוא קיבל את התוצאות, הוא שלח לי הודעה.`  
2. `מיד אחר כך הוא כתב:`  
3. `אני זוכר את ההתרגשות.`

Eyal’s source file (`file:///Users/nimrod/Documents/AOS_V5/EyalAmit.co.il-2026/docs/project/eyal-ceo-submissions-and-responses/from-eyal/2026-07-12--snoring-sleep-apnea-didgeridoo-CHECKED.md`) had quote 1 between 1 and 2, and quote 2 between 2 and 3. Both quotes are gone from that flow.

**Full list of dangling lead-ins in that narrative (only these two):**

1. **`מיד אחר כך הוא כתב:`** — colon, then Eyal’s next sentence, not the quote.  
2. **`וכשהוא קיבל את התוצאות, הוא שלח לי הודעה.`** — that sentence used to be followed by the message; it now runs into the second lead-in.

No other Yoni sentence now opens a missing quote. These two still have their objects in the prose: `2014: כ-250 הפסקות נשימה בלילה` and `לאחר התהליך: "בדיקת השינה לא הדגימה תסמונת הפסקות נשימה בשינה"` (no period before the closing quote in that line).

---

## Claim D — mechanics — **falsified on overflow; other conjuncts not falsified**

**Rungs/weights in the cards (not falsified).** html `font-size` 16px. Card: `15.3px` = `--fs-sm` `0.95625rem`, weight `300` = `--fw-light`. `strong`: `15.3px`, weight `500` = `--fw-medium`. Same at 390 and 1440.

**Headings (not falsified).** One `h1`. Levels `[1,2,2,2,2,3,3,3,2,2,2,2,2,2,2,2,2,2]`. No skip. None of those headings is in the cards.

**Overflow at 390 (falsified as a page claim; not in the cards).**  
`document.scrollWidth === clientWidth === 390` — the 0px metric the brief said not to trust. Element rects: **72 nodes outside the 390 box**. Cards: none (`right 342`). Yoni: none. Outside: closed `.nav__l` at `left 390 / right 780` (drawer sitting a full viewport to the right) plus `.arcs` at `left -220`. That is the controls-outside-the-viewport pattern.

---

No wording offered. No files written in the repo.