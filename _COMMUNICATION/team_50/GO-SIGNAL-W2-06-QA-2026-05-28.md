---
id: GO-SIGNAL-W2-06-QA-2026-05-28
from_team: team_100 (Chief System Architect)
to_team: team_50 (QA / L-GATE_BUILD)
wp: WP-W2-06 — Blog Migration
date: 2026-05-28
gate: L-GATE_BUILD
staging: http://eyalamit-co-il-2026.s887.upress.link
status: GO — W2-06 staging is ready for QA
---

# GO-SIGNAL — WP-W2-06 L-GATE_BUILD QA may proceed

The two manual gating steps are complete (executed by team_100 via REST/FTP — no wp-admin). W2-06 is ready for your L-GATE_BUILD QA per `MANDATE-TEAM50-WAVE2-L-GATE-BUILD-2026-05-28.md` §3.

## What team_100 completed
1. **Post import** — 54 posts, 6 categories, 47 tags imported via REST (Application Password). 0 failures. Author mapped to `eyaladmin`. Default "hello-world" post set to draft.
2. **Media localization (per team_00 directive)** — ALL embedded media downloaded and rehosted on the new site under `wp-content/uploads/` with **root-relative** links. 158/164 migrated; 46 posts/pages rewritten. The **6 remaining are dead at source** (already broken pre-migration) and are NON-BLOCKING:
   - `blog.muzza.co.il` (DNS dead) ×2
   - `www.namaste.co.il` (404) ×1
   - `gallery.mailchimp.com` (403) ×2
   - `www.eyalamit.co.il/.../צוותא-אייל-עמית.jpg` (404 on legacy) ×1
3. **301 redirects** — corrected and deployed to live `.htaccess` (backup taken). Legacy `/Blog/<slug>/` → **301 → `/<slug>/`** plus 26 page redirects.

## ⚠️ AUTHORITATIVE AC CORRECTION (overrides mandate §3 wording)
The documented permalink standard is **`/%postname%/`** (root) — "M2 standard", confirmed in `M2-G1-G2-CLOSEOUT-DECISIONS-2026-03-31.md`, `M2-IMPLEMENTATION-SUMMARY-2026-04-01.md`, and Eyal's dev spec ("permalink structure נקייה"). The legacy site used `/Blog/` (capital B). Therefore:

| AC | CORRECTED expectation |
|----|-----------------------|
| AC-01 | 54 posts return 200 at **root `/<slug>/`** (NOT `/blog/<slug>/`). Sample ≥10. |
| AC-05 | `curl -I /Blog/<slug>/` → **301 → `/<slug>/`** (root). Sample ≥5. |
| AC-03 | post images load from **new site** (`/wp-content/uploads/...`, relative). The 6 dead-source refs above are EXCLUDED. |

Other ACs (AC-02 meta, AC-04 archive pagination/filter, AC-06 validate_aos 0 FAIL, AC-07 no shortcode artifacts, AC-08 style.css version) unchanged.

`/blog/` remains the **archive** page (200). Posts themselves live at root by design.

## team_100 pre-verification (for your reference; re-verify independently)
- 5/5 sampled posts: root 200, `/Blog/` → 301 → root ✓
- archive `/blog/` 200 ✓
- migrated images 200 on new site ✓
- 54 published posts / 6 cats / 47 tags ✓

## Engine constraint (IR#1) — unchanged
Builder = claude-sonnet-4-6 → you MUST run on a non-Claude engine. Final L-GATE_VALIDATE = team_190 (Codex).

Deliverable: `_COMMUNICATION/team_50/QA-VERDICT-WP-W2-06-L-GATE-BUILD-2026-05-28.md`.

*team_100 — 2026-05-28*
