Cross-engine independent verification of today's accessibility fixes. Six focused lines, each one topic, run on `cursor-grok-4.6-high` via `scripts/run_cross_engine_validator.sh`, `AOS_VALIDATOR_TIMEOUT=900`, strictly serial with a pause between runs. Builder engine (Sonnet, team_10/team_100) ≠ validator engine (Grok) — Iron Rule #1 held at engine level for this wave, not just role level. Reference line XVAL-A (alt text + path-fix verification, run earlier today, PASSED) is folded in below since it is part of the same wave and C3 cites it.

None of these lines were given this project's conclusions — only scope, method constraints, and a set of traps that have already produced wrong answers on this project. Where a line disagreed with something asserted in the repo, that disagreement is reported below, not reconciled toward either side.

Prompts: `tmp/qa/xval-2026-09-18/{A,B1,B2,B3,C1,C2,C3,C3B}*.txt`
Raw outputs: `_COMMUNICATION/team_50/XVAL-{A-ALT-AND-PATH,B1-SKIPLINK-NAV-CONTRAST,B2-MUTED-TEXT-CONTRAST,B3-TAB-ORDER-FOCUS,C1-STRUCTURE-LANDMARKS,C2-KEYBOARD-MENU-ZOOM,C3-STATEMENT-VS-REALITY}-2026-09-18.md`

## What each line ran

- **A** (reference, ran earlier today): alt text + path-fix verification, 12-page set. 216 `<img>`, 208 real alt, 8 empty (2 decorative in `aria-hidden`, 6 genuinely missing on `/books/vekatavta/`). PASSED.
- **B1**: skip link + primary nav contrast, unfocused/focused, home page only. Real Tab (not simulated focus), painted-pixel background sampling, 800ms settle before reading.
- **B2**: muted/secondary text contrast across `/treatment/` `/lessons/` `/blog/` `/shop/`. The line's own full instance-by-instance write-up (selectors, every testimonial name, every blog date) landed as a second file it chose to create itself, `XVAL-B2-MUTED-CONTRAST-2026-09-18.md`; the filed `XVAL-B2-MUTED-TEXT-CONTRAST-2026-09-18.md` is its condensed stdout pointing at it. Both are consistent; neither contradicts the other.
- **B3**: real tab order + focus-indicator visibility, 40 stops each on `/` and `/contact/`, desktop 1440×900.
- **C1**: heading/landmark/duplicate-id/lang-dir structure, 12 pages, measurement only — no keyboard, no zoom, by instruction.
- **C2**: keyboard operation + mobile menu + 200% text at 390px, on `/` `/contact/` `/faq/` `/shop/`.
- **C3**: every factual claim in the `/accessibility/` statement, judged against the other six lines' measured reality. **First attempt timed out twice at 900s and failed loud (exit 124, empty stdout both times) — not a silent pass.** Per this project's own diagnosis (a focused line per topic; an oversized mandate returns empty; the fix is to split, not extend the timeout), it was re-split into **C3B**: same independent read of the statement, but citing the other five lines' same-day, same-engine numbers for claims they already covered instead of re-deriving everything from a fresh 12-page crawl. C3B completed well inside the timeout. Both prompts are kept on disk; C3B's output is what is filed as the C3 result below.

## Confirmed defects (new findings from this wave)

1. **Keyboard focus indicators fail the 3:1 UI-contrast threshold on several controls**, at the home page's unscrolled rest state: skip-link ring **1.06:1**, primary CTA ring **1.65:1**, brand-link ring **2.54:1**, EN-toggle ring **2.90–3.00:1** (borderline, depending on which side of the transition is sampled). Text colour itself is fine everywhere checked in B1 — only the focus ring is under threshold. — *B1*
2. **Two concrete contrast fails in "muted" text**: the *active* filter tag on `/treatment/` and `/lessons/` (`.dd__item--active .dd__tag`, **4.2573:1**, needs 4.5:1), and the footer brand/address/phone line on all four B2 pages (**4.4867:1**, needs 4.5:1 — a thin fail). Most other muted text (inactive chips, blog dates/categories, testimonial names, footer legal text) passes. — *B2*
3. **Mobile (390px) header Tab order contradicts the visual RTL reading order** — the burger menu button breaks the sequence — on all four C2 pages (`/`, `/contact/`, `/faq/`, `/shop/`). Desktop tab order at 1440×900 does **not** have this problem (B3 found it clean, matching visual RTL, on `/` and `/contact/`). This is a viewport-specific defect that a desktop-only check would miss entirely. — *C2*
4. **Elements receive keyboard focus while off-screen at 390px**: one `#peek` link on the home page (`y=2252` in an 844px-tall viewport) and six FAQ topic chips (up to 87.5px off-screen). Not found on `/contact/` or `/shop/` in the same walk. — *C2*
5. **Document-overflow = 0px is not sufficient evidence of "no clipping" at 200% text/zoom.** On the home page specifically, the skip link overlaps the brand link by ~24px and, under CSS `zoom:2`, the burger and EN controls land 100+px outside the viewport — all while the overflow metric itself reads 0px. — *C2*
6. **The `/accessibility/` statement makes blanket claims that are FALSE**, directly contradicted by findings 1–4: "full keyboard nav… visible focus on every control" and "contrast adapted for text and UI, including keyboard focus." The statement's own limitations section also cites a scanner finding "zero issues" alongside "162 undescribed images" in the same breath — an internal inconsistency, not reproduced by any line today (today's real number is 8, per XVAL-A). The statement's implied "last updated September 2026, after tests and fixes" is undercut by WordPress's own `dateModified`, still **2026-04-07**. — *C3*

## What passed / held up

- Image alt text: 208/216 real; the 8 empty are 2 correctly-decorative + 6 already known and already routed to Eyal as open questions on vekatavta. Not new — matches XVAL-A and team_10's own DONE report. — *A, C3*
- Page structure: one `<h1>` per page, no skipped heading levels, no duplicate ids, no dangling `aria-*` id references, correct `lang`/`dir` (including `/en/`'s `en`/`ltr`), every `<nav>` has an accessible name — zero findings across all 12 pages. — *C1*
- Desktop (1440×900) tab order matches visual RTL order on `/` and `/contact/`, no keyboard traps; the mobile menu itself is keyboard-operable (reachable by Tab, closes on Escape, closed items correctly unreachable at 0 of 28). — *B3, C2*
- Skip-link and primary-CTA **text** contrast: fine (5.67:1, 4.63:1). Language-toggle and brand-link **text**: fine (13–17:1 range). — *B1*

## Disagreements with prior claims, left unreconciled

- C1 falsifies specific claims in team_10's own [04-DONE-HEADING-STRUCTURE.md](file:///Users/nimrod/Documents/AOS_V5/EyalAmit.co.il-2026/_COMMUNICATION/team_10/A11Y-FIX-2026-09-18/04-DONE-HEADING-STRUCTURE.md) and [01-STRUCTURE-SEMANTICS-AUDIT.md](file:///Users/nimrod/Documents/AOS_V5/EyalAmit.co.il-2026/_COMMUNICATION/team_10/A11Y-AUDIT-2026-09-17/01-STRUCTURE-SEMANTICS-AUDIT.md) — that `/shop/`, `/qr/`, `/treatment/` "still serve pre-fix markup" (1/1/12 headings), and that `/treatment/`'s FAQ section has zero H3. Live: 6/49/34 headings and 22 H3 respectively. C1's own read: these are stale, already-superseded snapshots, not a current defect — C1 did not adopt the repo's numbers.
- C1 also does not match [04-LIVE-BROWSER-VERIFICATION.md](file:///Users/nimrod/Documents/AOS_V5/EyalAmit.co.il-2026/_COMMUNICATION/team_10/A11Y-AUDIT-2026-09-17/04-LIVE-BROWSER-VERIFICATION.md)'s home-page count of 11×H2 (accessibility-tree method) against its own DOM-measured 10×H2 — flagged as a different method, not merged toward either number.
- B1 disagrees with [01-DONE-FOCUS-CONTRAST.md](file:///Users/nimrod/Documents/AOS_V5/EyalAmit.co.il-2026/_COMMUNICATION/team_10/A11Y-FIX-2026-09-18/01-DONE-FOCUS-CONTRAST.md)'s reported 13.83:1 / 19.15:1 nav-link focus contrast: that report measured a scrolled, solid-background nav state; B1's unscrolled home-page rest-state measurement is 13.10:1 / 17.38:1 (EN) and 15.19:1 (brand, focused). Both may be correct for their respective scroll states — B1 explicitly declined to adopt the higher scrolled-state numbers for the rest state.
- C3 corrects an error team_10 introduced while writing the C3B retry prompt (not an error by B2 itself): the prompt's own paraphrase claimed the FAQ page's active topic chip fails contrast. C3 independently re-measured the live FAQ chip and found it **passes** at 5.6709:1 — the real fail B2 found is on `/treatment/` and `/lessons/`, a different selector. Reported here for transparency rather than silently corrected.

## Could not measure (aggregated across all six lines)

- Painted (screenshot-sampled) focus-ring colour on any control below the fold — Puppeteer returned a 0-height screenshot after auto-scroll on every off-viewport stop, in both B1 and B3. Computed-style `outline`/`box-shadow` values are reported instead and flagged throughout as weaker evidence than a painted sample.
- Whether the skip link's Enter-activation behaviour (measured on `/` by B1 and C2) generalizes to `/contact/`, `/faq/`, `/shop/` — not independently re-run on those three.
- End of tab order on `/faq/` at 390px — C2's walk was capped at stop 55, still inside FAQ content; the page did not wrap.
- Any screen-reader output, anywhere, by any line — every line states this explicitly, and none treats an accessibility-tree read as equivalent.
- No accessibility scanner was run by any line, by design (charter §8א clause 5: a clean scan is not evidence of correctness on this project).
- C1's structural check was not repeated at a narrow/mobile viewport (only 1440×900 was asserted for C1's own scope).
- Process claims in the statement (manual testing actually performed, an accessibility coordinator actually answering the phone/contact form, Eyal/legal sign-off) — no live measurement can verify these either way; all marked UNVERIFIED, not FALSE.

## Process note: the C3 timeout

C3's first attempt timed out twice (exit 124, empty stdout both times) and failed loud — the runner's guard worked exactly as designed and never read that as a no-finding pass. The likely cause matches this wave's own stated diagnosis: a single line asked to independently re-measure many dimensions (contrast, keyboard, structure, alt text) across 12 pages from scratch, inside one 900s budget. The fix applied was the one already settled on for this project — split further, not extend the timeout — by having the retry cite the other five same-day, same-engine lines' numbers for claims they already covered, and only take fresh measurements for claims none of them touched (screen-reader use, the stated review/approval status, the contact channel, the video-caption promise). That retry finished well inside budget and, in the process, caught and corrected a transcription error in its own retry prompt rather than accepting it — direct evidence the citation approach did not just launder team_10's claims through a second engine.
