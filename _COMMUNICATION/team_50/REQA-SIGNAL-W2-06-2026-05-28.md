---
id: REQA-SIGNAL-W2-06-2026-05-28
from_team: team_100
to_team: team_50 (QA / L-GATE_BUILD)
wp: WP-W2-06 — Blog Migration
date: 2026-05-28
fix_commit: 78edf9d
status: RE-QA REQUESTED (AC-04 remediated)
---

# RE-QA SIGNAL — WP-W2-06 AC-04 remediated

Your AC-04 FAIL (Wave2 blog archive never activated on `/blog/`) is remediated and deployed.

## Fix (commit 78edf9d)
Root cause: `/blog/` is the WP Posts page (`page_for_posts=18`, `is_home()`), so WP ignores its page template. Added `ea_w2_06_template_include` (priority 100) routing `is_home() && !is_front_page()` → `tpl-blog-archive.php` and `is_singular('post')` → `tpl-blog-single.php`, setting `ea_wave2_shell`. Deployed after FTP recovery.

## team_100 live verification
- `/blog/`: `ea-wave2-shell=1`, `ea-blog` grid markup present (119 refs) → Wave2 archive renders. ✓
- Single posts route to `tpl-blog-single.php` (Wave2 single view).

## Known residual (P3, non-blocking)
- `/blog/` still shows `vc_row` ×10 **in post excerpts** — the shortcode-cleanup mu-plugin strips VC on `the_content` but not on excerpts. This is the AC-07 P3 you already flagged ("archive excerpts show [vc_row], tied to AC-04"). The blocking AC-04 (archive renders) is now resolved. Fix path if you want it cleared: extend `ea-blog-shortcode-cleanup` to `get_the_excerpt`/`the_excerpt`. Classify as P3 unless you deem otherwise.

## Request
Re-run W2-06 L-GATE_BUILD per `GO-SIGNAL-W2-06-QA-2026-05-28.md` (root-URL ACs). AC-04 should now PASS (or PASS_WITH_FINDINGS on the P3 excerpt vc_row). Engine: non-Claude (IR#1).
Deliverable: update `_COMMUNICATION/team_50/QA-VERDICT-WP-W2-06-L-GATE-BUILD-2026-05-28.md`.

*team_100 — 2026-05-28*
