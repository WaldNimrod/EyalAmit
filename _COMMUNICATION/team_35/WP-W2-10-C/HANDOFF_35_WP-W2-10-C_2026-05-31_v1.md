---
id: HANDOFF_35_WP-W2-10-C_2026-05-31_v1
from_team: team_35 (Design Studio — claude-design)
to_team: team_100 → S2 Eyal sign-off
date: 2026-05-31
wp: WP-W2-10-C (Conversion cluster)
parent: WP-W2-10
mandate: MANDATE-TEAM35-W2-10-CLUSTERS-A-G-2026-05-31
stage: S1 (hi-fi mockup) — HALTS at S2 (Eyal sign-off gate)
status: READY_FOR_S2
---

# HANDOFF — WP-W2-10-C · Conversion cluster (S1 hi-fi mockups)

Authoritative index for the S1 package. Composition-only per AC-U1 — D-14 atoms/tokens
assembled, **no new atoms, no new token values**. STOP at S2 (no template edits, no self-validate).

## 1. Routes fetched (REAL content)
| Route | Template | Fetch | HTTP | Notes |
|-------|----------|-------|------|-------|
| `/contact` | `tpl-contact.php` | `http://eyalamit-co-il-2026.s887.upress.link/contact/` | **200** | Renders intro only; **CF7 form_id=0** → body shows placeholder "טופס צור קשר — יוגדר לאחר יצירת טופס CF7 ב־wp-admin (ראה inc/cf7-wave2-form.txt)". |
| `/faq` | `tpl-faq.php` | `http://eyalamit-co-il-2026.s887.upress.link/faq/` | **200** | Real `<select>` filter (11 slugs) + real Q&A. 5 of 10 categories have published answers; 5 are empty. |

*WebFetch upgrades HTTP→HTTPS and the staging TLS cert is expired (`certificate has expired`); content was fetched over plain HTTP via curl (both 200). No 404s.*

## 2. Deliverables in this package
- `mockup/conversion-contact.html` — contact-form archetype (form + WhatsApp A/B + trust signals).
- `mockup/conversion-faq.html` — faq-accordion archetype (sticky category filter + grouped accordion).
- `narrative/composition-notes.md` — screen-by-screen, citing D-14 atom IDs.
- `assets/asset-manifest.md` — asset inventory + Eyal gaps.
- This index.

## 3. D-14 atoms used (cite, never redefine)
`atom-nav-topnav` · `atom-interaction-contact-form` · `atom-interaction-whatsapp-cta` (inline A/B pair + float variant) · `atom-feedback-cta-pill` (`--primary`) · `atom-content-faq-item` · faq-filter `<select>` · `atom-data-display-footer-social` · helpers `.ea-skiplink` / `.ea-sr-only` / `.ea-container`. Tokens inlined verbatim from `ea-tokens.css`.

## 4. Composition decisions
**Contact (contact-form archetype):**
- Two-column `ea-contact-section`: form (right) | WhatsApp A/B + trust (left). Collapses to one column < 768px.
- Form field order: שם → טלפון → דוא״ל → נושא → הודעה → submit. Phone-first (primary conversion is callback/WhatsApp, email optional).
- WhatsApp rendered as the **A/B variant pair** the child spec requires (WP-W2-01 `ea-ab-testing`): `data-ab-experiment` + variant A/B, B `hidden`.
- a11y: explicit labels, `required`+`aria-required`, pre-wired `aria-describedby` error spans (hidden until invalid), `aria-invalid` styling hook, skip-link, single `h1`.

**FAQ (faq-accordion archetype):**
- Sticky `faq-filter` `<select>` with slugs **verbatim** from live control; `?topic=<slug>` URL state + `aria-live` count (AC-C3).
- Heading hierarchy h1 → category h2 → question h3.
- Accordion uses **`<button aria-expanded>` + `role="region"`** instead of D-14's native `<details>` so state is axe-explicit; visual mapping is 1:1, no new tokens. team_10 may revert to `<details>` at S3.
- 5 empty categories kept as `hidden` "תוכן בהכנה" groups for filter parity.

## 5. Open questions for Eyal (S2)
**Q1 (CF7 — primary, carries AC-C2).** The contact template calls `ea_wave2_render_contact_form()` but staging has **`form_id=0`** → no form renders live; the placeholder even points to `inc/cf7-wave2-form.txt`. **Eyal must create the CF7 form in wp-admin.** Once created, S3/team_10 wires it via `add_filter('ea_wave2_cf7_form_id', fn()=><ID>)`. The mockup shows the *intended* form layout for sign-off; AC-C2 carries forward with this documented dependency. → Does the field set in the mockup (name/phone/email/topic/message) match what Eyal wants in CF7?
**Q2 (FAQ empties).** 5 categories (תיקים, סטנדים לאחסון, סטנד רצפתי, תיקון וחידוש, שאלות כלליות) have no published answers. Keep them visible as "תוכן בהכנה", or hide from the filter until populated?
**Q3 (contact details).** Current `tpl-contact.php` shows no phone/email/address block. Add a visible contact-details block, or keep form + WhatsApp only?
**Q4 (WhatsApp A/B copy + number).** Placeholder `wa.me/0000000000`; variant copy is mockup-drafted. Confirm real number + approve/adjust A vs B copy.

## 6. Assumptions
- Live motion (breathe/fade) is the S3 build layer; mockups ship motion-off for a clean a11y/perf audit.
- Empty FAQ categories are content-pending, not removed from taxonomy.
- WhatsApp A/B is in-scope per child spec (ties to WP-W2-01 ea-ab-testing).

## 7. Targets (design-toward; validated downstream at S5, NOT here)
a11y 100 · axe 0 critical / 0 serious · Lighthouse mobile perf ≥ 85. Self-contained, no external requests (system-font fallback), semantic + ARIA + visible focus + AA contrast (`--ea-text-body`).

## 8. Paste-ready sandbox prompt (claude-design)
```
Open these two self-contained RTL Hebrew hi-fi mockups (composition-only, D-14 tokens inlined verbatim):
  _COMMUNICATION/team_35/WP-W2-10-C/mockup/conversion-contact.html
  _COMMUNICATION/team_35/WP-W2-10-C/mockup/conversion-faq.html
Context: WP-W2-10-C Conversion cluster, routes /contact (tpl-contact.php) + /faq (tpl-faq.php),
real content from http://eyalamit-co-il-2026.s887.upress.link/{contact,faq}/ (both HTTP 200).
This is an S1 layout sign-off artifact for Eyal — composition only, no new atoms/tokens, no template edits.
Verify visually: (1) contact = form + WhatsApp A/B variants + trust signals, two-column → single on mobile;
(2) faq = sticky category filter (URL state) + accessible accordion (button + aria-expanded), h1→h2→h3.
Known gap to surface, not fix: /contact CF7 form_id=0 (Eyal must create the form in wp-admin; S3 wires
add_filter('ea_wave2_cf7_form_id', fn()=><ID>)). Do not self-validate (S5/team_50+190). Halt at S2.
```

*team_35 → S2 Eyal sign-off. Iterate within this mandate on Eyal feedback.*
