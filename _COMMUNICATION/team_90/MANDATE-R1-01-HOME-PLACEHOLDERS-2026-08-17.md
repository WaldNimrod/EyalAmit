# MANDATE — team_90 · Composer · R1-01 homepage placeholders

**מאמת:** `composer-2.5` (Cursor) · **בנאי:** Cursor Grok 4.6 (הסשן הזה) · Iron Rule #1.
**היקף:** דסקטופ בלבד. מובייל = מחוץ להיקף.
**עמוד:** `http://eyalamit-co-il-2026.s887.upress.link/` (TLS פג בכוונה — `curl -sk` מותר כאן בלבד).

אסוף ראיות בעצמך (`curl -sk`). אל תסתמך על טענות הבנאי. פלט ריק = FAIL.

## ארבעת סעיפי החוזה (חובה)

1. **התאמת מקור** — כל מחרוזת חדשה חייבת להימצא במקור המצוטט. אחרת FAIL.
2. **האנק לא ממופה** — שינוי קוד שאינו ממופה לסעיף למטה → FAIL.
3. **Provenance** — מחרוזת שהשתנתה בלי הערת מקור בקוד → FAIL.
4. **פלט ריק = FAIL.**

אסור לאשר מחרוזת כי הבנאי כתב אותה. תנו את **המקור** ובדקו התאמה.

## מקורות (הבייטים, לא תיקייה)

- `EyalAmit_Site_GoogleDrive_Sync/סקירה דף הבית.xlsx` · גיליון1!B9 = `פרק 3 - וידאו`
- אותו קובץ · גיליון1!C15 מכיל: `גלריה / וידאו משני` · `תמונות אותנטיות בלבד (לא stock)` · `[לתיאום שיחת היכרות](/contact)` · ושלוש הפסקאות של «הצצה נוספת לחוויה»
- כיתוב קופסת הווידאו `כאן ייכנס וידאו 16:9` — הוראת team_00 לשיטת הפלייסהולדר (HANDOFF-CURRENT §ב.2). זה אינו תוכן אייל; זה תווית חסר.
- גוף פרק 3 הוא גיבריש לטיני במכוון (`Lorem ipsum…`). **PASS אם הוא גלוי כמציין-מקום. FAIL אם הוא מוצג כאילו הוא טקסט של אייל, או אם חסר.**

## סעיפים ממופים (רק אלה מותרים ב-diff)

קבצים מותרים:
- `site/wp-content/themes/ea-eyalamit/inc/chapters/defaults/home-defaults.php`
- `site/wp-content/themes/ea-eyalamit/page-templates/tpl-chapters-home.php`
- `site/wp-content/themes/ea-eyalamit/template-parts/chapters/section-home-03-video.php` (חדש)
- `site/wp-content/themes/ea-eyalamit/template-parts/chapters/section-home-09-peek.php` (חדש)

`git diff --name-only` מול HEAD חייב להיות תת-קבוצה של הרשימה הזו. קובץ אחר = FAIL.

## מה לבדוק ב-HTML החי (דסקטופ)

1. קיים `#video` עם תווית `פרק 03` וכותרת `וידאו`.
2. בתוך `#video` יש קופסה עם הטקסט `כאן ייכנס וידאו 16:9`. אין `youtube.com/watch?v=XXXXXXXXXXX`.
3. אחרי `#what` (פרק 02) מופיע `#video` (פרק 03) ואחריו פרק 04 (השוואה). פער 02→04 נסגר.
4. תוויות `פרק 02` … `פרק 11` מופיעות כולן. פרק 12 הוא CTA בלי כותרת «פרק 12» (אייל לא נתן כותרת) — זה תקין.
5. `#peek` מכיל את שלוש הפסקאות מ-C15 מילה-במילה, קופסת `גלריה · תמונות אותנטיות בלבד`, וקישור `לתיאום שיחת היכרות` אל `/contact/`.
6. `photo-band` עדיין קיים בין פרק 07 ל-08 — לא נדרש להסירו.

## מלכודות מדידה (בלי המסקנה)

- הקרוסלה מדפיסה כל כרטיס פעמיים — אם סופרים עדויות, סופרים שמות ייחודיים.
- `-k` רק מול הסטייג'ינג.
- אל תבדקו מובייל.

## פלט חובה

שורה ראשונה בדיוק אחת מ: `VERDICT: PASS` או `VERDICT: FAIL`
אחר כך רשימת בדיקות קצרה עם CONFIRMED/FAIL + ציטוט ראיה (שורה מ-HTML או מ-diff).
ריק = FAIL.
