# Requirements Coverage Sweep — SEO / AEO / GEO / Metadata / Structured Data / Sitemap

| | |
|---|---|
| Date | 2026-09-24 |
| From | team_90 (requirements coverage sweep, requested ahead of client meeting) |
| Staging measured | `http://eyalamit-co-il-2026.s887.upress.link` (plain HTTP by design; invalid staging cert is never a finding) |
| Scope | SEO / AEO / GEO / metadata / structured data / sitemap only. Accessibility and alt text are owned by other lines and excluded. |
| Method | Live HTTP measurement against the site today, no trust taken from prior reports without re-verification. Population enumerated via `wp-json/wp/v2/pages` and `wp-json/wp/v2/posts` (paginated, `status=publish`), not from `page-sitemap.xml`. |
| Role | Measurement and tabulation only. No fix was applied to the site or its code. |

---

## 0. Baselines read

1. `docs/project/eyal-ceo-submissions-and-responses/from-eyal/EYAL-SITEMAP-SEO-AEO-GEO--REQUIREMENTS-BASELINE-2026-04-02.md` — Eyal's original requirements (Category B = SEO-01..SEO-11 is this sweep's core scope; Category A/C items pulled in only where they touch sitemap/redirect hygiene).
2. `docs/project/eyal-ceo-submissions-and-responses/from-eyal/eyal_amit_dev_agent_build_spec.md` — build spec §13.1, §13.7, §15, §18, §19, §20, §22, §23.
3. `_COMMUNICATION/team_100/AEO-DEEP-AUDIT-AND-OPTIMIZATION-2026-07-25.md` and its exec summary — July's C1–C13 checklist and AEO-01..AEO-11 findings, re-verified live today, not trusted as still-current.
4. `_COMMUNICATION/team_110/R3-META-AUDIT-2026-09-24.md` and `R3-SEO-GEO-2026-09-24.md` — today's team_110 claims, treated as claims under test (Section 2).

One baseline item is flagged `UNVERIFIABLE` on sight: **SEO-11** ("Eyal report per the traffic-growth guide: search intents, cornerstone content, blog plan, FAQ policy, KPIs — checklist §6 of that guide") points at `EYAL-TRAFFIC-GROWTH-AEO-GEO-GUIDE-2026-03-29.md`, a document outside the four baselines this sweep was scoped to, and its content (search-intent strategy, KPI targets) is not something an HTTP sweep can check off — it needs a document-level review, not a live probe.

---

## 1. Numbered requirement checklist

| # | Requirement (source) | Status | Measurement |
|---|---|---|---|
| R-01 | Single H1 per page (SEO-01; spec §18.1, §23.5) | **GAP** (1 URL) | 136/137 live (200) pages carry exactly one `<h1>`. Exception: `/100-100-100-תודה/` (old blog post) has 4 `<h1>` tags ("100% !!!!!!! ... תודה !!!!!!!!", "", "🙂", "תודה!!!!!!"). Smallest correction: demote 3 of the 4 headings to `<h2>`/styled text in that post body, keep one true H1. |
| R-02 | H2 phrased as a question where it fits search intent (SEO-02, explicitly "desirable" not mandatory) | STANDS | Sampled `/treatment/`, `/method/`, `/lessons/`, `/sound-healing/`: all H2s use interrogative openers (מה/איך/למה); `/sound-healing/`'s H2s additionally carry a literal `?`. Requirement's own wording is non-mandatory ("רצוי"), and substance is met. |
| R-03 | ~40–100 word direct-answer paragraph immediately after H2 on service/method/core pages (SEO-03) | STANDS (with note) | Sampled 11 H2 blocks across 4 core pages: every H2 is immediately followed by direct-answer content (no orphan headings); word counts ranged 42–200, several inside the 40–100 target and several (113, 131, 163, 192, 200 words) running longer than "short." Not read as a hard gap since the source says "כ-40–100" (approximate) and content quality is present — flagged for owner awareness only. |
| R-04 | Entity consistency — name, location, value proposition, HE+EN (SEO-04) | STANDS | `Person`/`ProfessionalService` JSON-LD carries consistent name ("אייל עמית" / "cbDIDG"), address ("פרדס חנה-כרכור"), phone (`+972-52-482-2842`) site-wide; homepage meta description repeats the same terms; `/en/` exists with correct `hreflang` pairing to home. |
| R-05 | FAQ + Schema (FAQPage, LocalBusiness, etc.), no conflicting schemas (SEO-05) | STANDS | `FAQPage` present on 14 URLs incl. dedicated `/faq/` (133 Q&A, matches the historical CPT-consolidation number) and pillar `/snoring-sleep-apnea/` (7 Q&A, up from July's 6). Exactly one `<script type="application/ld+json">` block per page site-wide (0 pages with 2+ blocks) — no duplicate/conflicting schema found. |
| R-06 | Blog as growth channel + internal linking to services (SEO-06; spec §18.1 internal linking) | STANDS (structural) | `/blog/` live, 200, links internally to all 7 sampled service pages (treatment, method, lessons, sound-healing, shop, didgeridoos, repair). Publishing cadence/content plan is Eyal-gated (AEO-11) and outside this sweep's live-measurable scope. |
| R-07 | GSC + valid sitemap XML, single SEO plugin (SEO-07) | STANDS (technical layer) / **UNVERIFIABLE** (GSC submission) | `sitemap_index.xml` → `post-sitemap.xml` (53 URLs) + `page-sitemap.xml` (83 URLs), both 200, well-formed, generated by the single Yoast SEO plugin. All 136 sitemap URLs return 200 with no redirect (checked without following redirects). GSC submission is a live-production action and cannot be verified on a noindexed staging site — this is a correctly-deferred cutover step per the AEO exec summary, not a defect. |
| R-08 | Canonical — no critical duplicates, consistent with migration/301s (SEO-08) | **GAP** (1 URL) + otherwise STANDS | 136/137 live pages carry a self-referential, absolute canonical (verified after normalizing percent-encoded vs. raw-UTF-8 URLs, which produced false mismatches on first pass — resolved). Exception: `/learning/therapist-training/` (the IA-09 "coming soon" page) has **no canonical tag at all**. Separately: 16 legacy/duplicate-path URLs (`/about/`, `/muzza/*`, `/muzeh/*`, `/hashita/`, `/tools-and-accessories/*`, `/services/didgeridoo-*`, `/courses-soon/`) correctly 301-redirect to their canonical target rather than serving duplicate 200 content — see Section 2, claim 2. |
| R-09 | Performance: lean template, optimized images, caching (SEO-09) | **UNVERIFIABLE** here | Staging carries no CDN/caching layer and is noindexed; any Lighthouse/perf number measured here is a staging artifact per this project's own dev/staging discipline (confirmed independently by team_110's R3-SEO-GEO note: "Lighthouse score on staging is not a gate — not measured"). Needs re-measurement on the production domain post-cutover. |
| R-10 | alt text (SEO-10) | **OUT OF SCOPE** | Owned by another line; covered separately in team_110's `R3-ALT-JOIN-2026-09-24.md`. Not assessed here. |
| R-11 | Eyal's report per traffic-growth guide §6 (search intents, cornerstone, blog plan, FAQ policy, KPI) (SEO-11) | **UNVERIFIABLE** | Points to a document outside this sweep's 4 baselines; it is a content-strategy deliverable, not something a live HTTP sweep can check. Needs a separate document review against `EYAL-TRAFFIC-GROWTH-AEO-GEO-GUIDE-2026-03-29.md`. |
| R-12 | QR slug preservation, no redirect breaking scans (IA-15) | STANDS | All 48 live `/qr/qrN/` pages (qr1–qr48) return 200 directly; no redirect chain observed on any of them. |
| R-13 | Broken-link scan + 301 map, migration milestone (OPS-03) | STANDS (within this sweep's scope) | 0 unexpected non-200s among 153 enumerated pages/posts and 136 sitemap URLs. All 16 known redirect sources resolve in one hop to a live 200 canonical target (verified each). Scope note: this checks the enumerated population and sitemap, not a full in-body anchor-by-anchor crawl of every rendered page — that is a larger, separate exercise. |
| R-14 | SEO plugin capability bundle: title / meta description / schema / sitemap / OG / noindex-when-needed (spec §13.1) | STANDS | Yoast SEO live, producing all six: 137/137 titles present, 137/137 descriptions present, 137/137 pages carry a valid JSON-LD graph, sitemap live and clean, OG present (full or partial, see R-16), noindex correctly applied both site-wide (staging posture) and selectively (`/learning/therapist-training/` excluded from the sitemap while still directly reachable — matches the IA-09 "coming soon" intent). |
| R-15 | Custom JSON-LD injection capability (spec §13.7) | STANDS | Confirmed live: the `ea-w2-seo-schema.php` mu-plugin injects `FAQPage`/`Article`/`CollectionPage`/`VideoObject`/`Book` nodes into Yoast's single `@graph` without adding a second `<script>` tag — 0 pages found with more than 1 JSON-LD block. |
| R-16 | Open Graph completeness (spec §13.1 "open graph") | **GAP** (6 URLs) | `og:title`, `og:url`, `og:image` present on 137/137 pages. `og:description` present on 131/137; **missing** on 6: `/press/`, `/qr/`, `/qr/qr20/`, `/qr/qr29/`, `/qr/qr39/`, `/historical-articles/` — every one of these *does* carry a populated `<meta name="description">`, the value is simply not mirrored into `og:description`. Smallest correction: mirror the existing meta-description value into `og:description` for these 6 URLs (likely the same code path that recently rewrote their descriptions per team_110's pass — see Section 2). |
| R-17 | GEO consistency: "אייל עמית", "cbDIDG", "טיפול בנשימה באמצעות דיג'רידו", "פרדס חנה", "מאז 1999" (spec §18.2) | STANDS (schema layer) | All except "מאז 1999" confirmed present in homepage JSON-LD + meta description. "מאז 1999" is not literally present in the structured data sampled — it only appears as an example in the build spec's own suggested `Person.description` text, not as a standalone mandate elsewhere — noted, not scored as a gap. |
| R-18 | Required schema types: Person, ProfessionalService/LocalBusiness, Service, FAQPage, Article, Course (future) (spec §19.1) | STANDS, with one open question | `Person` and `ProfessionalService` on 137/137 pages. `FAQPage` on 14. `Article` on 102. `Course` absent site-wide — correct, since IA-08 routes courses to external links only (no internal course content to mark up). `Service` type found on only 3 pages, which reads thin against the ~7 service-shaped pages on the site (treatment/method/lessons/sound-healing/shop/didgeridoos/repair) — but the baseline does not enumerate which specific pages must carry `Service`, so this is flagged for owner attention rather than scored a hard gap. |
| R-19 | Prohibited schema choices: no `HealthAndBeautyBusiness`, no "Circular Breathing" as a central schema anchor, no `areaServed: Israel` default, no imprecise geo (spec §19.2) | STANDS | 0 instances of `HealthAndBeautyBusiness` or `AggregateRating` across all 137 JSON-LD graphs. `areaServed` uses a precise `GeoCircle` (lat 32.4637761 / lon 34.9760176 / radius 45000m), never a bare `"Israel"` string. "Circular Breathing" does not appear as an `@type` or top-level name anywhere; the Hebrew equivalent ("נשימה מעגלית") appears only inside a `knowsAbout` array, not as an anchor. |
| R-20 | `sameAs` required, including Wikipedia (spec §19.3) | STANDS | `Person.sameAs` and `ProfessionalService.sameAs` both include `https://he.wikipedia.org/wiki/...` site-wide. |
| R-21 | Approved base JSON-LD graph shape: Person + ProfessionalService(+founder) + standalone Service node (spec §20 example) | STANDS (core shape) / minor mismatch | Live `Person` and `ProfessionalService` nodes match the approved example's fields closely (name/url/image/jobTitle/worksFor/knowsAbout/sameAs; name/alternateName/url/image/telephone/founder/address/openingHoursSpecification/areaServed/sameAs). The example's standalone top-level `Service` node is not present on the homepage graph specifically — `Service` only appears on 3 other pages (already counted under R-18). This reads as an accepted implementation choice (matches AEO audit's own C5 scoping of `Service` to service-type pages, not the homepage) rather than a functional gap — flagged as a documentation/interpretation note. |
| R-22 | Two FAQ areas: short FAQ on homepage + full FAQ with `FAQPage` schema on a dedicated page (spec §15.1, §15.3; §23.2/§23.5 checklist "FAQ קיים") | **GAP** | Dedicated `/faq/` page is live with `FAQPage` schema (133 Q&A) — confirmed. Homepage: 0 occurrences of "faq" / "accordion" / any FAQ-shaped block found in the raw HTML or the visible text (the only "faq" string on the whole homepage HTML is the nav-menu link's href). Theme source `home-defaults.php` defines no FAQ block. The homepage short-FAQ area required by the build spec does not exist. Smallest correction: add a short FAQ block (a handful of Q&A pulled from the existing 133 on `/faq/`) to the homepage template; no schema change needed since spec only requires schema on the dedicated page. |
| R-23 | JSON-LD validity (implicit hard requirement — a parse failure is always a defect) | STANDS | 137/137 pages parse as valid JSON; 0 parse failures across 137 `ld+json` blocks (containing 331 `ListItem`, 274 `OpeningHoursSpecification`, 232 `Question`/232 `Answer` nodes, etc.). |
| R-24 | Staging carries `Disallow: /` + universal noindex, intentionally (AEO audit C1) | STANDS (intentional) | `robots.txt` = `Disallow: /`; `<meta name="robots" content="noindex, nofollow">` confirmed on 137/137 live pages today. Matches this project's own dev/staging discipline — never a finding on staging. The production robots swap (AEO-09) remains an open cutover-only item, unrelated to this sweep's staging measurement. |
| R-25 | `GeoCircle` values (AEO audit C6) | STANDS | Confirmed live: lat 32.4637761 / lon 34.9760176 / radius 45000m — unchanged since July. |
| R-26 | Title/description length sanity ("sane length," this sweep's own item 1) | **GAP** (30 URLs, low severity) | Title length range 14–160 chars (avg 49). 30/137 (22%) exceed 70 chars, several running 100–160 — **all 30 are old historical blog posts** (IA-13 "הופעות + כתבות היסטוריות" legacy content, explicitly excluded from the main menu by that same requirement). Full list in Section 3. Description length range 10–159 chars (avg 127); 10 short outliers (<50 chars) all trace to genuinely thin source content already characterized by team_110 (QR one-line story titles, legal-page one-liners, the two intentionally-gated "coming soon" pages, `/galleries/`, `/shop/`) — not silent truncation, not scored as a gap. |

---

## 2. Team 110 claim validation

| Claim (team_110, today) | My measurement | Verdict |
|---|---|---|
| 157 URLs read from the S007 tracking list; 156 returned 200; 1 returned 404 (`/services/`) | Today's REST enumeration (pages+posts, `status=publish`) returns 153 items, not 157. Diffed against the 2026-09-18 tracking list (157 rows): exactly 4 rows are no longer directly live — 2 posts (`סיפורים-מהנייר-עם-אייל-עמית`, `41-הטור-של-אייל-עמית-...`) 301→`/blog/`; `/about/` 301→`/eyal-amit/`; `/services/` 404. `/services/` 404 confirmed via GET and HEAD, without following redirects, and confirmed absent from both `page-sitemap.xml` and `post-sitemap.xml`. | **CONFIRMED** (404 and the "rest reads as live" claim both hold; the population is 153 not 157 only because 3 more items became redirects between 09-18 and today, which is exactly team_110's own "mirrors" framing, not a contradiction) |
| Duplicate titles/descriptions are mirrors of the same page, not missing content — pairs cited: `/about/` & `/eyal-amit/` (+ mokesh at both paths), `/books/` vs `/muzza/`/`/muzeh/` (+ every book at all three paths), `/method/` vs `/hashita/`, `/treatment/` vs its old service path, `/lessons/`, `/repair/`, `/shop/` | Every one of these "mirror" source paths returns **301** today, not a live 200 duplicate: `/about/moksha/`, `/muzza/`, `/muzza/vekatavt/`, `/muzza/tsva-bechol-ve-zorek-layam/`, `/muzeh/` (+2 children), `/hashita/`, `/tools-and-accessories/` (+2 children), `/services/didgeridoo-lessons/`, `/services/didgeridoo-treatment-breath/`, `/services/handmade-instruments/`, `/courses-soon/` — 16 URLs total, all 301 to their canonical target. Across the 137 pages that do return 200, title uniqueness is 137/137 and description uniqueness is 137/137 — zero duplicates. | **CONFIRMED**, and stronger than described: these aren't merely "the same title, explained" — the alternate path doesn't serve a 200 at all, so there is no duplicate-content signal in the live population whatsoever. |
| The two flagged S007-list posts redirect to `/blog/`, same title "because it's the same page" | Verified both: `סיפורים-מהנייר-עם-אייל-עמית` and `41-הטור-של-אייל-עמית-חארטה-בארטה` both return 301 → `/blog/`. | **CONFIRMED** |
| `/historical-articles/`: placeholder sentence removed from body; description now pulled from live archive content; `CollectionPage` schema present | Live description: "מופע הסיפורים של אייל עמית. מופע מפתיע וראשון מסוגו בישראל בז'אנר ה-'ספוקן סטוריז'..." — no "placeholder"/"אופציונלי" string found in body or meta. `CollectionPage` type confirmed present in its JSON-LD graph. | **CONFIRMED** |
| `/qr/qr20/` description = "אבא, אני רעב" | Confirmed exact live match. | **CONFIRMED** |
| `/qr/qr29/`, `/qr/qr39/`, `/qr/qr20/` fall back to the story title (no `qrNN` prefix) when the body has no paragraph | `qr29` = "לעבור דירה", `qr39` = "תחשוב חיובי", `qr20` = "אבא, אני רעב" — all match, and none carry a "qrNN" prefix in the description (the `qrNN -` prefix only appears in the `<title>`, not the description, exactly as claimed). | **CONFIRMED** |
| `/thank-you/` description = "תודה שפנית אליי…" | Confirmed exact live match: "תודה שפנית אליי 🙏 הפרטים התקבלו ואחזור אליך בהקדם." | **CONFIRMED** |
| A post with no excerpt now falls back to the first 30 words of the body, not the site tagline | Not independently isolated to a specific test case in this sweep — outcomes on short QR/story pages are consistent with a short-content fallback, but no specific no-excerpt post was traced word-by-word to confirm the "30 words" mechanic precisely. | **NOT INDEPENDENTLY VERIFIED** (plausible, not reproduced to the letter) |
| AEO audit's July finding AEO-07: sitemap HEAD returned `000` (connection failure) on `/services/` and `/books/` | Re-run today: `/books/` → HEAD 200, GET 200, present in `page-sitemap.xml`. `/services/` → HEAD 404, GET 404, correctly absent from every sitemap. No `000` reproduced on either URL. | **NOT-REPRODUCED** (as a current defect — resolved since July or was a transient staging network blip; `/services/` behaves correctly as a clean 404, not a `000`) |

---

## 3. Per-URL defect tables

### 3.1 Titles — GAP: 30 URLs exceed 70 characters (all legacy/historical posts, IA-13)

| Len | URL (decoded) |
|---|---|
| 160 | /ריברסינג-נשימה-מעגלית-דיגרידו/ |
| 141 | /פודקאסט-דיגרידו-ו-נשימה-אייל-עמית/ |
| 125 | /סרטים-מהחיים-מופע-הסיפורים-של-אייל-עמי/ |
| 123 | /אייל-עמית-תופעת-יחיד-מופע-סיפורים-spoken-stories-15/ |
| 121 | /שני-תאריכים-קרובים-למופע-הסיפורים-של-א/ |
| 113 | /עכשיו-מופע-הסיפורים-של-אייל-עמית-תופע/ |
| 111 | /טיפול-בפוסט-טראומה-לחייל-משוחרר-מגולנ/ |
| 105 | /וסיפרתָּ-מופע-הספוקן-סטוריז-של-אייל-עמ/ |
| 104 | /שנה-לחוק-הספרים-צניחה-של-35-במכירות-של-ספ/ |
| 99 | /לא-בעוד-רגע-לא-בעוד-שנייה-ע-כ-ש-י-ו/ |
| 97 | /47-הטור-של-אייל-עמית-זמן-חלום/ |
| 93 | /מוקש-דהימן-מאסטר-דיגרידו-ציור-מקורי-ח/ |
| 91 | /דיגרידו-פרדס-חנה-סטודיו-לבנייה-ונגינ/ |
| 90 | /מורה-לדיגרידו-מודה-למוריו-תלמידיו-ומט/ |
| 88 | /2228-2/ |
| 87 | /עמית-ביָדִית-מופע-הסיפורים-של-אייל-עמי/ |
| 86 | /סדנת-דיגרידו-קבוצתית-מקיפה-וייחודית-ל/ |
| 86 | /ביקורות-גולשים-אודות-עכשיו-מופע-הסיפ/ |
| 85 | /נשים-מנגנות-בדיגרידו-אישה-מנגנת-בדיג/ |
| 83 | /עוד-רגע-מחייו-של-מורה-לדיגרידו/ |
| 83 | /עכשיו-מופע-הסיפורים-של-אייל-עמית-15-11-14-בת/ |
| 82 | /מופע-הסיפורים-של-אייל-עמית-כתבה-מאת-רו/ |
| 79 | /כתבה-אודות-אייל-עמית-מורה-ומטפל-בדיגרי/ |
| 79 | /כתבה-על-תופעת-יחיד-ב-המקומון-גבעתיים-ר/ |
| 78 | /טיפול-בנשימה-באמצעות-דיגרידו-ללמוד-ל/ |
| 77 | /דף-פייסבוק-חדש-למופע-הזמנה-לשני-המופעי/ |
| 74 | /פודקאסט-דיגרידו-ו-נשימה-אייל-עמית-2/ |
| 74 | /תלמידים-ומטופלים-ממליצים-על-המרכז-לטי/ |
| 74 | /הזמנה-להשקת-הספר-החדש-וכתבתָ-אייל-עמ/ |
| 73 | /2-8-מופע-בצוותא-20-מהמקומות-באולם-חינם-לתוש/ |

Smallest correction (described, not applied): none required functionally — these are legacy/archive posts (IA-13) not in the main menu; if desired, shorten only the ones still linked from `/historical-articles/` or `/press/` for cleaner search snippets.

### 3.2 Meta descriptions — no gap found

0 missing, 0 duplicates across 137 live pages. 10 short (<50 char) outliers exist but all trace to genuinely thin/placeholder-free source content already explained in team_110's audit (QR story one-liners, legal one-line summaries, `/galleries/`, `/shop/`, and the two "coming soon" pages `/learning/courses-external/` and, separately, `/learning/therapist-training/`). Not counted as gaps per the "don't invent defects" discipline.

### 3.3 Canonical — GAP: 1 URL

| URL | Issue |
|---|---|
| `/learning/therapist-training/` | No `<link rel="canonical">` tag at all. |

### 3.4 JSON-LD parse failures — none

0 parse failures across 137 pages / 137 `ld+json` blocks. No hard defect here.

### 3.5 Robots meta — 137/137 pages carry `noindex, nofollow` (expected staging posture, not a finding)

Confirmed intentional and universal on staging, per this project's dev/staging discipline and the AEO audit's own C1. Production swap (AEO-09) remains open and cutover-only, tracked separately.

### 3.6 Open Graph — GAP: 6 URLs missing `og:description`

| URL | Meta description present? | og:description present? |
|---|---|---|
| `/press/` | Yes | **No** |
| `/qr/` | Yes | **No** |
| `/qr/qr20/` | Yes | **No** |
| `/qr/qr29/` | Yes | **No** |
| `/qr/qr39/` | Yes | **No** |
| `/historical-articles/` | Yes | **No** |

### 3.7 Sitemap hygiene — 0 defects

All 136 sitemap URLs (`page-sitemap.xml` 83 + `post-sitemap.xml` 53) return 200 when fetched without following redirects. No 301/302/4xx/5xx found in either sitemap.

### 3.8 H1 — GAP: 1 URL

| URL | H1 count | H1 texts |
|---|---|---|
| `/100-100-100-תודה/` | 4 | "100% !!!!!!! 100% !!!!!!!! 100% !!!!!!!! תודה !!!!!!!!!", "", "🙂", "תודה!!!!!!" |

### 3.9 `/services/` — confirmed 404, confirmed absent from sitemap

GET and HEAD both 404 without redirect following; `/services/` does not appear in `page-sitemap.xml`, `post-sitemap.xml`, or `sitemap_index.xml`.

---

## 4. Population accounting

| Metric | Count |
|---|---|
| Pages enumerated via REST (`status=publish`) | 101 |
| Posts enumerated via REST (`status=publish`) | 52 |
| **Total population** | **153** |
| Fetched live | 153 / 153 (100%) |
| Fetch errors (timeouts, connection failures) | 0 |
| Returned 200 | 137 |
| Returned 301 (all to a verified-live 200 canonical target, 1 hop) | 16 |
| Returned any other non-200 | 0 |
| Sitemap URLs (`page-sitemap.xml` + `post-sitemap.xml`) | 83 + 53 = 136 |
| Sitemap URLs returning 200 without redirect | 136 / 136 (100%) |
| Sitemap URLs returning non-200 or a redirect | 0 |

**Cross-check note:** the task brief referenced "~279 published URLs" as the reason `page-sitemap.xml` alone (~87) undercounts the site. This sweep's clean REST enumeration today is 153, and team_100's own tracking list from 6 days ago (`S007-SITEMAP-157-URLS-2026-09-18.tsv`) is 157 — both far below 279. I could not locate a live source supporting a ~279 figure and am not asserting one; flagging the discrepancy rather than reconciling it, since inventing a cause would violate this sweep's own discipline. The 153-vs-157 gap between today and 09-18 is fully explained (Section 2, claim 1): exactly 4 items became redirects/404 in the intervening days, 0 unexplained.

**Boundary of this sweep:** REST-enumerated pages/posts + both sitemap files, checked at the HTTP/HTML/JSON-LD layer. Not covered: a full in-body, anchor-by-anchor crawl of every internal link inside every page's rendered content (broader than OPS-03's own "broken link scan" as commonly scoped); the `ea_faq`/`ea_gallery`/`ea_testimonial` CPTs (already correctly de-listed from REST and the sitemap per the 2026-09-20 team_00 ruling documented in `functions.php`); production-domain performance/Lighthouch measurement (explicitly deferred by this project's own staging discipline).
