# MANDATE — team_90 · Composer · R1-02 E2E + עדשת אקסל

**מאמת:** `composer-2.5` (Cursor) · **בנאי:** Cursor Grok 4.6 · Iron Rule #1.
**היקף:** דסקטופ. העמוד שלנו = הגרסה המוצעת `/treatment/` (לא `?compare=eyal`, אלא אם בדיקה משווה במפורש).
**TLS:** `curl -sk` מותר מול הסטייג'ינג בלבד. אסוף ראיות בעצמך. פלט ריק = FAIL.

שורה ראשונה חייבת להיות בדיוק אחת מ: `VERDICT: PASS` או `VERDICT: FAIL`

שתי עדשות. FAIL באחת = FAIL כללי.

---

## עדשה א׳ — דיוק מידע, ממשקים, קישורים (העמוד המוצע)

URL: `http://eyalamit-co-il-2026.s887.upress.link/treatment/`

מקור בייטים: Drive `content 13.8.26/טיפול בדיג'רידו/treatment.md` (זהה ל-`docs/project/eyal-ceo-submissions-and-responses/from-eyal/תוכן לאתר 25.5.26/טיפול בדיג'רידו/treatment.md`).

### א1. מידע (CONFIRMED/FAIL + ציטוט)

1. H1 מכיל «טיפול» + «דיג׳רידו» / «דיג'רידו». כפתור ההירו `לתיאום שיחת היכרות` → `/contact/` ולא `#what`.
2. SECTION 03 («מה זה») אינו מכיל «עובדת הוותק» ואינו מכיל «שעוסק בתחום מאז 1999 בשיטת».
3. «למי זה מתאים» כולל «עייפות או חוסר יציבות אנרגטית» ו«מתמודד עם מחלה». אפס `.rcard`.
4. אין `ea-home-hero-720`. יש `כאן ייכנס סרטון מפגש`.
5. «סטודיו נשימה מעגלית» בסקשן «מי זה אייל עמית».
6. 13 שמות ממליצים ייחודיים (קרוסלה משכפלת כרטיסים — סופרים שמות). אפס כרטיס בלי טקסט.
7. קיים `<section class="bleed">` עם «הנשימה היא לכולם.» (זה מתועד כהפרש מול המסמך ב-T-01 — לא FAIL).
8. אקורדיון FAQ נראה ≥20 פריטים (15 מהמסמך + תוספות נחירות/CPAP). גם זה מתועד ב-T-01 — לא FAIL.

### א2. ממשקים

9. אין כרטיס/סקשן ריק (חוק «אין תוכן = אין רכיב»). פלייסהולדר הווידאו **כן** רכיב מלא: תג + כותרת «כאן ייכנס סרטון מפגש».
10. CTA תחתון `לתיאום שיחת היכרות` → `/contact/`.
11. `qa_probe` דסקטופ כבר PASS (1440, overflow false) ב-`tmp/qa/cdp/r1-02-e2e-2026-08-17` — אמת מחדש ב-curl שהעמוד 200 ושאין `#what` על כפתור ההירו.

### א3. קישורים — HEAD/GET עם `-sk -o /dev/null -w '%{http_code} %{url_effective}'`

בדוק **כל** אלה. PASS רק אם הקוד הוא 200 או 301 לאותו אתר (לא 404, לא 000). עוגנים בפנים (`#session`) לא דורשים HTTP נפרד.

פנימיים בגוף העמוד (לא תפריט גלובלי):
- `/contact/` (הירו + CTA)
- `/method/` (cbDIDG)
- `/eyal-amit/mokesh-dahiman/` (מוקש)
- `/treatment/` (כרטיס ההבחנה)
- `/sound-healing/`
- `/lessons/`

13 קישורי פייסבוק של SECTION 08 — HEAD; 200/301/302 = PASS. 404 = FAIL. חסימת Facebook (403/login) = הערה לא חוסמת אם ה-URL תואם למסמך מילה-במילה.

התפריט הראשי (דגימה, לא כל חנות): `/` `/method/` `/lessons/` `/sound-healing/` `/shop/` `/contact/` `/eyal-amit/` — 200 או 301.

`tel:+972524822842` ו-`wa.me/972524822842` — קיימים ב-HTML (footer). לא חובה לפתוח WhatsApp.

---

## עדשה ב׳ — חומר אייל + אקסל + כללי הנגשה לאייל

קבצים:
- `EyalAmit_Site_GoogleDrive_Sync/EA-CONTENT-TRACKER.xlsx` ו/או `_COMMUNICATION/team_100/S006/tracker/latest.csv` + `latest-items.csv`
- טאב `עמוד · טיפול בדיג'רידו` / `r1-02-items.json`
- אמנה: `_COMMUNICATION/team_100/S006/S006-MILESTONE-CHARTER.md` §2, §3א, §3ב

### ב1. גיליון ראשי (סבב-1-ליבה · R1-02)

12. שורה R1-02 קיימת, נתיב `/treatment/`.
13. `סטטוס מכונה` הוא `הוגש לבדיקה` (או `בעבודה` אם עוד לא סומן — ציין מה בפועל).
14. `ממתין ל` חייב להיות `אייל` אחרי הגשה. אם עדיין `team_100` — **FAIL של העדשה הזו** (זה הצעד שנותר לסוכן אחרי PASS, אבל אתה בודק מצב נוכחי: דווח CONFIRMED/FAIL לפי מה שכתוב עכשיו; אל תשנה את האקסל).
15. סוכן לא כתב בעמודות אנוש (`סטטוס אישור`, `הערות נימרוד`, `הערות אייל`, `תאריך אישור`).

### ב2. טאב העמוד — הנגשת שאלות לאייל

רק T-01 ו-T-02 אמורים להיות `ממתין לאייל`. T-03..T-06 `בוצע` / `ברור` — לא שאלות לאייל.

16. **שאלה אחת + בחירה מרשימה** על T-01: עמודת `מה נדרש ממך` היא משפט אנושי אחד, בלי שמות קבצים/PHP/git. `_picks` / דרופדאון = «גרסה מוצעת» ו«גרסה לפי המסמך».
17. T-01 מסביר את שני ההבדלים במפורש (רצועת תמונה + 5 שאלות) — לא «מצא את ההבדלים». שני URL חיים בעמודת האפשרויות.
18. T-02: שאלה אחת על הסרטון החסר + בקשה למילוי (קישור/קובץ או «אין»). ניסוח אנושי. לא דורשים ממנו לבחור בין קבצי קוד.
19. סעיפים שממתינים לאייל **אינם** משנים ניסוח שכבר הוגש (H-01/H-06/H-07/M-01a וכו' מחוץ להיקף). T-01/T-02 לא דורסים אותם.
20. אין להגיש לאייל Markdown; ההגשה היא סטייג'ינג + שורות אקסל. CONFIRMED אם אין קובץ `to-eyal` חדש ל-R1-02 ו-T-01 מצביע ל-URL חי.

### ב3. נעילת סיווג

21. T-01 ו-T-02 מסווגים `לא ברור` (מדיה לא צוינה; בחירת גרסה אינה בייטים יחידים). T-03..T-06 `ברור` ובוצעו. אם ברור סומן כממתין לאייל — FAIL.

---

## פלט

`VERDICT: PASS` או `VERDICT: FAIL` בשורה הראשונה.
טבלה קצרה: מספר בדיקה · CONFIRMED/FAIL · ציטוט (HTTP code / תא אקסל / קטע HTML).
ריק = FAIL.
