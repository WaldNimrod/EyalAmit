---
id: BUILD-COMPLETE-S006-M03-PHERO-LEDE-2026-09-07
schema_version: aos_v1_team_messaging
from_team: team_110
to_team: team_100
cc: [team_00, team_90]
date: 2026-09-07
type: build-complete
milestone: S006
mandate: M-03 · עיצוב .phero__lede (נגזרת M-01)
engine: claude-opus-5 (builder) — validator must differ (Iron Rule #1)
base: e480ba8
status: קוד + ראיות · לא נפרס · לא נדחף
---

# BUILD COMPLETE · M-03 · עיצוב `.phero__lede`

## קבצים — שניים, בדיוק ההיקף שאישרת

| קובץ | מה |
|---|---|
| `assets/css/chapters.css` | **3 סלקטורים חדשים, כולם תחת `.phero__lede`** + הערת מקור |
| `style.css` | `Version: 1.5.21` → **`1.5.22`**. שורה אחת, שום דבר אחר |

**הוכחת היקף:** `git diff` על `chapters.css` מראה **0 שורות שהוסרו** — תוספת טהורה.
לא נגעתי ב-`.phero__s` · `.phero__in` · `.phero` · ולא בשום סלקטור קיים.
בכל התמה השתנו **שני קבצים בלבד**.

```css
.phero__lede{margin-top:18px;max-width:60ch;font-weight:300;
             font-size:clamp(.98rem,1.2vw,1.08rem);line-height:1.85;
             color:rgba(255,255,255,.86)}
.phero__lede p{margin:0 0 14px}
.phero__lede p:last-child{margin-bottom:0}
```

**`.phero__lede p` הוא עדיין תחת הסלקטור החדש** ולא נוגע בקיים. הוא נדרש כי הקריטריון
שלך דורש מרווח בין פסקאות, והתמה מאפסת `p{margin:0}` גלובלית (`chapters.css:32`) —
בלי הכלל הזה שלוש הפסקאות נדבקות. המרווח העליון של הראשונה הוא ה-`0` שבאותה הצהרה.

## ההיגיון מול העוגן שנתת

`.phero__s` = `font-size:clamp(1.04rem,1.4vw,1.2rem)` · `.86` · `54ch` · `line-height:1.8`.
ה-lede לוקח **אותו משקל (300) ואותו צבע (`.86`)** — עקביות — אבל **קטן ממנו** כדי לא להתחרות.

יצא פרט נחמד שלא תכננתי: `54ch` על 19.2px ו-`60ch` על 17.28px נותנים **בדיוק אותו רוחב
מדוד — 576.6px**. כלומר הסאב וה-lede מיושרים לאותה מידה, ולגוש הטקסט בהירו יש קצה אחד נקי.

## ראיות — נמדדו בדפדפן, לא הוצהרו

נמדד על הרקע האמיתי: `--dark-grad` = `linear-gradient(160deg,#0B0703,#1C1109 55%,#2A1A0C 82%,#0B0703)`.
**מדדתי מול `#2A1A0C` — העצירה הבהירה ביותר, כלומר המקרה הגרוע ביותר לטקסט לבן.**

| קריטריון שלך | נמדד | |
|---|---|---|
| שלוש פסקאות קריאות בהירו | `paragraphs: 3` · `distinct_vertical_blocks: 3` | ✅ |
| מרווח בין פסקאות | `14px · 14px` | ✅ |
| אפס margin עליון על הראשונה | `first_p_marginTop: 0px` | ✅ |
| לא נשפכות מהמכל | `lede_overflows_container: false` · `page_h_overflow: false` | ✅ |
| לא נחתכות | `text_clipped: false` | ✅ |
| **ניגודיות AA — יחס מדוד** | **12.62:1** מול העצירה הבהירה | ✅ עובר גם **AAA** (7:1) |
| לא מתחרה בסאב | lede `17.28px` < sub `19.2px` | ✅ |
| 29 העמודים האחרים | hero ביקורת ללא `lede`: `control_has_lede: false` | ✅ |

**דסקטופ** (1440×900) — צילום מסך מצורף בשיחה.
**מובייל** — לא השקעתי, רק ודאתי שלא נשבר: viewport 375, `scrollW 375`,
`page_h_overflow: false`, `clipped: false`. סבב 3 ימשיך מכאן.

### איך נמדד — ולמה זה לא curl

האמנה אוסרת לאמת פריסה ב-curl. הסטייג'ינג עדיין לא מעודכן (FTP), ולכן בניתי הארנס
מקומי: **המרקאפ המדויק ש-`phero.php` פולט ל-`/shop/`** (בלי media/chap/cta) + **`chapters.css`
האמיתי מוטמע**, מוגש ב-HTTP ונמדד ב-DOM. הרפו לא זוהם — `git status site/` מראה רק את
שני הקבצים של M-03. השרת הזמני נסגר.

⚠ **תיקון עצמי:** המדידה הראשונה שלי במובייל החזירה viewport 980. זו הייתה שגיאה בהארנס —
שכחתי `<meta name="viewport">`, ולכן האמולציה נפלה ל-980. הוספתי ומדדתי מחדש; המספרים
בטבלה הם מהמדידה התקינה. לא דיווחתי את המדידה הפגומה כראיה.

## `php -l` / תקינות

לא נגעתי ב-PHP במנדט הזה. שני הקבצים של M-01 עדיין `php -l` נקי (נבדק אחרי המיזוג).

## הערה אחת, לא חוסמת, לא תיקנתי

`phero.php:21` מחשב `$dark = ! empty( $a['dark'] ) || '' === $media;` — **והמשתנה לא בשימוש
בשום מקום בקובץ.** קוד מת, לא משפיע על פלט, לא חוסם תוכן. לפי אמנה §6 זה לא עולה
ל-`CODE-BLOCKED-REGISTER` כי הוא אינו פוגע בהצגת תוכן. מדווח ולא נוגע.

## שערים

שער 3 סגור. **לא פרסתי ולא דחפתי.** `Version` הועלה ל-1.5.22 כהוראתך — כלומר הפריסה
הבאה תבטל את המטמון כנדרש. לא נעצרתי על אף סעיף בגלל סיווג לא-ברור.
