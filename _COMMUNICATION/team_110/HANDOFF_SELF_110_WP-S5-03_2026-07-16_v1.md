# Session Handoff — 110 | context snapshot


## 1. SESSION ACCOMPLISHED
*(No accomplishments recorded — session handoff for context only.)*

## 2. IDENTITY SNAPSHOT
## Team Identity
- **Team ID:** 110
- *(team details unavailable)*


## 3. CONTEXT SNAPSHOT
## Work Package — WP-S5-03
*(work package details unavailable — verify wp_id is correct)*


## 4. MANDATORY READS
- `_aos/governance/110.md`
- `_aos/roadmap.yaml`
- `methodology/AOS_IDENTITY_ONBOARDING_v1.0.0.md` (first AOS session only)


## 5. BLOCKERS / OPEN ITEMS
*(None recorded.)*

## 6. ACTIVATION PROMPT
```
HANDOFF_DEPTH: full
ACTIVATION_SCOPE: 110 only

## WP Context

**Read this block first.** These WP-specific references take precedence over the general Session Task below in case of any conflict.

- **Spec path:** `_COMMUNICATION/team_100/S005/WP-S5-03-LOD400-2026-07-16.md`

# Agent Onboarding — 110 / eyalamit

*Generated 2026-07-16T13:05:08.858691Z  ·  Depth: full*

## Session bootstrap (paste FIRST — sets identity, base, registered session)

```bash
export AOS_SESSION_TEAM_ID=110 AOS_PROJECT_ID=eyalamit
export AOS_API_BASE="${AOS_API_BASE:-http://100.125.98.56:8092}"
bash scripts/aos_session_ctl.sh start WP-S5-03
/AOS_mail check
```

## Activation TL;DR
- **Identity:** 110 · engine: — · role: —
- **Domain:** eyalamit · profile: L0
- **Assignment:** WP=WP-S5-03 —  · gate=—
- **Task:** —
- **Writes to (first 3):** `_COMMUNICATION/110/`
- **First reads:** `CLAUDE.md` · `_aos/governance/110.md` · `_aos/roadmap.yaml`
- **State:** team=110 project=eyalamit wp=WP-S5-03 gate=— depth=full

## AOS Environment
- **Hub:** agents-os (AOS platform — methodology engine + Lean Kit)
- **Platform:** AOS v3.1.2 dashboard / Lean Kit 3.1.10+
- **Universal Iron Rules:** CLAUDE.md §Iron Rules (1–9) — cross-engine, lean-kit snapshots, project roadmap authority, inter-team artifacts, activation prompts, gate authority split, routing display (ADR032), data authority (ADR034), port canon
- **Data authority:** ADR034 — DB-as-SSoT when online (API-only mutations for canonical fields); files retain gate_history + prose
- **Directory canon:** methodology/AOS_DIRECTORY_CANON_v1.0.0.md
- **Agent guide:** `AGENTS.md` (engine-neutral agent onboarding reference)

## Project Context
- **Project:** Eyal Amit — EyalAmit.co.il 2026
- **Profile:** L0
- **Type:** spoke
- **Enabled:** True
- **Local path:** `/data/projects/eyalamit` *(machine-local — your checkout may differ)*

## Active Modules
*(module status unavailable — verify lean-kit registry)*

## Team Identity
- **Team ID:** 110
- *(team details unavailable)*

## Governance Contract
*No governance file found for 110.*

## Work Package — WP-S5-03
*(work package details unavailable — verify wp_id is correct)*

## Session Task
*No task was set when this session was generated.*

**First action:** Before doing any substantive work, ask the user:
> *"What task should I focus on in this session?"*

Present these intuitive options (team-appropriate) so the user can pick quickly or describe a custom task:

- **[A] Continue current work** — resume where session left off
- **[B] Submit work product** — write output artifact to _COMMUNICATION/team_{ID}/
- **[C] Escalate to Team 00** — route decision or blocker to principal

**Completion criteria:** Once the user confirms a task, restate it back in one sentence and proceed. Report the deliverable path + a one-line summary to Team 00 via `_COMMUNICATION/110/` when done.

## Instructions
You are being onboarded as an AOS agent. Read the sections below carefully.

1. **Confirm your identity** — verify your team ID, engine, and role match the Team Identity section.
2. **Read the Governance Contract** — these are your Iron Rules and authority boundaries.
3. **Understand the project** — review the Project Context and Active Modules.
4. **Locate your working directories:**
   - Deliverables: `_COMMUNICATION/110/`
   - Onboarding: `_COMMUNICATION/110/__ONBOARDING_TEAM_*.md`
   - Governance: `_aos/governance/110.md`
5. **Confirm readiness** — respond with a brief summary of your role and current assignment.


FIRST ACTION:
Gate N/A is complete. Check _aos/roadmap.yaml for next gate (L-GATE_BUILD) and _COMMUNICATION/team_110/ for pending actions.
```


## 7. CANONICAL OPTIONS
- **[A] Continue current work** — resume where session left off
- **[B] Submit work product** — write output artifact to _COMMUNICATION/team_{ID}/
- **[C] Escalate to Team 00** — route decision or blocker to principal

## 8. CONTEXT CHECKPOINT (aos_handoff)
```aos-context-checkpoint
{
  "team_id": "110",
  "wp_id": "WP-S5-03",
  "domain": "eyalamit",
  "profile": {
    "depth": "FULL",
    "target": "RICH",
    "lifecycle": "NEW",
    "mission_source": "WP_REF"
  },
  "source_versions": {
    "base": "7b9153f29ac936bd",
    "team": "d359ed74ce9a7093",
    "domain": "3845125952f96d5f",
    "wp": "ae165a9134fb4870"
  }
}
```

<!-- ════════════════════════════════════════════════════════════════════ -->
<!-- SPOKE SUPPLEMENT (hand-authored by team_110 — the hub prompt-generate  -->
<!-- skeleton above is content-empty for the eyalamit L0 spoke: team/WP     -->
<!-- details returned "unavailable"). ROOT CAUSE CONFIRMED 2026-07-16:      -->
<!-- the hub DB's L0 eyalamit roadmap is a stale S002-era snapshot          -->
<!-- (active_milestone=S002, 24 WPs, ZERO WP-S5-*), so prompt-generate      -->
<!-- cannot resolve WP-S5-03. Raised to team_100 in                         -->
<!-- ROUTING-REQUEST-TEAM100-S005-REGISTRATION-2026-07-16.md §4.            -->
<!-- Ref memory: project_eyalamit-handoff-skeleton-content-empty            -->
<!-- ════════════════════════════════════════════════════════════════════ -->

## SPOKE SUPPLEMENT — WP-S5-03 real context (team_110 hand-authored)

### Predecessor chain — ALL GREEN (2026-07-16)
| WP | Build | Cross-engine (team_90 / composer-2.5) | Commit |
|----|-------|----------------------------------------|--------|
| WP-S5-01 (verify residuals) | PASS 5/5 | **PASS, 0 findings** | `e8b02b9` |
| WP-S5-02 (schema/meta build) | PASS | **PASS, 0 findings** | `6c42f20` |

Iron Rule #1 satisfied on both. Report: `_COMMUNICATION/team_110/REPORT-TEAM00-XVAL-WP-S5-01-02-2026-07-16.md`.

### WP-S5-03 — what it is
**Legacy/301 completeness** — the LAST team-executable build in S005. `blocked_by: []` — genuinely
independent, can start immediately on team_00 routing. Feeds the WP-S5-05 cutover gate.

- **Spec (LOD400, cross-engine validated clean):** `_COMMUNICATION/team_100/S005/WP-S5-03-LOD400-2026-07-16.md`
- **Research (ground truth):** `_COMMUNICATION/team_100/RESEARCH-WP-S5-03-GROUND-TRUTH-2026-07-16.md`
- **Parent index:** `_COMMUNICATION/team_100/S005/S005-PACKAGE-1-3-INDEX-2026-07-16.md`
- **next_wp:** `WP-S5-05` · **validator:** team_90 (cross-engine, Iron Rule #1)

### The headline finding — do NOT treat this as 400 separate problems
A 21-URL sample found **9 real gaps concentrated in 2 roots**, fixable in one pass, plus a documented
TODO (54 blog redirects), one orphan class (→410), and 2 out-of-staging-scope items. Scope is
**concentrated, not scattered**.

### ⚠ SSoT-first — the single most important build rule
```
hub/data/decisions/redirects-301-eyal-final-2026-05-27.json   (135 decisions ← EDIT ONLY HERE)
        │
        └── python3 scripts/gen_htaccess_301_from_decisions.py   (regenerate)
                ├── _COMMUNICATION/team_100/tools/htaccess_301_block.txt   (inert mirror — nginx ignores)
                └── site/wp-content/mu-plugins/ea-w209-legacy-301-redirects.php   (THE LIVE MECHANISM)
```
**NEVER hand-edit `ea-w209-legacy-301-redirects.php` or the htaccess block — both are build-artifacts.**
Live mechanism today: `template_redirect` priority 0; 32× exact 301, 2× 410, 1× regex `#^/Blog/(.+)$#`.

### Work items (per spec)
1. **§3.1 triage all 400** (not just the 21 sample) → `evidence/s5-03/triage-400/triage.csv`, classifying
   each as `covered` / `false-positive` / `needs-301` / `needs-410` / `needs-content-check` / `out-of-staging-scope`.
2. **§3.2 pattern A** — second legacy prefix `דיגרידו-סטודיו-נשימה-מעגלית-אייל-עמית-2/*` has **0 rules**;
   mirror it onto the covered prefix's targets, via the SSoT.
3. **§3.3 pattern B** — `/shop/books/*` → `/books/<slug>` (0 entries today; e.g. `/shop/books/וכתבת/`→`/books/vekatavta/`).
4. **§4** — 54 per-slug blog redirects: fold real ones into the SSoT, or justify drop per-slug (BUILD/DECIDE).
5. **§5** — `/פסח/`-class orphans → **410** (not 301-to-nowhere).
6. **§6** — 2 items are **out-of-staging-scope** (www/scheme canonicalization; a redirect target that is itself
   404) → documented, deferred to WP-S5-05 §7 items 3+7. **Do not demand staging proof for these.**
7. **§7 DO-NOT-RUN:** `_COMMUNICATION/team_100/tools/deploy_htaccess_301.py` (uploads .htaccess — misleading,
   nginx ignores it). The `*.QUARANTINED` W2-06 exports are intentionally quarantined, NOT a source of truth.

### 🔑 Hard-won operational lessons from S5-01/S5-02 — apply these
- **Staging probing WILL produce transient `curl-000`.** S5-01 hit 4 transient 000s on just **49** URLs.
  This WP probes **400** — expect many. `000` = transport timeout on the slow shared uPress host, **never**
  a redirect. **Probe sequentially / low-concurrency and re-probe individually**, or the triage.csv will be
  full of false failures. This alone can sink the WP if handled naively.
- **Staging TLS is expired BY DESIGN** → `curl -k` (dev-only). **Site-wide `noindex`** = `ea-staging-noindex.php`
  (host-conditional on `upress.link`), NOT editorial — absent on prod. Neither is a defect.
- **Cross-engine validation works and is fast.** Mirror the mandates that just passed cleanly:
  `_COMMUNICATION/team_90/MANDATE-TEAM90-WP-S5-0{1,2}-*.md` — especially their **Guardrails** section, which
  demonstrably prevented false-positive FAILs. Runner: `bash scripts/run_cross_engine_validation.sh team_90 . <mandate>`
  (`--dry-run` first). **Requires team_00 to authorize `cursor-agent -p --force --trust` with that literal
  phrasing** — generic approval is rejected by the safety classifier.

### ⛔ Do NOT auto-start
This handoff is **informational**. team_110 begins only after **explicit team_00 routing** (index §7.2 item 4).

---

## 🔄 UPDATE 2026-07-16 — this session ALSO carries WP-S5-06 (facade)

**team_00 reversed the §3.2 ruling** («ליזי מחוייב» — the lazy-load commitment is binding). team_110's
measurement proved it is **not** actually delivered, so the facade is now **MANDATORY**, registered as a
**separate WP: WP-S5-06**.

**Routing:** team_00 routes **WP-S5-03 AND WP-S5-06 to THIS session** — but they remain **two distinct WPs**,
deliberately not merged. S5-03's ACs, evidence tree, and team_90 verdict must stay uncontaminated by the
facade work (and vice versa). Two WPs · two mandates · two verdicts · two gates · one session.

- **WP-S5-06 input spec:** `_COMMUNICATION/team_110/ADDENDUM-TEAM100-WP-S5-06-FACADE-MANDATORY-2026-07-16.md`
- **LOD400:** to be authored by **team_100** (as for S5-01/02/03). **Do not begin S5-06 until its LOD400 exists
  and is cross-engine validated** — the addendum is the *input* to that spec, not a substitute for it.
- **Transcripts: EXCLUDED** (team_00 ruling) — an Eyal content task, not code.

### 🔴 The one thing that will silently break WP-S5-06 if missed
`ea-w2-seo-schema.php` **L266** emits the QR VideoObject nodes by regexing the raw **`post_content`** DB field —
not the rendered output:
```php
preg_match_all( '#youtube(?:-nocookie)?\.com/embed/([A-Za-z0-9_-]{6,})#', (string) $qr_obj->post_content, $qr_m )
```
The iframes live in the **seed data** → seeded into `post_content`. Therefore:
- ✅ facade as a **`the_content` render filter** → `post_content` untouched → VideoObject nodes survive.
- ❌ facade via **seed-data change + reseed** (the obvious route — exactly how §3.1 was just done) → the literal
  embed URL leaves `post_content` → **every VideoObject node silently vanishes from all 42 QR pages**, destroying
  the WP-S5-02 build that just earned a clean team_90 cross-engine PASS. No error, no warning.

**Mandate: render filter. The reseed route is forbidden.** Existing `the_content` filters sit at priorities
1 / 8 / 9 / 9 / 12 — the facade must run after content injection (suggest ≥20), scoped to QR routes only.

**Measured facts (evidence: `_COMMUNICATION/team_110/evidence/s5-02-facade-cwv/`, rerun `qr_cwv_probe.mjs`):**
~1.06 MB of YouTube payload loads on **each of 42** QR pages despite `loading="lazy"` (the first embed sits
inside Chrome's ~1250px lazy threshold); videos 2+ *are* genuinely deferred; **no CWV/LCP penalty measured** —
the win is data-transfer + privacy. **AC-2 is the regression guard:** VideoObject counts must stay qr2=1,
qr10=3, qr1=0, qr48=0.

### Out of scope for this WP
- WP-S5-05 cutover (blocked); WP-S5-04 (human/Eyal); M-EYAL-INPUTS (Eyal-blocked).
- Roadmap registration/gate-advance = team_100/team_00 (`_aos/` is off-limits to team_110).
- Transcripts (Eyal content task).
