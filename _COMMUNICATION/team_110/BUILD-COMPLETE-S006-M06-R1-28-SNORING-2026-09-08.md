---
id: BUILD-COMPLETE-S006-M06-R1-28-SNORING-2026-09-08
schema_version: aos_v1_team_messaging
from_team: team_110
to_team: team_100
cc: [team_00, team_90]
date: 2026-09-08
type: build-complete
milestone: S006
mandate: _COMMUNICATION/team_110/MANDATE-S006-M06-R1-28-SNORING-2026-09-08.md
page: R1-28 · /snoring-sleep-apnea/
engine: claude-opus-5 (builder)
status: קוד + ראיות · לא נפרס · לא נדחף · ⚠ נדרשת העלאת Version
---

# M-06 · שתי תמונות המסמך — קטנות וחתוכות

## אימות המנדט לפני בנייה — הכול נכון, שני תיקוני נתיב

| טענה | אימות |
|---|---|
| הקבצים כבר באתר באותה רזולוציה | ✅ `sips`: **1294×781** ו-**1080×2032**, בדיוק |
| `.gallery` = רשת 3 עמודות | ✅ `chapters.css:740` |
| `.gfig img` כופה `aspect-ratio:4/3;object-fit:cover` | ✅ `chapters.css:744` |
| `gallery` ב-10 קובצי defaults | ✅ ספרתי 10 |
| גלריות תמונה-בודדת רק בעמוד הזה | ✅ **11 סקשני gallery בסך הכול, מהם 2 בודדים — שניהם כאן** |

**שני תיקונים קטנים לרישום:** הנתיבים הם `assets/images/chapters/snoring/…` (המנדט קיצר
ל-`snoring/…`), והסקשנים הם בשורות **89** ו-**168** ולא 93/172. לא משנה דבר — מסומן לדיוק.

**ההכרעה שלך ש-§4 לא חל — מאושרת מהמדידה:** אין נכס חדש להביא. זה CSS.

## הפעולה — הצטרפות מפורשת

| קובץ | |
|---|---|
| `parts/gallery.php` | +8/−2 · ארגומנט `doc` → מוסיף `.gallery--doc` **רק כשמועבר** |
| `assets/css/chapters.css` | **+11/−0 — תוספת טהורה** |
| `defaults/snoring-sleep-apnea-defaults.php` | +10/−0 · `'doc' => true` בשני הסקשנים + provenance |

```css
.gallery--doc{grid-template-columns:1fr;max-width:820px;margin-inline:auto}
.gallery--doc .gfig img{aspect-ratio:auto;height:auto;object-fit:contain}
.gallery--doc .gfig:hover img{transform:none}
```

**`.gallery` ו-`.gfig` לא נגעו** — אפס שורות הוסרו מ-`chapters.css`.
**`style.css` לא נגע** — עדיין 1.5.23. **אתה מעלה.**

**הצטרפות מפורשת ולא הסקה, כפי שדרשת.** אני מוסיף את הנימוק שמצדיק את זה מעבר לעיקרון:
היום הסקה מספירת פריטים הייתה נותנת בדיוק אותה תוצאה — הפריטים הבודדים היחידים הם שניהם
כאן. כלומר ההבדל אינו נראה היום **ויתגלה רק ביום שבו מישהו יוסיף גלריה של תמונה אחת
במקום אחר** — ואז היא תשתנה לבדה בלי שאיש ביקש. זה בדיוק סוג הכשל שלא מתגלה בבדיקה.

## ראיות — נמדד ב-DOM ב-1440×900

| | לפני | אחרי | |
|---|---|---|---|
| רוחב מוצג (שתיהן) | **357.3px** | **820px** | **×2.29** ✅ (נדרש ×2) |
| מכבי — יחס מוצג | 1.333 (=4/3) | **1.657** | = טבעי `1294/781` ✅ |
| יוני — יחס מוצג | 1.333 (=4/3) | **0.531** | = טבעי `1080/2032` ✅ |
| יוני — גובה מוצג | 268px | **1542.8px** | המסמך במלואו |

**«חתוכה» מוכח ומתוקן:** לפני — שתיהן הוצגו ב-1.333 בדיוק, כלומר נכפה עליהן 4:3.
אחרי — כל אחת ביחס הטבעי שלה, סטייה < 0.02.

**תשעת העמודים האחרים — פלט זהה בבייט.** רינדרתי את **כל** תשעת סקשני ה-gallery האחרים
דרך `gallery.php` הישן והחדש והשוויתי מחרוזות: **9/9 זהים.** אף אחד מהם אינו מעביר `doc`
(`grep` על כל ה-defaults → אפס מופעים מחוץ ל-snoring).

**מובייל — לא נשבר:** 375px, אפס גלישה אופקית, שתי התמונות 279px ברוחב וביחס טבעי.

**ויזואלית:** צילומי מסך בשיחה. מסמך מכבי קריא; ההתכתבות נקראת במלואה והפרט המזהה
נשאר מטושטש כפי שההערה בקוד מציינת.

## שאלה אחת של שיקול דעת — שלך, לא שלי

`max-width:820px` הוא הכפתור היחיד כאן, והוא נותן ×2.29. **הוא גם מייצר תמונה בגובה
1543px ליוני** — כי צילום מסך 1080×2032 שמוצג לא-חתוך *הוא* גבוה. זה בדיוק מה שאייל
ביקש («לא ניתן לקרוא את כולה»), אבל זה תופס ~1.7 מסכים.

הורדה ל-`720px` עדיין עומדת בדרישה (×2.02) ומקצרת את יוני ב-~190px.
**לא שיניתי** — זו הכרעת הצגה בעמוד שאייל יסקור, ולכן שלך. שינוי של מספר אחד.

## מה נדרש ממך

1. **`Version`** 1.5.23 → 1.5.24. `chapters.css` הוא נכס מגורסן — בלי זה הדפדפנים
   ימשיכו להגיש CSS ישן וייראה כאילו לא קרה כלום.
2. פריסה. **הבסיס נשאר 507** — לא נוצרו קבצים חדשים, שלושתם `M`.

לא פרסתי · לא דחפתי · לא נגעתי בטרקר · לא נעצרתי על סיווג.
