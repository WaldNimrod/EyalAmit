---
id: BUILD-COMPLETE-S006-M05-R1-25-FAQ-2026-09-08
schema_version: aos_v1_team_messaging
from_team: team_110
to_team: team_100
cc: [team_00, team_90]
date: 2026-09-08
type: build-complete
milestone: S006
mandate: _COMMUNICATION/team_110/MANDATE-S006-M05-R1-25-FAQ-2026-09-08.md
page: R1-25 · /faq/
engine: claude-opus-5 (builder)
status: קוד + ראיות · לא נפרס · לא נדחף · ⚠ נדרשת העלאת Version
---

# M-05 · `/faq/` — הכותרת מוסתרת אחרי קפיצה לנושא

## אימות ההיקף שביקשת — ציטוט, לפני שנגעתי

`inc/chapters/chapters-enqueue.php:100`

```php
if ( is_admin() || ! is_page( 'faq' ) ) {
    return;
}
```

שני הנכסים נטענים **רק** ב-`/faq/` (`:107` ה-CSS, `:113` ה-JS). **אין §4.**
ובאותה נשימה — `:103` הוא `$ver = wp_get_theme()->get( 'Version' );` ולכן **חובה להעלות
`Version`**, אחרת הדפדפנים ימשיכו להגיש את הישן.

## קבצים — שניים בדיוק

| קובץ | |
|---|---|
| `assets/css/faq-toc.css` | +14 / −3 |
| `assets/js/ea-faq-toc.js` | +52 / −0 |

**`style.css` לא נגע** — `Version` עדיין `1.5.22`. **אתה מעלה.**
לא נגעתי ב-`ea-tokens.css` · `ea-atoms.css` · `chapters.css` · `block-faq-list.php` ·
`faq-inline.php` · `inc/data/*.json`.

## 🔴 ממצא שמשנה את פעולה 3 שלך

המנדט ביקש «ליישר את ה-`top` הדביק ל-72px». **לא עשיתי זאת דרך `--ea-nav-height`,
כי הוא טוקן גלובלי:**

`ea-tokens.css:63` → `--ea-nav-height: 64px` — **מוגדר**, ומשמש עוד **שמונה** מקומות:
`ea-atoms.css:142` · `:1891` · `:1919` · `w2-14e-catalog.css:57` · `:98` · `ea-blog.css:389`.

כלומר 64px אינו «ברירת מחדל שגויה» אלא **תיאור נכון של ניווט Wave2** — פשוט לא של
ניווט chapters, שהוא 72px (`chapters.css:67`). שינוי הטוקן היה מזיז קטלוג, בלוג ו-atoms.
זו שוב חבות שני-המבנים.

**מה שעשיתי במקום:** משתנה FAQ-בלבד, `--ea-faq-toc-top`, עם `72px` כ-fallback.
הטוקן הגלובלי לא נגע.

## הפעולה

```css
.ea-faq-category { scroll-margin-top: var(--ea-faq-scroll-offset, 12rem); }
@media (min-width: 768px) { .ea-faq-toc { top: var(--ea-faq-toc-top, 72px); } }
```

`ea-faq-toc.js` מודד `nav` + `toc` וכותב את שניהם ל-`:root`, ומחשב מחדש ב-`resize`
(מצומצם, 150ms) וב-`load` (רוחב הפונט משנה את מספר השורות).
**מגן מפני המלכודת שהזהרת ממנה:** מדידה כש-`innerWidth×innerHeight` הוא `0` **נזרקת**
ולא נכתבת — אחרת היא הייתה מכווצת את ההיסט במקום להגדילו.

## ראיות — נמדד ב-DOM, בוויופורט אמיתי

⚠ לא נמדד בגלילה, לפי אזהרתך — ה-Browser pane אינו גולל. הבדיקה היא **מתמטיקת פריסה**:
`scroll-margin-top ≥ nav + toc` פירושו שהכותרת מנקה את שני הפסים, וזה בדיוק מה
ש-`scrollIntoView` מכבד.

| ויופורט | nav | toc | נדרש | scroll-margin | מנקה | עודף |
|---|---|---|---|---|---|---|
| **1440×900** | 72.0 | 201.5 | 273.5 | **285** | ✅ | 11.5px |
| **1280×900** | 72.0 | 201.5 | 273.5 | **285** | ✅ | 11.5px |
| **1024×900** | 72.0 | 201.5 | 273.5 | **285** | ✅ | 11.5px |

המספרים שלי זהים לשלך: **72.0 ו-201.5**. שתי מדידות בלתי תלויות באותו מקום.

| קריטריון | תוצאה |
|---|---|
| הפס הדביק צמוד לניווט | `top` = **72px** = גובה הניווט. **8px החפיפה נעלמו** |
| **בלי JS — אין נסיגה** | הסרתי את שני המשתנים: `scroll-margin` חוזר ל-**192px** בדיוק, והדביק ל-**72px** |
| **הוספת נושא מגדילה מעצמה** | +4 לשוניות → toc 201.5→249.8, היסט **285→334**. הסרה מחזירה ל-285 |
| `.ea-faq-category` בלי id | **0 כאלה** בעמוד — אין מה לשבור |
| 16 נושאים | נספרו |

## מובייל — לא נשבר, ושתי הערות לסבב 3

`375×812`: אפס גלישה אופקית · הפס הדביק נשאר **56px**, כלומר טוקן המובייל
(`faq-toc.css:16`) **לא הושפע** מהשינוי שלי בדסקטופ.

שתי הערות **לסבב 3, לא טיפלתי**:

1. `scroll-margin` במובייל ירד מ-192px ל-**150px**. עדיין מנקה בנוחות (נדרש 122.3,
   עודף 27.7px) — אבל זו התנהגות שהשתנתה במובייל, ומובייל אינו בסקואופ. מדווח כדי
   שלא תתגלה כהפתעה.
2. **אותו באג 56-מול-72 קיים גם במובייל** ולא תוקן: הפס נדבק ב-56px בעוד שהניווט
   הנמדד הוא ~72px, כלומר ~16px מהתפריט מתחת לניווט. `faq-toc.css:16` — סבב 3.

## איך נמדד

הסטייג'ינג עדיין לא נושא את השינוי, ו-curl אינו רואה פריסה (אמנה §8). לכן: לקחתי את
ה-HTML **החי** של `/faq/`, החלפתי **רק** את שני נכסי ה-FAQ בעותקים המקומיים, והגשתי
ב-HTTP. כל שאר הנכסים נטענו מהאתר החי. הרפו לא זוהם — `git status site/` מראה שני
קבצים. השרת הזמני נסגר.

## מה נדרש ממך

1. **להעלות `Version`** ב-`style.css` (1.5.22 → 1.5.23). בלי זה השינוי לא יגיע לדפדפנים.
2. פריסה.

לא פרסתי · לא דחפתי · לא נגעתי בטרקר · לא נעצרתי על סיווג.
