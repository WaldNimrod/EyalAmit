# סבב 3 — JSON-LD ו־Open Graph

**תאריך:** 2026-09-24 · **צוות:** 110  
**אמנה:** `_COMMUNICATION/team_100/LOD200-SEO-GEO-OPTIMIZATION-2026-06-16.md`  
ציון Lighthouse על הסטייג'ינג אינו שער (noindex, בלי CDN). לא נמדד.

## מה כבר חי, לפני הסבב

נמדד ב־HTML של הסטייג'ינג, לא בקובץ התמה. יוסט פולט `@graph` אחד, והתוסף `ea-w2-seo-schema.php` מוסיף אליו:

- `Person` (אייל), `ProfessionalService` עם NAP מ־`ea_nap()` (שם, עמל 8, טלפון, שעות, GeoCircle)
- `FAQPage`, `Product`, `Book`, `Article` בעמודי QR ובפוסטים, `VideoObject` במוקש
- `BreadcrumbList` כבר נפלט על ידי יוסט — לא נוסף עותק שני
- תמונת OG ברירת מחדל ב־`ea-w2-og-default.php` כשאין תמונה ראשית; כשיש, יוסט לבד

hreflang he/en כבר ב־`wave2-w2-08.php` על הבית ועל `/en/`.

## מה נוסף בסבב

באותו `@graph`, בלי סקריפט שני:

- `CollectionPage` לעמוד `/historical-articles/` («מופע הסיפורים של אייל עמית»)
- `Article` לפוסט רק אם יוסט לא פלט `Article` או `BlogPosting`
- `BreadcrumbList` רק אם יוסט לא פלט אחד

תוכן העמודים לא נכתב מחדש.
