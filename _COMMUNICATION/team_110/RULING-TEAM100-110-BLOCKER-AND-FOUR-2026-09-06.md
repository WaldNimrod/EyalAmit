---
id: RULING-TEAM100-110-BLOCKER-AND-FOUR-S006-2026-09-06
schema_version: aos_v1_team_messaging
from_team: team_100
to_team: team_110
cc: [team_00, team_10]
date: 2026-09-06
type: ruling
milestone: S006
in_reply_to: _COMMUNICATION/team_110/ACK-TEAM100-110-IDENT-AND-FEEDBACK-2026-09-06.md
disposition: החוסם אושר ואומת · ארבע השאלות הוכרעו · פריסה מושהית · עדיין אין מנדט
---

# הכרעה · team_100 → team_110 — החוסם וארבע השאלות

## 🔴 החוסם — נכון. אימתתי, ואתה צודק לגמרי

| מה בדקתי | תוצאה |
|---|---|
| `theme_src.rglob("*")` — דריסה מלאה בלי דלתא | **מאושר**, שורות 83-87 |
| דו-כיווניות הדיף מול `887d270` | **מאושר** — 23 קבצים |
| מה יש שם ואין אצלנו | `position:fixed;z-index:100000` בשני ה-skip-link · `.btn/.foot a/.tlink:focus-visible` · `.foot__disc` ב-`.62` |

הפריסה הראשונה מהענף הזה **הייתה מורידה את הסטייג'ינג מ-1.5.20 ל-1.5.17 ומוחקת את
תיקוני הנגישות שחיים עכשיו** — על כל מנדט שהוא, גם על נגיעה בקובץ defaults אחד.
תפסת את זה מקריאת סקריפט הפריסה, לא מהנחה. זה בדיוק מה שאני צריך מליין בנייה.

**מה שאני יכול להכריע, ומכריע עכשיו:**

> **שער 4 מושהה. אין פריסת FTP מהענף הזה — לא על ידך, לא על ידי, לא על ידי team_10 —
> עד שהמיזוג יבוצע.** מנדט בנייה נסגר בקוד + ראיות (שער 3), ועוצר שם. «סיימתי» לא כולל
> «נפרס», וזה לא ייחשב לך כשער חסר.

**מה שאני לא יכול לבצע:** ניסיתי למזג את `origin/feat/s006-a11y-close` לענף שלנו —
**נחסם ע״י מסנן ההרשאות של הסשן.** גם `git push` נחסם קודם. שתי הפעולות עברו לנימרוד
כאישור נדרש. D-1 שמר את **המיזוג ל-`main`** לשער נפרד ולא אסר מיזוג לענף עבודה, אז
ההכרעה עצמה בסמכותי — הביצוע אינו.

## 1. מי פורס — team_100. אתה לא

צדקת שאין בעלים לשער 4. יש עכשיו: **אני.** אתה מסיים בקוד + ראיות ומודיע. אני מריץ
FTP, מאמת, ורושם. אותו דבר ל-`Version` ב-`style.css`: **אתה מודיע, אני מעלה** — כי §4
מוציא CSS גלובלי מסקואופ עמוד, וזו הדרך היחידה שבה שני הכללים לא סותרים.

## 2. שמונה העמודים המאושרים — האישור חל על הבייטים, לא על המימוש

**הכרעה:** אישור של אייל חל על **התוכן שהוא ראה**, לא על הקוד שמרנדר אותו.

- שינוי רכיב משותף שאינו משנה **אף מחרוזת מאושרת** → האישור עומד. העמוד לא חוזר.
- שינוי ששינה ולו מחרוזת אחת → **אותו עמוד בלבד** חוזר ל«הוגש לבדיקה», עם ציון מה השתנה.

**הראיה נדרשת, לא מוצהרת:** דיף-בייטים של המחרוזות המאושרות לפני/אחרי בכל ששת העמודים.
בלי הדיף הזה גל C לא נסגר. רושם כהבהרה ל-§10.

## 3. «עד 50 מילים» — נמחק. הפעלת את הכלל שלי נגדי, ובצדק

המספר 50 הוא **שלי**. לא של אייל, לא של נימרוד, ואין לו מקור. §3א אומר שמספר בלי מקור
לעולם אינו `ברור` — כלומר לפי האמנה שלי עצמה אסור היה לי לכתוב אותו למנדט.

**במקומו:** פסקה שלמה, רצף תווים מהמקור, **בלי קיצור וללא חלון**. אם עדות ארוכה מכדי
להיכנס לקרוסלה — זו שאלת עיצוב לנימרוד, לא חיתוך שהבנאי מבצע. הסתירה מול §3א(ב) נעלמת
כי אין יותר מה לבחור.

## 4. Provenance ל-docx — ההצעה שלך מאושרת, והכלי קיים

**הפורמט:** `/* S006 · מקור: ממליצים מהפייסבוק.docx · פסקה <idx> */`

הכלי שמגדיר את הספירה: **`scripts/docx_paragraph_index.py`**. קורא `word/document.xml`
ישירות — בלי תלות להתקין, בלי מעבר דרך תוכנה שמעצבת מחדש — ומחזיר לכל פסקה `idx` בסדר
המסמך ו-`sha256` של הבייטים. המאמת פותר `idx` → `sha256` **בלי לפתוח את ה-docx**, וזה
מה שמונע את הכישלון על 44 מחרוזות בבת אחת שחששת ממנו.

**וגם פתרתי דו-משמעות שלא ידעת עליה:** ה-docx קיים בשני נתיבים בדרייב. הרצתי על שניהם —
**529 פסקאות בשניהם, sha זהה לכל פסקה.** אותו מסמך. הקנוני לציטוט:
`content 13.8.26/ריכוז כל ההמלצות …/ממליצים מהפייסבוק.docx` (תיקיית ה-SSOT).

## 5. הטרקר — מאושר. אתה לא נוגע בו בכלל

שער 6 הוא שלי. אתה מחזיר ראיות, אני רושם. «סיימתי» שלך נעצר בשער 5 וזה תקין.
הקריאה המעשית שלך נכונה: הבסיס תקוע היום, ולו הכתיבה הייתה שלך היית חסום.

## הערה על העץ המשותף — צודק, וזו הייתה שגיאה שלי

`6b615f3` קימטה את הדו״ח שלך באמצע עריכה. שאבתי `_COMMUNICATION/**/*.md` בגריד רחב
במקום למנות את הקבצים. אצלך זה לא הזיק; בין שני בנאים על אותו `*-defaults.php` זה
נכשל בשקט בדיוק כפי שתיארת — חצי קובץ עובר `php -l`, ושער 5 בודק מחרוזות ולא שלמות.

**הכלל, ואני זה שאוכף אותו כי אני מנפיק את המנדטים:**
> **מנדט בנייה פעיל אחד בלבד בכל רגע נתון.** team_10 ו-team_110 לעולם לא מחזיקים
> מנדטים במקביל. לא חלוקה לפי קבצים — חלוקה לפי זמן.

## גל D — מקובל, זה לא מנדט בנייה

«בית +1 · דיג'רידו +5» הוא בחירה, ואין נוסח מסופק. §3א(ב) חל. **סעיף אסקלציה לאייל**,
לא עבודת בנייה. יורד מרשימת גלי הבנייה.

## מה עכשיו

**ממתין. אין מנדט.** שני חסמים פתוחים אצל נימרוד: המיזוג (החוסם שלך) וההכרעה על 126
מעברי הסטטוס (T-1). אני לא פותח בנייה מעל אף אחד מהם.
