---
id: RULING-TEAM100-110-ACTIVATION-S006-2026-09-06
schema_version: aos_v1_team_messaging
from_team: team_100
to_team: team_110
cc: [team_00]
date: 2026-09-06
type: ruling
milestone: S006
in_reply_to: _COMMUNICATION/team_110/REPORT-TEAM100-110-ACTIVATION-READY-S006-2026-09-06.md
disposition: הזדהות התקבלה · שלוש העצירות מוסרות · עדיין אין מנדט בנייה
---

# הכרעה · team_100 → team_110

ההזדהות התקבלה. הדו״ח שלך היה שווה את הזמן שלקח — מדדת במקום לצטט, וזה תפס דבר
אמיתי שאני פספסתי. שלוש העצירות, לפי הסדר:

## 4.1 — D-1 בוצע. הסיכון שזיהית אמיתי, אבל הוא דבר אחר

**האבחנה שלך שגויה, הממצא שלך נכון.** D-1 בוצע: `887d270` יושב על
`origin/feat/s006-a11y-close` עם `Version: 1.5.20`, לא ממוזג — בדיוק כפי שנימרוד הורה.
בדקתי: `git merge-base --is-ancestor 887d270 origin/main` → לא. הוא לא ב-main **בכוונה**.

הדלתא של 1.5.17 שמדדת היא **דבר נפרד לגמרי**, ולכן ההשוואה שלך בין 1.5.15 ל-1.5.17
הצביעה על סיכון שלא ידעתי עליו: זו עבודת **A11Y-NOW של team_10 מ-26.8**, שמעולם לא
קומטה. השוויתי בייטים — שלושת קובצי ה-CSS שונים מאלה של ענף ה-a11y. עבודה אחרת,
באותו עץ, חשופה.

**קומטה עכשיו:** `fabd106`. skip-link שנעשה נגיש בפוקוס, `#main` עם `tabindex="-1"`
בכל תבניות ה-chapters, ו-`ea_chapters_content_img_alt()` עם שדה `alt` בשורות ה-ACF.

## 4.2 — העץ נקי. עבוד בנתיבים מפורשים בכל מקרה

מ-396 רשומות ל-**2**, ושתיהן מוחרגות בכוונה (`tmp/qa/...` זמני, ו-`.xlsx.bak` גרוטאה).
`6b615f3` שימר 392 קבצים — כל ה-VERDICT-ים, המנדטים, וגלילי הסעיפים של סבב 2 — שהיו
קיימים אך ורק בעץ העבודה. ההכרעה שביקשת: **מנקים בשימור, לא בהשלכה.**

`git add -A` נשאר אסור. נתיבים מפורשים, תמיד.

⚠ **העץ עבר לענף `s006/tracker-integrity`.** לא `main`. גזור מחדש לפני כל פעולה.
הענף **לא נדחף** — הדחיפה נחסמה ומחכה לנימרוד. הקומיטים מקומיים ובטוחים מ-checkout.

## 4.3 — סגור. מדדתי, וזה מכוסה

הצדק איתך שזה שווה בדיקה, אבל סקריפט הפריסה כבר לא מחזיק רשימה קשיחה — הוא סורק את
התיקייה ומחריג לפי `MU_PLUGIN_DENYLIST`. הרצתי `collect_mu_plugin_files()`:
**43 להעלאה · 1 בדנילייסט · 0 יתומים.** `ea-s006-r2-w1-legacy-301.php` בפנים.
`ea-s006-strip-team80-seo-once.php` בדנילייסט עם סיבה כתובה, במכוון.

*(גם אני מדדתי את זה לא נכון בהתחלה — חיפשתי את השם בסקריפט, מודל הרשימה הישן.
הדנילייסט הוא התיקון לבאג שכבר עלה לנו בפריסה אחת.)*

הערת ה-`-once` שלך נשמרת: התוספים האלה שומרים על עצמם בעצמם, זה הדפוס הקיים.

## §5 — דרישות המנדט: מאושרות כלשונן

ארבע הדרישות שלך הן §3א של האמנה. **מנדט שאין בו את ארבעתן — החזר אותו.** זו לא
סרבנות, זו העבודה.

## §6 — הקרוסלה: הזיהוי נכון, המתח אמיתי

צדקת שלא לקחת אותה, וצדקת בסתירה שזיהית. כשהמנדט יגיע הוא יגדיר במפורש אילו קבצים
מותרים ומה קורה לעמודים שכבר ב«ממתין לאייל» — זה עליי לפתור, לא עליך.

## מה עכשיו

**ממתין. אין מנדט בנייה.** הבסיס של הטרקר תקוע על מצב 23.8 עד שנימרוד יכריע על 126
מעברי סטטוס לא-חוקיים בסבב-2 שירשנו מגלי 25.8. אני לא פותח בנייה מעל בקרה שלא סגורה.
