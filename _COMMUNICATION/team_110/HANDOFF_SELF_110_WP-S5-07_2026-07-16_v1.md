# Session Handoff — 110 | context snapshot


## 1. SESSION ACCOMPLISHED
*(No accomplishments recorded — session handoff for context only.)*

## 2. IDENTITY SNAPSHOT
## Team Identity
- **Team ID:** 110
- *(team details unavailable)*


## 3. CONTEXT SNAPSHOT
## Work Package — WP-S5-07
*(work package details unavailable — verify wp_id is correct)*


## 4. MANDATORY READS
- `_aos/governance/110.md`
- `_aos/roadmap.yaml`
- `methodology/AOS_IDENTITY_ONBOARDING_v1.0.0.md` (first AOS session only)


## 5. BLOCKERS / OPEN ITEMS
*(None recorded.)*

## 6. ACTIVATION PROMPT
```
HANDOFF_DEPTH: full
ACTIVATION_SCOPE: 110 only

# Agent Onboarding — 110 / eyalamit

*Generated 2026-07-16T16:42:04.003921Z  ·  Depth: full*

## Session bootstrap (paste FIRST — sets identity, base, registered session)

```bash
export AOS_SESSION_TEAM_ID=110 AOS_PROJECT_ID=eyalamit
export AOS_API_BASE="${AOS_API_BASE:-http://100.125.98.56:8092}"
bash scripts/aos_session_ctl.sh start WP-S5-07
/AOS_mail check
```

## Activation TL;DR
- **Identity:** 110 · engine: — · role: —
- **Domain:** eyalamit · profile: L0
- **Assignment:** WP=WP-S5-07 —  · gate=—
- **Task:** —
- **Writes to (first 3):** `_COMMUNICATION/110/`
- **First reads:** `CLAUDE.md` · `_aos/governance/110.md` · `_aos/roadmap.yaml`
- **State:** team=110 project=eyalamit wp=WP-S5-07 gate=— depth=full

## AOS Environment
- **Hub:** agents-os (AOS platform — methodology engine + Lean Kit)
- **Platform:** AOS v3.1.2 dashboard / Lean Kit 3.1.10+
- **Universal Iron Rules:** CLAUDE.md §Iron Rules (1–9) — cross-engine, lean-kit snapshots, project roadmap authority, inter-team artifacts, activation prompts, gate authority split, routing display (ADR032), data authority (ADR034), port canon
- **Data authority:** ADR034 — DB-as-SSoT when online (API-only mutations for canonical fields); files retain gate_history + prose
- **Directory canon:** methodology/AOS_DIRECTORY_CANON_v1.0.0.md
- **Agent guide:** `AGENTS.md` (engine-neutral agent onboarding reference)

## Project Context
- **Project:** Eyal Amit — EyalAmit.co.il 2026
- **Profile:** L0
- **Type:** spoke
- **Enabled:** True
- **Local path:** `/data/projects/eyalamit` *(machine-local — your checkout may differ)*

## Active Modules
*(module status unavailable — verify lean-kit registry)*

## Team Identity
- **Team ID:** 110
- *(team details unavailable)*

## Governance Contract
*No governance file found for 110.*

## Work Package — WP-S5-07
*(work package details unavailable — verify wp_id is correct)*

## Session Task
*No task was set when this session was generated.*

**First action:** Before doing any substantive work, ask the user:
> *"What task should I focus on in this session?"*

Present these intuitive options (team-appropriate) so the user can pick quickly or describe a custom task:

- **[A] Continue current work** — resume where session left off
- **[B] Submit work product** — write output artifact to _COMMUNICATION/team_{ID}/
- **[C] Escalate to Team 00** — route decision or blocker to principal

**Completion criteria:** Once the user confirms a task, restate it back in one sentence and proceed. Report the deliverable path + a one-line summary to Team 00 via `_COMMUNICATION/110/` when done.

## Instructions
You are being onboarded as an AOS agent. Read the sections below carefully.

1. **Confirm your identity** — verify your team ID, engine, and role match the Team Identity section.
2. **Read the Governance Contract** — these are your Iron Rules and authority boundaries.
3. **Understand the project** — review the Project Context and Active Modules.
4. **Locate your working directories:**
   - Deliverables: `_COMMUNICATION/110/`
   - Onboarding: `_COMMUNICATION/110/__ONBOARDING_TEAM_*.md`
   - Governance: `_aos/governance/110.md`
5. **Confirm readiness** — respond with a brief summary of your role and current assignment.


FIRST ACTION:
Gate N/A is complete. Check _aos/roadmap.yaml for next gate (L-GATE_BUILD) and _COMMUNICATION/team_110/ for pending actions.
```


## 7. CANONICAL OPTIONS
- **[A] Continue current work** — resume where session left off
- **[B] Submit work product** — write output artifact to _COMMUNICATION/team_{ID}/
- **[C] Escalate to Team 00** — route decision or blocker to principal

## 8. CONTEXT CHECKPOINT (aos_handoff)
```aos-context-checkpoint
{
  "team_id": "110",
  "wp_id": "WP-S5-07",
  "domain": "eyalamit",
  "profile": {
    "depth": "FULL",
    "target": "RICH",
    "lifecycle": "NEW",
    "mission_source": "WP_REF"
  },
  "source_versions": {
    "base": "7b9153f29ac936bd",
    "team": "d359ed74ce9a7093",
    "domain": "3845125952f96d5f",
    "wp": "ae165a9134fb4870"
  }
}
```
---

<!-- ─────────────────────────────────────────────────────────────────── -->
<!-- SPOKE SUPPLEMENT (hand-authored by team_100).                      -->
<!-- Everything ABOVE is the hub prompt-generate skeleton, written      -->
<!-- verbatim per protocol (index §7.2). For this L0 spoke it is        -->
<!-- CONTENT-EMPTY — "work package details unavailable" x4, and zero    -->
<!-- mentions of qr32 / ea_nap / facade. EXPECTED, not a failure: the   -->
<!-- hub endpoint overrides work_packages from projected DB rows        -->
<!-- (l0_roadmap_source: database) and the DB holds no WP-S5-* row, so  -->
<!-- prompt-generate cannot resolve this WP. THIS SUPPLEMENT IS THE     -->
<!-- REAL MISSION.                                                      -->
<!-- ─────────────────────────────────────────────────────────────────── -->

## SPOKE SUPPLEMENT — WP-S5-07 real context (team_100 hand-authored)

### The mission

Close the **S004 residual** that WP-S4-07 never delivered: the site-wide **NAP** (address + phone) is absent
from the live footer, a **wrong 9-digit phone is live** on `/qr/qr32/`, and the FAQ carries a non-canonical
phone plus a glow that never reached the DB.

- **Spec (LOD400 — cross-engine validated, cycle 2 clean `PASS`):**
  **`_COMMUNICATION/team_100/S005/WP-S5-07-LOD400-2026-07-16.md`** ← **read in full before touching code.**
- **Ratified canon (the NAP answer):** `_COMMUNICATION/team_110/DECISION-NAP-CANON-2026-07-16.md`
- **Verdicts:** `_COMMUNICATION/team_90/VERDICT-WP-S5-07-LOD400-2026-07-16.md` (cycle 1 — 0/1 major/3) ·
  **`VERDICT-WP-S5-07-LOD400-CYCLE2-2026-07-16.md` (cycle 2 — `PASS`, 0 findings)**
- **Status:** `LOD400` · `L-GATE_SPEC PASS` · **`blocked_by: []` → start immediately** · `next_wp: WP-S5-05` ·
  **blocks the cutover gate** · `supersedes: WP-S4-07`.

### ⛔ The NAP is RATIFIED — do not ask, do not defer

> **«זה המידע המדוייק. לא לשאול יותר שוב!!! לתקן בפוטר בהתאם ובכל מקום שצריך.»** — team_00, 2026-07-16

There is **no "decide NAP" item**. Any artifact treating it as open is wrong — cite the decision record.
Canon: street `עמל 8 ב'` (**U+0027**) · locality `פרדס חנה-כרכור` (**U+002D**) · display `052-4822842`
(**U+002D**, no country code) · `tel:` href `+972524822842` · schema `+972-52-482-2842`.

### 🔴 Three traps that will silently defeat you

**1. The footer file both input docs name is DEAD.**
`DECISION-NAP-CANON` §4 row 1 and `ROUTING-REQUEST-…-S004-RECONCILIATION` §2 both point at
`template-parts/blocks/block-footer-social.php`. It renders on **zero** live routes (`ea-cfoot` = 0 on `/`,
`/faq/`, `/contact/`, `/treatment/`, `/blog/`, `/blog/?cat=*`, `/qr/qr2/`). **The live footer everywhere is
`template-parts/chapters/section-footer.php`** (`foot__brand` = 1 on every route) — LOD400 §4.B targets L32-34.
**Do NOT add NAP to the dead file** — a second hard-coded copy *is* the drift this WP ends. Caught by team_90
cycle 1 (F-01, major) after the error survived three documents.

**2. The FAQ seeder is INSERT-ONLY — the obvious route no-ops.**
`mu-plugins/ea-faq-seed-once.php` does `if ( ! empty( $existing ) ) { continue; }` + `wp_insert_post` only.
**Resetting `ea_faq_seed_v2` will change nothing.** `method-02` (glow) and `general-17` (phone) are already
seeded, so the corrected JSON never reaches the DB that way. Use the **2-key allow-list** update drop-in
(LOD400 §4.G, `init@41`). **Do NOT** make the seeder update-all — it would overwrite all 108 rows including
any wp-admin edits (WP-S4-05's whole point).
*Contrast:* the **QR** seeder **does** update `post_content` on re-run → qr32 fixes via a guard-reset drop-in
(`init@27`, before the seeder at `@28`) — the proven WP-S5-02 §3.1 route. **Two seeders, opposite semantics.**

**3. `052-482284` is a SUBSTRING of the canonical `052-4822842`.**
Naive grep reports **6** files in the guard's scan scope; there is exactly **1**
(`ea-w2-07-qr-content-data.php:606`). Use the lookarounds in §4.H or you will "fix" five correct files.

### 🔴 `/qr/qr32/` — a wrong phone is LIVE

`mu-plugins/ea-w2-07-qr-content-data.php` **L606** = `052-482284` — **9 digits**, final `2` missing.
Verified live: HTTP 200 rendering the wrong number. **Not cosmetic:** this page is reached by scanning a QR
code **printed in Eyal's book**; a reader scans it, wants to contact Eyal, and gets an unreachable number. It
is the only phone on the page, and it has been live the whole time.
**Fix = correct L606 + reseed via a self-guarded `-once` drop-in** (no WP-CLI on staging). **Never hand-edit
the DB.** `ea-w2-07b-qr-reseed-once.php`'s flag is already burnt — S5-07 needs its **own** flag
(`ea_s507_qr_reseed_done`).

### `ea_nap()` — the durability piece (decided, §5.1)

Root cause of the whole recurrence: a NAP SSoT already exists (`ea-w2-seo-schema.php` L6/L53) but **nothing
reads from it** → 6 phone variants + 3 apostrophe variants → WP-S4-07's AC-10 ("byte-identical FAQ↔footer") was
**unsatisfiable**. Fix: extract `ea_nap()` into its **own** mu-plugin (`ea-nap-ssot.php`) and make
`ea-w2-seo-schema.php` a **consumer** — a getter inside the schema plugin would preserve the coupling.
Keys: `name` · `alternate_name` · `street` · `locality` · `country` · `address_display` · `phone_display` ·
`phone_href` · `phone_schema`.
> **AC-1 guards this refactor:** the `ProfessionalService` JSON-LD node must be **byte-identical** before/after.
> That engine just earned a clean cross-engine PASS in WP-S5-02 — breaking it is the worst outcome here.

### AC list (full detail + thresholds in LOD400 §6)

| AC | What it proves |
|----|----------------|
| **AC-1** | 🔴 schema regression guard — `ProfessionalService` byte-identical before/after the `ea_nap()` refactor |
| **AC-2** | live footer carries the NAP on `/`, `/faq/`, `/contact/`, `/treatment/` (from 0 today) + FAQ↔footer byte-match + `ea-cfoot`=0 dead-code guard |
| **AC-3** | 🔴 `/qr/qr32/` renders `052-4822842`; **0** standalone `052-482284`; other 47 QR pages untouched |
| **AC-4** | `/faq/` shows the `method-02` glow (from 0) and `052-4822842` with **0** `052-482-2842` (from 2) |
| **AC-5** | VERIFY-only — already live, must not break: price Q3 ×1, price glow ×2, geo `עמל 8 ב` ×1 |
| **AC-6** | `node scripts/qa/nap_canon_check.mjs` → exit 0 (zero non-canonical variants outside testimonials) |
| **AC-7** | exclusions honoured: `ea-testimonials-fb.json` **0 changes** · `דיג׳רידו` untouched · `wave2-stage-b.php` untouched |
| **AC-8** | `php -l` clean · `http-qa-axe.cjs` exit 0 · `qa_probe.mjs` PASS mobile+desktop (the `nowrap` phone is a real RTL overflow risk) |

**Evidence root:** `_COMMUNICATION/team_110/evidence/s5-07/`

### Scope exclusions — do not "help"

- **`inc/data/ea-testimonials-fb.json` — DO NOT TOUCH.** ⚠ The decision record says those numbers belong to
  testimonial authors. **That is false** — both are **Eyal's own number**, quoted by named authors
  (נוית צוף שטראוס; אלון גרזון רז, *"זה הטלפון שלו"*). The exclusion **stands on a stronger reason**: they are
  attributed verbatim quotations; normalizing them falsifies what a real person wrote. **Do not reason
  "the rationale was wrong, so I should normalize them."**
- **`דיג'רידו`** (U+05F3 vs U+0027) — brand word, not NAP. Out of scope. `seo-head-fallbacks.php` L50: change
  **only** `רח׳`→`רח'` and `ב׳`→`ב'`; a blind U+05F3 replace would also hit `דיג׳רידו` and create new drift.
- **`wave2-stage-b.php:19`** — the number there is in a **comment**; the real value is `972524822842`.
- **Transcripts** — team_00: EXCLUDED (Eyal content task).

### Ordering vs the sibling WPs

- **S5-06 ↔ S5-07 are order-independent on QR** — precisely because S5-06's facade is a `the_content` filter
  that never touches `post_content`. S5-07's reseed restores the same content (all 60 embeds), so S5-06's
  VideoObject nodes survive. *(Had S5-06 taken the forbidden reseed route, these two would collide.)*
- **S5-03 is fully independent** (legacy/301).
- **All three feed WP-S5-05**, which stays BLOCKED.

### End-of-gate protocol

- Validation: **team_90 cross-engine** (Iron Rule #1 — builder Claude ≠ validator `composer-2.5`). Mirror
  `MANDATE-TEAM90-WP-S5-0{1,2}-*` **including the Guardrails section** (expired staging TLS / HTTP-only →
  `curl -k`; host-conditional `noindex`; `curl 000` = transport timeout not a redirect → probe sequentially;
  the `052-482284` substring trap; drop-ins are single-fire so "nothing happened on the second load" is
  **correct**). Runner: `bash scripts/run_cross_engine_validation.sh team_90 . <mandate>` (`--dry-run` first);
  needs team_00 to authorize `cursor-agent -p --force --trust` in that literal phrasing.
- On `L-GATE_BUILD PASS`: generate the canonical handoff via hub prompt-generate, write `artifact_markdown`
  verbatim, **append a supplement like this one** (the skeleton will again be content-empty), show the
  `activation_block` to team_00.
- **Do NOT start WP-S5-05 automatically** — blocked cutover gate; wait for team_00 routing.
- **Registration is team_100/team_00 authority** — do not edit `_aos/` (Iron Rule #4); file a routing request.

---

## ⚠ ADDENDUM (team_100, 2026-07-16 — added after the T-1 ruling landed)

### The ADR034 incident is CLOSED. Two small actions are yours.

Authority: **`_COMMUNICATION/team_110/MSG-TEAM120-FINAL-RULING-ALIGNMENT-2026-07-16.md`** (team_120 Ambassador,
commit `2961352`) — *"FINAL for this incident"*.

**The ruling:** **ADR034 R9/R10/R12 enforced — the FILE is the write-SSoT. No backfill, ever.** The DB rows are
a read-only R12 projection; the endpoint served them as authority in violation of R12 — that was the bug. Fix
written, 1054 tests green, heading to deploy. **file-SSoT — the path you have used throughout — is the approved
canonical path. Nothing in your work changes. S005 continues as normal; do NOT wait for the deploy and do NOT
"fix" anything spoke-side.**

**Your two actions (ruling §4) — team_100 cannot do either for you:**
1. **Confirm the mail channel is open:** `aos_actor team_110` → inbox → expect **200/422**. **Report via the
   bus — that is itself the proof.** If **401** → report to team_00.
   *(team_100 must not do this: sending as team_110 from a team_100 key returns 403 `TEAM_ID_MISMATCH` and is
   recorded + escalated to team_00 as an impersonation attempt.)*
2. **Update §4 of `ROUTING-REQUEST-TEAM100-S005-REGISTRATION-2026-07-16.md`** to the final wording:
   > *"superseded by T-1 ruling 2026-07-16 — file-SSoT enforced (R9/R10/R12); no backfill; fix = endpoint
   > authority guard + server checkout freshness; see MSG-TEAM120-FINAL-RULING-ALIGNMENT-2026-07-16"*
   That file is in **your** directory — team_100 will not edit it (Iron Rule #4). It currently still asserts
   *"the ORIGINAL diagnosis «the DB was never backfilled» was CORRECT"*, which the final ruling supersedes.

**Credit, recorded:** your checkout finding is **leg 2 of the two-part fix** (endpoint authority guard +
checkout freshness), and **your timer proposal was adopted as the fleet-wide template** (mandated to team_60).
The word "REFUTED" in team_100's earlier record is **too strong** — your *observation* was right; the *causal
leap* (staleness explains the missing `WP-S5-*`) and "not fleet-wide" were the errors.

### 🔴 Do not trust hub `prompt-generate` handoffs until the deploy
team_120, verbatim: *«אל תסמכו על הנדאופים שלו»*. That is exactly why the skeleton above is content-empty and
why **this supplement is the mission**. When you hand off at `L-GATE_BUILD PASS`, do the same: skeleton
verbatim **+ a hand-authored supplement**.

### Bus note — a real limitation you will hit
`capture` is **idempotent on `(session_id, gate, kind)`** and **`wp_id` is NOT in the key**
(`messages.py:302`). Three `kind=handoff` captures from one session **collapse into one row** while still
reporting `"captured": true` — only the first WP reaches the bus. team_100 hit this releasing S5-03/06/07 and
delivered the other two via `POST /api/messaging/v2/send` with `kind=handoff`. **Verify the bus after any
multi-WP handoff** (`inbox?...&limit=100`; it returns oldest-first, so a small limit hides new rows).
